# 13 сентября 2026 — точечный follow-up после автономного FAIL

Исправление0e11a3fb проверено новым source harness: native34/34,
параметры6/6, подмены13/13. Runtime изменился; новый stage/слот ещё не выданы.
[Текущий отчёт и комплект](../plans/loginom-dock/node12-acceptance-preparation-2/README.md).
Исторические результаты ниже не являются приёмкой нового runtime.

# Подплан 12: диагностика и реализация

Состояние: черновой обработчик реализован, выполняется прямая отладка полного source node.apply. К автономной приёмке ещё не готов.
Рабочая ветка `codex/node-12-duplicates`, база `a3b419bde8a660e1905284ee62a46362d5a49e09`.

## Допуск среды

Source MCP работает из собственного worktree, Node 24.19.0. Проверены все
148 записей clientSourceManifest: несовпадений нет. Сессия
`8d403b79-1f8b-4cdc-9e04-5da8a99aadbd`, её профиль и состояние находятся в
`.dock/stream-runtime`. Skill закреплён на
`afa295bf48dc48da5d3c995665a06ef2190ff620bae3243d46557e0371536790`.
Native archiveActive=false; отсутствие всех глобальных hook-процессов не проверялось.
OpenViking health успешен, обращения выполнялись с actor scope без override Peer.
Каталог памяти нового worktree не найден; это не недоступность сервиса.

Вход через UI как `test-1`, Loginom 7.4.2. Окно 1508×949 при доступной области
1512×949, viewport=null, рабочая область 1508×862; --start-maximized подтверждён
в конфигурации отдельного браузера. Корень `/test-1` открыт через файловый UI.
Создан и подтверждён там `/test-1/Node12-diagnostics-20260912-8d403b79.lgp`.
Приватный checkpoint: `.dock/stream-preflight.json`.

## Наблюдения

Источники Dock: Help `data/processors/scrutiny/duplicates.md`,
`data/workflow/ports/table-interface.md`; E2E `bg/helpers/workflow/ports.ts`.

Узел имеет один табличный вход и выход. Мастер начинается с
`TuneDataSourceInputPortWizard`: назначения меняются через `EditTuneColumnDefForm`.
Доступны только «Не задано», «Входное», «Выходное»; имя, метка, тип и вид данных
в этом редакторе отключены. В локальных записях поля роли находятся в UsageType:
0, 3, 4 соответственно (DefaultUsageType не отражает назначенную роль).
После Далее открывается DoneWizard; отдельной страницы алгоритма нет.
Узел автоматически меняет метку по ролям, поэтому после завершения нужны свежие refs.

Контрольный CSV содержит 12 строк и поля Id, Key, Sub, Value, Amount.
Входные Key/Sub, выходные Value/Amount, Id «Не задано»:
все 12 строк и исходные значения сохранены; 7 дублей в 3 группах,
5 противоречий в 2 группах. Пара Id=1/2 одновременно входит в группу противоречий
1/2/3. Тройка 4/5/6 отмечена полностью. Уникальные 7/8 не отмечены.
Пустые значения 9/10 совпали, текст null в 11 отличается от пустого в 12.
Различение Null и пустой строки ещё требует отдельной проверки.

Повторный запуск с выходными полями «Не задано» успешен: 12 строк,
10 дублей в 4 группах (1/2/3, 4/5/6, 9/10, 11/12), противоречий нет.
Служебные имена: Duplicate, DuplicateGroup, Contradiction, ContradictionGroup;
метки соответствуют Help. Приватные DOM evidence:
`.dock/node12-first-output.json`, `.dock/node12-key-only-output.json`.

## Следующее действие

Завершить live-матрицу (Null/пустая строка, пустой вход, типы и конфликт имён),
расширить существующий node.apply, добавить независимый auditor и source harness
с полным dock_prepare/remote pinning. Сверить старые config pins с исходниками;
финальному кандидату нужен отдельный каталог. Production не переключать.
Hermes, ревью, push, сборка и установка не запускались.

## Промежуточный checkpoint прямой отладки

Добавлены research.duplicates / mark, полные списки input_fields/output_fields,
чтение UsageType из полной локальной коллекции, замена всех ролей и readback.
Код пока не принят: полный node.apply не дошёл до выполнения и чтения результата.

На реальном UI выявлены и исправлены: точный возврат из мастера порта при
многострочной автоматической метке (native key берётся из связанного breadcrumb),
распознавание role-grid без btnAddMappingColumn, выбор глобального выпадающего
списка назначений. Прямо проверены выбор роли и apply_output_column с обратным
чтением строки. Добавлено ожидание доступной кнопки редактора при асинхронном
появлении окна; это последнее изменение ещё требует полного source-прогона.

Общий прогон до последних изменений: 1389 тестов, 9 отказов sandbox;
14 тестов соответствующих сетевых/процессных suites повторно прошли вне sandbox.
Промежуточные workspace-ui + duplicates suites: 272 PASS.
Текущие профильные duplicates suites: 11 PASS. Новые global-role и прежние
output-column focused проверки прошли; общий финальный прогон ещё не выполнен.

Source-сессии и operator/execution-events находятся только в .dock/stream-runtime.
Последняя завершившаяся диагностическая попытка: 3ccfe64e-242a-4894-b294-f6fe0c29cb85,
node12-existing-roles-01: AMBIGUOUS configure, без execution; не считать успехом.
Копии /test-1/Node12-handler-20260912-live02.lgp ... live05.lgp созданы для
диагностики. После завершения браузерного процесса серверная блокировка пакета
может сохраняться, поэтому копию явно закрывать через меню Loginom перед передачей.
Приватный .dock/node12-source-harness.mjs передаёт длинные requests через args_path:
прямая строка JSON в macOS PTY упиралась в ограничение длины ввода.

Остаются полная handler-матрица, независимый auditor с отрицательными подменами,
Null/empty и пустой вход, save/reopen/reexecute, отдельный candidate-каталог,
финальная документация и локальные коммиты. Hermes не запускать без слота координатора.

## Checkpoint после разрыва связи

Source-сессия `f42723c9-ba40-47cf-97f0-b3c4e1c358d1`, runtime
`1d09c34e29a5223775a4cc9747ab255164fc11bfe00228e5438dcaea18420d19`,
пакет `/test-1/Node12-handler-20260912-live06.lgp`:
`node12-existing-roles-01` остановилась на input_mapping с AMBIGUOUS,
`OUTPUT_PORT_OPEN_UNCONFIRMED`, execution `not_requested`.
Свежая UI-диагностика обнаружила диалог «Восстановление сессии — Обнаружен
разрыв связи». Такой же диалог присутствовал в отдельной ручной сессии.
Публичные repair/restore и resume исходной операции отказали из-за
неразрешённой внутренней фазы; её результат не переобозначался успешным.
Harness закрыт, все operator-ответы и execution-events сохранены приватно.

Ручная сессия восстановлена штатной кнопкой. Live06 всё ещё открывался только
для чтения; создана копия `/test-1/Node12-handler-20260912-live07.lgp`, её новое
имя без признака read-only проверено в меню, пакет явно закрыт. Ручное окно
вернулось на «Начало». Новый source harness не дошёл до dock_prepare:
`UND_ERR_CONNECT_TIMEOUT` к `loginom.duckdns.org:443`. Отдельная проверка HTTPS
также получила SSL connection timeout. Пины и провайдеры не подменялись.

Полный набор клиента перед последней локальной правкой: **1398 PASS / 1 SKIP**,
без отказов (`.dock/node12-final-source-tests.log`). Readback дополнительно
исправлен по контракту сохранения исходных полей: текущий порядок выходного
mapping может отличаться от порядка role-grid. Проверяется точное взаимно
однозначное соответствие имён, меток, типов и native source records, без
навязывания перестановки столбцов. Это покрыто отдельной проверкой; живой
успех обработчика этим тестом не заменяется.
После этой правки профильные suites: **12 PASS**; Python: **508 PASS**;
`git diff --check` прошёл. Повторная проверка HTTPS снова получила SSL timeout.

После восстановления API: запустить новый source harness с полным prepare
на live07, сначала подтвердить редактируемость пакета и состояние соединения.
Затем проверить последнее ожидание role picker полным node.apply. Нельзя
считать список оставшихся проверок выполненным на основании модульных тестов.

## Продолжение прямой QA — 13 сентября 2026

Доступ восстановлен; личная OpenViking health-проверка прошла. Подтверждены
source runtime Node 24.19.0, собственное состояние `.dock/stream-runtime`,
аккаунт `test-1` и окно 1508×862 с viewport:null. Установленные плагины и VPS
не менялись; общая проверка маршрутизации native hooks не подменяется archive=false.

На source pin `a4f8b9ab957692707d0ff2d7cb92c7914e3ec61525492daf1b2c542344ff1cac`
в сессии `ae2d9d34-e86c-4508-a2b1-a4f657efcb5a` прошли полные операции:
`node12-live10-roles-01`, `node12-live10-keyonly`, `node12-live10-done`,
`node12-live10-reexecuted`. Исправлены общие UI guards для автоматической метки
с точкой с запятой и переводами строк: точный native key связан с GUID узла,
а текст по-прежнему сверяется. Это устранило неподтверждённый Done и возврат
из панели процессов. Предыдущие AMBIGUOUS попытки остаются неуспешными.

Независимый Python-аудит raw конфигурации прошёл на всех четырёх операциях;
11/11 подмен параметров/phase receipts отклонены. Повтор keyonly с тем же
operation ID вернул полностью идентичный завершённый ответ и прежний execution ID.
Done подтвердил execution not_requested. Штатные save_checkpoint и save_as
успешно сохранили и закрыли/открыли `/test-1/Node12-handler-20260913-live10.lgp`.
Первое чтение ролей после открытия: Key/Sub=3, Value/Amount=4, Id=0; назначения
совпали до/после, повторное выполнение получило свежий execution ID `:191:5`.
Все результаты этой 12-строчной таблицы читают sample 10 и возвращают partial;
это **не** полный независимый аудит результата. Последующий harness закрыт;
отдельное финальное закрытие пакета после reexecute не подтверждено.

Общий клиентский прогон: 1391 PASS, 1 SKIP и 9 отказов sandbox. Все 14 тестов
соответствующих archive/clipboard/managed-shutdown suites прошли отдельно вне
sandbox; итоговый охват 1400 PASS / 1 SKIP без неразобранных отказов.

Для полного результата начата отдельная source-сессия
`4ccd7eb7-0c9c-43ce-90a6-db35b1ebb2cd`, собственный новый черновик. Три CSV
допущены штатными startup artifacts и доставлены с byte/SHA readback:
`Node12-main10.csv`, `Node12-null8.csv`, `Node12-empty.csv` в `/test-1`.
Добавлен связанный аудитор полного выхода; положительная live-проверка ещё впереди.
Текущая операция `node12-main10-import2` выполняется. Первый запрос импорта
отказал до эффектов из-за delivery ID вместо возвращённого upload_operation_id;
исправленный запрос использует точный `node12-main10-upload:upload`.

Этап разработки пока не завершён. Остались полные main/null/empty результаты,
новый узел/Close/ошибочные поля, итоговый проверяемый acceptance-набор и документация.
Отдельный candidate-каталог не собран/не опубликован; решение об этом требуется
до Hermes, а VPS-сборка сейчас не разрешена. Hermes и ревью не запускались.

### Полная прямая матрица и найденные отказы

Source-сессия `02adc9e0-b38c-4b28-bc1c-73546f15170a`, pin
`6834e5b6024a740ebd01b7ea7d6023092664f9197ba93f4ab788dba4d5863060`:
новые main10/null8/empty импорты и узлы разметки прошли полные операции.
Для каждого независимо подтверждены доставка байтов, свежий импорт, конфигурация,
точная новая связь/GUID и все строки выхода (10/8/0). Main10: 7 дублей, 3
противоречия; null8: 6 дублей, 4 противоречия. Пустой вход сохранил схему.
Key-only main10: 8 дублей, 0 противоречий. Close с альтернативными ролями
завершился без применения/выполнения; raw-чтение перед восстановлением прежних
параметров подтвердило неизменённые роли. Все перечисленные независимые аудиты прошли.

Новая карточка находилась ниже видимой части палитры: до графических эффектов
получен Target is covered. Добавлен scrollIntoViewIfNeeded только точного
компонента до обычной проверки точки и неизменности графа; новые узлы выше
успешно созданы этим путём.

В отдельной read-only диагностике live10 подтверждён реальный edge tid с `;`
в метке Дубликатов. Исправлены native graph reader, UI observation/gesture guard
и save graph snapshot. Текущее чтение реального пакета видит одну связь правильных
GUID, foreign_links=[]; unit-проверка выявляет её удаление после reopen.
330 профильных shared tests прошли после этих изменений.

Негативный запрос `node12-final-main10-unknown` выявил отказ после открытия
input_mapping: AMBIGUOUS, без применения настроек и execution. Inspect подтвердил
pending phase; штатная UI repair отказала: original node phase must be verified.
Непроверенная фаза не переобозначена успешной. Этот черновик не был сохранён,
поэтому его результаты остаются доказательствами прямой QA, не persistence.
Подготовлено исправление: async validation hook закрывает неизменённый мастер,
сохраняет подтверждение отказа и возвращает FAILED с cleanup_complete=true;
возможная деактивация честно остаётся effect_possible=true. При неполном cleanup
или сбое журнала сохраняется AMBIGUOUS. 102 профильных теста прошли; живая
перепроверка этого исправления ещё обязательна.

Добавлены независимые graph/close/output verifiers и воспроизводимые fixtures.
Подготовлены goal и startup descriptors для будущего Hermes; интеграция полного
goal auditor ещё не завершена. Hermes не запускался, слот не запрашивался.

### 2026-09-13: итоговая прямая QA на текущем исходном runtime

Сессия `343124f4-1fef-41e0-bc4f-e9a988339b2d`, runtime
`81a65c886ba64fb72ea1ac180ea79cb674d50958a144971a2a0e14aea98b2505`.
Все последующие клиентские проверки выполнены без изменения этого runtime.
Loginom 7.4.2, `test-1`, видимое окно 1508×862, отдельный source harness.

- Три файла доставлены с точными SHA/bytes; все 3 новых импорта и 3 новых
  узла разметки SUCCEEDED. Независимо прошли source/configuration/output/graph
  проверки: main10 — 7 дублей и 3 противоречия, null8 — 6 и 4, empty — 0 строк
  с полной схемой. Подтверждены NULL, пустая строка и буквальный текст `null`.
- Неизвестное поле существующего узла: FAILED, pending_phase=null,
  cleanup_complete=true, execution=not_requested. Журнал содержит единственное
  подтверждение `duplicates_input_validation_refused`; выполнялись только
  open_input_port/click/confirm_wizard_close. Следующая корректная операция в
  той же сессии SUCCEEDED; исходные роли подтверждены до нового изменения.
- Key-only — 8 дублей, 0 противоречий. Done не выполнял узел. Close с другими
  ролями их не применил; отдельный аудитор подтвердил сохранение прежних ролей
  до восстановления исходной конфигурации. Повторный полный результат прошёл.
- Пакет `/test-1/Node12-complete-20260913.lgp` сохранён и повторно открыт.
  В обоих save receipts ровно 6 узлов и 3 связи; после reopen все 6 существующих
  узлов снова выполнились. Независимые 17 проверок persistence-цепочки прошли:
  неизменённые настройки источников, роли до любых правок, исходные данные,
  полный свежий выход 10/8/0, неизменённый граф и save/reopen receipts.
- 8/8 новых отрицательных подмен evidence отвергнуты: ключ, роль, флаг,
  членство группы, ID строки, ID выполнения, исходное значение, удаление строки.
  Ранее также отвергнуты 11/11 подмен конфигурационных доказательств.
- Полный клиентский набор: 1403 PASS, 1 SKIP, 0 FAIL. Полный Python-набор:
  516 PASS. `git diff --check` прошёл. Production build не выполнялся.

Доказательства доступны в `.dock/stream-runtime/sessions/<session>/` (operator и
execution-events.jsonl) и `.dock/node12-qa2-{complete,persistence}-audit.json`,
`.dock/node12-qa2-{audit-negatives,input-refusal-audit}.json`.
Технические ошибки диагностического оператора (неверное имя timeout параметра,
неполный target) сохранены в журнале и не засчитаны как успешные операции.

Подготовлен независимый `duplicates_node_acceptance.py`, goal из 16 операций,
три воспроизводимых файла, run-specific startup descriptors и интеграция run.py.
Он требует public-call accounting, неизменённые runtime/harness/skill, доставку
байтов, конфигурацию и полный выход, Done/Close, save chain и 6 reopen bindings.
Его низкоуровневые проверяющие функции подтверждены живой QA; полного положительного
автономного прогона goal пока нет. Ревью и Hermes не запускались.

Границы: живая числовая матрица содержит integer, строковая — string;
boolean/real/datetime не объявляются отдельно проверенными живыми наборами.
Удаление дублей и выбор представителя не реализованы. Исторический shared candidate
`2026.09.11-parallel-pilot.1-candidate` использован только для разрешённой диагностики.
До Hermes нужен отдельный согласованный node12 candidate с точными URI/SHA;
VPS-сборка/публикация, установка клиента, push/merge не выполнялись.

Однократный MEMORY_ACCESS_CHECK по запросу координатора выполнен: адресные
find/read общего project-memory root успешны, health healthy; настройки не менялись.
Workspace resolver сохранил Peer `-Users-kartamyshev-Git-loginom-dock--worktrees-node-12-duplicates`.
Фактический actor header MCP health не раскрывает; эта граница сообщена координатору.

Последний live-negative также прошёл: source со столбцом `Duplicate` вызвал
NOT_APPLIED (`reserved service field name or label`), до target checkpoint и
без выполнения. Этот дополнительный источник сохранён отдельно в
`/test-1/Node12-negative-20260913.lgp`; основной complete-пакет остался с шестью
узлами и тремя связями. Доказательство: `.dock/node12-qa2-collision-audit.json`.

Этап development завершён, готов к одному ревью по отдельной команде координатора.
Готовность к автономной приёмке пока не заявляется: отдельный candidate-каталог,
результат ревью/доработок и разрешённый слот Hermes остаются необходимыми.

### 2026-09-13: один раунд доработки по ревью завершён

N12-R1 / P2 подтверждён на копии свежих живых доказательств и закрыт.
Исправление/тесты зафиксированы коммитом `ce10e177`; подробный
[отчёт исправления](../plans/loginom-dock/12-review-fix-2026-09-13.md) содержит
методику, изменённые файлы, ссылки на evidence и ограничения.
Исходный отчёт ревью сохранён; повторное ревью не запускалось.

Живая source-сессия `167febc5-6994-495a-96aa-359602c13b2f` проверила empty на
отдельной `/test-1/Node12-R1-diagnostic-20260913.lgp` до и после save/reopen.
Теперь schema expected поступает из fixture + независимого контракта служебных
столбцов, включая пустую таблицу; сохранённая перестановка полей допускается.
11/11 положительных output-аудитов, 3/3 отказа согласованным подменам схемы,
519 Python tests прошли. Runtime pin не изменён; новый harness manifest hash
`b73e69f020fa7a2d8b0a7c30e721d8bca940ccf6476baf1e1de1328aae94ffd0`
и 239 file hashes записаны в `.dock/node12-r1-fix/pins.json`.
Это не автономная приёмка: node12 candidate URI/SHA и слот Hermes ожидаются.
