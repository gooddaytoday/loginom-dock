# Живая перепроверка ревью Join — 12 сентября 2026

Оба замечания ревью **подтверждены и исправлены в исходном клиенте**.
Совместная живая проверка и заключительная автономная приёмка прошли:
**58/58 PASS** независимого аудитора с заранее закреплённым runtime и harness.
Установленный клиент, сервер и пользовательская конфигурация Dock не изменены.
Работа последовательная, только по запрошенному follow-up; пилот и следующий узел
не возобновлены. Исходная приёмка подплана09 остаётся отдельной исторической записью.

## Подтверждённые дефекты и исправления

| Дефект | Живое подтверждение | Исправление |
| --- | --- | --- |
| Ключи вне видимой области нельзя добавить/удалить | Два входа по 40 полей: `colSourceName_L40` недоступен, обе сетки показывают только первые 21–22 строки; после ручной прокрутки L40/R40 связываются | Ограниченная прокрутка собственной сетки перед созданием и удалением пары; проверяются поле/record ID, сторона, мастер, схема, ключи и прогресс |
| Сохранённое исключение блокирует промежуточный mapping при смене ключей | При удалении второй пары и отключении правых ключей — `Unsupported retained join output`; в UI одновременно сохранённый исключённый Part и устаревший KeyRight | Учитывается `exclusion_source`, удаляются только несвязанные устаревшие поля, сохраняются исключение, alias, порядок и autosync |

Совместный широкий случай обнаружил и позволил исправить связанные причины:

- Промежуточная таблица нумерует активные и исключённые строки отдельно.
  Чтение теперь проверяет оба групповых индекса.
- После чтения 79 полей завершение порта запрашивало уже скрытую страницу 0.
  Завершение адресует последнюю прочитанную страницу определения.
- Ячейка может присутствовать в DOM, оставаясь за пределами области прокрутки.
  Перед кликом/редактированием раскрывается конкретное поле с неизменной схемой.
- У сгруппированного CollectionProxy `total` может считать только активные поля
  или оставаться прежним после удаления. Допуск ограничен полным локальным кэшем:
  page=1, remote filter/sort=false, pending={}, состав исходной коллекции совпадает.
  Все отрисованные record IDs, поля и групповые индексы продолжают проверяться.

## Живые доказательства

Loginom **7.4.2**, `http://logi-test-plan.bg.local/app/?testable=true`, аккаунт
`test-1`, явный каталог `/test-1`. Отдельные видимые Chromium: `--start-maximized`,
viewport=null; фактическая область 1508 × 862, внешнее окно 1508 × 949.
E2E селекторы/сценарии Слияния и локальный Loginom Help прочитаны до исправлений.

Все каталоги ниже находятся в `.dock/join-20260912/`:

| Каталог | Результат |
| --- | --- |
| `live-1789179946365` | Исходный сбой сохранённого исключения воспроизведён |
| `live-1789180686426` | Малый случай исправлен: 11 × 4, configuration audit PASS; прочитан образец 10 строк. Здесь же воспроизведён сбой ключей L40/R40 |
| `live-1789181197302` | Прокрутка ключей прошла; обнаружен сбой завершения широкого порта |
| `live-1789181519798` | Базовый широкий Join: 1 × 79, configuration/output audits PASS; обнаружена отрисованная, но скрытая ячейка |
| `live-1789181897686` | Исключение L01 + RightText + autosync=false: 1 × 79, оба аудитора PASS; смена ключа выявила total=79 при 80 полных записях |
| `live-1789182383046` | Оба исправленных прохода SUCCEEDED: Inner L40/R40 → 1 × 79; Left L39/R39 без правого ключа → 1 × 78. Полная строка, все настройки и исключение подтверждены; по 11/11 отрицательных мутаций отклонены |

Итоговый runtime:
`fe2641eb0e73a5810b12e95f3a0991cd47623db3d330817f56675897a442f0b3`.
Доказательство совместной проверки: `live-1789182383046/wide-combined-audit.json`
и `execution-events.jsonl`. Ожидания рассчитаны независимо по двум фиксированным CSV.
Диагностическая копия `/test-1/packages/Join-review-wide-20260912.lgp` закрыта
без сохранения изменений; оригинальные приёмочные пакеты не перезаписаны.
Неизвестные операции из неудачных диагностических сессий не объявлялись успешными.

## Автономная приёмка и проверка аудитора

Профиль во всех попытках: existing ChatGPT **openai-codex / gpt-5.6-sol / low**.
Каталог: `2026.09.11-parallel-pilot.1-candidate`, SHA
`4ac827fc9e0cefa609bf2cb8fd7d3d79318dd3fe92999decc7e385fae51fc6e2`.
Goal `join-review-complete`: два импорта по 40 строковых полей, широкий Join,
изменение того же узла с сохранением исключения/alias/output order/autosync,
Done, Close, checkpoint, save_as/reopen и повторное выполнение всех трёх узлов.

Каталоги попыток: `.dock/join-review-20260912/hermes-runs/`.

1. `20260912-060958-2f8b15e4`: конфигурация по умолчанию направила на dev-test
   Loginom 7.5.0-alpha. `UI_BUILD_MISMATCH` остановил подготовку без изменений.
   Аудит FAIL, 0/9 операций. Дальше явно выбрана прежняя отдельная
   `.dock/join-20260912/dock-config.json` для проверенного Loginom 7.4.2.
2. `20260912-061255-ee91e1d3`: 9/9 операций SUCCEEDED, 1 × 79 → 1 × 78,
   пакет сохранён/переоткрыт, свежий выход 1 × 78. Первоначальный frozen audit FAIL.
   Исправленный содержательный аудит: **55/55 PASS**, семь подмен отклонены.
   Старый frozen FAIL не подменяется этим последующим результатом.
3. `20260912-064347-12f52e45`: 9/9 операций SUCCEEDED, сохранение и свежий выход
   1 × 78 подтверждены журналом; frozen audit FAIL. Все семь чтений выбраны с
   require_exact_numbers=false. Общий независимый аудитор требует отдельное
   применение/восстановление формата таблицы; это условие отсутствовало в задании.
   Goal уточнён: во всех семи чтениях require_exact_numbers=true, sample_rows=1;
   аудитор проверяет это отдельным обязательным условием. Проверки формата
   не ослаблялись, код клиента не менялся.
4. `20260912-065436-43d5fd56`: **58/58 frozen PASS**, 9/9 полных операций,
   две подтверждённые доставки, checkpoint и save_as/reopen. Исходный Inner дал
   1 × 79, изменённый Left — 1 × 78; после переоткрытия все три узла выполнены
   заново, новый выход — 1 × 78. Настройки, исключение, RightText, порядок выхода,
   две связи и идентичность узлов сохранены. UI-резерв не использован.
   Пакет: `/test-1/packages/Dock-acceptance-20260912-065436-43d5fd56.lgp`.
   Runtime, native skill и весь harness неизменны до/во время/после запуска.
   **7/7 подмен** полного аудитора отклонены.
   Hermes: 27 модельных API-вызовов, выход без ошибки/тайм-аута.
   [Машинная сводка](join-review-acceptance-2026-09-12.json).

Два уточнения аудитора доказаны отдельно:

- Ранний запрос импорта без обязательного source_path был отклонён `request.validate`
  до выделения операции. Hermes исправил его с тем же ID согласно контракту.
  Аудитор ошибочно сопоставлял этот отклонённый вызов с выполненной декларацией.
  Теперь допускается только явный unallocated/no-effect отказ, предшествующий
  всем реальным запускам того же ID. Поздний отказ, неизвестный эффект, выделенная
  операция и неоднозначная декларация отвергаются. Реальные вызовы строго связаны
  с единственной декларацией, завершением и публичными ответами.
- Loginom при сохранении autosync=true поднимает ключевые поля входов наверх.
  Независимое открытие сохранённого пакета только для чтения в
  `live-1789183721960` подтвердило L39/R39 первыми и прежний порядок остальных.
  Разрешена только эта точная стабильная перестановка, только для широкого review
  и autosync=true. Иные перестановки, изменения alias/source/flags/output отвергаются;
  при autosync=false порядок строгий. Фактический readback не переписывается.
  Пустая диагностическая сессия предварительно восстановлена после сообщения
  о разрыве связи; неизвестные операции узла не повторялись. Мастер отменён,
  пакет и диагностический браузер закрыты.

## Проверки и границы

- Клиент: **1344 PASS / 1 SKIP / 0 FAIL**.
- Python: **492 PASS** после уточнений аудитора.
- `git diff --check`: PASS.
- Отрицательные проверки включают чужую сетку/мастер/record, отсутствие прогресса,
  потерянный ответ, скрытую ячейку, изменённую схему, remote/pending/filtered cache,
  сохранённое исключение, неправильную пару, источник, публичную выборку и путь пакета.

Live проверены входы по 40 полей и выход до 80 определений с исключением.
Предельная схема 1000 полей в живом Join этой проверкой не подтверждена.
Поддержка прочих режимов соединения и восстановление ранее исключённого поля
не расширялись. Сборка, установка, deploy, commit и push не выполнялись.
