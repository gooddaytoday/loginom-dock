# Node16 — один раунд исправлений R16-1–3

Назначение `node16:review-fix:1:6d086da4:R16-1-3`; проверенный review HEAD
`6d086da4be8439679ae351ce2a25c57cca42c19f`, ветка `codex/node-16-collapse-columns`.
Повторное полное review и Hermes не запускались. Этот отчёт относится к исходникам
ветки, а не к main, серверу или установленному клиенту.

## Исправления

**R16-1 подтверждён live.** В собственной копии пакета файл A был заменён файлом B
по тому же пути. Старые import и exact вернули SUCCEEDED и значение
`9007199254740995` из B при сохранённом доказательстве A (`9aac…`, вместо `76da…`).
Доказательство: `.dock/node16/live-1789303045205/fix-reproduce-upload.json` и
привязанные browser/journal записи.

Теперь executor предоставляет внутреннюю полную упорядоченную историю загрузок,
включая незавершённые и неопределённые записи. Import проверяет актуальность
выбранного upload до настройки; exact дополнительно сопоставляет историю с
порядковым номером выполненного импорта. Подтверждённая замена теми же байтами
допускается. Изменённые или неизвестные байты после выполнения запрещают старое
доказательство, даже если позднее старые байты восстановлены. Обнаруженные
операции с доказанным отсутствием записи не считаются перезаписью.

Свежая live-проверка stale A после B дала NOT_APPLIED, effect_possible=false,
cleanup_complete=true. Current B и восстановленный A успешно импортировались.
На итоговом runtime повторная загрузка тех же байтов после выполнения импорта
сохранила успешное полное чтение 60 ячеек. Отдельная подтверждённая перезапись
после выполнения дала отказ внутреннего exact gate.

В отдельной `.dock/node16/live-1789305127771` native upload завершил submit,
после чего операторский harness потерял ответ. Inspect/recovery/replay этого
upload не выполнялись. Private history сохранила transport_uncertain; production
import source lineage отклонила proofA. Использованы настоящий private completed
import и uploads из executor. Первый диагностический completedStaticImports
через callback самого импорта исключал текущую операцию и отказал раньше lineage;
он сохранён, но не считается доказательством этого условия. Финальное
свидетельство — `fixed-unknown-lineage-gate.json`. На сервере собственный CSV
после этой диагностики может содержать изменённые байты; сохранная копия пакета
не объявляется готовым неизменным fixture. Проверка сохранения выполнена раньше.

**R16-2 подтверждён live.** Повторное скачивание frontend-файла не доказывало,
какая функция уже загружена в документ. Теперь проверяются SHA-256 исходного
представления 53 конкретных критических функций, восемь констант заголовка и
идентичность используемых session/manager/transport/socket/message объектов.
Проверка повторяется вокруг чтения, для выделенных request/response и перед
публикацией. Старые хеши frontend остаются дополнительной проверкой.

Контролируемая обёртка DispatchMessageAsync на собственной странице отклонена
без RPC: счётчик 2585 → 2585. Новая привязка изменённой функции также отвергнута
по хешу; после восстановления подтверждены все 53 функции. Сервер и общий
frontend не менялись. Финальный журнал сохраняет несекретные binding_id;
проверка реального redactor предотвращает повторную потерю этого поля.

**R16-3 исправлен.** Типы описывают sample/full coverage, discriminated native
cells, exact_table, coverage/consistency/binding и оба вида output port.
Полный режим разрешён только Collapse. Локальный TypeScript 5.2.2 проверил
строгий клиент без приведения типов и три отрицательных случая; новые зависимости
не устанавливались. Числовые границы и форматы строк продолжает проверять runtime.

## Проверки итоговых исходников

Runtime `db6d957169c50bcd5cb169db80d744fd846b8ffa4eee683178619c001fb22bab`,
169 файлов; Loginom 7.4.2, Node 24.19.0. Полный client: **1483 PASS, 0 FAIL,
1 SKIP**. Focused154 PASS; после исправления имени journal-поля —29 PASS и
повторная полная регрессия. TypeScript strict/noEmit PASS.

Независимый проверяющий скрипт сопоставляет точные значения, реальные MCP
text/structured replies, бюджет1MiB, private source lineage, 53 loaded pins,
освобождение native requests/responses, исходники и хеши свидетельств.
Итоговый verifier PASS. Финальные public случаи: mixed60 cells, ignore44 cells, reopen60 cells.
Дополнительно проверен default sample10of15; exact-поле в нём отсутствует.

Сохранена только собственная копия
`/test-1/node16-20260913-a56c2488/Node16-review-fix-20260913.lgp`.
В новом документе import использовал settings={}, Collapse parameters={},
оба mappings=[]; сравниваются схема, все60ячеек, роли, порядок, ignore_empty,
сопоставления и настройки текстового импорта. Диагностическое повторное
допущение и проверка исходного CSV не заменяет проверку сохранных настроек.

Manifest: `tools/loginom-acceptance/collapse/review-fix/provenance.json`.
Verifier: `python3 tools/loginom-acceptance/collapse/review-fix/verify.py`.
Локальные исходные browser/journal свидетельства находятся в игнорируемой `.dock`;
для переноса проверки нужны эти файлы по manifest, один checkout недостаточен.

## Сохранённые неуспешные проверки и границы

- Исторические exact-wiring manifest и старый evidence hole не переписаны.
  Их прежние live PASS относятся к прежнему runtime72c…, а не к текущему.
- В промежуточной сессии `.dock/node16/live-1789303712898` deliveryD закончилась
  destination AMBIGUOUS/UI_ROOT_STALE до native upload; её не повторяли и не
  переобозначали. Отдельное exact-чтение60cells закончилось AMBIGUOUS при
  подтверждении журнала: redactor скрыл token. Это исправлено binding_id, а
  исходный результат оставлен без изменения.
- В итоговой `.dock/node16/live-1789304119268` header-only upload также остановился
  destination AMBIGUOUS. После наблюдения закрыты только собственные Files tabs.
  Другой all-null fixture загрузился и импортировался; Collapse остановился
  finish AMBIGUOUS, console_not_ready, execution not_requested. Операцию не
  повторяли. All-null/all-null-ignore/restored не засчитаны как финальные PASS.
- Запуск `.dock/node16/live-1789304814545` по ошибочному URL без /app/ завершился
  до login; его браузер закрыт. Это не продуктовый отказ.
- Первая диагностическая инъекция потери upload-ответа не сработала, поскольку
  native upload возвращает submitted/AMBIGUOUS до серверной проверки, а harness
  ожидал SUCCEEDED. Доставка завершилась SUCCEEDED, подтверждённая перезапись
  отклонена source gate. Только операторский перехват исправлен по наблюдаемому
  native receipt; это не повтор предыдущей неопределённой операции.
- Проверка функций связывает выбранную критическую реализацию с документом;
  она не доказывает подлинность всего JavaScript. Private history относится к
  собственному runtime и не исключает внешних писателей. Серверная атомарность,
  незамеченный ABA, server cancellation и общий временной контракт не доказаны.
  Даты остаются native_serial_only.

Все собственные диагностические браузеры закрыты; проверка процессов не нашла
оставшихся браузеров этих сессий.

Полная приёмка узла/Hermes остаётся отдельным незавершённым этапом. Merge, push,
deploy, общий plugin, VPS и маршрутизация памяти в этом раунде не менялись.
