# Обновление клиента: возврат к сценарию и компактные результаты — 12 сентября 2026

Установлен **0.1.0-dev.20260910.3-c68743ca2658**. Код отправлен в GitHub main:
`8da19462f6803fe93f0c2775c194b22beeb5aa41`. Плагины Codex и Hermes обновлены.
[Исходная приёмка](process-node-focus-fix-20260911.md): 31/31 PASS,
11 подмен отклонены; обычный Hermes `openai-codex/gpt-5.6-sol/low`.

## Состав и целостность

Комплект собран на VPS в
`/opt/loginom-dock/client-build/process-focus-20260912-8da19462`.
Чистый установочный снимок: `4a492a1707021e3b523c86637502b860edcf29d3`.
346 build inputs совпали с принятым main; четыре файла сохранили прежнюю локальную
диагностику API: `client/bin/diagnostic.mjs`, `client/lib/local-diagnostics.mjs`,
`client/test/local-diagnostics.test.mjs`, `plugins/loginom-dock-hermes/__init__.py`.
Эти файлы побайтно сверены с предыдущим установленным клиентом. Локальный снимок
не опубликован в GitHub. Все 4164 файла комплекта проверены до и после установки.

Runtime снимка, комплекта и живой установленной сессии совпал:
`149b777b4c88dffe33a961f1a744bef31812ed4a8e3cd2d36383b2182d206787`.
Отличие от принятого source runtime связано только с сохранённой диагностикой API.
SHA-256 полученного установочного архива:
`e94b1984279f0831d1364228e673c29c6d1d1adee8248ddc6b0a78e9fe22e895`.

Первый build остановился до сборки: в клоне репозитория не хватало prerequisite
коммита. Передан полный Git bundle; повтор прошёл с проверкой равенства исходников
установочному коммиту. Production-сервисы и каталоги действий не переключались.
Публичный GitHub Release и изменение лендинга не выполнялись.

## Проверки

- Установочный снимок на Mac: **1317 PASS / 1 SKIP**, 0 FAIL; 39,45 с.
  Пропуск — прежний тест Windows. Зависимости взяты из проверенного VPS-комплекта.
- Новые подключения через установленный launcher: Codex — 48 инструментов,
  Hermes — 34; `dock_prepare` присутствует в обоих.
- Шесть файлов Codex-плагина и три файла Hermes совпали с комплектом.
- Dock config и Hermes user-profile сохранились побайтно; Hermes config —
  семантически целиком. В Codex config изменился только путь marketplace Dock.
- Шлюз Hermes перезапущен: прежний PID 888, новый launchd PID 13640;
  `state=running` подтверждён. Прежнее предупреждение о stale service definition
  сохраняется; определение launchd не изменялось.

## Живая проверка установки

Отдельная сессия Loginom 7.4.2 под `test-1`, видимое максимизированное окно,
`viewport:null`, 1508×862. Повторён проблемный порядок: «Файлы» → личный каталог →
«Начало» → новый черновик → доставка CSV → импорт → выполнение → «Показать узел».

Независимый аудит **7/7 PASS**: один жест «Показать узел», одно восстановление
вкладки, тот же узел и процесс, свежее выполнение, три поля и все 18 значений
шести строк совпали с CSV. Выполнение не повторялось. Отдельно проверено, что
установленный `compactNodeResult` сохранил readback целиком и все шесть строк.
Это операторская проверка установки; автономность и сохранение/переоткрытие
подтверждены отдельной исходной приёмкой 31/31, не подменены этой диагностикой.
Диагностический браузер закрыт; новый черновик не объявляется сохранённым пакетом.

Доказательства в `.dock/client-update-process-focus-20260912/`:
`bundle-verification.json`, `final-bundle-verification.json`, `configuration-verification.json`,
`mcp-smoke.json`, `installation.json`, `client-tests.log`, `live-audit.json`,
`installed-compact-check.json`, `live-1789164481602/`.

## Откат и продолжение

Предыдущий комплект сохранён:
`releases/0.1.0-dev.20260910.3-38f8be0b843b`.
Резервные настройки и регистрации — в приватном `before-install/`;
регистрационные квитанции содержат previousRelease. Для обновлённых инструментов
Codex начать новую задачу. Пилот остаётся остановленным; новые узлы не начаты.
