# Пауза: Hermes / Xiaomi MiMo — 16 сентября 2026

**Историческая точка паузы. Пользователь дал команду продолжить 16 сентября, работа возобновлена около20:16MSK.**

## Среда и сохранность

- Ветка `codex/mimo-scenario-stability`, база `9b46c85f68d054a27afad732d74785332098c0ba`. Изменения сохранены в рабочем дереве, коммитов этой работы нет.
- Сторонние 38 файлов остались в stash `pre-mimo-stability-20260916 preserved unrelated work` и `.dock/mimo-stability-20260916/preexisting/`. Не смешивать и не восстанавливать поверх текущих файлов.
- Не относятся к работе: `landing/concepts/`, `landing/sites-ai/`, `docs/loginom-dock/windows-rc8-debug-2026-09-16.md`.
- Стенд `http://10.200.11.224/app/?testable=true`, Loginom7.4.2; отдельный пользователь **mimo**, пароль пустой.
- Только существующая подписка **xiaomi / mimo-v2.5 / medium**, native hooks и штатное CSV-вложение. Другие модели не использовать.
- Main, сервер, общий плагин не обновлялись. Слияние и публикация требуют отдельной команды.
- OpenViking health проверен: healthy. Канонический checkpoint хранится здесь; project capture выполняется штатными hooks, без ручных записей в restricted peers.

## Точное место остановки

Прогон задачи21 **`20260916-183006-fc786284`**, каталог
`.dock/mimo-stability-20260916/runs/20260916-183006-fc786284`.
Выделенная папка Loginom `/mimo/MiMo-task21-diagnostic1-20260916`.

По просьбе пользователя отправлен SIGTERM только Hermes PID81510. Запускатель завершился и экспортировал13вызовов инструментов, результаты и этапы; returncode−15,282.740с. Отсутствие PID81502/81510/81511/81512 и процессов с приватным каталогом запуска проверено. Общий пользовательский Hermes gateway не останавливался. Hermes-слот освобождён; история сохранена со статусом paused_by_user.

Подтверждённо завершились:

- `import-001`:300строк,8полей, GUID `e88e861e-be0d-46b5-bbca-79b6a7b55038`.
- `calc-001`:возрастные группы,категории риска,кредит/доход,залог/кредит; GUID `a13ada0c-3125-4580-835d-2f98150e03f5`.
- `group-age-001`: GUID `c119e8cb-6793-4d77-a3df-4eadf9f7690a`.

Операция `group-risk-001` была незавершённой в последнем ответе; её фактический финальный эффект не установлен. **Сохранение пакета не подтверждено**, независимый аудит задачи21 не проводился. Это прерывание пользователем, не FAIL модели. При завершении отдельные reasoning-поля временной базы очищены, private-cleanup PASS. Доказательства не удалялись. Подробности `run/pause.json`.

Нельзя продолжать старые operation_id после завершения runtime. После команды продолжить сначала проверить состояние хранилища и возможный оставшийся пакет/блокировку, затем использовать новый изолированный запуск задачи21; прерванный запуск сохранить.

## Подтверждённые результаты

- Задача02 `20260916-153230-f976fa41`:независимый полный аудит PASS,366строк2024года, все9портов, сохранение/повторное открытие/настройки/связи. Это диагностический запуск.
- Задача47 `20260916-174615-e0b292a7`:граф/настройки/7полных таблиц186строк PASS, полное задание FAIL по итоговому тексту.
- Задача47 **`20260916-181009-dd48d6de`**:11аналитических узлов/10связей,8полных таблиц323строки и3CSV23/4/6строк, все настройки и повторное открытие PASS. Полное задание FAIL:8групп вместо7выше100%, среднее абсолютного процента вместо знакового, недостаточно обоснованные причинные выводы. Предыдущие выдуманный невидимый рейтинг экономии и среднее групп как общий показатель не повторились. `audit-task47-fix5-1/full-audit.json`.
- Исправлены технические значения по умолчанию, ошибки контрактов/вложений, привязки workflow, изменения выходных полей, новая calculator preflight, ID-only resume, маска перед связью, разные native-ключи одноимённых узлов, повторное использование Files и экспорт, ограничения неполного предпросмотра.
- Последняя runtime-правка:точный путь неизвестного export-параметра и один следующий шаг к схеме выбранного типа. Pending effects по-прежнему inspect/wait.
- Клиентский набор1893уникальных PASS/1SKIP:1884PASS сразу,9отказов sandbox для localhost/clipboard перепроверены в4файлах —14PASS. Логи `client-suite-export-schema*.txt`. Предыдущий полный проход1892PASS/1SKIP без повторов в `client-suite-report-grounding.txt`.
- Python731PASS (`acceptance-python-suite-fix5-v2.txt`); перед этим исправлена только устаревшая fixture с отсутствующим loginom_user.
- Последняя незавершённая разработка аудитора: `audit_scalar_expression.py` и тесты. Независимый ограниченный разбор Decimal-арифметики,сравнений,IF,ABS без eval/exec.3адресных теста PASS. **Ещё не подключён к аудиту задачи21**, полный Python-набор после добавления этих3тестов не повторялся.

## Следующие шаги после команды продолжить

1. Прочитать этот checkpoint, план и промежуточный отчёт; проверить branch/status/память и отсутствие активного исполнителя.
2. Сверить сохранность task21 после остановки, не считать неизвестный эффект завершённым. Подготовить новый изолированный запуск с исходным description.md/native CSV.
3. Завершить независимый аудитор задачи21 на выбранных моделью формулах:все300строк,широкие выходы,настройки и связи после сохранения/повторного открытия. В данных есть risk score, но нет факта просрочки:не приравнивать одно к другому.
4. Продолжить основные задачи19/34/38; исправлять реальные причины через отдельную live-диагностику. Не повторять неизменный неуспешный прогон.
5. Остаются восстановление подтверждённой ошибки выходного мастера, gap preflight существующего калькулятора, живая fault-проверка доставки и потерянного ответа. Нельзя снимать блокировку одним Cancel без сверки.
6. После исправлений заморозить runtime и выполнить согласованную серию38запусков:6основных×3,20прочихдопущенных×1. **Эта финальная серия ещё не начата.**

Во время модели и независимого reopen не менять client/lib или plugins: они входят в runtime pin. Изменения аудиторов/tools/docs вне этого pin допустимы. Использовать pinned Node `/Users/kartamyshev/.loginom-dock/current/runtime/node`. Browser/network/localhost-тесты требуют разрешения sandbox.

## Указатели

- [План и подробные checkpoints](../plans/loginom-dock/mimo-scenario-stability.md).
- [Промежуточный отчёт](mimo-stability-report.md).
- Основные доказательства `.dock/mimo-stability-20260916/`.
- Запускатель `tools/loginom-acceptance/analytic_corpus.py`; настройка папки `scenario-storage.mjs`; независимый reopen `scenario-reopen.mjs`.
- Старые неуспехи, аудиторы, результаты и stash сохранить. Никаких фоновых продолжений/автоматизаций не создано.
