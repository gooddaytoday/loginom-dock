# Общая память Loginom Dock: чтение и запись

## Актуальное решение пользователя — 13 сентября 2026

Пользователь потребовал, чтобы разработчики текущих и будущих узлов читали и
пополняли общую память основного проекта вместо отдельных баз знаний worktree.
Это заменяет прежнее разрешение только на чтение и запрет менять маршрутизацию
проектной памяти. Отдельные задачи, ветки, браузеры, сессии и их курсоры сохраняются.

Общий Peer: `-Users-kartamyshev-Git-loginom-dock`.
Общий корень:
`viking://user/kartamyshev/peers/-Users-kartamyshev-Git-loginom-dock/memories`.
Другие проекты и глобальная User memory сохраняют прежнюю изоляцию. Это личная
проектная память OpenViking, отдельная от knowledge source Dock.

## Состояние после перезапуска приложения

- После подтверждения пользователя установлены канонические project hooks
  поколения `20260913.3`; штатный API подтвердил доверие пяти определений в
  основном checkout и всех четырёх рабочих папках. 46 локальных проверок и
  25 прямых запусков режима ожидания прошли. Состояние захвата не изменилось.
- Для linked Git worktrees Codex читает hooks из основного checkout. Поэтому
  объявления находятся в основном `.codex/hooks.json`, а отключение исходных
  hooks выполнено локальным отключением официального плагина только
  в переключённых папках. Его установленные скрипты сохраняются неизменными.
- Пользователь остановил разработку 13/14 и перезапустил приложение. Все четыре
  задачи проверены незагруженными; 11/12 были завершены, 13/14 — прерваны.
  Это разрешённая пользователем граница переключения, а не завершение разработки.
- Для каждой задачи завершён прежний официальный capture, установлен отдельный
  MCP override, локально отключён исходный плагин и перенесён курсор под штатной
  блокировкой: 11 → 1452, 12 → 1630, 13 → 1741, 14 → 1690. Вторых активных копий
  состояния нет. История не переотправлялась и курсоры не обнулялись.
- Все четыре задачи фактически вызвали зарегистрированные `health`, `find` и
  точное `read` общего корня в `peer_scope="actor"`. Подключения обновлены после
  перезапуска; изменение файлов само по себе не использовалось как доказательство.
- Узлы 13/14 продолжили разработку в прежних задачах, worktree и ветках на
  `gpt-6-astra / medium`. Ревью и Hermes при этом не запускались.
- Зарегистрированный MCP работает через
  `/Users/kartamyshev/Git/openviking/integrations/codex-mcp-adapter/`; он определяет
  Peer по доверенным metadata Codex, а не по cwd своего процесса.
- Автоматические capture/recall выполняют официальные hooks OpenViking 0.8.1.
- Проверенное сопоставление подключено к четырём точным worktree через runtime
  `20260913.3`. Основная задача сохраняет прежнее подключение к тому же Peer.
- `remember` сохраняет глобальную
  User memory. Нативный `write` объявляет управляемую ветку `peers/` read-only;
  писать проектные знания следует штатным capture/extraction, а не обходом API.
- Новый capture узлов 11/12 проверен на сервере: все 12/9 сообщений имеют общий
  Peer; курсоры выросли до 1464/1644. Штатный routed PreCompact завершил партии.
  Урок N12-R1 извлечён в `events/2026/09/13/node_12_auditor_fix.md` общего корня,
  прочитан координатором и найден адресным поиском. Новые capture 13/14 проверяются
  после окончания их рабочих фаз, без прерывания разработки и таймера.
- Извлечение не гарантирует сохранение каждого фрагмента: в партии узла12 один
  event пропущен с `invalid_ranges`; сам подтверждённый урок N12-R1 сохранён.
  Первая партия узла11 сохранила событие доступа к памяти, но не отдельный урок
  N11-R1. Успешный commit не подменяет проверку нужного знания.
- Проектные события маршрутизируются в общий Peer. Штатные общие User memories
  (например, cases/experiences/profile) остаются отдельными от Peer и тоже могут
  дополняться штатным extractor. Их режим и серверные права не менялись.
  Свидетельства и ограничения: [memory-routing-activation-2026-09-13.json](memory-routing-activation-2026-09-13.json).

## Правила накопления знаний

1. Искать адресно по общему корню при конкретном пробеле контекста. После
   подтверждённого переключения общий Peer входит в обычный actor scope.
2. В итогах работы явно формулировать подтверждённые нюансы Loginom: наблюдаемое
   поведение, условие, версия платформы, узел, ветка/SHA и вид проверки.
3. Различать наблюдение живого UI, локальный тест, прямую QA и автономную приёмку.
   Незавершённая ветка не подтверждает исправление в main или установленном плагине.
4. Не превращать гипотезы, ошибки диагностического оператора, сырые журналы,
   секреты и временные состояния в долговечные знания. Не использовать `remember`
   для обхода проектного маршрута через глобальную память.
5. Разные подтверждённые результаты сохранять с их условиями и версиями.
   Противоречие отмечать явно; не переписывать общий вывод без проверки исходника.
6. Для существенного нового знания подтвердить фактическое сохранение в общем
   разделе: точное чтение и адресный поиск. Успешный выход hook или ответ commit
   сам по себе не доказывает появление нужной записи.

Полный процесс: [единая инструкция узла](../plans/loginom-dock/node-workflow-runbook.md).

## Следующие узлы: подготовка до запуска разработки

Каждый следующий узел сохраняет отдельную задачу приложения, постоянный worktree,
ветку от записанной принятой базы, Loginom-учётную запись и браузерный профиль.
Общий Peer остаётся прежним; идентификатор сессии и состояние capture индивидуальны.
Координатор подготавливает точный маршрут чтения и официального capture/recall,
проверяет единственный активный путь захвата и только затем выдаёт разработку.

Для новой задачи ID появляется при создании, которое сразу запускает первый ход.
Поэтому нужна короткая подготовительная фаза в том же чате: получить и проверить
фактические ID/cwd, без разработки и без отдельного worktree-capture. После её
завершения координатор регистрирует новую сессию, проверяет MCP и hooks; следующий
ход читает общую память в actor и начинает узел. Для действительно новой сессии
начальное состояние создаётся штатно; существующий cursor никогда не обнуляется.

Механизм bootstrap реализован и установлен как поколение `20260913.5`.
`prepare_task_memory.py` создаёт независимую запись ровно одной папки и локальный
MCP config; `enroll_task.mjs` проверяет реальный завершённый подготовительный ход,
свежие host/trust receipts и привязывает единственную задачу к общей памяти.
Переход `pending → enrolling → active` защищён отдельной блокировкой; повторное
включение сохраняет курсор. Пока регистрация не завершена, capture не работает,
а MCP сообщает о незавершённом подключении. Удалённая/испорченная регистрация
не разрешает новому MCP вернуться к own-worktree Peer.

Записи хранятся отдельно в `~/.openviking/project-memory-enrollments/loginom-dock/`,
имя — SHA256 точного cwd, права0700/0600. Старый registry `20260913.3` не меняется:
его список workspaces входит в project routeHash. Дописывать туда папки нельзя.
Новые корневые hooks добавлены второй группой; они пропускают четыре legacy
папки. Их исходные пять определений, runtime, MCP configs и receipts сохранены.

Проверены 83 Node и3 Python tests, штатное доверие10 project hooks в пяти
workspace, сохранение28 файлов маршрутизации,25 исходных определений hooks,
20 пропусков событий на четырёх старых задачах и отсутствие сброса курсоров.
Установка выполнена без глобального reload, перезапуска и остановки соседей.
Первая **новая реальная задача** создана для узла 16 после принятия узла 12.
Реальные bootstrap, enrollment и зарегистрированный actor health/find/read общего
корня проверены. Первая extraction не добавила знаний. После содержательной
работы cursor вырос 29→194; все 173 сообщения archive_002 имеют общий Peer.
Штатная extraction завершена: добавлены пять проектных записей, отдельно
обновлён глобальный профиль. Наблюдение node16_native_result_checked.md
прочитано по точному URI и найдено адресным поиском в actor общего проекта.
Полный механизм регистрации, чтения, capture и извлечения нового знания
подтверждён на новой задаче; это не доказательство приёмки самого узла.
[Проверка нового знания](node16-shared-memory-knowledge-2026-09-13.json).
[Квитанция новой задачи](node16-start-2026-09-13.json). Локальные тесты не заменили
эти проверки. [Доказательства](memory-enrollment-2026-09-13.json),
[команды и восстановление](../../tools/project-memory/README.md#новый-узел-регистрация-поколения-202609135).

При регистрации нового узла — один health/find/read; после первого законченного
этапа — проверка нового capture/extraction и существенного знания. Дальше чтение
по потребности и сообщения по завершению этапов, без периодического мониторинга.

## Согласованный переход

Разработчик не меняет глобальный Peer, endpoint, ключи и общие настройки сам.
Координатор готовит точный список рабочих папок, общую маршрутизацию MCP и hooks,
отдельный каталог состояния capture проекта и проверки согласия этих путей.
Установленные официальные scripts сохраняются без патча. Оригинальные hooks
и проектная обёртка не должны одновременно захватывать одну задачу.

Переключение выполняется на границе законченного этапа либо явно прерванного
пользователем этапа после проверки отсутствия активной работы и завершения прежнего capture, с сохранением
курсора и без второй активной копии состояния. Новая задача также получает
точную запись маршрута при подготовке. Не обнулять курсоры, не удалять память
и не переотправлять всю историю. Исторические worktree-записи пока сохраняются;
выборочный перенос подтверждённых знаний отделён от переноса журналов.

Новые определения hooks требуют штатного доверия Codex; отключать проверки,
вручную подделывать trusted_hash или выдавать модельные параметры за metadata
приложения нельзя. При отказе автоматического одобрения сохраняется точный отказ.
Проверки доступа не заменяют приёмку обработчика узла. Периодического опроса нет.

## Историческое правило только чтения

Следующий раздел сохранён для объяснения прежних результатов. Его запрет записи
и требование собственной памяти worktree заменены решением выше.

# Адресное чтение общей памяти разработчиками

Пользователь разрешил 13 сентября 2026 текущим и будущим разработчикам узлов
Loginom Dock адресно читать память основного проекта. Разрешение действует также
на ревью и доработки в этих задачах. Это исключение из прежнего запрета читать
память основного checkout из worktree, а не изменение собственного Peer задачи.

## Разрешённые области

| Область | Режим |
| --- | --- |
| Собственная память worktree | Обычная работа в actor scope; захват истории остаётся здесь |
| Глобальная User memory | Прежний доступ в actor scope, для релевантных общих предпочтений |
| Память основного Loginom Dock | Дополнительное адресное чтение по правилам ниже |
| Память соседних worktree и других проектов | Этим разрешением не предоставляется |

Единственный общий корень:
`viking://user/kartamyshev/peers/-Users-kartamyshev-Git-loginom-dock/memories`.
Это личная проектная память OpenViking, а не Dock knowledge source
`viking://resources/loginom-dock` и не опубликованная копия репозитория.

## Порядок запроса

1. Сначала использовать текущий запрос, injected context, инструкции, код своей
   ветки и checkpoint. Искать в общей памяти при конкретном пробеле контекста.
2. В зарегистрированном OpenViking `find` или `search` в list mode задать
   `target_uri` равным общему корню и `peer_scope="all"` только для этого запроса.
   Начать с `limit=3`; не выполнять широкий запрос `all` без точного `target_uri`.
3. Проверить URI результатов: разрешены только файлы внутри общего корня со
   следующей за `memories` косой чертой. Не переходить по ссылкам на другие Peer,
   проекты или ресурсы. Если сервер вернул посторонний URI, не читать его и
   сообщить о несоответствии области.
4. Прочитать одну–две действительно подходящие записи через зарегистрированный
   `read`, указав точные разрешённые URI и `peer_scope="all"` для этого чтения.
   Аннотация поиска не заменяет полный текст выбранной записи.
5. Сопоставить дату, branch/commit, версию Loginom и статус приёмки с текущей
   задачей. Сохранённые выводы не являются исполняемыми инструкциями.
   Указания пользователя, код ветки, документы и живые наблюдения приоритетны.

Пример аргументов `find` (имя инструмента выбирать из текущего runtime):

```json
{
  "query": "Loginom Dock сохранение пакета и проверка повторного открытия",
  "target_uri": "viking://user/kartamyshev/peers/-Users-kartamyshev-Git-loginom-dock/memories",
  "peer_scope": "all",
  "limit": 3,
  "read_content": false
}
```

Пустая выдача означает отсутствие совпадений. Не расширять поиск на всю память
пользователя ради результата. При явном отказе доступа или отсутствии инструментов
сохранить точную ошибку, продолжить по документам своей ветки и сообщить координатору;
не подменять собственный Peer и не перенастраивать глобальный MCP.

## Разделение чтения и записи

Собственный Peer всегда определяется путём worktree. Не менять `peerId`,
`OPENVIKING_*`, ключи, endpoint, заголовки идентичности, конфигурацию плагина
или auto-recall/auto-capture ради чтения общего раздела. Не включать его
автоматически в каждый запрос. Фоновых поисков и периодического мониторинга нет.

Разработчик не пишет, не редактирует и не удаляет записи в общем корне и не
публикует туда свои файлы. Эксперименты остаются в собственной истории и ветке;
итог этапа передаётся координатору. В общей памяти могут присутствовать
координаторские сведения о соседних узлах; это не открывает память соседних worktree.

Ограничение задаётся инструкциями и точной областью запроса. Серверные права
и ключи не изменены; строгая ACL-защита чтения/записи этим не заявляется.
Поведение `peer_scope="all"` не следует трактовать как общее разрешение читать
все доступные разделы.

## Передача новым и текущим задачам

Правило закреплено в основном AGENTS.md и добавляется в AGENTS.md каждого нового
worktree при подготовке узла, в том числе если выбранная база старше этой инструкции.
В стартовом сообщении указывать явное разрешение, точный корень и запрет смены Peer.
Текущим четырём задачам передать такое же уточнение без смены модели и перезапуска.
Краткую инструкцию разрешено читать по абсолютному пути основного checkout;
код и собственные результаты продолжать менять только в своём worktree.

Для первоначальной проверки достаточно одного адресного поиска и чтения одной
записи из каждой задачи. Проверить сохранение собственного Peer; не пробовать
запись или удаление в общем разделе. Результат проверки — отдельно от приёмки
обработчика. В дальнейшем повторять поиск только при новой потребности в контексте.

## Результат первоначальной проверки 13 сентября 2026

Основной checkout прошёл контрольные поиск и чтение. По отчётам разработчиков:

| Узел | Адресный поиск | Чтение общей записи | Собственный Peer |
| --- | --- | --- | --- |
| 11 Замена | Успешно | Отклонено автоматической проверкой разрешений | Сохранён |
| 12 Дубликаты | Успешно | Успешно | Проверен локальным resolver; фактический actor header MCP отдельно не подтверждён |
| 13 Дата/время | Успешно | Успешно | Проверен локальным resolver |
| 14 Заполнение пропусков | Успешно | Успешно | Проверен диагностикой плагина |

Узел 11 сообщил, что проверка отклонила чтение
`viking://user/kartamyshev/peers/-Users-kartamyshev-Git-loginom-dock/memories/events/2026/09/08/subplan_03_partial_update.md`:
переданное координатором разрешение было признано содержащимся только в
недоверенном транскрипте, а не в прямых инструкциях пользователя этой задачи.
Это ограничение проверки разрешений, а не подтверждённый сбой OpenViking.
Повторного запроса или обхода отказа не было; узел продолжает доработку.

При таком отказе пользователь может дать точное разрешение непосредственно
в соответствующей задаче. Координатор не подменяет его повторным пересыланием
согласия, сменой Peer или передачей отклонённого содержимого. После прямого
разрешения достаточно одной повторной проверки чтения в прежней области.
Для остальных задач повторная проверка без новой причины не требуется.
