# Слияние — последовательная реализация 12 сентября 2026

Статус: **in_progress**, не принято, не опубликовано. Пользователь запросил
выполнение `09-join.md`; это возобновляет только подплан09, не параллельный пилот.

## Проверенная исходная точка

Рабочая копия была чистой. Личный OpenViking health успешен, scoped find и чтение
опыта выполнены. Dock prepare успешен, skill `afa295bf48dc48da5d3c995665a06ef2190ff620bae3243d46557e0371536790`.
Через Dock прочитаны Help `data/processors/transformation/join/{README,inner,left}.md`,
E2E `bg/sels/transform/sJoinData.ts`, `tests/acceptance/wizards/joindata.ts`.

## Живое исследование

Loginom7.4.2, отдельный аккаунт `test-1`, каталог `/test-1` подтверждён в UI.
Видимый браузер запущен с `--start-maximized`, viewport=null, 1508×862.
Два независимо подготовленных CSV: слева6строк, справа5, оба импорта выполнены
через настоящий публичный MCP dispatch. У узла подтверждены оба входа и связи.

Мастер: `JoinDataWizard`, режимы0/1 — inner/left, явные два флага. Ключи находятся
в полных локальных `Ext.data.Store` через взаимный `ConnectedRecord`. Владение
сверяется с `@@TestCmpController`, `FSourceStore`, `FTargetStore`, `FWizardForm`.
`FLinks` и `FJoinedColumns` — RPC proxies, читать их свойства нельзя.
Два последовательных drag без ожидания не гарантируют вторую связь: после каждого
нужно дождаться полного ожидаемого множества пар. Контекстное удаление пары:
`mn;btnRemoveSelectedLinks`, проверено открытие меню.

Next → DoneWizard. После промежуточного Done отдельный выходной порт открывает
`DerivedDataSourceOutputSocketWizard`; без правых ключей наблюдались LKey,Part,
LValue,RValue. Составной ключ LKey↔RKey и Part↔PartR сохранён и прочитан после
повторного открытия мастера. Начальная гонка чтения свойств до стабилизации
отклонена; повторное стабильное чтение прошло без ослабления проверок.

Диагностический пакет сохранён через `package.save_checkpoint`:
`/test-1/packages/Join-diagnostic-20260912.lgp`. Квитанция подтверждает завершение
сохранения, но независимое чтение сохранённого результата пока не выполнено.
Первая диагностическая сессия закрыта штатно после сохранения.

## Исходники и проверки

Добавляются join-context/parameters/procedure/mappings/readback/node и регистрация
в общей оболочке. Расширяется проверяемый второй вход, API и схема readback.
Общий запуск, ожидание, output reading и журнал остаются прежними.
12 новых focused tests PASS;22 проверок API/сортировки/Close PASS.
Полный клиентский прогон, живая проверка полного handler и независимый аудитор
ещё впереди. Фикстуры пока диагностические: правило Null-ключей и независимый
oracle приёмки ещё не утверждены наблюдениями. Hermes не запускался.

Локальные материалы: `.dock/join-20260912/commands/`, исходное исследование
`.dock/process-node-focus-20260911/live-1789164933898/`. Последний каталог содержит
`imports-full.json`, `reader-stable.json`, `join-output.json`, `saved.json` и журнал.
Новые сессии запускаются через `tools/loginom-acceptance/join-live.mjs` и сохраняют
свои собственные source pins в `.dock/join-20260912/live-*/session.json`.

## Следующие проверки

1. Полный node.apply на текущих исходниках через MCP, оба режима, параметры,
   mapping обоих входов/выхода, Done/Close, сохранение/reopen/reexecute.
2. Предварительная проверка существующего узла до изменения связей; имена с
   коллизиями и изменение включения правых ключей, сохранённые output exclusions.
3. Независимый oracle, матрица данных и негативные подмены/отказы/потери ответа.
4. Финальный обычный Hermes openai-codex/gpt-5.6-sol/low, отдельный независимый
   аудит полного результата, затем итоговая документация. V4/V5 не закрывать.

## Дополнение: первые попытки полного цикла

Полный клиент:1328 PASS/1 SKIP,0FAIL (`client-full-unrestricted.log`). Первичный
sandbox-прогон имел9отказов локальных сокетов/clipboard (`EPERM`); повтор с
разрешёнными локальными сокетами прошёл. Это ещё не окончательная проверка кода.

- `live-1789165926106`: открытый диагностический пакет был read-only из-за старой
  серверной сессии; создана отдельная копия `/test-1/packages/Join-working-20260912.lgp`.
  При первом node.apply мешало оставленное оператором меню «Пакеты».
  Отказ до жеста открытия порта, настройки/выполнение не начались. Пакет закрыт.
- `live-1789166161752`: оба входных порта прочитаны и сохранены, настройки ключей
  прочитаны. Ошибка в join-node: готовность требовала node_join, но новое наблюдение
  его не запрашивало. Исправлено: перед validation.perform передаётся initialObservation
  с readJoin. Эта попытка осталась AMBIGUOUS; исполнения handler не было.
- Затем в той же отдельной диагностике Codex вручную выполнил Next/Execute и
  прочитал быстрый просмотр (`oracle-preview.json`). Left + составной ключ дал8строк:
  четыре A/1совпадения от2×2, a/1, B/1→Null, Null/1→nullright, A/2→part2right.
  **Null совпадает с Null** на проверенном Loginom7.4.2. Это отдельная проверка
  правила до фиксации приёмочного oracle, не успешная попытка полного handler.
  Пакет закрыт, затем браузер закрыт штатно.

## Полный цикл и дополнительные проверки (12 сентября)

- `live-1789166362796`, runtime `3e5b30fc0373a0c1d864d4ac823d0b1e22df6e770325f154af52e0f9f6354129`:
  полный existing Left/composite завершился SUCCEEDED, 8 строк прочитаны полностью.
- `live-1789166920357`, runtime `57708e2e09ae79ee43588888dd0f19b68d046a7968805784f75ec4f0bebef74f`:
  Inner + right keys SUCCEEDED (7 строк/6 полей), mapped Left SUCCEEDED (8 строк),
  переименования обоих входов, порядок/переименование/исключение выхода проверены.
  Preserve Done SUCCEEDED, execution not_requested / output not_refreshed.
- Предварительная проверка existing теперь читает и отменяет оба входных порта
  до настройки целевого узла. Для явной замены входа требуется полный input mapping.
  Общий UI и независимый auditor расширены для Close второго входа с его квитанцией.
- Независимый `join_configuration_evidence.py` принял Inner и mapped-проходы;
  `join_evidence_negative.py` отверг 11/11 подмен (`join-auditor-negatives.json`).
- Независимые fixtures/oracle созданы в `tools/loginom-acceptance/fixtures/join`.
  Эти CSV ещё не использовались для final acceptance; правило Null зафиксировано ранее.
- Обнаружено: при autosync=false включение right keys сохраняло неполный выход.
  Readback отверг его после выполнения (AMBIGUOUS, не принимать как успех).
  В UI проверена кнопка Synchronize: сохраняет aliases/exclusions и добавляет новые поля.
  Добавлен `join-output-sync.mjs` с проверкой сохранённых source field IDs и output IDs.
  Полный повтор этого исправления ещё не завершён. Диагностические изменения
  не сохранялись; рабочая копия закрыта с отменой собственных изменений.

Осталось: проверить output sync в полном цикле, new node/key edits, CSV matrix,
живые отрицательные случаи и recovery, readback tests, save/reopen/reexecute,
полный тестовый прогон, final Hermes Sol/low и независимый полный аудит.

## Сохранение, матрица и исправление удаления ключа

`live-1789167414555` (runtime `ed5fd2ccd3a79684a4a1ed0bdb52ee2c818ba8899ac4bba51f7bf2da0c01631f`):
- mapped и include-retained SUCCEEDED: синхронизация добавила правые ключи,
  сохранив aliases/порядок/исключение Part при autosync=false.
- Close SUCCEEDED, settings discarded, execution not_requested.
- checkpoint + save_as/reopen SUCCEEDED; reexecute SUCCEEDED, новый execution_id,
  8 строк, все настройки сохранены. Независимый configuration auditor принял
  mapped/include-retained/reexecute.
- Доставлены и выполнены 5 новых CSV матрицы: 5/4/4/3/0 строк.
  Одна навигация доставки right.csv получила UI_EPOCH_CHANGED до жеста
  (NOT_APPLIED, effect_possible=false, cleanup_complete=true, upload not started).
  После прямой проверки папки выполнена новая доставка того же разрешённого
  artifact/grant; повторной загрузки после возможной отправки не было.
- Новый Matrix-Join через MCP SUCCEEDED, Inner/composite 6 строк. Main-left 7 строк.
- Удаление Part→PartR остановилось после right_click: меню `mn` вне root мастера.
  Связь не удалялась. Прямой UI подтвердил выбранный Part и нужное меню.
  Исправлены root-discovery и выбор меню; reader проверяет selected_left перед удалением.
- Диагностический мастер отменён. Для продолжения сохранён через прямой UI
  `/test-1/packages/Join-matrix-20260912.lgp` (5 импортов + Matrix-Join), затем закрыт.
  Это только восстановительный артефакт, не автономная приёмка.
- Уточнение общего graph contract: занятый вход не переподключается к другому
  источнику. Матрица case/empty использует отдельные Join; существующий изменяется
  при сохранении своих связей. Ошибочное обещание rewiring убрано из skill/preflight.
- Полные тесты на промежуточном коде: 1333 PASS/1 SKIP; Python auditors 483 PASS.
  Последующие Join unit tests также прошли. После последних UI-правок нужен повтор.
- Добавлен goal-only сценарий и независимый `join_node_acceptance.py`, поддержка
  двух startup artifacts в общем harness. Preflight Sol/low прошёл; Hermes не запускался.

## Матрица завершена; подготовка автономной приёмки

`live-1789168545059`, runtime `1c8dbd6ee539d65036026df2f154558e8c5f9eb541a9c3166a61e31f6ef53a99`:
- Все шесть вариантов SUCCEEDED и независимо проверены по CSV: main-left 7,
  main-single 10, case-sensitive 2, case-insensitive 3, empty-inner 0, empty-left 5.
  После удаления второго ключа существующий выход сохраняет RValue перед вновь
  добавленным PartR; oracle проверяет этот порядок отдельно от порядка нового узла.
- Живые missing/type отказы: NOT_APPLIED, effect_possible=false, cleanup_complete=true.
  Чужой workflow и конфликт aliases отвергнуты публичной валидацией без browser calls.
- Общие негативные тесты дополняются Join-проверками потерянного ответа на связь
  и 11 отрицательными проверками независимого configuration auditor.
- Замер unchanged Done: полный локальный цикл 18 087 мс; совпадающие фазы
  open/configure/node_finish 3 804 + 1 952 + 1 752 = 7 508 мс.
  Прямая диагностика этих фаз: 2 289 мс, 4 browser MCP вызова. Это единичный
  диагностический замер: локальный цикл выполняет дополнительные guards/readback;
  ускорение wall time не заявляется. Внешние вызовы Hermes будут посчитаны отдельно.
- Диагностический пакет закрыт с отменой собственных несохранённых изменений,
  браузер завершён. Доказательства сохранены в `.dock/join-20260912/`.
- Python suite: 485 PASS. Первый полный клиентский повтор без sandbox: 1336 PASS,
  1 SKIP, 1 сбой неизменённого workspace deadline теста (2 мс); отдельный повтор
  workspace suite прошёл. Идёт полный подтверждающий повтор. Sandbox-вариант
  не применим к тестам системных lease/локальных процессов.
- Перед финальной приёмкой убран ненужный клон options в new-source preflight;
  используется тот же operation state общей оболочки. Финальный runtime будет
  закреплён заново, диагностические pins не подменяются.

## Первая автономная попытка: остановлена из-за каталога

Полный клиентский подтверждающий повтор: **1337 PASS /1 SKIP /0 FAIL**.
Hermes `20260912-023104-a840a04c`, Sol/low, runtime `25b841574df52d3871558ac00d0c5434b9588d96b04fdbddeee2e26f14cc55d0`:
оба импорта, Inner (10×5), изменённый Left (7×5), Done и Close SUCCEEDED.
Независимые промежуточные configuration/output проверки прошли.
Ошибочно выбран исторический каталог 2026.09.08 с allowed_roots=/user/dock-p3.
Запрошенный /test-1 путь отвергнут без эффекта. Hermes попытался подставить
старый разрешённый корень; Codex остановил группу приёмки SIGTERM. Последний
журнал содержит prepared для fallback save, без подтверждённого сохранения.
Попытка **не принята** (model/efficiency/nine_operations failed); результат
не выдаётся за подтверждённый пакет. Новые попытки используют другое имя.

В отдельном видимом Loginom `live-1789169887226` подтверждены `/test-1` и
его папка packages; /user отсутствует в доступном корне аккаунта.
Через Dock перечитаны manifest/actions уже опубликованного кандидата
2026.09.11-parallel-pilot.1-candidate, SHA
`4ac827fc9e0cefa609bf2cb8fd7d3d79318dd3fe92999decc7e385fae51fc6e2`:
оба save revision2 разрешают /test-1,/test-2,/test-3. Сервер не менялся.
Join harness теперь отклоняет другой manifest/SHA или несовместимый storage
до запуска модели. Skill запрещает заменять выбранное пользователем хранилище
другим allowed_root. Отдельный диагностический браузер закрыт.

## Вторая автономная попытка: полный сценарий; исправлен независимый аудитор

`20260912-024022-8602c3eb`: все9 операций SUCCEEDED, два save SUCCEEDED,
после reopen получены новые7×5. Runtime и harness во время исполнения не менялись.
Первый вызов полного аудитора выявил NameError в построении expected columns.
После устранения опечатки диагностическая проверка показала две старые предпосылки:
legacy public verifier не принимал user-v1, а save-chain разрешал overwrite
своего checkpoint только следующим checkpoint, но не запрошенным save_as.
Использован существующий независимый normalize_user_evidence, проверяющий точную
проекцию компактных ответов до legacy audit. Save-chain расширен только на файл,
доказанный непосредственно предшествующим успешным checkpoint; новые негативные
тесты отклоняют чужой путь, отсутствие квитанции и неполное подтверждение overwrite.
Все **54 проверки** записанного прогона теперь прошли. Это диагностическая
проверка исправленного аудитора, а не frozen acceptance: нужен финальный прогон
с его исправленной версией, закреплённой до запуска. Клиентский Join не менялся.

## Третья попытка: потеря браузерной сессии

`20260912-025102-d219edf4`, тот же runtime `c684d6c8d049fff0b7f8739f33443c5313852337c1e4997d1d216fb22e3ee1d2`:
до сохранения все6 узловых операций SUCCEEDED; checkpoint/save_as SUCCEEDED;
повторный левый импорт SUCCEEDED. Правый импорт остановился на output-mapping
Next: `Pinned browser capability returned no typed result`, AMBIGUOUS,
execution not_requested, pending retained. Последующие наблюдения показывают
новый document epoch, authenticated=false, затем target_state=not_open.
Причина потери страницы из доступного журнала не установлена; это не доказательство
неисправности Join. Повторы resume с pending отклонены; аудит попытку не принял.

Codex открыл сохранённый пакет в отдельной новой видимой сессии
`live-1789171150696`, воспроизвёл тот же переход правого импорта через source,
format и output-mapping Next: достигнут Done, авторизация сохранена.
Никакие настройки не менялись и запуск не повторялся. Диагностический мастер
отменён перед закрытием пакета. Старая неопределённая операция не объявляется
успешной. Следующая приёмка — новый изолированный браузер и новый пакет;
изменений Join и обхода unknown-effect guards нет.

## Завершение

Четвёртая попытка `20260912-030137-87f5043f`: **56/56 frozen PASS**.
Все9 операций, обе доставки и оба сохранения подтверждены, итог после reopen7×5.
Сводка требований, метрик и ограничений: [09-completion-audit](../plans/loginom-dock/09-completion-audit.md).
Подплан09 завершён в source runtime; production/client install не обновлялись.
