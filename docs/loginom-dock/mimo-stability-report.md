> **Пауза по просьбе пользователя, 17 сентября 2026.** [Передача MiMo в новую сессию](mimo-session-handoff-2026-09-17.md). Последний run19 остановлен; финальные38 запусков не начаты. Новая правка mapping-table-view — непроверенный черновик. Этот checkpoint заменяет прежние статусы active для MiMo.

## 2026-09-17: retry19 прошла прежний сбой; подготовлены oracle04/17

Run20260917-151127-b640c212 продолжает работу terminal5349.
EffectiveprofileVERIFIED в session20260917_151129_774ee0:
xiaomi/mimo-v2.5/medium. Native delivery/importPASS; calc-001 с формулой
revenue-budgetSUCCEEDED без квадратных скобок, replace:false. Это прогресс
на прежнем проблемном шаге, не доказательство полной устойчивости.
Группировки по channel и channel+target_audience начаты; суммы и средние
необходимо различать при аудите конечных коэффициентов. Busyзапрос возвращён
какrunning; пока не считать его отдельным окончательным отказом.
Клиент/плагины source952003a неизменны и заморожены до концаrun/reader.

Для оставшегося корпуса добавлены operator-only independent расчёты:
monthly_sales_descriptive(task04) и geographic_sales_efficiency(task17).
04: уникальные месяцы, max/min ranking, суммы/средние по годам,календарным
месяцам иholidayflag.17: store-level sales/area, суммы и обе различимые
агрегации эффективности по city,region,city+region. Эти числа не доказывают
причинность, влияние бюджета/факторов проверяется отдельно по выбранному
моделью сравнению. Модели oracle не передаются; это не бизнес-приёмка.
11testsPASS включают неравные размеры групп, ties, duplicate/zeroarea
отказы. На настоящихdatasets расчёты выполнены:04—36месяцев,
17—400магазинов. Артефакты task04-business-reference.json и
 task17-business-reference.json; состав итоговой серии не уменьшен.

Следующий шаг: продолжать наблюдение terminal5349, после завершения сверить
коэффициенты после агрегации и полноту всех бизнес-пунктов19; независимый
reopen по recorded reviewed plan. Общая серия38PENDING.

## 2026-09-17: восстановление Calculator подтверждено вживую; retry19

`live-1789646734022`, sourcec2fc8d93: отдельная recovery-check.lgp копия.
Синтаксическая ошибка квадратных скобок далаFAILED с проверенным восстановлением
blankExpr1. user-v1 next_step содержит replace:false/inputs:[]/mappings:[].
Затем invalidreplace:true при inputs:[] дал подтверждённыйFAILED; исправление
того же узла с replace:false далоSUCCEEDED. Узлов10/связей8, новых дублей нет.
Повтор успешногоID дал0браузерных действий (wirebefore=after992).
Три строки прибыли сверены с независимымDecimal, maxdelta3e-11;
общие120строк не проверялись целиком в этом адресном тесте.
`repair-audit.json` сохраняет точный объём доказательства.
Cleanup диагностической копииSUCCEEDED с явнымdiscard тестовых изменений,
logouttrue; исходные пакеты не менялись. Terminal4833 завершёнexit0.

Дополнительно blankExpr1 распознаётся после отклонённого запроса к EXISTING
узлу, а не толькоNEW; иначе второй отказ предлагал read пустого выражения.
64node-apply testsPASS включая new/existing и отрицательные проверки proof.
Ранее87адресных тестов schema/parameters/refusal/runtimePASS.
Повтор с явно переданными соединениями всё ещё не объявляется no-effect:
не ослаблены guards неизвестных изменений. Автономный эффект уточнений
нужно подтвердить новой модельной попыткой.

Task19 retry2 запущена: `20260917-151127-b640c212`, terminal5349,
source952003a177eb4485076e7e5c632ad936b6677ab502cb0294184e01338ca2f1d3, storage
/mimo/MiMo-task19-diagnostic-retry2-20260917 (UIsetup+mimologoutPASS).
Xiaomi/mimo-v2.5/medium, description+nativeCSV,120turns/3600s.
Не менять клиент/плагины до завершения модели и независимого reader.
Следующий шаг — profileverification и наблюдение terminal5349;
если повторяется blocker, прямое Codex воспроизведение перед новым запуском.
Предыдущий run19 exit130/teardownUNCONFIRMED сохранён, не перезаписан.
Итоговая серия38PENDING; цель не завершена.

## 2026-09-17: задача19 остановлена на восстановлении Calculator

Run20260917-145906-f6691f46:20вызовов,operatorSIGTERM,returncode130,
timeoutfalse. EffectiveprofileVERIFIED,source3c75709f. Не считать приёмкой.
Исходные500строк импортированы. Формула [revenue]-[budget] отклонена Loginom
из-за квадратных скобок; узел восстановлен до blankExpr1 с проверкой настроек.
Модель передала existingExpr1 с replace:true и повторной inputconnection;
получила Replacement input field is missing/AMBIGUOUS. Попытка создать другой
узел отклонена pendingguard. После повторных recovery попыток модель остановлена
для прямой диагностики. TeardownUNCONFIRMED/CLEANUP_OR_OPERATION_UNCONFIRMED;
сохранения нет. Журналы сохранены; calculator-recovery-chain.json содержит
tools-only цепочку. Не закрывать этот pending простым отменением мастера.

Причина неподтверждённого отказа: recoverRejectedRequest допускается только
для existingnode с inputs:[]/mappings:[]; модель явно повторила подключение.
replace означает замену INPUT поля, а не обновление существующего выражения.
Схема теперь объясняет это и bare field syntax; инструкция после syntaxfailure
явно указывает replace:false для blankExpr1 и inputs:[]/mappings:[] для
сохранения связей.87адресных schema/parameters/request-refusal/runtime testsPASS.
Это уточнение контракта, не доказанное исправление всего recoveryпути.

Прямой calculator-live запущен terminal4833 с отдельной копией:
/mimo/MiMo-task19-diagnostic-current-20260917/recovery-check.lgp
из сохранённой task47. Проверить READY и продолжить воспроизведение на копии.
Первый вызов с слишком узким storage отклонён до браузера; повтор storage/mimo
допускает оба явно заданных пути. Модельные пакеты не исправлять вручную.
Oracle19 создан только для оператора:5каналов,4аудитории,20сочетаний.
Общая цель/38итоговых прогонов остаются открыты.

## 2026-09-17: конкретизирована проверка выводов; task19 запущена

Hermes skill заменил общий совет о проверке всех строк конкретным способом:
рейтинги — из сортировки нужного уровня группировки, универсальные утверждения —
после вычисленной проверки исключений либо ограничить вывод конкретными строками.
Формулы/показатель/направление/условие по-прежнему выбирает модель. Ответов и
порогов задач в skill нет. Это гипотеза улучшения, ещё не доказанная приёмкой.

Новый run19 `20260917-145906-f6691f46`, terminal86663,PID4278,
source3c75709fb608c2026fa820af5eacc4a1b1ba7d76d96d534dbaacbd8ee40d5cb1.
Папка /mimo/MiMo-task19-diagnostic-current-20260917 создана в UI;
mimo,2044×1035,setup logoutSUCCEEDED. Native description+CSV,120turns,
3600s,xiaomi/mimo-v2.5/medium.24tools preflightPASS; effective profileVERIFIED
в persisted session20260917_145908_274a0c. Клиент/плагины заморожены до terminal
и независимого чтения.1campaign expectation+7corpus oracle testsPASS.
Dataset не имеет даты/activeflag: фильтрация года/активности отдельно не
доказуема; модель должна трактовать заявленную область исходного задания,
не придумывать фильтры. Влияние бюджета требует отдельного сравнения;
общая сводка сама по себе не доказывает причинность.

Уточнение контекста: userNodeTool projection даёт apply.inputSchema6303символа,
resume183; прежние21k относились к исходной диагностической схеме.
Hermes converter передаёт inputSchema иdescription, outputSchemaне передаёт.
Evidence user-node-schema-size-audit.json. Это размер символов, не токены и
не измерение фактического полного API запроса. Не считать перегрузку доказанной.

Следующий шаг: наблюдать run19 terminal86663, разбирать конкретный blocker
до повторного модельного запуска; после завершения получить reviewed plan и
полностью проверить outputs/settings/graph и бизнес-ответ.38final runsPENDING.

## 2026-09-17: полный диагностический аудит47 завершён; вычисления PASS, выводы FAIL

Read-only открытие блокировало не выполнение: мешало нативное предупреждение
об открытии только на чтение (toast), которое procedure считал незакрытым
диалогом. Текст и точный путь наблюдались в живом UI task47-toast-diagnosis.
Operator-only scenario-readonly-warning.mjs подтверждает только этот точный
warning при bound document/session/account/package/ReadOnly=true и отсутствии
running nodes. Для других сообщений закрытие запрещено.3теста PASS;
wrong-package live отвергнут, fresh exact warning ACKNOWLEDGED в следующем
реальном reader. Остальные диалоги/ошибки продолжают блокировать действия.

`task47-readonly-acknowledged`, terminal58698 завершён exit0,8выходов прочитаны
полностью;2544ячейки,284строки. Независимый Decimal расчёт всех таблиц,
сохранённые настройки и граф9узлов/7связей PASS. Runtime reader1ea6fa7,
model9283894b; по обоим manifest изменился ТОЛЬКО package-cleanup.mjs.
Аудит явно диагностический, не итоговая приёмка:
`diagnostic-table-settings-audit.json`. Cleanup/logout SUCCEEDED безdiscard.

Исходный бизнес-ответ остаётся FAIL: ложный перерасход HR/Operations во всех
кварталах; неверный список крупнейших квартальных перерасходов, пропущен
Sales Q1 879884.48. Это ошибки интерпретации верных сохранённых таблиц.
Серверный lock оригинала не снимался принудительно. Read-only копия позволила
закончить диагностику без изменения сохранённого сценария и чужих сессий.

В общий auditor добавлен явный отказ принимать read_only_diagnostic как
acceptance evidence;5corpus-table tests PASS. Ledger: диагностический FAIL47
отдельно,38итоговых запусковPENDING. Source runtime не менять молча в evidence.
Следующий шаг: отладить проверки модельных выводов по фактическим агрегатам,
затем основные задачи21/19/34/38 и зафиксированная итоговая серия.

### Уточнение результата read-only exploration

Reader6642 завершился exit1: NodeReadinessTimeout при подготовке process console
первого узла, до разрешённой мутации. Поэтому запуск узлов/полное чтение в
read-only режиме НЕ подтверждены; прежняя промежуточная формулировка о
выполнении чтения была предварительной. Не повторять неизменённый запуск.
Проверить последний UI snapshot и доступность native process console для
read-only пакета; прежняя серверная сессия остаётся отдельной задачей.

## 2026-09-17: финальная read-only cleanup проверка PASS; полное чтение выполняется

`task47-cleanup-final-live`: source1ea6fa706c9ac5093226b0a8976c0ae46c127a73badd0eb6f081e9a73712ab3d,
настоящая read-only копия, финальный параметризованный cleanup SUCCEEDED,
packages1→0,logged_out=true,unsaved_changes_discarded=false.
Model source9283894b сохранён отдельно; новый diagnostic plan не пригоден
для приёмки (`acceptance_eligible=false`). Серверный lock оригинала остаётся.

В scenario-reopen добавлен явный --read-only-diagnostic только для планов
acceptance_eligible=false. Обычный gate не ослаблен; индекс маркируется
`diagnostic-read-only-saved-graph`. Пробное полное чтение запущено в
`task47-readonly-exploration`, terminal6642. Подготовка прошла и выполняется
чтение импорта; ещё нет полного index/аудита. Клиент и плагины не менять,
пока reader не завершится. Никаких модельных запусков в этом этапе.

Дополнительная business ошибка47: крупнейший перерасход Sales Q1 879884.48
пропущен в ответе. Настоящие top3: Sales Q1,Operations Q3 763067.35,
HR Q3 708072.68. Evidence business-answer-audit.json дополнен.
Это проверка вывода, не изменение модели или сохранённого сценария.
Следующий шаг: дождаться reader6642; если full read/settings прошли,
сверить все таблицы независимо с явной атрибуцией новой версии reader.
Итоговые38запусков остаютсяPENDING.

## 2026-09-17: исправлено закрытие принадлежащей диагностике read-only копии

Живое наблюдение `task47-session-diagnosis2`: mimo,Loginom7.4.2,
2044×1035, один пакет task47,ReadOnly=true,running=false.
Диагностический повтор штатного ClosePackage с требованием ReadOnly=true
прошёл: package_closed=true,logged_out=true,unsaved_changes_discarded=false,
packages1→0 (operator evidence в этом каталоге). Редактируемый оригинал
и чужие сессии не закрывались. Серверная блокировка оригинала ещё не снята.

В package-cleanup добавлен явный diagnosticReadOnly (defaultfalse):
точные document/account/path/one-package/running/dirty guards сохранены;
тип доступа проверяется повторно после серверного чтения dirty-state.
scenario-reopen передаёт read-only режим только в cleanup своего prepared
пакета; сам аудит по-прежнему не принимает read-only открытие.
24адресных теста package-cleanup+managed-shutdown PASS с необходимым
доступом к loopback/дочерним процессам. Первые managed-shutdown проверки
в sandbox завершились таймаутом; повтор вне sandbox PASS.
Живой proof предшествовал параметризации и проверил тот же нативный путь;
свежий живой запуск финального параметризованного кода ещё требуется.

MCP schema investigation: Hermes tools/mcp_tool_schema.py::_convert_mcp_schema
передаёт name/description/inputSchema, не outputSchema. Поэтому полные58–87k
определения инструментов НЕ являются доказательством такого объёма на модели.
apply/resume inputSchema21k каждый остаются направлением измерения.

Следующий шаг: проверить финальную cleanup-реализацию вживую, установить
состояние старой серверной сессии после reboot без закрытия чужих пакетов,
завершить независимый аудит47 либо сохранить точный недостающий gate.
Task47 business FAIL остаётся подтверждён. Итоговые38запусков не начаты.

## Уточнение пользователя: причиной прерывания была перезагрузка Mac

Пользователь подтвердил перезагрузку компьютера. Первый independent reader
задачи47 оборвался по этой причине; само завершение процесса не является
доказательством дефекта Dock. Подтверждённое последующее наблюдение остаётся:
повторное открытие дало read-only пакет, а cleanup вернул
PACKAGE_IDENTITY_CHANGED. Нужно исследовать восстановление после потери
браузера/серверную блокировку и закрытие принадлежащей диагностике read-only
копии. Не приписывать причину блокировки конкретному компоненту до проверки.
Сохранённый пакет и model run сохраняются; новый модельный прогон не требуется.

## 2026-09-17: task47 — модель завершила, обнаружена ошибка выводов и блокировка аудита

Run `20260917-141717-072ebb4e` завершён: exit0, timeoutfalse,25вызовов.
Сохранение подтверждено; восемь завершённых узлов и семь связей включены
в `task47-current-plan-reviewed.json`. Независимые Decimal expectations:
120/120/1/23/6/4/6/4 строки по восьми выходам; это ещё не проверка пакета.
Аудитор поддерживает целочисленный ключ1 через ограниченный parser;5тестов PASS.

Business review FAIL: утверждение «Operations и HR стабильно превышают бюджет
во всех кварталах» противоречит данным: Operations Q1 −218167.25,
HR Q4 −591448.79. Зафиксировано в run/business-answer-audit.json.
Модельные выводы и сохранённый пакет не исправлялись вручную.

Первый независимый reader был прерван; handle отсутствует, проверка списка
процессов подтвердила отсутствие scenario-reopen. Полного index.json нет.
Повтор `task47-current-independent-retry1` завершился exit1: сохранённый пакет
открыт только для чтения, сервер удерживает блокировку прежней сессии.
Его cleanup BLOCKED/PACKAGE_IDENTITY_CHANGED, package_closed=false,
logged_out=false,unsaved_changes_discarded=false. Требуется диагностика
оставшейся серверной сессии и закрытия read-only копии; не считать аудит PASS.
Не выполнять слепые повторные открытия. Исходный пакет сохранить.

Измерены только экспортируемые определения node/delivery tools:
inputSchema apply21503/resume21379символов. Полные определения гораздо
больше из-за outputSchema; пока НЕ подтверждено, что outputSchema доходит
до модели через Hermes. Evidence node-tool-schema-size-audit.json.
Это направление диагностики контекста, не доказанная причина ошибок.

Итоговая серия по-прежнему38PENDING; source9283894b не менялся.
Inbox сверён:185resolved+7handled,46helperscompleted,необработанных нет.

## Задача02 прошла полную диагностическую проверку; выполняется47 — 17 сентября 2026

Task02 run `20260917-140005-59459432`, source9283894b:505.516с,
30tool calls/26API, exit0. Input90399/output9466/cache-read1611392/cache-write0;
эффективный профиль VERIFIED, исходники неизменны, save/logout PASS.
Независимый reader13749 завершён, cleanup PASS. Все8таблиц/924строки/6276ячеек,
500исходных и366строк2024года, суммы+количества транзакций по5категориям,
4регионам и20сочетаниям,3сортировки, настройки и7связей прошли проверку.
Выручка365345.55; maxdelta2e-11. Аудит:
`.dock/mimo-stability-20260916/task02-current-independent/full-audit-identity-checked.json`.
10намеренных нарушений в копиях evidence отвергнуты, оригиналы сохранены.

Итоговый текст:5категорий,4региона,top10сочетаний и4минимальных сочетания
численно и по порядку проверены. Полные20строк есть в сохранённом выходе.
Небольшие ограничения представления: округление до целых в перечне минимальных
сочетаний не подписано; общую фразу sample_complete следует ограничивать
перечисленными агрегатами (импорт/фильтр в модельном preview были частичными).
Это успешная диагностика, не один из38итоговых запусков. Ledger сохраняет
38PENDING и отдельную diagnostic_evidence.

Аудитор02 расширен под фактический допустимый граф: необязательный COUNT,
полуоткрытый интервал2024года, reviewed plan с полным списком читаемых выходов.
Проверяет все суммы/счётчики, labels, package/source/node identity, native settings,
порядок рейтингов; не требует непрошенный отрицательный порт фильтра и третью
сортировку как обязательную часть задания. Legacy путь сохраняет9портов.
6unit+7oracle tests и реальная positive/10negative проверка PASS.

Task47 запущена без изменения клиента: run `20260917-141717-072ebb4e`, terminal88722,
source `9283894bd2f8e093c3c45954754a6ce2e0527e3aa74277fce7c36e49990407d8`. Папка /mimo/MiMo-task47-diagnostic-current-20260917
проверена в UI (mimo,2044×1035,logout PASS). Исходные description.md+native CSV,
Xiaomi/MiMo/medium. Проверить профиль и продолжать88722; client/plugin не менять
до завершения модели и reader. Объём исходного задания/38серии сохраняется.

---

## Diagnostic15 завершён; исправлен ответ ожидания, запущена задача02 — 17 сентября 2026

Diagnostic15 `20260917-134439-d16b467e`:709.851с,24tool/API calls,
exit0, sourcebfebf452 неизменен, профиль VERIFIED; save/logout PASS,
discard=false. Input109201/output18038/cache-read1817152/cache-write0.
Материалы: completion-summary.json, business-answer-audit.json внутри run.
**Business FAIL:** матрица и общая кривая содержат только месяцы1–6 из12;
четыре разности M3−M6 искажены предварительным округлением (Cohort3:3.5 вместо3.6;
Cohort4:−0.9 вместо−1.0; Cohort5:11.7 вместо11.6; Cohort6:17.3 вместо17.2п.п.).
Общая разность M1−M6 добавлена без поля вычисления и равна30.6 вместо30.5п.п.
Направления сравнений верны; общая кривая представленных месяцев берёт исходные
суммы. Предупреждение Round было получено, но не исправило зависимые вычисления.
Полная saved-package проверка15 не выполнена: провал уже подтверждён формулами,
полным текущим выходом и итоговым текстом. Пропуск месяцев не принят как ограничение.

В этом же прогоне observe во время импорта получил AMBIGUOUS без next_step,
несмотря на известный active_node_job. Теперь actionReply для user-v1 переводит
именно request_rejected+известный running job в compact running с одним
`dock_node_wait` исходного ID. Raw receipt и диагностический профиль сохранены;
при неизвестном исходе остаётся inspect. 70адресных тестов PASS; на исходном
отказе проверена новая проекция (busy-observe-fixed-projection.json). Нового
живого повторения этого конкурентного отказа пока нет.

Чтобы не повторять один смысловой сбой удержания с общими инструкциями,
проверка продолжается на исходной основной задаче02 (это диагностика, не замена
объёма приёмки). Run `20260917-140005-59459432`, terminal12755, source
`9283894bd2f8e093c3c45954754a6ce2e0527e3aa74277fce7c36e49990407d8`. Папка /mimo/MiMo-task02-diagnostic-current-20260917 создана и проверена
под mimo, подготовительный logout PASS, окно2044×1035. Исходный description.md
и native CSV; профиль Xiaomi/mimo-v2.5/medium. Проверить фактический профиль,
2024год, полные суммы/рейтинги и сочетания категория×регион, сохранение и reopen.
Client/plugin не менять до окончания модели/аудитора. Полная серия38 ещё не начата.

---

## Пояснения к округлению и явным нулям; diagnostic15 запущен — 17 сентября 2026

Реальный reader повторно открыл diagnostic14 и прочитал12×6 итоговой кривой,
обе сохранённые формулы Round и расхождение M5/M9 подтверждены. Настройки не
менялись, cleanup/logout PASS. Материалы: task38-diagnostic14-rounding-live,
rounding-audit.json. Это адресная диагностика, не полная приёмка diagnostic14.
Help mathematical.md/Round согласуется с наблюдениями. Integration round.ts
покрывает RoundUp/RoundDown, не доказывает этот случай Round.

Новый calculator-precision-warning.mjs проецирует предупреждение только для
успешной операции, наблюдённого Calculator readback с тем же владельцем и
ведущего вызова Round; учитывает переименование выхода. Формулы и значения
не исправляет. Вложенный Round не анализируется: отсутствие предупреждения
не доказывает отсутствие округления. user-results дополнительно считает только
явные числовые нули прочитанной выборки, включая decimal-only форму. Число
нулей не означает пропуски/будущие периоды. Skill требует неокруглённых входов
для производных показателей и проверенного основания для направления сравнения.

Full client2065PASS/1SKIP; после уточнения decimal-zero повторены18адресных
тестов PASS. Реальная квитанция72строк сохраняет все значения и полноту,
33913байт, оба пояснения присутствуют: precision-and-zero-observed-audit.json.
Это проверка нового сериализатора на реальном прежнем readback, не модельная
приёмка. Исправления пока не доказывают устранение смысловых ошибок MiMo.

Diagnostic15: run `20260917-134439-d16b467e`, terminal7137, source
`bfebf45255f0fb3c5591773b4fae5cfb1a0cfd5e713b59597572c84930ffdc7a`.
Папка /mimo/MiMo-task38-diagnostic15-20260917 создана через UI, account mimo,
окно2044×1035, подготовительный logout PASS. Обычный Hermes Xiaomi/MiMo/medium,
исходный description.md и native CSV; новых подсказок/эталона в задании нет.
Следующий шаг — проверить эффективный профиль и наблюдать7137, затем проверить
результат. Client/plugin не менять до окончания модели и независимого reader.
Полная серия38 запусков ещё не начата.

---

## Diagnostic14 завершён: полнота улучшилась, смысловые ошибки сохранились — 17 сентября 2026

Run `20260917-132151-3a7507fe`:787.008с,27tool calls/27API, exit0,
timeout=false. Sourcece6d3b81 сохранён; эффективный профиль VERIFIED.
Input124032/output13183/cache-read2359296/cache-write0 из журнала Hermes;
не суммировать их как общий счётчик без определения провайдера.
Сохранение пакета и экспорт2CSV подтверждены. Подробный итог и teardown:
`completion-summary.json` внутри run.

Общая12-месячная кривая теперь вычислена, однако финальный ответ выбрал среднее
из уже округлённых когортных процентов: M5=41.27 вместо41.28 и M9=16.94
вместо16.95. Правильный avg_active_pct также рассчитан и прочитан моделью;
все12его значений сверены с CSV. Сохранены mean-live-preview/audit.json и
rounding-risk-audit.json. Нули снова без основания объявлены отсутствием
наблюдений; текст называет42.86% больше46.43%, а33.33% больше42.86%.
Добавлены дельты без соответствующих вычисленных полей. `business-answer-audit.json`
содержит подтверждённый FAIL, не полную приёмку всех требований.

Изменение skill обеспечило таблицу проверки и наличие общей кривой в одном
запуске, но **не предотвратило ошибки**. Причинный эффект и стабильность этим
не доказаны. Следующий шаг — диагностика происхождения округлённых показателей
и проверки сравнений; неизменённый Hermes повторять нельзя. План повторного
открытия diagnostic14 подготовлен, ещё не рассмотрен/не запущен. Отрицательный
бизнес-результат уже доказан исходными формулами и полным live-выходом.
Все модельные процессы этого этапа завершены. Ветка остаётся отдельной,
полная серия38 запусков не начата. Ничего не опубликовано и не слито.

---

## Diagnostic14 запущен после уточнения проверки результатов — 17 сентября 2026

Run `20260917-132151-3a7507fe`, terminal32639, source
`ce6d3b81aad3f99c59827c34c61eb926a40d45abfd18decb346c144e88a7da33`.
Клиент/plugin не менять до завершения модели и независимого аудита.
Папка `/mimo/MiMo-task38-diagnostic14-20260917` создана и проверена в UI;
окно2044×1035, подготовительный logout PASS. Эффективный agent profile
Xiaomi/mimo-v2.5/medium VERIFIED. Исходное задание и штатный CSV не менялись,
эталон/готовый граф не передавались. Изменена только общая инструкция проверки
результата перед сохранением. Импорт, Collapse и выделение номера месяца,
группировка когорта×месяц прошли; калькулятор процентов ещё выполняется.
Модель округляет процент в формуле: проверить производные показатели по
исходным счётчикам. Итоговая успешность ещё неизвестна.

Полные source tests2060PASS/1SKIP/0FAIL; audit-retention5, audit-tables5,
inline-secret-redaction2, effective-profile3 PASS. Inbox192события:
185resolved+7handled,46helper assignments completed, необработанных нет.

---

## Diagnostic13: сохранённые данные верны, полнота задания не пройдена — 17 сентября 2026

Run `20260917-125531-4e0733cf` завершён:712.221с,25вызовов инструментов,
25API-вызовов, exit0. Профиль Xiaomi/mimo-v2.5/medium подтверждён настройкой
агента; семантика effort на стороне провайдера отдельно не доказана.
Сохранение и штатный logout PASS. Независимый reader session30428 завершён,
cleanup PASS, настройки4узлов,3связи и все9872ячейки четырёх таблиц проверены.
Доказательства: `.dock/mimo-stability-20260916/task38-diagnostic13-independent/`
`numerical-audit.json`, `configuration-audit.json`, `index.json`.
Максимальное численное расхождение1.05e-14; импорт596×16 точный.

**Business FAIL:** вместо общей кривой построены6кривых по acquisition_month;
нулевые значения без подтверждения названы отсутствующими наблюдениями;
утверждение об оттоке всех когорт противоречит росту Cohort4 M3→M6 на0.96п.п.
108чисел/позиций итогового текста проверены, но это не снимает смысловые ошибки.
`business-answer-audit.json` обновлён результатами повторного открытия.
Повторный безопасный экспорт завершён, `post-export-redaction.json` PASS.
Source307fa0c6 больше не занят моделью или аудитором.

В Hermes skill уточнена наблюдаемая проверка перед сохранением: каждый пункт
задания сопоставляется с узлом, полями и смыслом строки; среднее требует единицы
усреднения/весов, универсальные утверждения — проверки исключений. Это изменение
инструкции, его влияние на MiMo ещё не проверено. Эталон/готовый граф модели не
передаются. Полная серия38 запусков не начата; диагностический успех не включён
в её метрики.

---

## Diagnostic13 продолжается: матрица проверена по CSV — 17 сентября 2026

Run `20260917-125531-4e0733cf`, PID53764, terminal session19434 живой при
последнем опросе. Source307fa0c6 не менять до окончания. Импорт и широкая
когортная группировка завершены автономно; все72 коэффициента (6когорт×12месяцев)
сверены с CSV, maxdelta6.92e-17. Это проверка полного live-preview, не повторное
открытие сохранённого пакета. Материалы: matrix-live-preview.json и
matrix-live-audit.json внутри run. Средняя кривая group-avg-retention-001:
фазы до execute включительно подтверждены; идёт чтение выхода. Последний
безопасный экспорт показал17tool calls без ошибок Dock.

В обычном Hermes CLI служебная заметка о повторе ответа может содержать JSON
аргументов с session capability. Экспортёр теперь скрывает такие значения и
внутри текстовых заметок;2негативных теста и13history tests PASS. Текущий
запускатель загрузил прежнюю функцию clean **до исправления**. После завершения
обязательно повторить безопасный export_history и редактирование evidence.json
через актуальный clean, не меняя остальные данные. Это удаление секретов,
не перезапись результатов прогона. Требование записано в post-export-actions.json.
Не печатать сырые текстовые notices или аргументы со служебными токенами.

Сформирован `.dock/mimo-stability-20260916/final-acceptance-ledger.json`:
38слотов, все PENDING, итоговая ревизия ещё не назначена;24исключённых задачи
с причинами и хешами. Диагностические прогоны не повышают долю итогового успеха.

---

## Продолжение входного мастера, полный ответ72×6 и исправление профиля Hermes — 17 сентября 2026

**Полная приёмка не пройдена; серия38 запусков ещё не начата.**

- Восстановление после подтверждённого Input Done подключено к Calculator и
  Filter через существующий generic input recovery. Исходный operation_id,
  completed browser receipt, источник/связь и сохранённые mappings проверяются
  до продолжения. Без подтверждённой квитанции повторный Done не разрешён.
- Живые fault-injection прогоны source
  `a61d378dda239a1b611d714b927ba01bb52ee10e588d52ad6fe7f7d5948e0c60`:
  `.dock/text-export/live-1789637923482` (Filter) и
  `.dock/text-export/live-1789638101972` (Calculator). Обрыв транспорта после
  успешного Input Done + потерянный ответ -> AMBIGUOUS -> inspect -> resume
  исходной операции -> SUCCEEDED. Ровно1новый узел/1связь, same-ID без новых
  browser calls. Фильтр12×6/72ячейки, калькулятор72×6/432ячейки полностью
  сверены с CSV; maxdelta8.46e-15 и1.05e-14. Источник предварительно выполнял
  оператор; это адресная диагностика, не автономная модельная приёмка.
  Временные узлы удалены штатным discard только диагностической копии;
  cleanup/logout обоих сеансов PASS, браузеры закрыты. Client2059PASS/1SKIP.
- Числа preview сокращаются до более короткой десятичной записи только при
  Object.is round-trip в тот же binary64 (signed zero сохраняется). Бюджет
  основного ответа46K оставляет4K до Hermes spillover. Реальная таблица72×6
  теперь целиком помещается:45305символов с обёрткой, все432значения и признаки
  точности неизменны. 13адресных preview/user tests PASS; native full evidence
  не сокращается. Большие таблицы могут по-прежнему быть явно неполными.

### Ошибка профиля в старом запускателе

Diagnostic12 `20260917-124740-c2311a04`, source
`307fa0c6881eeccd98d9462682e252cf41a7cf6fc60544d33547998cd4e9813a`,
остановлен оператором: argv содержал --reasoning medium, но установленный
Hermes `-z` создаёт AIAgent без reasoning_config и max_iterations.
DB подтвердил xiaomi/mimo-v2.5, reasoning_config=null. Это не корректная
проверка заявленного профиля; исторические -z прогоны также не доказывают
эффективный medium. Их наблюдения о Dock сохраняют диагностическую ценность.
8tool calls, exit-15, timed_out=false; import выполнен, unpivot первоначально
отклонён до эффектов за отсутствующий empty policy. Сохранение не подтверждено,
shutdown receipt BLOCKED/CLEANUP_OR_OPERATION_UNCONFIRMED. Архив, причина
остановки и usage-recovery сохранены; private cleanup выполнен запускателем.

Запускатель теперь использует `hermes chat --quiet --query-file ...` с явными
--reasoning medium/--max-turns120; стандартные hooks/skills/вложения сохранены.
Общий Hermes не изменялся. Эффективный профиль читается из session SQLite
без сообщений/prompts; acceptance-plan требует VERIFIED. Счётчики расхода
берутся из той же безопасной выборки при отсутствии usage-file. 12Python tests
(profile, audit-plan, usage, teardown) PASS. Проверка agent configuration сама
по себе не доказывает поддержку уровня effort на стороне API провайдера.

Диагностика13 подготовлена в `/mimo/MiMo-task38-diagnostic13-20260917` и
запущена новым маршрутом: run `20260917-125531-4e0733cf`, PID53764,
terminal session19434. Профиль xiaomi/mimo-v2.5 и agent reasoning
`{enabled:true,effort:medium}` подтверждён живой SQLite; sidecar
profile-verification-start.json. Source307fa0c6 зафиксирован до завершения.
Следующий шаг: наблюдать результат и независимо проверить сохранённый пакет
и финальный текст. Неподтверждённый Input Done с оставшимся мастером, сохранение
поздних узлов при остановке и очень широкие/длинные ответы остаются открытыми.

---

## Восстановление исходной сессии: адресная живая проверка — 17 сентября 2026, 12:36 MSK

**Полная приёмка ещё не пройдена.** Новый source
`cff827b868a1d97604e914358ee4d1079c7b996db79f8521fe03277c550ee874`,
ветка `codex/mimo-scenario-stability`; main, сервер и общий плагин не изменялись.

### Подтверждённое поведение

- Loginom различает штатное закрытие WebSocket и аварийную потерю транспорта:
  OnTransportErrorHandler предлагает восстановление при `WasClean=false`.
  Штатный close(1000) даёт отдельную ошибку; это не эквивалент сетевого разрыва.
  Наблюдение и read-only исходники native обработчиков:
  `.dock/text-export/live-1789636882745`. Его браузер закрыт, но server logout
  не подтверждён; диагностическая копия после этого оказалась read-only.
- Browser offline не разорвал установленный WebSocket за12с. Для воспроизводимой
  диагностики добавлен локальный proxy: только целевой HTTP origin/8080,
  управление одним браузером, обрыв его tunnels, без журналирования данных.
  Это operator-only инструмент; настройки стенда и Hermes не меняются.
- `.dock/text-export/live-1789637317579`: обрыв одного tunnel вызвал штатное
  восстановление. После нажатия сохранены ссылки connection/remote/session,
  document и package. Cleanup/logout PASS, discard=false.

### Изменение клиента

`connection-recovery.mjs` вызывается существующим storage identity guard перед
операцией. При Connected=true запоминает локальные ссылки сессии и пакетов в
подготовленном документе. При аварийном разрыве проверяет прежний origin/build,
account/document, native session, объекты и пути пакетов, точный видимый диалог.
Затем резервирует единственное нажатие «Восстановить» и ждёт подтверждения
Connected=true/FReconnecting=false. Подмена сессии, другой диалог, отсутствие
исходного наблюдения, повтор неизвестной попытки дают отказ до кода вызывающего.
Ссылки и native session объекты не сериализуются. Код вызывающего не повторяется;
операционные pending checkpoints не снимаются восстановлением связи.

### Проверки

- Полный client: **2059PASS /1SKIP /0FAIL**. Первый sandbox-прогон упал на
  EPERM локальных портов; повтор с разрешёнными loopback-портами прошёл.
  26 адресных storage/reconnect/browser tests PASS; proxy test PASS.
- `.dock/text-export/live-1789637513338`, source выше: guard самостоятельно
  восстановил связь после аварийного обрыва, сохранив исходную сессию/пакет.
- После восстановления public node operation создала и выполнила фильтр:
 12×6, ровно1новый узел/1связь; same-ID не вызвал новых действий. Все72ячейки
  сверены с исходным CSV, max delta8.46153846154e-15. Источник предварительно
  выполнен оператором; это адресная проверка, не автономная приёмка Hermes.
  Первый operator preexecute после второго разрыва закономерно заблокировался
  диалогом (он использует raw diagnostic execute); после guard успешно продолжен.
- Потеря ответа после реального восстановительного нажатия: вызывающий код не
  запущен, последующий guard сверил завершённую исходную попытку; нажатие ровно1.
- Итоговый cleanup/logout PASS; discard=true относится только к временному
  новому фильтру в `reconnect-proxy1.lgp`. Все браузеры этого этапа закрыты;
  оригинальный `scenario.lgp` не менялся. Source не менялся во время live.

### Следующая точка продолжения

1. Проверить разрыв в середине node operation и продолжение исходного pending:
   восстановление связи уже готово, но незавершённый жест без receipt ещё нельзя
   считать выполненным. Существующий input recovery включён только для
   Missing values; generic boundary уже существует. Filter/calculator и
   незавершённый input Done требуют отдельной проверки/доработки.
2. Решить сохранение незаписанных поздних узлов при остановке и чтение широких
   таблиц (72×6 превышает основной ответ); не смешивать выборку с полной таблицей.
3. Затем новый Xiaomi diagnostic и независимый аудит графа/таблиц/финального
   текста. Полная серия38 запусков пока не начата.

---

## Diagnostic11: выявлен разрыв соединения; исправлены контракт фильтра и диагностика — 17 сентября 2026

**Полная приёмка не пройдена.** Изменения только в `codex/mimo-scenario-stability`;
main, общий плагин и сервер не обновлялись.

### Модельный прогон

- Run `20260917-112322-218e953e`: Xiaomi / mimo-v2.5 / medium, source
  `d6731cb4ce2bcac938569521e669dafdb647dc5679e23bf8245c663c4169c8b0` неизменен.
  35 tool calls, 1485.068с до операторской остановки, exit -15, timeout=false.
  Исходное description.md и штатный dataset; ни oracle, ни готовый граф модели не давались.
- Все 72 строки матрицы коэффициентов сверены с исходным CSV: PASS,
  max delta 6.93e-17. Проценты в более широкой таблице вернулись 70/72 строк;
  неполнота явно обозначена. Это остаётся ограничением объёма, а не полной таблицей.
- Два отказа фильтра до эффектов: неверная структура groups и требование обоих
  выходов. Один запрос во время работающей операции отклонён общей блокировкой;
  модель дождалась нужной операции и продолжила без дубликатов.
- После первого save MiMo добавила M3/M6-фильтры. Затем между успешным M6 и
  запросом слияния прошло 389.552с без tool calls. Новый запрос и последующая
  инспекция упали с `Pinned browser capability call failed`. Операция слияния
  остановилась на workflow, до создания узла. После повторного отказа процесс
  остановлен для диагностики; итогового ответа нет. Улучшение финального текста
  этим прогоном не доказано, даже несмотря на доставленные reporting_constraints.
- Пакет `/mimo/MiMo-task38-diagnostic11-20260917/scenario.lgp` повторно открыт
  через отдельную копию: 8 созданных узлов + переменные, 7 связей. Два поздних
  фильтра в сохранённом файле отсутствуют. Полный независимый аудит этого пакета
  не заявляется. Original session cleanup UNCONFIRMED; диагностический logout
  не подменяет это свидетельство.
- Безопасный экспорт и private cleanup PASS. `usage-recovery.json`:32 API calls,
  input143703/output17276/cache-read2999808; итоговая сумма/цена не выводятся.

### Живая причина и границы вывода

В `.dock/text-export/live-1789635228922` при завершении мастера входного порта
воспроизведён `WIZARD_CONTEXT_CHANGED`; затем cleanup ошибочно дал ACCOUNT_CHANGED.
Прямое наблюдение показало прежние document_id/account=mimo, но Connected=false.
UI показывал «Обнаружен разрыв связи. Восстановить сессию?». Штатное восстановление
вернуло Connected=true и прежний открытый мастер. Инспекция исходной операции
при этом правильно оставила pending, без доступного resume: восстановленная связь
не доказывает завершение жеста. Оператор отменил только мастер диагностической
копии, сверил прежний граф9/7; cleanup после восстановления SUCCEEDED, без discard.

Это подтверждённый класс сбоя связи и ошибочная его классификация. Нельзя утверждать,
что именно он вызвал исходный отказ Hermes: старая версия отбросила текст ошибки
браузера. Причина транспортного разрыва (стенд/сеть/клиент) ещё не локализована.
Не менять сетевые/серверные настройки на основании этой гипотезы.

### Исправлено и проверено

- Filter execute допускает выбранный непустой набор выходов [0], [1] либо [0,1].
  Параметры фильтра и настройки обоих выходов не изменены; defaults прежние.
- Ошибки browser capability получают фиксированную безопасную категорию:
  disconnected, identity changed, browser closed, context destroyed, connection
  closed, timeout, receipt capacity, unclassified. Сырые страницы/скрипты/секреты
  не передаются в сообщение. Реальный MCP-ответ с отказом identity guard
  воспроизведён и классифицирован, unit-negative проверки утечек PASS.
- Storage identity guard различает прежнюю личность с отключённым соединением и
  смену личности. Cleanup сообщает CONNECTION_DISCONNECTED отдельно от ACCOUNT_CHANGED.
  Это диагностика; автоматического восстановления и снятия pending блокировки нет.
- Экспортёр истории сохраняет dock_node_read и прямым вызовом, и через router;
  11 Python history/compaction tests PASS. В diagnostic11 read не вызывался.
- Итоговый source `ada1c8145312d39553d5414359dd032def7121d42f7a7fbeaaa44a20486e1908`:
  полный client2046PASS/1SKIP/0FAIL. Живой public MCP в
  `.dock/text-export/live-1789635606445`: новый фильтр с read.ports=[0],12×6,
  все72ячейки PASS (max8.47e-15), один новый узел/связь, same-ID без браузерных действий.
  Источник предварительно выполнен оператором; это адресная живая проверка,
  не автономная приёмка Hermes. Диагностический новый узел отброшен только в копии;
  cleanup/logout SUCCEEDED. Все диагностические браузеры закрыты.

### Следующая стадия

1. Без нового Hermes сначала локализовать разрыв соединения и реализовать bounded
   восстановление исходной сессии с проверкой личности/документа/пакета и сверкой
   незавершённого жеста. Не снимать блокировку одним закрытием мастера.
2. Учесть безопасное сохранение при остановке после промежуточного save: в этом
   запуске два последующих узла не попали в файл. Не считать первый save достаточным.
3. Рассмотреть ограниченную проекцию/постраничное чтение широких таблиц: 70/72
   строки нельзя объявлять полной выборкой. Текущий лимит остаётся явным.
4. После адресных live-проверок провести следующий Xiaomi diagnostic, затем
   полный независимый аудит результата и финального текста. Итоговая серия38
   запусков ещё не начата; новую модельную попытку не использовать вместо диагностики.

## Контрольная точка: чтение выхода без мастера — 17 сентября 2026, 11:00 MSK

Добавлен `dock_node_read`: новый operation_id, source_operation_id успешной локальной
операции и необязательные настройки чтения. Формулы, связи, mappings и finish модель
не передаёт. Общая блокировка, квитанции, ожидание и идентификатор свежего исполнения
сохранены. Конфигурация результата имеет status `not_requested`; source schema
проверяется по именам, меткам и типам, включая допустимую перестановку столбцов.
Полное native-чтение через этот API не заявляется: максимум 100 строк с явной полнотой.

Живая проверка выявила два последовательных дефекта нового маршрута: уже активный
узел нельзя просто выполнить повторно; после активации внутренний native.tooltip
остаётся «Выполнить узел», хотя отображаемая подсказка и значок уже означают деактивацию.
Для read-маршрута добавлена защищённая деактивация выбранного узла перед свежим запуском.
Проверяются конкретный узел, текущая подсказка и значок, состояние перед жестом;
потеря ответа запрещает повторный жест. Обычный launch не получил автоматическую
деактивацию. Сверено с Help interface/workflow.md, interface/packages.md и E2E
issues/00k/00k0/441.ts, issues/08k/08k0/8447.ts. Деактивация может деактивировать
потомков по штатной семантике Loginom; read не выполняет повторный экспорт файлов.

### Проверено на source 8f4113a761b796e9f6670c76ed5cb2a61b60a2d7d996d742e6d5e4d5ef5d70c2

- Публичный MCP, `.dock/text-export/live-1789631647451`, Loginom7.4.2/mimo,
  только `/mimo/MiMo-task38-diagnostic10-20260917/read-route-diagnostic-copy.lgp`.
- 12×4: PASS, 20.950с. 72×5: PASS, 35.079с. Настроечные мастера не открывались;
  граф/координаты/связи не изменились; same-ID повтор и неизвестный source не вызвали
  браузерных действий. Начальная успешная операция для source получена публичным apply.
- Независимый аудит всех 408 ячеек по исходным CSV-расчётам: PASS,
  максимальная абсолютная разница 1.2484e-14. `read-numeric-audit.json`.
- Пользовательский MCP-ответ: все 72 строки, sample_complete=true,
  37 423 символа с обёрткой Hermes, без потери точности. `read-compact.json`.
- Cleanup SUCCEEDED: диагностическая копия закрыта, logout подтверждён,
  изменения диагностических просмотров отброшены явно; исходный пакет сохранён.
- Два ранних FAIL сохранены в live-1789631218273 и live-1789631477595, оба закрыты
  с подтверждённым logout. Ошибка первоначальной проверки сравнивала изменяемый
  header_tid окна просмотра; сравнение исправлено на полную идентичность полей/ячеек.
- Полный клиент: 2038 PASS / 1 SKIP / 0 FAIL; 16 сценарных Python тестов PASS,
  включая сохранение аналитических параметров при добавлении read в план аудита.
  Исправлено также булево acceptance_eligible (раньше перекрывалось словарём операций).

### Продолжение

Нового Hermes-прогона в этой контрольной точке нет. Read API принят локальными и
живыми проверками, но его самостоятельное использование MiMo ещё не проверено.
Diagnostic10 остаётся численно корректным, общей бизнес-приёмкой FAIL из-за четырёх
дельт в тексте и неподтверждённой трактовки нулей. Следующий шаг: разобрать привязку
численных утверждений итогового ответа к выходным данным и провести diagnostic11
на неизменной версии (Xiaomi/mimo-v2.5/medium), затем независимый аудит.
Итоговая серия 38 запусков ещё не начата. Не заявлять ускорение всего сценария по этим
двум измерениям; это время только повторного выполнения/чтения. Продолжение после
неопределённого эффекта, таймаут при открытом мастере формата и безопасное завершение
прерванного несохранённого Hermes остаются отдельными ограничениями.
Все браузеры этой диагностики закрыты. Main/публикация/общий плагин не менялись.

## MiMo создала проверенный численный сценарий; текстовая приёмка ещё FAIL — 17 сентября 2026

**Подтверждено улучшение технического маршрута, но полная задача ещё не принята.**
Ветка `codex/mimo-scenario-stability`, source
`a8b24f4f5b0252b6b69e90436fa222fd8ad050dc03e95a26b4048d5216c87513`.
Main, общий плагин и развёрнутый сервер не обновлялись.

### Исправлено и проверено

В diagnostic9 ответ с 72 строками занимал 61 КБ. Hermes переносил его в файл,
для которого у модели не было инструмента чтения. Обычный предпросмотр теперь
удаляет только доказанно избыточные представления значений, сохраняя точность,
NULL и полноту. При вынужденном сокращении строк полнота явно снимается.
Полная исходная квитанция остаётся локально. Native full-table доказательства
не сокращаются; возможное превышение ими лимита остаётся отдельным ограничением.

На живом Loginom проверены 360 ячеек; штатная функция ограничения Hermes сохранила
ответ inline. В новом автономном прогоне модель получила все 72 строки (40 155
символов), продолжила вычисления, создала среднюю кривую и два экспорта. После
ошибки неизвестной Length она исправила формулу в том же калькуляторе. Ошибки
finish/order также исправлены без ручной помощи и без дублирования узлов.

### Завершённый прогон и независимая проверка

- Run `20260917-044845-ed4e92ee`: Xiaomi / mimo-v2.5 / medium, 31 вызов инструмента,
  1199.624 секунды, returncode 0, исходники неизменны. Девять созданных узлов,
  восемь связей; десятый узел — штатные переменные сценария.
- Пакет `/mimo/MiMo-task38-diagnostic10-20260917/scenario.lgp` сохранён;
  закрытие и logout подтверждены без отбрасывания изменений.
- Независимый аудит `task38-diagnostic10-independent3/independent-audit.json`:
  пять таблиц целиком — 596×16, 72×4, 72×5, 12×3, 12×4; настройки всех девяти
  узлов и неизменность связей PASS. Максимальная численная разница 1.25e-14.
- Два промежуточных выхода по 7152 строки: проверены исполнение и настройки;
  их полное чтение не заявляется. Все конечные таблицы прочитаны полностью.
- Отдельный аудит задачи подтвердил все 72 значения матрицы, все 12 значений
  взвешенной кривой и хронологический порядок месяцев. Оба CSV проверены целиком.
- В независимом аудите разрешена подтверждённая перестановка именованных полей
  группировок и их калькуляторов. Типы/подписи/значения проверены строго; задача
  не фиксирует порядок этих столбцов. Первые два частичных аудита сохранены,
  каждый завершён подтверждённым logout. Итоговый аудит проведён с нуля.

Клиент: **2030 PASS / 1 SKIP / 0 FAIL**. Адресные проверки аудитора: 28 Python
численных, 15 Python сценарных, 9 проверок ожиданий и 6 JS scope/reveal — PASS.
`git diff --check` PASS. Проверены восстановленное происхождение калькулятора,
повторное чтение без потери формул и явное перечисление непрочитанных промежуточных
таблиц. Эти проверки не равнозначны всей итоговой серии корпуса.

### Оставшиеся ошибки и точка продолжения

**Общая бизнес-приёмка diagnostic10 — FAIL.** В тексте четыре дельты M3→M6
посчитаны из округлённых процентов: ошибки по 0.1 п.п. для когорт 3–6.
Само удержание M3/M6 и рейтинг по M6 верны. Утверждение о поздних нулях как
об отсутствии наблюдений не доказано отдельной маской/датой среза и противоречит
включению этих значений в среднее. Evidence: `business-answer-audit.json`.

Далее: устранить вычисление новых чисел из округлённого текста и неподтверждённую
интерпретацию нулей; упростить повторное чтение существующего выхода без лишних
параметров настройки; улучшить диагностику order (только имена выражений).
Открыты также безопасное завершение прерванного несохранённого сеанса, продолжение
таймаута чтения с открытым форматированием и preflight пересечения явных координат.
Новый модельный прогон в момент этой записи не запущен, диагностические браузеры
закрыты. Итоговая серия **38 прогонов ещё не начата**. Пропуски задач не подменяют
неуспешные прогоны; универсальная надёжность не заявляется.

---

## Ответы предпросмотра проходят ограничение Hermes — 17 сентября 2026

Diagnostic9 `20260917-041651-3d5f6593` остановлен на подтверждённом сбое
доставки ответа: 29 вызовов, 808.765 секунды, returncode -15, исходники неизменны.
Фактическая модель Xiaomi / mimo-v2.5 / medium. Сохранение пакета и logout
не подтверждены: cleanup требует подтверждённого сохранения. Это не успех.

MiMo исправила синтаксическую ошибку SubStr и продолжила на том же калькуляторе
без дубликата; затем выполнила группировку, расчёт удержания и экспорт. Ответ
чтения 72 строк локально завершился успешно, но занял 61 018 символов. Hermes
перенёс его в spillover-файл и предложил read_file, отсутствующий в профиле.
Модель стала обращаться к неподходящим инструментам чтения. Корень этого сбоя
— доставка результата, а не неуспешное вычисление Loginom.

Добавлен бюджет обычного предпросмотра: убираются доказанно избыточные
представления значений, сохраняются тип, точность, NULL и точное значение.
При необходимости сокращается только число целых строк с явным признаком
неполноты; исходная полная квитанция сохраняется локально. Native full-table
доказательства не сокращаются. Их возможное превышение лимита остаётся открытым.
Уточнены инструкции чтения decimal и обязательность трёх аргументов SubStr.

Проверено на source `a8b24f4f5b0252b6b69e90436fa222fd8ad050dc03e95a26b4048d5216c87513`:
- Клиент: 2030 PASS, 1 SKIP, 0 FAIL.
- Повтор исходного большого ответа: 61 → 38 КБ, все 72 строки сохранены.
- Реальный Loginom, `live-1789609210796`: 72×5, все 360 ячеек совпали с полной
  квитанцией, sample_complete=true; создан ровно один узел и одна связь.
- Реальная функция ограничения Hermes с настройками по умолчанию сохранила
  ответ 41 920 символов inline без переноса в файл.
- Диагностическая копия закрыта с разрешённым discard; logout подтверждён.

Операторский аудитор дополнен явной областью проверки промежуточных узлов:
исполнение и настройки обязательны, пропуск полной промежуточной таблицы
возможен только в проверенном плане с достижимым проверяемым конечным выходом.
Исходный импорт и конечные таблицы не исключаются. Добавлена строгая проверка
происхождения калькулятора после доказанного отката ошибочной настройки.
Адресные тесты прошли; эти два расширения ещё не приняты на живом переоткрытии.

Следующий шаг — новый изолированный diagnostic10 на той же Xiaomi-модели.
Итоговая серия из 38 запусков не начата. Ошибки бизнес-полноты diagnostic7,
закрытие прерванного несохранённого сеанса и восстановление таймаута чтения
остаются открытыми. Ветка codex/mimo-scenario-stability; main и общий плагин
не изменены.

---

## Исправлен контракт калькулятора; diagnostic9 запущен — 17 сентября 2026

Diagnostic8 выявил4доэффектныхотказа: схема допускала пропущенныйreplace и
лишнийtarget.name, исполнитель их отклонял. Run18calls/574.320s остановлен;
сохранения нет, cleanup BLOCKED/CONFIRMED_SAVE_REQUIRED. Его не считать успехом.

Схемаnew/existingвыражений согласована с исполнителем, ошибки возвращают путь
до индекса/поля.2026clienttestsPASS/1SKIP/0FAIL. Native2отказа без изменений→
1корректныйузел/1связь PASS;10точныхчисел проверены; диагностическаякопия
закрыта/logoutPASS. Replay4записанныхзапросов подтвердил исправления; дополнительный
устаревшийorder вcalc-003 выявляется существующейвалидацией.

Операторскаяподготовкапапки больше не создаёт пустойпакет и подтверждаетlogout.
Устранена наблюдённаяstartupгонкаLoginom при преждевременном открытиихранилища:
ожидается регистрацияегоузла. Nativeinspect/create обаPASS,0пакетов,logouttrue.
Интерактивныйharness отказывает до браузера безTTY, еслиpipe явно не разрешён.

Diagnostic9 `20260917-041651-3d5f6593`, PID37811,source `b265f979dac9f214280f931e854d6ff754d1db99c6946f02d54a4a847992c66c`
запущен после23toolspreflight, толькоXiaomi/mimo-v2.5/medium. Папка
`/mimo/MiMo-task38-diagnostic9-20260917`. Client/plugin source заморожены.
Итог ещё не получен. Последний полностьюпроверенный diagnostic7 имеет верные
таблицы/настройки/экспорт, но бизнесFAIL из-за среднейкривой и рекомендации.
Итоговая38runсерия не начата. Main/общийплагиннеизменены.

---

## Diagnostic7 проверен; diagnostic8 запущен — 17 сентября 2026

Независимое переоткрытие diagnostic7 завершено: все3таблицы596×16/6×14/6×26,
настройки4узлов, связи, матрица72ячейки и отдельный CSV прошли проверку.
Закрытие пакета/logout подтверждены. Бизнес-приёмка FAIL: средняя кривая
не вычислена в узлах, неполна и неверна в ответе; есть ложное описание роста.

Основной Hermes skill сокращён41647→10581байт; новые показатели предписано
вычислять в узлах. Уточнены пути ошибок экспорта. Клиент2025PASS/1SKIP/0FAIL,
37focused PASS, skill validation PASS. Исправлен аудиторский выбор экспорта за
границей окна: native2zoomsteps, граф неизменен,3адресных тестаPASS.

Новый diagnostic8 `20260917-035258-bd5157de`, PID36165, source
`65b3648ceceec6c645a11d40998453def76383ddfe7095a9fc2d95a33387d003`, запущен
на Xiaomi/mimo-v2.5/medium после23toolspreflight. Отдельная папка
`/mimo/MiMo-task38-diagnostic8-20260917`; только исходное задание и вложение.
Исходники заморожены на время запуска. Эффект сокращения инструкций пока не доказан.
Итоговая серия38прогонов не начата; main и общий плагин не изменялись.

---

## Diagnostic7: сохранение прошло, бизнес-проверка выявила ошибки — 17 сентября 2026

MiMo `20260917-031027-d0ee7343` завершила автономный прогон:21вызов инструмента,
908.223s, фактический Xiaomi/mimo-v2.5. Source `b637a1d5…` не изменялся.
Созданы импорт, группировка, калькулятор и экспорт. Пакет сохранён в
`/mimo/MiMo-task38-diagnostic7-20260917/scenario.lgp`; исходная сессия закрыта,
logout подтверждён без отбрасывания изменений. Два ошибочных запроса экспорта
исправлены моделью до побочных эффектов; дубликатов не создано.

Независимая сверка экспортированного CSV:6строк×26полей PASS. Матрица в ответе:
72значения PASS; сравненияM3/M6 и шесть показателей липкости численно верны.
Но средняя кривая создана в тексте, не в узлах:7из8приведённых значений неверны,
ещё4месяца отсутствуют. Одно снижение названо ростом. Бизнес-приёмка FAIL,
техническое завершение не засчитывается как выполнение всей задачи.

Первое независимое переоткрытие прочитало596×16,6×14,6×26 и настройки трёх
аналитических узлов. Аудитор остановился на экспорте вне правой границы окна;
это дефект аудитора. Его cleanup/logout PASS. Добавлено ограниченное раскрытие
точного узла штатным масштабированием с проверкой неизменности полного графа;
3адресных теста PASS. Повторный полный аудит выполняется в отдельной директории
`task38-diagnostic7-independent2`, на прежних неизменных client/plugin исходниках.

Итоговая серия38прогонов не начата. Изменения только в отдельной ветке;
main, общий плагин и сервер не обновлялись.

---

## Широкие таблицы и восстановление соединения — 17 сентября 2026

В отдельной ветке исправлены лишние обходы форматов, привязка перерисованного
порта и продолжение после подтверждённого отказа связи, включая потерянный ответ.
Живые проверки:28полей/10строк с точными числами;210числовых ячеек проверены;
оба отказа восстановлены без дублирования узла/связи. Чтению предоставлено
оставшееся total_ms вместо configure_ms; общий предел сохранён.
Клиент2024PASS/1SKIP,0FAIL. Заключительная source-проверка бюджета чтения прошла:
read114.724s при configure100s/total300s; операция179.282s.

MiMo diagnostic6 прошла соединение и выполнение калькулятора, но остановилась
на прежнем120sлимите точного чтения.24toolcalls/816.943s, модельmimo-v2.5,
source unchanged, export/private cleanup PASS. Сохранение и logout этого
автономного прогона не подтверждены (pending/read). Последующие ручные
диагностические копии закрыты с подтверждённым logout. Новая автономная
проверка последних изменений выполняется в diagnostic7 (`20260917-031027-d0ee7343`);
итоговая серия38run не начата.

Quick view действительно показывает содержимое и общий счётчик, но остаётся
отдельным диагностическим маршрутом.775ms против27983ms относятся к точному
Table; обычный non-exact путь MiMo занимал6–8s. Полнота считанных данных
и выполнение всех требований аналитической задачи проверяются отдельно.
Main и общий плагин не изменялись. Подробности — в актуальном плане.

---

## Поздняя перерисовка: исправление проверено — 17 сентября 2026

MiMo diagnostic5 прошла7 аналитических узлов, но повторила отказ на соединении
сортировки. Предварительного наведения оказалось недостаточно. Добавлена полная
сверка графа непосредственно перед жестом и безопасная привязка нового SVG.
Живой тест с намеренной поздней перерисовкой прошёл: ровно1связь, без дублей.
Клиент2008PASS/1SKIP. Собственные диагностические копии закрыты, logout проверен.
Последняя правка ещё требует нового автономного прогона; итоговая серия не начата.

Чтение до100 строк ранее прошло публичный live-тест и независимую сверку72×7.
Быстрый просмотр10 строк измерен отдельно:0,775с против27,983с точного пути
для тех же10 строк. Это разные гарантии и диагностический замер, а не измерение
ускорения всего сценария. Быстрый маршрут в продукт ещё не включён.

---

## Проверка соединения и чтения72 строк прошла — 17 сентября 2026

В отдельной ветке исправлены перерисовка при наведении перед соединением,
ожидание загрузки графа и открытие результата выбранного узла. Чтение сводок
расширено до100 строк с ограничениями объёма и явным признаком полноты.
Живой public node.apply создал1узел/1связь, выполнил и прочитал72×7 значений;
независимое сравнение всех значений и кратностей строк прошло.
Клиент1993PASS/1SKIP,0FAIL. Следующий шаг — автономный MiMo diagnostic5.
Это техническая проверка; полная аналитическая успешность корпуса ещё не доказана.
Main и общий плагин не изменялись.

---

# Устойчивость Hermes / Xiaomi MiMo: промежуточный отчёт

17 сентября 2026. Работа продолжается; итоговая серия корпуса не выполнена. Историческая [точка паузы](mimo-stability-pause-2026-09-16.md); актуальные checkpoints — в [плане](../plans/loginom-dock/mimo-scenario-stability.md).

## Текущее состояние

### Последняя диагностика: соединение и чтение результата

Diagnostic4 задачи38 остановлен после44 вызовов без продвижения на соединении
сортировки. Фактическая модель — mimo-v2.5; версия клиента во время прогона
не менялась. На живой копии установлено, что уход указателя с выбранного узла
может заменить его экранный элемент. Добавлены предварительное наведение,
ограниченное ожидание доступности порта и полная повторная сверка графа.
Создание, соединение, настройка и выполнение нового узла прошли вживую;
полный проход после последующего исправления открытия результатов проверяется.

Отдельно исправляется выбор уже выделенного узла: пропускать жест можно только
при доступной кнопке визуализаторов. Расширение чтения сводок до100 строк
перенесено в клиент: порции по10, контроль целостности строк/полей, лимиты10000
ячеек и512KiB исходных UI-строк. По умолчанию остаётся10 строк. Частный прототип
прошёл сравнение всех72×7 значений с независимым аудитом; адресные105 проверок
после переноса прошли. Итоговая публичная живая проверка ещё выполняется.

Быстрый просмотр выходного порта проверен отдельно: счётчик72 и содержимое
доступны, но отрисованы57 строк. Он перспективен для промежуточных проверок,
не доказывает полноту чтения сам по себе и округляет отображаемые значения.
Ускорение сценария не измерено; автоматический быстрый маршрут ещё не включён.


Исправлены два воспроизведённых технических сбоя:

- Масштабирование искало фиксированную кнопку первой мини-карты. У второго
  сценария Loginom присваивает другой ID. Поиск теперь ограничен текущим
  сценарием; живая проверка двух открытых пакетов прошла, чужая карта не затронута.
- После сохранения с переоткрытием Loginom иногда снова отмечает пакет
  изменённым. Добавлена проверка состояния после сохранения и точный следующий
  шаг для модели: обычное сохранение того же файла. Живая проверка завершилась
  закрытием пакета и выходом без отбрасывания изменений.

Новый прогон задачи38 (`20260917-004756-ce501443`) обнаружил следующий отказ:
MiMo использовала неподдержанную функцию MID. Ошибка синтаксиса оставляла
мастер заблокированным. Реализован проверяемый откат только этого отказа:
отмена, повторное открытие, сверка настроек и графа. Живая проверка исправленного
запроса на том же узле прошла без дублирования узлов и связей. Неизвестный
результат и потеря ответа не разрешают такой откат.

Полная клиентская регрессия после этих исправлений: **1960 PASS / 1 SKIP**.
Затем уточнена справка по преобразованию строк в число и добавлено предупреждение
о полностью пустых полях прочитанной выборки; адресные проверки прошли.
Полный повтор со всеми последними изменениями: **1961 PASS / 1 SKIP**, падений нет. Последняя полная Python-регрессия: **749 PASS**.

Диагностический прогон38 `20260917-011818-215198d2` выявил новый крайний
случай: явные координаты8/8 обрезали подпись и порты созданного импорта.
Продолжение остановилось на переименовании; прогон FAIL,13вызовов/343.63секунды,
пакет не сохранён. Прямая диагностика подтвердила, что zoom это не устраняет.
Теперь опубликованная схема и исполнитель требуют координаты64..10000;
положение можно опустить для автоматического выбора. Живая проверка отказа
до изменения графа и исправленного запроса прошла: ровно1новый узел/1связь.
Полная регрессия последней версии: **1963 PASS / 1 SKIP**,0FAIL.
По подсказке пользователя проверено автоматическое упорядочивание:6узлов
перемещены,7идентификаторов и5связей сохранены. Вместе с подбором масштаба
оно также устранило обрезание узла8/8 в диагностическом черновике. Это пока
проверенный диагностический приём; автоматическое применение требует отдельной
квитанции изменения координат всего графа. Готовится diagnostic4;
итоговая серия38прогонов не начата.

В предыдущих диагностических запусках задачи19,21,34,38 выявлены также
аналитические ошибки или неполнота решения. Успешное вычисление отдельных
таблиц не засчитано как выполнение всего задания; подробности ниже и в плане.

## Условия

- Ветка `codex/mimo-scenario-stability`, исходная ревизия `9b46c85f68d054a27afad732d74785332098c0ba`.
- Loginom 7.4.2: `http://10.200.11.224/app/?testable=true`; отдельный пользователь `mimo` без пароля.
- Только подписочное подключение `xiaomi / mimo-v2.5 / medium`; отдельные настройки, браузер и папка каждого запуска.
- Задание — исходный `description.md`, данные — штатное native-вложение `data/dataset.csv`. Эталонные расчёты модели не передавались.
- `main`, сервер и общий плагин не обновлялись. Незавершённые сторонние изменения сохранены отдельно.

## Что подтвердилось и исправлено

1. **Избыточные технические параметры.** Добавлены локальные привязки сценария, свободное положение нового узла и обычные настройки чтения, импорта и экспорта. Явный выбор модели сохраняется; перезапись файлов автоматически не включается.
2. **Контракты и выходные поля.** Согласована оболочка параметров выбранного обработчика; ошибки имён и дат стали точнее. Добавлены отдельные изменения выходных полей и предварительная проверка новых калькуляторов. Полный явный список полей по-прежнему должен быть полным.
3. **Чтение экспортированного файла.** Исправлены устаревший корень наблюдения, возвращение прежней вкладки Files в другую папку и повторное использование этой вкладки. Неопределённое скачивание не повторяется; повтор разрешён только после доказанного отказа до действия.
4. **Одинаковые названия узлов.** У одного узла Loginom использует разные ключи на графе (`@1`) и в навигации (`-3`). Проверки мастеров и возврата на граф согласованы через проверенные идентификаторы узла, документа и сценария.
5. **Загрузка перед созданием связи.** Ожидание исчезновения маски распространено на все поддержанные связи. После ожидания исходный граф проверяется заново.
6. **Продолжение операции.** Пользовательский `dock_node_resume` принимает только исходный ID. Полный неизменяемый запрос хранится локально. Это не снимает блокировку неизвестного результата и не означает, что восстановление всех этапов уже реализовано.
7. **Неполные выборки.** После подтверждённых ошибок итогового текста добавлено явное предупреждение о количестве возвращённых строк и общие требования подтверждать отчётные показатели выполненными узлами.
8. **Вложения и журналы.** Точные ошибки native-вложений сохраняются вместо пустого списка. Экспорт диагностики не читает рассуждения; отдельные reasoning-поля временной базы завершённого изолированного запуска очищаются после экспорта.

## Проверенные результаты

| Проверка | Результат |
|---|---|
| Клиентский набор после исправления привязки SVG портов | 1905 PASS, 1 SKIP, 0 FAIL; полный запуск с доступом к локальным сокетам |
| Python-набор проверок | 742 PASS, включая фильтры, mappings, завершение сеанса и взвешенный ROI |
| Прямой тройной экспорт | Все три CSV по 500 строк сверены полностью; прежняя Files-вкладка использована повторно |
| Два одноимённых узла | Полный цикл прошёл; разные GUID; все пять отображённых строк каждой группировки совпали с CSV |
| Повторное обращение по ID к завершённому узлу | Тот же GUID и первая попытка, без повторного выполнения |
| Задача 02, `20260916-153230-f976fa41` | Независимый аудит PASS: сохранение, повторное открытие, настройки, связи, все девять портов; 366 строк 2024 года |
| Задача 47, `20260916-174615-e0b292a7` | Сохранённый граф, настройки и семь полных таблиц / 186 строк — PASS; полное бизнес-задание — FAIL из-за итогового текста |

В задаче 47 модель привела неподтверждённый рейтинг экономии, использовала среднее групп как общий показатель и сделала противоречивый вывод о бюджете R&D. Поэтому успешное сохранение пакета не засчитано как успех всей задачи. Повтор `20260916-181009-dd48d6de` завершился: сохранённые настройки всех 11 узлов, восемь полных таблиц / 323 строки и три CSV прошли независимую проверку. Полное задание снова FAIL: неверное количество групп выше порога (8 вместо 7), среднее абсолютного процента вместо знакового и неподтверждённые причинные выводы. Невидимый рейтинг экономии и среднее групп как общий показатель в этом повторе не использовались.

Прогон47fix4 занял487секунд/20вызовов,47fix5 —638секунд/32вызова. Для47fix5 журнал подтвердил `mimo-v2.5`, неизменный runtime и2 012 539суммарных токенов, включая1 908 544кэшированных. Сравнение эффективности между разными графами не является доказательством улучшения модели.

## Что остаётся

- Диагностировать оставшиеся аналитические ошибки задачи 47; проверить стартовые задачи 21, 19, 34, 38. После возобновления в задаче21 исправлен новый blocker: отсутствие входа калькулятора теперь отклоняется до операции. Живая проверка исправления PASS; новый MiMo-прогон `20260916-202750-2426c41e` завершил6узлов и сохранил пакет (508секунд/20вызовов). Полная задача FAIL: нет разреза по доходу, неподтверждённые общие средние и вывод о кредитном рейтинге. Завершение сеанса заблокировано UNSAVED_CHANGES, оригинал открывается только для чтения. Проверка таблиц ведётся на отдельной диагностической копии и не заменяет приёмку оригинала.
- Доработать восстановление подтверждённых ошибок выходных мастеров и выполнить живую проверку сбоя доставки.
- Выполнить согласованную итоговую серию на одной неизменной версии: шесть основных задач по три запуска, остальные допущенные — по одному.
- Свести метрики завершённости, правильности, ошибок, времени и токенов. Предварительные пропуски корпуса не считать успехами.

Публикация, слияние и обновление общего плагина остаются отдельным этапом.

## Доказательства и продолжение

- [План и подробные checkpoints](../plans/loginom-dock/mimo-scenario-stability.md).
- `.dock/mimo-stability-20260916/client-suite-report-grounding.txt`.
- `.dock/mimo-stability-20260916/live-export-tab-reuse1/file-audit.json`.
- `.dock/mimo-stability-20260916/live-duplicate-label-fix5/displayed-result-audit.json`.
- `.dock/mimo-stability-20260916/audit-task02-fix2-7/numerical-audit.json`.
- `.dock/mimo-stability-20260916/audit-task47-fix4-2/full-audit-v2.json`.
- `.dock/mimo-stability-20260916/audit-task47-fix5-1/full-audit.json`.

Каталог `.dock` содержит локальные доказательства и не публикуется вместе с исходниками.

### Дополнительные проверки после возобновления

741 Python-тест PASS (`acceptance-python-suite-teardown.txt`). Завершение сеанса теперь фиксируется отдельно от exit0 Hermes; проверены отсутствие квитанции, чужой профиль, неверный владелец и несохранённые изменения. Старый аудитор широких таблиц исчерпал бюджет на221-й строке калькулятора; его временные изменения отброшены, пакет закрыт, сеанс завершён. Чтение блоками по10строк:6адресных тестов PASS, живой повтор выполняется. Это исправление проверочного инструмента, а не свидетельство ускорения Hermes.

### Исправление чтения графа после выходного мастера

На Loginom 7.4.2 выявлены два SVG одного порта после Cancel: data-tid остаётся на одном элементе, а внутренний shape указывает на другой. Dock теперь проверяет прежние native GUID, объект порта, ячейку, владельца, схему и попадание в ту же ячейку графа. Исчезнувший либо чужой порт не принимается. Живой проход по выходным мастерам всех пяти аналитических узлов PASS; отдельный сеанс закрыт без сохранения или отбрасывания изменений. Полный повтор copy4 прошёл: все1005строк, настройки6узлов и заключительный граф проверены. Это PASS_DIAGNOSTIC копии, не автономная приёмка исходного задания.

### Проверено восстановление выходного мастера калькулятора

После намеренной потери ответа на подтверждённое Done исходная операция продолжена по тому жеID. Формулы и выходные поля перечитаны; один узел, одна связь, одно Done, без повторного создания. Переименование и исключение сохранились. Проверены300строк по количеству и3строки численно; это адресная проверка восстановления, не полный аудит нового аналитического задания. Другие неизвестные сбои мастера автоматически не разблокируются.

### Task19: техническое завершение и оставшиеся ошибки

MiMo самостоятельно сохранила6аналитических узлов и штатно завершила сеанс
(518секунд/30вызовов). Экспорт отклонён до изменения графа: заданная координата
не достигалась в видимой области. Бизнес-проверка FAIL: средние проценты вместо
ROI каналов, неподтверждённый топ аудиторий и неполное покрытие задания.
Сверка всех7таблиц/1035строк после открытия пока не выполнена: стенд вернул
пустой HTTP-ответ, браузер не загрузил страницу. Это внешняя блокировка аудита,
а не подтверждённая ошибка сохранения. Исходная версия Dock оставлена для
воспроизводимого продолжения; улучшение общей доли успеха ещё не измерено.

Локальные проверки аудитора после task19: полный набор **745 Python-тестов PASS**
(74.526секунды), `git diff --check` PASS. Новые модельные прогоны не запускались.
Повторная прямая HTTP-проверка снова получила Empty reply; аудит оригинала остаётся
заблокирован доступом к стенду.

### Доступ восстановлен: исходный ROI-пакет проверен

После восстановления стенда независимо открыли оригинал task19 на той же версии
Dock. Все7таблиц/1035строк, настройки6узлов, связи, сортировка и закрытие пакета
прошли проверку. Полное бизнес-задание по-прежнему FAIL по ранее найденным
ошибкам показателей и выводов.

Сбой размещения экспорта воспроизведён напрямую. Исправляется работа с масштабом
и копиями SVG в карте сценария: заданные координаты сохраняются в координатах
модели, действующий узел/порт определяется внутри native-полотна, а не среди
копий всего документа. До полного живого прохода это исправление не принято.


### Масштабирование и автоматическое размещение: живые проверки пройдены

`live-1789588591547`, runtime `17b309e839790a4909c41706edecd5ca067f795c277c376786d109a20ceed4c2`:
экспорт за границей первоначального холста прошёл создание, связь, настройку,
исполнение и штатное скачивание. Запрошенная точка1300/100 штатно привязана
к сетке1304/104; Dock сам изменил scale1→0.82644628. Повтор operation_id
вернул тот же узел. CSV12957bytes/SHA113a6c48b17a945a100d9420eac71726d29fe93e6ed0fadf227c52d64ec8bbad
совпадает с независимо проверенным предыдущим экспортом: все300строк×8полей.
Следующий калькулятор без координат успешно создан на уменьшенном масштабе
в96/408, ровно один узел и одна связь добавлены. Пакет закрыт с явным discard,
logout SUCCEEDED; исходные сохранённые сценарии не изменены.

Дополнительно исправлено чтение выходных портов после открытия мини-карты:
скрытые SVG-копии вне native canvas не являются вторым портом. Проверки
однозначности внутри полотна сохранены;26тестов output context PASS.
Повторяемая операторская проверка: `tools/loginom-acceptance/placement.operator.js`.
Полный регрессионный набор выполняется; найден и исправлен устаревший mock
opaque UI identity в executor-fixture (20адресных тестов скачивания PASS).
Это техническая диагностика Codex; новой успешной автономной MiMo-приёмкой
не считается. Итоговые38прогонов ещё не начинались.


2026-09-16, MiMo placement regression: полный client suite **1919PASS/1SKIP**,0FAIL (1920tests,52.65s). Evidence `.dock/mimo-stability-20260916/client-suite-placement-outputs.txt`. Live export + same-ID replay + automatic calculator placement PASS на runtime17b309e. Общая автономная приёмка корпуса ещё не завершена; следующий диагностический прогон — task34.


### Task34 остановлена на подтверждённом техническом сбое

Прогон `20260916-230022-afe19c54` остановлен оператором после повторных
отказов восстановления grp-books,45вызовов/1781.76секунды. Девять узлов
SUCCEEDED, триCSV независимо сверены полностью:32×14,6×5,500×21. Пакет не
сохранён; cleanup BLOCKED/CLEANUP_OR_OPERATION_UNCONFIRMED. Browser process
после остановки завершён. Это FAIL диагностического прогона, не приёмка.
Runtime17b309e неизменён. Подтверждены43APIcalls,input166386/output28004/
cache3518720 из session DB; это неполная статистика после SIGTERM, usage-file
не сформирован. Reasoning очищен штатным приватным cleanup без чтения.

Причина: target400/900 после zoom оказался с видимой точкой сброса, но
частично скрытым входом. Нативная операция open_input_port вернулаNOT_APPLIED,
а созданный target уже имел эффект, поэтому вся операция осталасьAMBIGUOUS.
На отдельной копии `live-1789590704427` воспроизведено без модели: canvas
324/100,1178×756,scale0.826446; export node body655/838,46×86 и вход641/871,
12×20, вне нижней границы856. Диагностическая копия discard/logoutSUCCEEDED.

Исправление проверяет после создания реальные DOM-границы узла и портов,
добавляет ограниченный UI zoom при тождественном графе. Скрытые копии
мини-карты исключены также при выборе узла для rename/move.60адресных тестов
PASS; живой повтор новой версии выполняется. Новый модельный запуск не начинать
до этой проверки. Ограничение полного чтения группировок остаётся открытым.


### Проверка нижнего края завершена

`live-1789590999310`, runtimee5f37e677ca832572989f12733cc513beaf72d1e6a96e3f7378d357a0268ab94:
export400/900→400/904 PASS целиком, same-ID replay без дубля, CSV12957bytes
с прежним провереннымSHA113a6c48. После него калькулятор без координат PASS,
автоматическая позиция1056/120. Копия закрыта с discard/logoutSUCCEEDED.
Полная регрессия1921PASS/1SKIP,0FAIL (59.43s). Затем уточнены сообщения
ошибок read.ports/read.coverage с точным parameter_path;117адресных тестовPASS.
`placement.operator.js --placement-edge` воспроизводит адресный нижний край
через тот же harness. Следующий новый сценарий — task38; задача34 не принята,
её повтор отложен до разбора полного чтения небольших сводок. Общий плагин
не обновлён, main не изменён, итоговые38прогонов не начаты.
# Точка продолжения — 17 сентября 2026, задача 19

При восстановлении контекста после сообщения пользователя о перезагрузке
проверены реальные процессы: текущий изолированный Hermes PID5346 жив,
прогон `20260917-151127-b640c212` продолжает работу. Стенд отвечает HTTP200.
Повторный запуск не создавался. Проверенный профиль — Xiaomi / mimo-v2.5 /
medium; runtime `952003a177eb4485076e7e5c632ad936b6677ab502cb0294184e01338ca2f1d3`.
До завершения прогона клиент и инструкции модели не изменять.

Последний подтверждённый результат: импорт, калькулятор прибыли, группировки
и калькулятор ROI из суммарной прибыли/бюджета завершены; сортировка запущена.
Обнаружен новый технический барьер: после успешного выполнения группировки
с `read.ports=[]` отдельный `dock_node_read` отказывает из-за отсутствия ранее
прочитанной схемы. MiMo создала дополнительную группировку с чтением выхода.
Причина локализована в `buildNodeReadRequest`; исправление ещё не внесено.
Следующий шаг: сохранить итог прогона, проверить пакет и полный бизнес-ответ,
затем воспроизвести и устранить отказ первого чтения без ослабления проверок
принадлежности узла и схемы. Итоговые 38 приёмочных прогонов не начаты.

Ранее завершённые диагностические проверки: задача02 — восемь таблиц,
924 строки/6276 ячеек и настройки проверены; задача47 — восемь таблиц,
284 строки/2544 ячейки проверены при диагностическом открытии только для чтения,
но бизнес-ответ MiMo содержит ошибочные выводы о кварталах и ранжировании.
Это не заменяет финальную серию на одной неизменной версии.

Машинная точка продолжения:
`.dock/mimo-stability-20260916/resume-checkpoint-20260917-task19.json`.
