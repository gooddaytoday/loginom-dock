# Совместная проверка RC — 14 сентября 2026

Статус: PASS — все три сценария прошли полный независимый аудит. Ветка `codex/rc-six-nodes-20260914`, интеграционная база `740efd3039b98cc6e51e55161d00f7d07374416a`. Прогоны не изменяют main, серверный release или общий плагин.

## Состав

| Сценарий | Обработчики | Проверяемый результат |
| --- | --- | --- |
| cleaning | 2 × текстовый импорт → замена → заполнение пропусков → дубликаты; слияние со справочником → объединение | Точные значения, дубликаты/противоречия, корректные ключи соединения, две отдельные связи к входам объединения, 10 итоговых строк |
| calendar | текстовый импорт → калькулятор → дата/время → фильтр → группировка → сортировка | Производная сумма, месяц даты, оба выхода фильтра, суммы 300 и 120, порядок по убыванию |
| export | текстовый импорт → параметры полей → свёртка → текстовый экспорт | Согласованные имена/типы, 6 длинных строк, точные байты CSV |

Всего 17 экземпляров узлов, 14 разных типов. Параметры, источники и независимые ожидаемые значения закреплены в `tools/loginom-acceptance/rc_combined_goal.py`, `goals/rc-combined-*.txt` и `fixtures/rc-combined/`.

Каждый сценарий создаётся Hermes через существующую подписку ChatGPT: `openai-codex / gpt-5.6-sol / low`. Все жесты настройки и чтения выполняют готовые локальные обработчики. Модель задаёт граф и параметры. Проверяются реальные raw-cell/execution/delivery/save доказательства; итог модели сам по себе не считается приёмкой.

После одного финального сохранения клиент закрывает собственный пакет и завершает Loginom-сессию. Независимый браузер открывает исходный файл, проверяет те же GUID/типы/связи, запускает конечные узлы и читает результаты без переоткрытия мастеров. Временные изменения диагностического чтения не сохраняются. Закрытие/Logout обоих браузеров входит в проверку.

## Проверенные этапы

- Интеграция шести принятых веток завершена: [отчёт интеграции](rc-integration-2026-09-14.md).
- [Безопасное завершение сессии](rc-package-cleanup-proposal-2026-09-14.md) реализовано и прошло адресные тесты и живые проверки Loginom 7.4.2.
- Runtime новой серии: `17f729249fec38b5e91da15b9f02540ab3bf3df1fdb2ee89922ce06450449a12`.
- Preflight новой серии пройден без запуска модели; модель/подписка/браузер/pins проверены.
- cleaning `20260914-164641-6f525862`: модель завершилась, 29/29 проверок PASS; сохранение и автоматический cleanup подтверждены. Calendar/export и окончательный аудит также завершены, итоговые результаты ниже.

Предыдущий cleaning `20260914-154152-367a2504` прошёл 28 модельных проверок, но независимое открытие остановилось на read-only уведомлении оставшейся сессии. Этот результат и исходный FAIL сохранены; они относятся к прежнему runtime `04ad4e85f616cad71f220a3e17ec7dba457dab1c4b566c9aee5bd39e00119747` и не заменяют проверку новой версии.

## Коррекция задания импорта после полного прогона

`20260914-163433-b1cb3679`: все 7 узлов выполнились, проверенные таблицы совпали с ожидаемыми, итоговый пакет сохранился и был автоматически закрыт с logout. Модельный аудит остановился после 23 успешных проверок: у Orders модель задала пустой `settings.format.null_marker`, тогда как источник использует `NULL`. Старый числовой verifier выдал AttributeError при разборе этой некорректной настройки; traceback сохранён в выводе координатора, точный диагноз — `audit-diagnosis.json` прогона.

Первоначальная гипотеза о предварительной MCP-проверке опровергнута traceback. Настройки источника нельзя считать принятыми только из совпадения итоговых таблиц. В тексты трёх заданий добавлено явное имя параметра `null_marker="NULL"`, RC-аудитор теперь проверяет его перед разбором числовых ячеек. Обработчики и runtime не изменены. Повтор разрешён после этой диагностики; исходные результаты и FAIL сохранены без правок.

## Независимое чтение cleaning

В `reopen-cleaning-2` исходный файл открылся без read-only, подтвердив закрытие writer-сессии. Диагностический читатель остановился при строгом сравнении порядка полей Duplicates: после сохранения Loginom переставляет неиспользуемое Id после Client/Amount. Это уже принято в подплане12 (`12-review-fix-2026-09-13.md`, `duplicates_output_evidence.py`); цель cleaning не фиксирует порядок этих исходных полей.

Добавлены версионные `rc-combined-reopen-v2.mjs` и `rc_combined_acceptance_v2.py`: только для точного узла Duplicates разрешена перестановка при полном совпадении независимых имён/меток/типов; ожидаемые значения переставляются по именам, а не заимствуются из результата. Остальные таблицы проверяются с прежним точным порядком. Исходные frozen файлы, модельные доказательства и отказ reader2 сохранены без изменений. Семь адресных Python-проверок прошли.

У reader2 закрытие находилось только в успешной ветке; после ошибки был закрыт браузер без Logout. V2 выполняет собственный cleanup в finally и сохраняет браузер при BLOCKED. Возможная старая блокировка принадлежит диагностической Dock-сессии `477e326e-34f7-48a6-b838-0576afc32564`, документу `1789394137351-1mploregy33`, точному файлу `/test-2/packages/Dock-acceptance-20260914-164641-6f525862.lgp`. Административное массовое закрытие не выполнялось. Пока ожидается освобождение этой старой сессии, calendar использует отдельный файл.

## Calendar — PASS

`20260914-171044-adf9b38b`: 25/25 модельных проверок прошли. Независимый `reopen-calendar-1` открыл оригинал без read-only, выполнил Sorted и прочитал `Month/Total = [[1,300],[2,120]]`. GUID всех шести аналитических узлов и все пять связей совпали до и после выполнения. Writer и независимый reader закрыли свои пакеты и вышли из Loginom; временные диагностические изменения не сохранены.

Первый полный аудит v2 отклонил штатный значок «Переменные сценария» как седьмой узел. Новый тонкий адаптер `rc_combined_acceptance_v3.py` использует существующую проверку `date_time_sales_acceptance.analytical_nodes`: не более одного штатного значка с точным типом/меткой, без входных/выходных табличных портов и без связей. Неизвестные узлы по-прежнему отклоняются. Повторный аудит тех же неизменённых native доказательств — `full-audit-v3-1.json`, PASS. Повтор модели и перенастройка пакета не выполнялись. Исходный FAIL сохранён.

До окончания процесса координатор один раз слишком рано вызвал аудит и получил FileNotFoundError для ещё не экспортированного evidence.json; результат не создавался. Последующие аудиты выполнялись после подтверждённого выхода runner.

## Итог

| Сценарий | Прогон | Проверки модельного этапа | Независимое открытие и аудит |
| --- | --- | --- | --- |
| cleaning | 20260914-164641-6f525862 | 29 PASS | reopen-cleaning-3, PASS |
| calendar | 20260914-171044-adf9b38b | 25 PASS | reopen-calendar-1, PASS |
| export | 20260914-172241-7a748db9 | 21 PASS | reopen-export-1, PASS |

Все три `full-audit-v3-1.json` имеют `passed=true`. Точные пути, SHA256 и квитанции закрытия — в [машинном отчёте](rc-combined-verification-2026-09-14.json). При финальном чтении cleaning исходный файл уже открылся без read-only; получены 5 строк Duplicates и 10 строк Combined. Исходные поля Duplicates сверены по именам с независимыми типами/метками/значениями; граф и связи проверены тем же строгим правилом. Три writer-сессии и три успешных независимых reader-сессии подтвердили закрытие пакетов и Logout. Старую блокировку reader2 не снимали административно.

Экспорт проверен по сохранённому native download, включая точные байты CSV; после открытия пакета повторно проверены шесть строк Long. Повторное исполнение Export с Reject не требуется: оно конфликтовало бы с уже созданным и проверенным файлом.

Заключительные адресные Python-проверки: 8/8 PASS (RC-цели, перестановка полей и существующий guard служебного узла). Синтаксис читателя и `git diff --check` прошли. Ранее выполненные тесты cleanup/bridge/config/shutdown и живые safety-проверки описаны в отчёте механизма закрытия. Финальная проверка процессов подтвердила отсутствие работающих RC/Hermes runners.

Совместная проверка охватывает 17 экземпляров и все 14 зарегистрированных типов обработчиков в перечисленных режимах на Loginom 7.4.2. Это не проверка всех возможных настроек платформы. Основной main, production и установленный общий плагин не изменены. Слияние и выпуск остаются отдельным этапом по команде пользователя.
