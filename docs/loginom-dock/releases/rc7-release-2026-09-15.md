# Loginom Dock 0.1.0-rc.7 — выпуск и установка

RC7 опубликован15сентября2026: [GitHub Release](https://github.com/kartamyshev-dev/loginom-dock/releases/tag/loginom-dock%400.1.0-rc.7).
Исправлены общая сессия нескольких чатов Hermes, диагностика недоступного
каталога загрузки и восстановление после подтверждённого WORKFLOW_LOST.
Состав прежний:14типов узлов, без новых узлов и оптимизации точного чтения.
[Причина и проверка маршрутизации](hermes-session-routing-2026-09-15.md).

## Ревизии и разрешения

- Client source/tag: `c63b3b36ddc44c2a867bc953f5b6755cc68daee4`.
- Site source: `8ad84954c1983cf6b6e2c5239290bf8800607fe0`.
- Mac runtime: `0.1.0-rc.7-62c8dab64544`.
- Build VPS: `/opt/loginom-dock/client-build/rc7-c63b3b36`.
- Site current: `/opt/loginom-dock/releases/20260915-rc7-8ad84954`;
  Caddy `loginom-dock:landing-rc7-8ad84954`.
- Backend сохранил прежний container ID и образ
  `loginom-dock:studio-landing-70411dfe`.

Пользователь разрешил выпуск, обновление обоих плагинов и перезапуск Hermes.
Автоматическая проверка отклонила первоначальный fast-forward main как недостаточно
явно разрешённый; действие не выполнилось. После отдельного ответа
«Разрешаю перенос RC7 и push main» main обновлён обычным fast-forward и push.
39локальных файлов сохранены; точный checkpoint:
`.dock/rc7-release-20260915/main-checkpoint/`. Другие незавершённые документы,
включая simple-explanation.md, сохранены вне коммитов выпуска.

## Проверка комплекта

Чистый snapshot506входов сверяется с Git objects точного source commit.
Три комплекта собраны на VPS с прежними закреплёнными Node24.19.0 и зависимостями.
В каждом4320manifest entries; новые Hermes entrypoint/router вошли в комплект.
Целостность проверена до и после packaged tests. Все5публичных файлов после
публикации независимо скачаны с GitHub на VPS; bytes/SHA совпали.

| Платформа | Байты | SHA256 |
| --- | ---: | --- |
| macOS arm64 | 47893296 | `3f81dd831f93e33295ad06cc2af3381d6430a8bc860b5e332610f82633ef8c0d` |
| Linux x64 | 53027454 | `f1526549c6f6a4ef044734302713cf9da1582e6c174f162c83f6dcbd02be0017` |
| Windows x64 | 45432675 | `c8764266d5488a44588e671c984881349bb95b84e7757608dc95dbe346eca804` |

Source и готовый Mac-комплект: по1843PASS/1SKIP. Native adapter Python:10PASS.
Готовый Linux-комплект:102адресных PASS. Первый source run внутри sandbox
показал отказы IPC/kernel-lock/signal checks; тот же полный набор вне sandbox
прошёл. Исходный FAIL сохранён. Предыдущая живая проверка native manager A→B→A
подтвердила изоляцию, доставку CSV, сохранение и guarded close/logout обоих пакетов;
полные границы этого опыта указаны в отчёте маршрутизации.

## Установка и загрузка

Оба штатных install.sh --agent выполнены из серверного Mac-комплекта.
Hermes Python adapter установлен из GitHub по тому же sourceCommit, его байты
и оба skill совпали с bundle. Обе Dock storage-конфигурации побайтно прежние;
в Hermes config изменена только регистрация Dock. Модель, provider и другие
настройки сохранились. Шесть точных RC7 hooks Codex штатно подтверждены через
hooks/list → config/batchWrite → hooks/list:6/6trusted. Чужие hooks не менялись.

Перезапуск затронул оба разных Hermes host:

- Desktop: обычный macOS Quit настоящего `com.nousresearch.hermes`, с сохранённой
  штатной проверкой активной работы; старые app/core/MCP55563/55637/55759 завершились.
  Открыто то же приложение из `hermes-agent/apps/desktop/release/mac-arm64/Hermes.app`,
  новые app/core50846/50897. `/Applications/Hermes.app` — другой установщик и не использовался.
- Messaging gateway: штатный gateway restart, старый39123 завершён, новый51817,
  свежий active_agents0. Его перезапуск сам по себе не обновляет Desktop.
- Новые router PIDs51057/51836 используют установленный hermes-mcp.mjs RC7.
  Native origin receipts обоих подтвердили initialize/tools/list и корректное
  закрытие временного discovery bridge. Это не завершение внешнего router:
  процесс остаётся готов к пользовательским задачам.

Computer Use, UI-клики, принудительное подтверждение Quit, модели и новые
аналитические задачи не запускались. Утерянный прежний workflow автоматически
не восстанавливался. После перезапуска требуется новое пользовательское сообщение
с файлом/явной ссылкой, чтобы native hook выдал новый current-turn ticket.

## Сайт, ограничения и откат

release.json обновлён только после публичного download/hash read-back.
Серверный preview проверен скриптом:6сочетаний агент/ОС, ссылки, копирование,
FAQ, desktop/mobile без переполнения. Снимки просмотрены. Новый Caddy валиден;
переключён только caddy с backup deploy.env и автоматическим rollback при ошибке.
Сайт/static200, API health200, Studio200/redirect302, API MCP401 без credentials,
публичный MCP404 и cache headers подтверждены. Временный preview удалён.

GUI composer replay после установки пока не выполнен. Банковский сценарий
в рамках исправления не создавался. Новая GUI-приёмка Linux/Windows остаётся
отложенной по решению пользователя; здесь проверены сборки и Linux source tests.
XLSX-узлы и вложения мессенджеров не добавлены. Один Hermes router удерживает
до16изолированных задач до закрытия подключения; автоматического вытеснения нет.

Откат клиента при необходимости: сначала сохранить пакеты, остановить нужные
Hermes host; выполнить штатный rollback Hermes, затем Codex, чтобы последним
вернулся runtime RC6, а не RC7, уже текущий на момент установки второго агента.
Проверить оба native plugin ref, runtime, storage и trust hooks; затем перезапустить
Hermes. Прежний runtime `0.1.0-rc.6-b85bccf2723b` и installation receipts сохранены.

Откат сайта: новый VPS release содержит deploy.env.before, previous-current,
deployment-before.json. Прежний current — `20260914-rc6-fc606397`, Caddy
`loginom-dock:landing-rc6-fc606397`; вернуть только Caddy/его сохранённую
конфигурацию по operations.md. Backend/data не трогать.

Private evidence: `.dock/rc7-release-20260915/`: source/packaged tests,
builds.json, public-verification.txt, install-*.txt, installed-verification.json,
native-files-verified.json, hooks-trusted.json, desktop-restart.json,
native-startup.json, gateway-after.json, site-visual/, deploy-site.txt.
