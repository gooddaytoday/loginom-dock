# Объединение шести принятых веток для RC — 14 сентября 2026

Шесть принятых веток 11, 12, 13, 14, 16 и 17 объединены в `codex/rc-six-nodes-20260914`. База — `main` `5e90c8ea87ec6f8f3306dfb0a0b3d34b00be0a93`. Каждая ветка включена отдельным merge-коммитом с сохранением истории. Полные SHA приведены в [машинном отчёте](rc-integration-2026-09-14.json).

## Разрешённые конфликты

- Объединены регистрации, параметры, чтение настроек и результаты всех 14 обработчиков. Одноимённое `parameters.fields` поддерживает схемы даты/времени и заполнения пропусков через `anyOf`, при сохранении проверки конкретным обработчиком.
- Сохранены продолжение настройки даты/времени, восстановление входного сопоставления, строгая отмена мастера, подтверждённые отказы, точное чтение свёртки и файловые результаты экспорта.
- Согласован общий вызов получения выходных полей. Существующие исключения сохраняют фактические имена и метки; новые исключения в отдельном мастере используют проверенную семантику Loginom.
- Объединены категории палитры и UI-наблюдения. Экспорт сохраняет собственную журналируемую прокрутку; общий предварительный scroll к нему не применяется. Сохранены ожидания завершения Save As и доступности Close.
- Python-проекция результата согласована с точными типизированными значениями и метаданными чтения клиента. Добавлена проверка отказа при удалении обязательных данных из публичного результата.
- В общем запускателе сохранены все задания, ограничения и отдельные параметры узлов, в том числе каталог `fixtures/text-export/input`.
- Исторические журналы не переписывались. Для двух фиксированных live fixtures тесты явно используют старые функции чтения `project_node` и, где требуется, `bound_wizard_close_confirmation` из принятого узла 14. SHA исходных файлов и fixtures закреплены; адаптер используется только в тестах. Актуальная строгая проверка Close и новая проекция проверяются отдельно.

## Проверки

- Node 24.19.0: полный клиентский набор — 1790 проверок. Первоначально 1776 PASS, 13 FAIL, 1 SKIP. Четыре ошибки объединения исправлены; адресные повторы — 34/34 и 272/272. Девять ошибок ограниченной среды устранены проверкой соответствующих системных тестов вне sandbox — 14/14. Итог с учётом повторов: 1789 PASS, 1 SKIP.
- Python: итоговый полный запуск — 693 проверки, из них 691 PASS и два отказа чтения исторической проекции. После явного закрепления старого test-only reader соответствующий набор прошёл полностью. Итог с учётом адресного повтора: 693 PASS. Более ранние отказы сохранены в журналах.
- Проверены синтаксис клиентских модулей/Python, отсутствие маркеров конфликтов, регистрация 14 обработчиков и отсутствие ошибок пробелов в изменяемом коде. Исторические transfer patches, архивный проверяющий код и точные CRLF/BOM-файлы экспорта сохранены побайтово и исключены из проверки пробелов. Незакоммиченные файлы исходного checkout сверены по SHA256 и сохранены без изменений.

Логи и их контрольные суммы перечислены в машинном отчёте; локальный каталог — `.dock/rc-integration-20260914` основного checkout.

## Граница результата

Это завершение пункта 1: объединение и устранение конфликтов. Совместный живой сценарий, итоговая Hermes-приёмка объединённой версии и сборка RC не выполнялись. Прежние приёмки подтверждают исходные ветки, но не заменяют следующий этап проверки объединения.

Известное медленное точное чтение сохранено. Ограничение текстового экспорта `/test-2` также сохранено. Версия релиза не менялась; main, удалённый репозиторий, сервер и установленный общий плагин не переключались. [Дальнейший план](rc-preparation-2026-09-14.md).
