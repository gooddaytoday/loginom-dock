# OpenViking восстановлен, разработчики возобновлены — 13 сентября 2026

После нового сообщения пользователя выполнены проверки установленным модулем
`ov-memory-doctor` с сохранением проектного peer из cwd и штатных credentials.
Сетевые probes не вызывали `/ready` или модели.

| Проверка личной OpenViking | Результат |
| --- | --- |
| `/health` | HTTP200, 475 ms |
| `/health` с авторизацией | HTTP200, 681 ms; правильные account/user |
| `/api/v1/system/status` | HTTP200, 217 ms |
| Доступ к `viking://~/memories` | HTTP200, 239 ms |
| MCP `tools/list` | HTTP200, 230 ms, 15 инструментов |
| Auto-recall в текущем чате | Контекст вновь поступил |

Dock `dock_diagnostics(checkConnections=true)` также снова подтвердил сервер,
три источника, skill и доступность страницы Loginom. Peer координатора:
`-Users-kartamyshev-Git-loginom-dock`; глобальные настройки не менялись.

Ограничение: отдельные инструменты личной памяти в текущем runtime координатора
пока не зарегистрированы. Работоспособность endpoint проверена диагностическим
модулем, новый поиск Experience не имитировался. Offline doctor сообщил об
отсутствующем явном `plugin_hooks`, но фактический auto-recall поступил;
это сообщение не принято за доказательство неработающих hooks.
Новую запись и извлечение памяти эта проверка не тестировала.

Четырём прежним задачам узлов 11/12/13/14 передано продолжение на
`gpt-6-astra` / `medium`; приложение подтвердило все четыре состояния active
и новые выполнения inProgress. Worktree и ветки независимо сверены до отправки;
сохранены рабочие изменения и уточнён текущий HEAD узла13: `3fd205cf`.
Разработчики проверяют свои новые harness/pins, аккаунты, пакеты и живой UI.
Потоки 1/2/4 уже сообщили о правильных cwd/ветках и восстановлении личной памяти;
2/4 также подтвердили Dock. Поток 2 создал свежую source-сессию, открыл live06
для проверки состояния: два узла, связь, без открытого мастера. Прежний журнал
AMBIGUOUS сохранён; выполнение не объявлено успешным. Потоки 1/4 поднимают свои
свежие harness. Это сообщения разработчиков, не независимый аудит их окружений.
Поток 3 также подтвердил личную память, свежий source harness, dock_prepare и
remote pinning; Loginom 7.4.2/test-3, новая редактируемая копия
`/test-3/N13-55f13a4c.lgp`, новый workflow_ref, viewport:null 1508×862.
Он продолжил живую проверку исправления флажков. Таким образом, все четыре
разработчика сообщили о возобновлении. Не выполнены приёмка обработчиков,
ревью, Hermes, новые назначения, merge или release.

Локальные свидетельства:
`.dock/server-diagnosis-20260913/openviking-recovery-redacted.json` и
`.dock/server-diagnosis-20260913/developers-resumed-redacted.json`.
Реестр `.dock/node-streams-20260912/state.json` обновлён.
Ниже сохранён предшествующий снимок с уже снятой TLS-ошибкой личной памяти.

---

# Доступность Dock восстановлена — 13 сентября 2026

После сообщения пользователя о восстановлении связи координатор подтвердил
доступ к Dock обычным сетевым путём с Mac: оба публичных домена ответили HTTP200,
SSH прошёл авторизацию, диагностика установленного клиента и реальное чтение
документа через MCP завершились успешно. Длительная устойчивость ещё не оценивалась.
Четыре потока сохранили checkpoints и ожидают ручного продолжения.
Исходная база документации: `d34f896e`.

## Проверка после восстановления

| Проверка | Результат |
| --- | --- |
| `https://loginom.duckdns.org/health`, обычный маршрут | HTTP200, TLS 0.239 s, всего 0.452 s |
| `https://loginom-dock.duckdns.org/`, обычный маршрут | HTTP200, TLS 0.253 s, всего 0.465 s |
| SSH к `82.22.23.10`, обычный маршрут | Host key проверен, парольная авторизация и `id -u` успешны, exit 0 |
| Установленный Dock, `dock_diagnostics(checkConnections=true)` | `connection.ok=true`; сервер, три источника, skill и доступность страницы Loginom подтверждены |
| Dock MCP `read` | Документ Help `data/integration/export/excel.md` прочитан, `isError=false` |
| Отдельная личная память `https://ov.kartamyshev.dev/health` | TLS `SSL_ERROR_SYSCALL`, exit 35, HTTP000 |

Доступны источники `ai-skills`, `e2e-tests`, `loginom-help`. Ревизия skill:
`afa295bf48dc48da5d3c995665a06ef2190ff620bae3243d46557e0371536790`.
Прочитан URI
`viking://resources/loginom-dock/sources/loginom-help/data/integration/export/excel.md`.
Диагностика подтвердила доступность страницы Loginom, но не выполняла новый вход
в браузере, запуск сценария или приёмку Excel.

## Предшествующий сбой и сравнение маршрутов

Обычный маршрут давал таймауты HTTP, TLS и SSH banner exchange. Один `/health`
ответил HTTP200 за 5.678 s, следующие проверки завершались без ответа.
TLS 1.2 проблему не снял: один handshake прошёл за 2.124 s, затем истёк таймаут HTTP.
После сообщения пользователя о перезагрузке VPS обычные проверки ещё давали
SSL timeout 8 s и SSH banner timeout до авторизации.

Маршрут до VPS проходил через `utun7`, MTU 4064. Системные HTTP/HTTPS/SOCKS/PAC
прокси были выключены. Наблюдались Karing, его расширение и OpenVPN Connect;
принадлежность `utun7` конкретному процессу не установлена.

Ограниченные диагностические соединения через Wi-Fi `en0` к тому же VPS тогда
проходили: API HTTP200 за 0.654 s, лендинг HTTP200 за 0.924 s, SSH успешен.
Сохранялись исходные домены, SNI, проверка сертификатов и SSH host key.
Сравнение локализовало прежний сбой на сетевом пути через туннель. Точная причина
внутри этого пути и связь восстановления с перезагрузкой VPS не установлены.
VPN, глобальные маршруты и серверные настройки координатор не менял.
Для последней успешной проверки привязка к `en0` уже не требовалась.

## Проверка VPS через диагностическое SSH-соединение

- `current`: `/opt/loginom-dock/releases/20260904-landing-7b711846`;
  `source.commit`: `7b7118468353eacab83551a3570efb4de76b6b2f`.
- Caddy, основной OpenViking, LFS proxy, GitLab gateway и Ollama работали
  около 19 часов; основной OpenViking имел статус healthy.
  Старый `loginom-dock-stage2-qa` был unhealthy; это отдельный QA-контейнер.
- Loopback API `/health`: HTTP200 за 0.004 s; HTTPS через Caddy с исходным
  доменом и проверкой сертификата: HTTP200 за 0.047 s.
- Порты 80/443 слушались публично, 1933 — на loopback.
- Uptime снимка: 10 дней 9 часов; load 0.05/0.04/0.00.
  Доступная память около 2.49 GB, swap занят на 869 MB.
- Корневой диск заполнен на 90%, свободно около 11 GB. Это наблюдение для
  обслуживания, не установленная причина сбоя.

Пользователь уточнил, что перезагружал VPS Dock `82.22.23.10`. Показатели относятся
к прочитанному снимку и не подтверждают момент перезагрузки. Серверные журналы
и firewall в этой диагностике не читались. Перезапуск, очистка и deploy не выполнялись.

## Память и продолжение потоков

Личная OpenViking и Dock — разные подключения. Ранее личные `health/find`
завершились ошибками MCP; offline doctor подтвердил endpoint `ov.kartamyshev.dev`
и правильный workspace peer. Последний health этого endpoint всё ещё дал
TLS-ошибку. Конфигурация памяти не менялась; работа продолжена по актуальному
правилу проекта из файлов и наблюдений.

Сетевой блокер Dock снят на стороне координатора. Перед ручным продолжением
каждого потока нужен свежий собственный source harness с проверенными pins,
аккаунтом и пакетом из его checkpoint. Готовность обработчиков и старых
браузерных сессий не подтверждена. Прежняя AMBIGUOUS операция потока 2
не объявлена успешной. Ревью, Hermes, следующие узлы, слияние и поставка
автоматически не запускались.

Результаты без credentials сохранены в `.dock/server-diagnosis-20260913/`:
`probe-after-user-reboot-redacted.json`, `probe-direct-redacted.json`,
`ssh-stage-redacted.json`, `recovery-redacted.json`.
Реестр координатора: `.dock/node-streams-20260912/state.json`.

Для пассивной проверки соединения используется `/health`. В текущем source
`/ready` вызывает embedding, а `verify-server.py` вызывает `/ready`; они не
запускались. Монитор loopback сам по себе не покрывает публичный TLS.
Изменены документация и локальные диагностические checkpoints.
Ветки разработчиков, установленные клиенты и сетевые настройки сохранены.
