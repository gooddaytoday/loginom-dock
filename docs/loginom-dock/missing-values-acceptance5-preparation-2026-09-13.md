# Node14: подготовка пятой приёмки — 13 сентября 2026

Назначение: `node14:acceptance5-preparation:1:611a9f0c`.
Подготовка не разрешает запуск модели: `launch_authorized=false`, слот отсутствует.
Исправление ожидания REMOVE_LINK принято только как улучшение структурированного
отказа; устранение исходного триггера run4 не заявляется.

## Новый комплект

Версия `2026.09.13-node14-acceptance5.1`, private root
`.dock/node14-acceptance5-preparation/kit-2026.09.13-node14-acceptance5.1/`.
Runtime code commit `611a9f0c2e159e7fe0f8f3e9b8b9c42d5908243c`;
привязки комплекта сохранены коммитом `34c64710`.
Runtime SHA256 `68e8e7a7cac00b8afa45ac8f5b60bdcfa8832efc86ed34445f2bd4f1baf10e62`.

Снимок содержит 156 runtime inputs и 256 harness inputs (412 файлов,
12 196 799 байт), каждый проверен по SHA256. Снимки — источник сверки;
запуск выполняется из согласованного рабочего дерева, а не из копии harness.
Harness digest (canonical sorted JSON, compact separators):
`d1892694f20a8859efd5baa4f30ad1e1ea7849c256a7934323359c49c97fa0a7`.

Изменены только runtime PIN в `missing_values_goal.py` и проверке
`missing-values-reopen.mjs`. Текст задания, fixtures, независимые ожидаемые
результаты и `missing_values_acceptance.py` неизменны. Требуются прежние
27 успешных операций, 9 импортов, 12 конечных результатов, настоящее сохранение
и независимое повторное открытие с проверками данных и настроек.

Неизменённый `2026.09.13-node14-test4.2-candidate` прочитан заново по сети:
4/4 файла совпали; SHA manifest
`cadd80dfd8490f40c851475008a1ccd68d7a617dbcf6a77de25034ff8da7b2ad`.
Настоящий `pinActionCatalog` текущего runtime принял кандидат (5 действий).
Source/subscription preflight прошёл без запуска модели, включая проверку
версии Hermes 0.21.0 и auth guard. Проверки: 44 JavaScript, 18 Missing Values
Python и 7 import-placement-refusal Python — все прошли.

## План возможного единственного запуска

Точные аргументы находятся в `launch-plan.json` комплекта. Параметры:
существующая подписка Hermes `openai-codex / gpt-5.6-sol / low`, профиль
`chatgpt-sol`, timeout 3600 секунд, max_turns 200, planned_attempts=1,
без fallback и без автоматической ротации общей авторизации.

До запуска необходимы новый слот и явное разрешение координатора, повторная
сверка source/runtime/harness/candidate/goal/fixtures, действительность подписки
более чем на 4200 секунд, свежий тест записи/fsync/readback экспорта и ресурсный
допуск. Затем создаётся единственный эксклюзивный маркер запуска. При отказе
любого gate модель не запускается, отказ не считается попыткой полной приёмки.

Новые пути: `.dock/node14-autonomous-acceptance-5/runs/<fresh-run-id>/`,
отдельные `private/hermes-home`, `private/dock-state`, session/document/profile,
новые имена файлов и `/test-4/packages/Dock-acceptance-<fresh-run-id>.lgp`.
Ни run4, ни текущая диагностическая копия не возобновляются как новая приёмка.
Окно должно быть развёрнуто, `viewport=null`, фактические размеры проверены.

Диск проверяется до запуска, в ходе работы и перед экспортом. Предлагаемый
консервативный минимальный свободный запас — **12 884 901 888 байт (12 ГиБ)**.
Это операционный резерв, а не доказанная верхняя оценка полного экспорта.
Для сравнения, уже run4 создал экспорт 712 246 884 байт и около 1 ГиБ общего
каталога; полный прогон длиннее и добавляет независимый reopen, временные файлы
и журналы. Порог не снижается ради допуска, существующие evidence не удаляются.

Проверка записи 62 794 байт, fsync и JSON readback прошла. Она доказывает только
малую запись сейчас, не возможность будущего полного экспорта. Ресурсный gate
вернул exit 2 и BLOCKED. Точные последние свободные/необходимые bytes приведены
в машинной сводке. Координатор выполнил очистку только регенерируемого uv cache;
дальнейшее хранение и место решаются отдельно.

При ENOSPC, ошибке экспорта или неполном журнале сохраняются оригинальные
attempt/DB/WAL/journals; результат — FAIL/incomplete evidence. Фактический код
launcher фиксируется отдельно. Неизвестные child returncode/timed_out остаются
неизвестными; завершение usage не подменяет код процесса. Допустимо отдельно
восстановить экспорт чтением после устранения ресурса, без перезапуска модели.
Полный успех устанавливает прежний независимый аудитор; summary модели,
операторская диагностика или один успешный REMOVE_LINK его не заменяют.

## Живая проверка расширенного контекста

Новая сессия `c062a0a7-bd93-4cdf-857b-0e44a0aaf75f`, документ
`1789303724651-evuolxfyz9w`, test-4, source runtime `68e8e7a7…`.
Воспроизведены **18/18 успешных операций** run4 до threshold41: 9 импортов,
создание/изменение/Done/Close/reexecute «Основного», остальные предшествующие
узлы. Старый отказ размещения import-120 в (80,800) пропущен; использовано
его уже успешное размещение в (300,620). Входные файлы получили новые имена;
ссылки на узлы и артефакты перепривязаны к новой сессии.

Read-only сравнение подтвердило исходные 15 объектов до threshold41:
метки, типы, координаты, входы/выходы/прочие порты, locked и пять связей
совпали после нормализации новых GUID. Создание и переименование «Порог 41»
породили ожидаемую шестую автосвязь «Только строка» → «Порог 41».

Удаление выполнено ровно один раз с бюджетом 180 секунд. Наблюдатель записал
результаты штатных чтений selection и штатный mouse click, не добавляя жестов
или дополнительных браузерных чтений между ними: выбран ровно указанный edge,
повторная проверка непосредственно перед удалением — true. Квитанция
SUCCEEDED/cleanup=true. Независимый `verifyNodeTargetEffect` подтвердил точный
полный graph delta: исчезла только эта связь, остальные пять связей и объекты
сохранены. После удаления нет диалогов и выделения.

**12/12** проверок прямых живых доказательств прошли. Размер развёрнутого окна
подтверждён, viewport=null; итоговый скриншот просмотрен. На нём «Основной»
неактивен после дальнейших изменений графа: проверено воспроизведение успешной
последовательности операций и топологии, а не тождество всех внутренних
состояний исходного браузера. Дополнительное перевыполнение не навязывалось.
Причина отказа run4 не проявилась, root_cause_fixed=false. Новая проверка не
заменяет полную автономную приёмку и save/reopen.

## Ограничение диагностического экспорта

UI-проверки и закрытие bridge завершились, но private helper при финальном
`JSON.stringify` общего массива events получил `RangeError: Invalid string
length`; фактический код процесса **1**, combined_export_complete=false.
Это отдельный сбой монолитного экспорта диагностического helper, не ENOSPC и
не новая неудача REMOVE_LINK. Сериализация не дошла до записи нового общего
JSON; исходный `public-evidence.json` и JSONL остались сохранены.

Операции не повторялись. Прямой независимый verifier прочитал отдельные
create/rename/remove receipts, наблюдение выделения, metadata исходной сессии
и полные before/after graphs. Все **10 637** записей исходного журнала
(345 473 569 байт) успешно разобраны построчно. Большая дублирующая копия не
создавалась. Частичный общий экспорт не объявлен полным. Для будущего запуска
используется прежний Python acceptance exporter, а не этот private helper;
возможность будущего полного экспорта остаётся отдельной проверкой.

Собственные процессы завершены, проверка — 0. Старые receipt sets неизменны:
диагностика REMOVE_LINK 144/144, run4 38/38, run3 14/14, run2 13/13.
Исходный run4 остаётся FAIL с прежним pending/cleanup=false и неизвестными
child returncode/timed_out. Доказательства не удалялись и не переписывались.

## Состояние допуска

На 13:09:56 UTC свободно **2 614 120 448 байт**, до порога 12 ГиБ не хватает
**10 270 781 440 байт** (около 9,57 ГиБ). Дисковый gate BLOCKED; слот и
launch_authorized отсутствуют. Подписка на момент проверки действительна
более 4200 секунд, малая запись/fsync/readback прошла, но всё это требуется
перепроверить непосредственно перед будущим запуском.

Подготовка комплекта и ограниченная live-проверка завершены; допуска к модели
нет. Следующие gates: достаточное место и проверка экспорта, новый слот,
явное разрешение, повторная сверка frozen pins. Полный review, main/push/VPS,
общий плагин и маршрутизация памяти не менялись.

[Машинная сводка и квитанции](missing-values-acceptance5-preparation-pins-2026-09-13.json).
