# Узел 13: живая проверка кандидата — контрольная точка

Назначение `node13:candidate-live-preflight:1:db38f7f2`, исходный HEAD
`36442231367ebef35d3101ef16921e56f328a464`, ветка `codex/node-13-date-time`.
Это диагностика Codex без модели. Hermes не запускался; слот, run_id и бюджет
будущего запуска не назначались. Клиентский runtime не изменён:
`2488fdaa08e4d6da9b7a31fb7598f675a8ad972efd65640b34cbc851feb16e2b`.

## Подтверждённые результаты

Кандидат `2026.09.13-node13-acceptance.1-5a4c46fc-candidate` заново скачан:
четыре файла совпали с опубликованными координатором bytes. Manifest SHA256
`db38f7f249c0c051d8b77a22e0ac80a817ecb96a9820bfb0f74b8199a496c634`.
Настоящий parser клиента подтвердил только `/test-3`, checkpoint revision `2`,
Loginom 7.4.2/macOS/Chromium. Активация и VPS-изменения не выполнялись.

Native MCP initialize/list_tools прошёл с отдельным состоянием после исправления
его прав на 0700. Первый отказ сохранён. Это проверка доступности native tools,
не полный native UI run. Локальный environment preflight прошёл без модели.
Серверный skill revision остался
`afa295bf48dc48da5d3c995665a06ef2190ff620bae3243d46557e0371536790`;
frontend fingerprint охватывает HTML и 17 статических ресурсов, не все динамические модули.

В сессии `dd1f6b0c-66ac-4f04-aad5-92c886b88be3` реальный импорт принятого CSV
413 bytes / SHA256 `2a74b48ca02f457c012d67db60582d1840731cf4463b8a276ad6626c6433bcf2`
прошёл. Независимо прочитаны все 10×4 значения. Доставка подтверждена отдельными
upload/verify receipts. Первый журнал не содержал target identity; поэтому
полный import/source-chain audit не объявлен пройденным. Сохранён узкий аудит
полной таблицы с корректным доказательством запуска через Finish мастера.

## Обнаруженное противоречие замороженных входов

ТЗ задаёт обеим входным датам метку «Дата», но ожидает у исключённого DateB
метку `DateB`. Живой output mapping сохранил метку «Дата»; действующий обработчик
запрещает переименование исключённого источника. Исходная операция
`node13-lost-flag` прошла проверку draft и продолжила configure, затем остановилась
на output_mapping: `Date/time output: excluded source cannot be renamed`.
Это не успешное восстановление всей операции и не приёмка полного графа.

Доказательство: `frozen-excluded-label-conflict.json` в указанной сессии;
`r1-loss.json`, `r1-negative.json`, `r1-inspect.json`, `r1-resume.json` сохранены.
Исходный FAIL и frozen-v1 сохранены неизменными. Решением координатора
`node13:excluded-DateB-label:1` разрешена единственная коррекция метки исключённого
DateB на «Дата». В frozen-v2 изменены только эта метка и соответствующее указание
ТЗ; значения, имена, типы, размеры, операции и критерии переоткрытия сохранены.
[Версия и SHA](../../tools/loginom-acceptance/fixtures/date-time/version.json). Черновик Package1 закрыт без сохранения; браузер завершён.
Загруженный собственный CSV `N13-preflight-dd1f6b0c.csv` сохранён как диагностика.

## Исправления проверочных инструментов

- Startup artifact descriptor использует поддерживаемую политику `reject` вместо
  недопустимой `fail`. Настоящий artifact store проверен обоими вариантами.
- Служебный значок «Переменные сценария» отдельно проверяется и не считается
  седьмым аналитическим узлом; дополнительные пользовательские узлы запрещены.
- Живой диагностический harness привязывает журнал к фактически подтверждённой
  target identity. Старые журналы не дописывались.
- Аудитор полной таблицы поддерживает явный wizard launch для импорта;
  остальные вызовы сохраняют обязательный graph launch.

Проверки: 34 адресных теста; полный Python suite — 540 PASS. Inventory проверочных инструментов (с явным version.json)
перезакреплён. Клиентские файлы не менялись. Чужие AGENTS.md и .gitignore сохранены.

## Продолжение

Сессия `d95e3c46-d818-49ba-9df4-02c09993f5fe` использует отдельную копию
`/test-3/N13-d95e3c46.lgp` принятого ручного примера. Удаление quarter/hour прошло;
проверка потерянного ответа при повторном добавлении прошла независимый R1-аудит.
Исходная операция завершилась, повторные apply/resume не вызвали браузерных
эффектов; 8/8 отрицательных подмен отклонены. Аудит ручного выходного mapping
также прошёл: два точных добавления при сохранённых старых полях и исключении. Это адресный
пример 4×8, не замена полного нового графа 10×27. SaveAs использовался только
для создания собственной диагностической копии.

Прямое переоткрытие нового полного шестизвенного графа ещё не подтверждено.
Дополнительно требуется проверить зависимость import node.apply от session-local
upload receipts: существующий direct-reopen adapter передаёт IDs прежней сессии.
Ограничение впоследствии подтверждено адресной live-пробой (см. ниже).
Admission не разрешает Hermes до закрытия этих проверок и явного слота координатора.

Результат адресного исполнения независимо проверен: 4×8 PASS. Выполнен
package.save_checkpoint с подтверждённым save_completed; это ещё не доказательство
сохранённого содержимого. Сессия d95e3c46 закрыта. Прямое переоткрытие выполняется
в новой сессии.

## Согласованный протокол переоткрытия v2

Решение `node13:persisted-import-reopen:1`: критерий — независимо проверить
все шесть сохранённых узлов, без обязательных шести node.apply. Live source probe
в сессии `e3e5be95-68ba-407b-9ecc-a3a841f1088f` вернул NOT_APPLIED до браузерных
действий. Публичный persisted-import node.apply не объявлен поддержанным.

В диагностическом adapter v2 импорт читается через штатный мастер (полные source,
format, columns и native mapping), мастер отменяется, исходный CSV скачивается
через UI и проверяется побайтово. Затем импорт выполняется с графа, читаются все
10×4. Upload, инъекция receipts, изменение настроек и сохранение запрещены.
Остальные пять узлов используют node.apply с пустыми parameters/mappings и
отдельным запретом исправляющих UI-действий. Исходный adapter v1 сохранён рядом
с frozen-v1. Новый adapter ещё требует полного живого прогона.

Адресная persistence-проверка `d95e3c46` → `e3e5be95` прошла: прежняя конфигурация
и все значения 4×8 подтверждены после прямого открытия точного файла. Вторая
сессия закрыта с отказом от сохранения изменений кэша исполнения; процессы завершены.

Полный frozen-v2 диагностический run `20260913-123121-5a971d69` в сессии
`a0ce4c75-57a5-4703-9328-114d17a23070` остановился на первом календаре. Импорт
10×4 и integrated delivery/source-bytes audit прошли. Через 1 800 012 мс
операция вернула AMBIGUOUS/read, cleanup_complete=false: настройка и исполнение
подтверждены, полное чтение не завершено. Исходный pending сохранён без replay
и продления deadline. Доступный UI прочитан без применения настроек; диалог
формата отменён, свой Package1 отброшен без сохранения, сессия закрыта.

После расширения протокола: Python 543 PASS; адресные Date/time 37 PASS;
клиентские continuation проверки 31 PASS. Подготовлены 13 отрицательных подмен
saved-import evidence, которые запускаются только после положительного live baseline.


## Frozen-v3 после подтверждённого timeout

Решение координатора `node13:wide-calendar-timeout-split:1` применено только
после фактического 30-минутного timeout. Frozen-v1 и frozen-v2, исходный FAIL и
журнал сохранены. [Квитанция timeout](node13-live-preflight/frozen-v2-timeout.json)
фиксирует исходные статусы и SHA приватных журналов; закрытие диагностической
сессии не меняет незавершённый статус исходной операции.

Frozen-v3 сохраняет шесть узлов, все значения, схемы и настройки frozen-v2.
Изменён только порядок: создание двух широких календарей завершается Done,
затем тот же GUID исполняется existing/Execute с пустыми parameters/mappings.
Итого девять запросов: два Done и семь полных Execute. Аудитор отдельно требует
успеха обоих Done, того же GUID, полного равенства настроек и отсутствия
исправляющих действий в мастере при Execute. Итоговый save и независимая
проверка всех шести узлов обязательны; промежуточные сохранения запрещены.

Это исключение для приёмки, с дополнительной стоимостью повторного чтения
мастера. Обычный продуктовый путь без повторного открытия мастеров не изменён.
Оптимизация скорости широких mappings/чтения остаётся отдельным follow-up;
runtime и candidate остаются прежними, лимиты не увеличены.

Свежая диагностическая сессия frozen-v3:
`9d3ccd0a-7196-495c-9179-a02cc2581032`, run `20260913-131824-2c55b3ff`.
Проверены Loginom 7.4.2, viewport=null, развернутое окно и прежний runtime.
Модель и archive capture не запускались. Новая полная проверка приостановлена;
её результат, save/reopen и admission пока не подтверждены. Python-набор после
изменения аудитора и отрицательных проверок: 545 PASS. Отрицательные проверки нового split требуют
успешного live baseline.

В свежей frozen-v3 копии импорт 10×4 завершён за 382,8 с; независимые
output/source-bytes проверки прошли. Первый календарь завершил Done; результат независимого аудита и пауза приведены ниже.


## Пауза после Done: ресурс и различие меток

`node13-full-calendar-done` завершился SUCCEEDED/node_ready/cleanup_complete=true
за 1211,5 с, без исполнения. Независимый аудит подтвердил полную конфигурацию,
проекцию 27 активных полей и публичный успех, но отверг единственное сравнение
исключённой метки. Native observation от 13:45:07.625 UTC содержит одновременно
source DateB/«Дата» и excluded target DateB/«DateB». Readback сохраняет метку
исключённого target, а правило настройки исключения проверяет метку источника.
Frozen-v3 требует «Дата» и поэтому остаётся FAIL; ожидание не исправлялось
задним числом. [Аудит](node13-live-preflight/frozen-v3-done-audit.json),
[неизменённые входы](node13-live-preflight/frozen-v3/manifest.json).

По ресурсному предупреждению координатора новые тяжёлые шаги остановлены:
фактически доступно около 1 GiB, собственная текущая сессия занимает 974 MiB.
Дискового отказа не наблюдалось; причина паузы — недостаточный запас, а не
предполагаемая ошибка записи. Новые live-сессии, монолитный duplicate export и
самостоятельная очистка evidence/profiles запрещены до ресурсного решения.

[Контрольная квитанция](node13-live-preflight/frozen-v3-resource-pause.json)
содержит SHA существующих журналов и read-only графа. В сессии
`9d3ccd0a-7196-495c-9179-a02cc2581032` оставлен свой несохранённый Package1:
источник и настроенный календарь, один линк, graph interaction_ready=true.
PTY 32106 остаётся открытым и ожидает команд. Execute не запускался, пакет
не сохранялся. Это не полная шестизвенная приёмка и не persistence PASS.

Для продолжения требуются ресурсное решение координатора и согласование
раздельных меток source/excluded target в новой frozen-версии. Исходный v3 FAIL
сохранён. Не продолжать граф вслепую и не запускать прежний full export с
дублированием всего массива events; для дальнейшей передачи использовать
ссылку на исходный журнал с SHA либо иной согласованный компактный формат.
После решения проверить актуальный граф и свободное место, затем выполнить
отдельный existing/Execute с запретом изменения настроек, продолжить оставшиеся
узлы, финальный save, независимый reopen и отрицательные проверки. Runtime,
candidate, слот Hermes, бюджет и будущий run_id не изменены.


## Frozen-v4: только offline-переоценка

Решение координатора `node13:source-vs-excluded-target-label:1` разрешило
разделить два инварианта: requested/source DateB/«Дата» без переименования;
native excluded target DateB/«DateB»/excluded=true. В `excluded_sources`
зафиксирован источник, в `excluded` — служебная запись, отдельно от всех
эффективных таблиц. Все значения, 27 активных колонок, шесть узлов и порядок
2 Done + 7 Execute сохранены. Запросов к UI в этой offline-фазе не было.

[Компактный packet](node13-live-preflight/frozen-v4-evidence-packet.json)
ссылается на исходный execution-events.jsonl по SHA: native observation —
строка 2759, target_fields[27] и его exclusion_source; mapping receipt — строка
2774; checkpoint — строка 2783. Исходник date-time-output.mjs явно различает
служебную метку target и сохраняемую source.label; date-time-readback.mjs
копирует target.label и source_name. [Проверка публичной схемы](node13-live-preflight/frozen-v4-public-schema.json)
на записанном результате прошла. Противоречия между данным результатом,
действующей runtime-схемой и этим поведением handler не обнаружено.

[Переоценка](node13-live-preflight/frozen-v4-reassessment.json): конфигурационный
Done PASS, 8/8 отрицательных подмен отклонены (имя/метка/record_id источника,
excluded flag, неожиданная target label, подмена readback и попытки переименования
источника в запросе). Это reassessment старых raw, не новый live-прогон и не
полный PASS цели. Frozen-v3 FAIL и его архив остались неизменными.
[Diff v3→v4](node13-live-preflight/frozen-v3-to-v4.diff) включает разделение
ожиданий и строгую проверку; прежние версии двух изменённых аудиторов
восстановлены побайтово и сверены с SHA archived inputs v3.

Python: 546 PASS. Ресурсная пауза сохраняется; Execute, новые сессии,
дублирующий export, Hermes и дополнительное сохранение не запускались.
Согласование меток закрыто, но продолжение live требует отдельного ресурсного
решения. Adapter сохранённого импорта и полноценный split/reopen всё ещё
требуют живого подтверждения. Промежуточный commit фиксирует подготовку и
контрольную точку, а не завершённый admission.
