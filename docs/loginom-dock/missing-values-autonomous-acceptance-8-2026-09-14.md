# Missing Values: восьмой полный прогон и остановка аудитора

**Замороженный полный аудит — FAIL.** Run `20260914-124616-9e88066c`
выполнил все 27 требуемых операций и native save. Независимое открытие
не запускалось: предварительный аудит остановился на проверке повторного
импорта изменённого источника. Успех модели не принят за полную приёмку.

Назначение node14:hermes-full:8:40873812, слот node14-hermes-20260914-full8.
HEAD3e0600ff, source40873812, runtimee909a974…, harness e0b1c613….
Запущен точный launch-plan acceptance8.1 после свежего preflight:
412 файлов совпали, candidate test4.2/cadd80df… повторно прочитан и принят
parser, auth более4200секунд, свободно73876336640байт, других моделей нет.
Фактический user-v1/executor-replay/test-4 подтверждён до старта модели.
Существующая подписка openai-codex/gpt-5.6-sol/low,3600s/200turns,
без fallback. Полное задание совпало с предыдущим, кроме run_id.

## Рабочая часть

- 27 SUCCEEDED: 9 исходных импортов, 1 обновление источника,17 Missing Values.
- 1 NOT_APPLIED при размещении all-null импорта; отдельный строгий proof
  подтверждён,3внутренних попытки, следующая операция успешна.
- Все границы и режимы достигнуты: Done/Close/reexecute, только строка,
  точные/асимметричные числа,40/41/42,120строк,all-null,empty,перестановка,
  создание и пересчёт узла смены источника.
- package.save_checkpoint — SUCCEEDED. Экспорт1115092622байта,
  16461событие,72tool calls/72replies, pairing errors0,64API calls.
- Model/launcher returncode0, timed_out=false, usage.completed=true.
  Auth connection, runtime, harness и native skill после модели не изменились.

PID9316 и launcher8884 завершены. В10:18:20UTC отсутствие собственных
процессов/браузера проверено, локальная квитанция освобождения слота сохранена.
Повторных попыток ранее отклонённой auto-review пересылки не было;
координатор может независимо прочитать задачу/документы. Registry не менялся.

## Аудит и точные ограничения

Первое исполнение аудитора остановилось на отсутствии локальной копии
candidate/stage-report.json. Оригинал найден в coordinator candidate,
байты скопированы без изменений, digest сопоставлен со свежим network readback.
Исходный pre-audit сохранён; аудитор повторён с полными входными файлами.

Из145условий прошли143. Отказы:
source_bytes:import-mutable-change-source-01 (target_graph_identity) и
independent_reopen_present. Настройки, результаты всех Missing Values,
точная матрица операций, сохранение и публичный журнал проверены успешно.

Причина первого отказа — полный аудитор использует verifier создания нового
импорта для existing request, где метка законно не повторяется. Сам graph node
связан точным ref с ранее выполненным импортом. Существующий специализированный
verifier уже проверяет эту связь и старые/новые байты, но при явно переданном
mapping одновременно подставляет сохранённый порядок; нижний verifier
отклоняет конфликт этих двух способов задания output columns.

Оба пробела направлены в адресную доработку проверочного кода. Ни события,
ни запрос модели, ни замороженный итог run8 не переписываются. Reopen guard
не обходится. После исправления нужны его положительные/отрицательные
проверки и новый зафиксированный комплект следующего полного прогона.
Новых моделей этой диагностикой не запущено. Main/merge/push/deploy/plugin
не изменялись, пользовательские файлы сохранены.

[Исходные pins, итог и45квитанций](missing-values-autonomous-acceptance-8-pins-2026-09-14.json).
