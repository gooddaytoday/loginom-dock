# Node14: отчёт разработки и прямой проверки

Этап разработки/direct QA завершён; готов к отдельно назначаемому review с указанными ограничениями.

Ветка: `codex/node-14-missing-values`, базовый SHA `0f085c5310c67820f6f6386ae16a7df768c152ca`.
Проверенный source runtime: `71ad8762badfc799c4f6939f2bbe4e94a00ae3a7c9d87cfb47c118282b889054`.
Loginom 7.4.2, отдельная учётная запись `test-4`, видимый браузер с viewport:null.
Хронология и неуспешные диагностические попытки сохранены в
[рабочем отчёте](missing-values-progress-2026-09-12.md).

## Реализация

Добавлен `preprocessing.data_recovery / impute`: создание или поиск узла,
соединение, проверка типов/видов, настройка полного списка полей, завершение,
выполнение и чтение выхода одним `node.apply`. Поддержаны среднее для непрерывных
Integer/Real и константа для дискретной строки, включая пустую строку; явный порог
0–100%, неупорядоченный набор. Неперечисленные поля отключаются. Существующий
узел допускает `{}` для сохранения поддерживаемых настроек. Среднее вычисляет Loginom.

Общий UI-слой дополнен только наблюдёнными необходимыми случаями: группа палитры
«Предобработка», точное открытие порта при hover-клоне, привязка контекста отмены,
ограниченное ожидание перехода поверхности, открытие частично скрытой карточки
визуализатора и прокрутка до неё. Неактивные нативно скрытые Table не расходуют
лимит обхода определений. Ячейки виртуализованной таблицы привязаны к cached record
и DOM-строке; суффикс cell data-tid не считается номером записи и может повторяться
в разных строках. Защиты владельца, кэша/DOM, свежести и границы обхода сохранены.

## Проверки

- Клиентский набор: **1420 PASS, 1 SKIP, 0 FAIL**; журнал `.dock/node14-current-full-client2.log`.
- Независимый Python-контракт: **3 теста PASS**, включая 18 отрицательных подмен
  основного контракта и 5 подмен повторного выполнения источника как зависимости.
- Дополнительные подмены реальных evidence: **7/7** отклонены для нового узла,
  **8/8** отклонены для сохранения/повторного открытия.
- Синтаксис инструментов аудита и `git diff --check` проверены.

Полные независимые live-аудиты с подтверждённым источником:

| Случай | Результат |
|---|---|
| Точность Float, отрицательное дробное среднее Integer | PASS, полный3×5; −1.5 округлено до−2, precision повторён |
| Среднее для 0,0,9,Null | PASS:3, отличается от медианы; нули и строки сохранены |
| Доля5/12, пороги40/41/42% | PASS: при40 Null сохранены, при41/42 заполнены |
| Доля1/120, порог0% | PASS, полный120×5; все119 прочих строк сохранены |
| Полностью пустые выбранные поля, порог100% | PASS: Null сохранились, включая строковую константу |
| Пустой набор | PASS,0 строк и5 полей |
| Только Note в полном списке обработок | PASS: числовые Null и неперечисленные поля сохранены |
| Done, Close, preserve{} | PASS: применение/отмена без выполнения; затем сохранённая policy дала полный3×5 |
| Новый узел, переставленные поля, одинаковые метки Same | PASS, связь+полный3×5; привязка по именам |
| Повтор operation ID | PASS: Done266→266, Close284→284, новый узел958→958 действий |
| Потеря ответа после реального finish_wizard | AMBIGUOUS/pending input_mapping, execution:not_requested; повтор226→226, жест не повторён |
| Дискретное число/mean, неизвестное поле | Отказ, cleanup:true, граф7 native GUID неизменен |
| Сохранение/новый браузер/повторное выполнение | PASS, настройки+источник+связь+полный3×5 |

Основные evidence:
`.dock/node14-live-1789252182267`, `1789256602857`, `1789257444202`,
`1789259644441`, `1789261946921`, `1789263912227` (последующие числа — суффиксы
того же префикса `.dock/node14-live-`). Они локальные, содержимое не публиковалось.

## Сохранение

Диагностическая копия: `/test-4/Node14-20260913-final.lgp`.
Исходный пакет после разрыва прежнего сеанса был read-only; выполнен штатный
Save As в собственное хранилище, исходный файл не перезаписан. Новый пакет закрыт
и открыт в отдельном браузере с новым document ID. GUID узлов сохранены.

Инспектор открыл исходный импорт, прочитал source/format/все определения и отменил
черновик. После этого существующий Missing узел выполнился с parameters:{},
inputs:[], mappings:[]. Независимо проверены переход источника inactive→active,
порядок операций, свежий execution ID, граф и полная таблица. Отдельный execution ID
источнику не приписывался: он выполнился как зависимость. SHA/байты источника
проверялись при первоначальной загрузке; повторная проверка байтов после открытия
не заявляется. `reopen2-reopen-audit.json`: persistence_verified:true.

## Границы приёмки

Это source-разработка и Codex direct QA. Обзор, обычный Hermes и автономная приёмка
не запускались; публикация, merge, deploy и переустановка общего клиента не выполнялись.
Нужен отдельный диспетчерский запуск следующих этапов.

Native `package.save_checkpoint` для `/test-4` блокирует закреплённый каталог,
разрешающий только `/test-1..3`. Запрос нового изолированного immutable candidate
описан в [заявке](missing-values-candidate-request-2026-09-13.md); текущий каталог
не менялся. Ручной Save As не считается проверкой native checkpoint.

Реальное отсутствие CSV вызвало terminal failed в Loginom. Общая квитанция оставила
AMBIGUOUS/pending execute/cleanup:false и не выдала успешный выход. Отдельное чтение
подтвердило failed той же группы; файл восстановлен и панель закрыта вручную,
прежний pending runtime завершён. Автоматическое завершение такого общего отказа
как FAILED/cleanup:true остаётся ограничением, которое должен видеть ревьюер.

Существующий Text Import при смене CSV сохранил прежние определения колонок;
перестановка массива columns в запросе сама по себе их не переставляет. Этот
невалидный источник отклонён аудитом. Для проверки Node14 создан отдельный импорт,
его схема и данные подтверждены. Обработчик Text Import не изменялся.

После reopened QA и fault-проверки пакет закрыт с отказом от сохранения только
последующих диагностических изменений. Проверенная сохранённая копия сохранена.
Собственные runtime, MCP и браузер завершены; неразрешённая операция не переносилась
в новый runtime; повтор её ID не повторял жесты. Автоматическое восстановление после lost reply
не заявляется: проверен безопасный pending и идемпотентный повтор без жестов.
