# Результат: готов к Hermes, слот ожидается

Полная диагностика frozen-v4 без модели: девять публичных запросов (2 Done + 7 Execute),
шесть аналитических узлов, ровно один финальный save. Общий аудит **23/23 PASS**.
Независимый reopen в новой сессии: **31/31 PASS**, включая **13/13** отрицательных
подмен source evidence; обе пары Done→Execute: по **9/9** отрицательных проверок.
Полные данные и схемы, исходный CSV, сохранённые настройки, GUID и связи проверены.
[Сводка сохранения](node13-live-preflight/resume-v4-full-save-summary.json),
[сводка reopen](node13-live-preflight/resume-v4-reopen-summary.json).

Узкое исправление диагностического чтения хранилища обработало реальную смену
снимка при дорисовке; повторяются только чтения, остальные ошибки не скрываются.
Добавлена передача исходного журнала по проверенной ссылке/SHA без большого
дублирующего export. Python **548 PASS**, continuation **31 PASS**.
Runtime `2488fdaa…`, candidate `db38f7f2…` не изменены. Старые FAIL сохранены.
Пакет: `/test-3/packages/Dock-date-time-20260913-131824-2c55b3ff.lgp`.

Hermes ещё не запускался. Единственное оставшееся организационное условие —
слот координатора; run_id и бюджет до его назначения остаются пустыми.
Автономная приёмка целого узла ещё не заявляется. Public persisted-import в
новой сессии не поддержан: источник независимо проверен согласованным протоколом
read/cancel/download/graph Execute. Main/merge/push/deploy/общий plugin не менялись.

---

# Node13: возобновление frozen-v4

Назначение `node13:resume-frozen-v4:1:786f5517` разрешило закончить текущий
узел после очистки исторических данных координатором. Уточнение пользователя
от 2026-09-13 через координатора разрешило самостоятельно исправлять подтверждённые
недостатки и выполнять необходимые проверки до готовности к Hermes, без отдельного
согласования каждого шага. Полный Hermes Sol/low разрешён после выделения
единственного слота координатором; слот пока не получен. Новые узлы, повторное
полное review, main/merge/push/deploy/общий plugin не разрешены. Runtime 2488fdaa
и candidate db38f7f2 пока сохранены. Первичная проверка диска показала 60 GiB свободно.

Сессия `9d3ccd0a-7196-495c-9179-a02cc2581032` и собственный несохранённый
Package1 сохранились. Document `1789305504826-clhc8rgaqof`, calendar GUID
`c636f546-b1f2-4a00-a57c-1c2c47b5a39f`, источник, связь и готовность совпали
с контрольной точкой. Done и прежние AMBIGUOUS не повторялись.

Перед Execute выполнено отдельное чтение input/output mapping и обеих матриц
по 29 строк с отменой всех трёх мастеров. Независимый аудит журнала подтвердил
сохранность настроек и отсутствие изменяющих настройки действий. Вспомогательный
скрипт сначала остановился до UI из-за отсутствующего return journal ack;
затем обнаружил необходимость выбора узла после отмены входного порта.
Эти диагностические отказы сохранены. После полного успешного чтения финальный
read graph получил отказ из-за hover overlay; уход указателя на наблюдаемую
вкладку вернул полный graph/interaction_ready. Повторной настройки не было.

`node13-full-calendar-initial` запущен как отдельный existing/Execute с
parameters={}, mappings=[], на прежнем GUID. Операция завершилась за 1605,8 с: SUCCEEDED/cleanup=true, полный выход 10×27. Независимо проверены все 270 значений, схема, сохранность настроек и GUID после Done; 9/9 отрицательных подмен отвергнуты.
`node13-full-calendar-final` также завершился SUCCEEDED/cleanup=true. Независимый аудит подтвердил все 270 значений и итоговую схему с RowId, SalesAmount, SavedYearA, включая точное исключение DateB.
«Месяцы» и «Кварталы» также завершились SUCCEEDED/cleanup=true; оба независимых аудита полного выхода и настроек PASS.
Фильтр «Нет продаж» завершился SUCCEEDED/cleanup=true; независимый аудит обоих полных выходов PASS.
«Пустой календарь» завершил новый Done SUCCEEDED/cleanup=true; независимый аудит полной конфигурации и отсутствия исполнения PASS. Выполняется existing/Execute на том же GUID без новых параметров и mapping.
Пустой Execute завершился SUCCEEDED/cleanup=true за 1587,7 с. Общий независимый аудит всех девяти запросов и одного финального save — 23/23 PASS; негативные проверки второй пары Done→Execute — 9/9 PASS. Исходная сессия закрыта без дополнительного сохранения, журнал запечатан ссылкой и SHA. Независимый reopen и готовность к Hermes ещё открыты.

Для последующей передачи диагностики добавлена ссылка на журнал с проверкой SHA,
вместо дублирования массива events. Проверки отвергают изменение хэша/байтов,
чужой путь и symlink за пределы собственных sessions. Обычный полный экспорт
Hermes продолжает поддерживаться. Исходный журнал append-only; [квитанция
префикса](node13-live-preflight/resumed-journal-prefix.json) доказывает сохранение
101604331 исходного байта (2784 строк) с прежним SHA. Frozen-v3 FAIL не изменён.

Исходная сессия PTY 32106 закрыта с exit0. Полный граф сохранён один раз:
`/test-3/packages/Dock-date-time-20260913-131824-2c55b3ff.lgp`.
Компактный export: `.dock/node13-acceptance/runs/20260913-131824-2c55b3ff`.
Не создавать монолитный duplicate export. Ссылку на исходный журнал запечатывать
после собственной очистки пакета, чтобы дальнейшие события не меняли SHA.

Python после возобновления: 547 PASS. Следующий gate — независимый полный
аудит всех девяти операций до финального save, затем отдельный reopen.

## Независимое повторное открытие

Первая сессия `19bc1cee-b7a0-44ff-b793-aca4182fdc6c` прочитала и отменила
мастер источника с совпавшими настройками. Чтение файлового хранилища остановилось
из-за смены DOM между roots/detail во время дорисовки после навигации. Старый
FAIL и квитанции сохранены; пакет закрыт без save, сессия завершилась exit0.
Диагностический адаптер теперь при этом точном отказе заново читает roots в
существующем 15-секундном лимите, без повторного navigation gesture; прочие
ошибки продолжают прерывать проверку. Адресный тест это подтвердил.

Повторная сессия `e5c05a99-044d-47d1-8d69-0d9106963289`, PTY 36839,
с тем же runtime и проверенной геометрией. Источник успешно прочитан/отменён,
существующий CSV скачан: 413 байт, SHA 2a74b48ca02f457c012d67db60582d1840731cf4463b8a276ad6626c6433bcf2.
Graph Execute и полные 10×4 завершились SUCCEEDED/cleanup=true. Независимый
`verify_saved_import` PASS. Календарь, Месяцы, Кварталы и Нет продаж завершились SUCCEEDED/cleanup=true;
независимые полные component-аудиты календаря и обеих группировок PASS.
Последний `node13-reopen-5` (Пустой календарь) завершился SUCCEEDED/cleanup=true за 1605,9 с.
Все шесть узлов прочитаны. Пакет закрыт без save, diagnostics.json запечатан
после закрытия, PTY 36839 завершился exit0. Общий независимый аудит PASS: 31/31, включая 13/13 отклонённых
подмен доказательств saved-import. Настройки всех шести узлов и их GUID сохранены,
свежие полные выходы совпали, диагностика не меняла настройки и не сохраняла пакет. Запущен один `reopen-full`;
скрипт продолжает последовательно до шести узлов, не сохраняет пакет.
После завершения закрыть собственный пакет без save, выполнить `seal-reopen.mjs`,
закрыть harness и `audit_reopen_full.py`.

Python после исправления: 548 PASS; continuation: 31 PASS. Inputs: 275 файлов,
SHA 3889fabebbb094e85c33f5b32b0cba5f22754d24e6e8eee200faa53eba755884.
Подготовлен частный `prepare-final-admission.py`: заполняет существующий
admission после успешного reopen и финального commit, оставляет slot/run_id/budget
пустыми до назначения координатора. Это не новый слой проверок.
