# MiMo: передача работы в новую сессию, 17 сентября 2026

**Статус: ОСТАНОВЛЕНО по явной просьбе пользователя 17 сентября. Полная приёмка НЕ достигнута.**
Этот документ заменяет устаревшие сообщения «run active» в предыдущих checkpoints.
Продолжать разработку только после команды пользователя в новой сессии.


## Актуальная точка остановки — читать первой

Работа остановлена по команде «остановись в безопасном месте и запомни где остановился».
Новых запусков/исправлений не начинать до явной команды продолжения. Записи ниже
о выполняющихся run/audit — история; актуален этот раздел и последний checkpoint.

- Task19 `20260917-160352-21fec178` завершён: модель Xiaomi/mimo-v2.5/medium,
  business FAIL. Все7рабочих таблиц (1147строк/15515ячеек) и сохранённые настройки
  7рабочих узлов независимо сверены, составной диагностический technical PASS.
  Лишний незавершённый калькулятор остался в сохранённом графе. Неверные метрики,
  top5 и рекомендации — ошибки логики/выводов модели, не арифметики Loginom.
- Исправлен полный lifecycle одноимённых узлов: native GUID-bound breadcrumb,
  подтверждение деактивации, проверка открытия, отмена→тот же узел. Runtime
  `a7a3f594ca7f6c91567733ee303b7e81bbdb3aba910f9dbd2157facb939806dd`.
  Live `live-1789652668363` PASS, 386/386адресных тестов PASS; cleanup/logout PASS.
- Task34 `20260917-165351-f4fc705e` остановлен ДО создания узлов. Были только
  пустой draft, передача CSV и описание контрактов. Hermes PID17595 сначала
  приостановлен, затем штатно завершён SIGTERM; exit130,4toolcalls,212.547с,
  timeout=false. После выхода процессы этого run/browser не обнаружены.
- Task34 папка `/mimo/MiMo-task34-resume-20260917`, CSV
  `dataset-6ee35c8267e0.csv` (12854байта, hash в evidence). Аналитический пакет
  НЕ сохранён. Cleanup `CONFIRMED_SAVE_REQUIRED`, logout **UNCONFIRMED**;
  не объявлять пустой draft сохранённым или корректно закрытым. Попытка
  визуальной проверки CUA недоступна из-за ошибки macOS screen capture -3811.
- Последняя проверочная сессия task19 закрыта; старые ранее остановленные
  run/audit cleanup остаются UNCONFIRMED. Существующие артефакты сохранены.
- Final38 НЕ начат:38PENDING. Никаких commit/merge/deploy/plugin update.
  Все изменения незакоммичены; чужие изменения сохранять.
- Inbox сверён:192события handled/resolved, новых helpers/streams нет.

Продолжение: проверить память/ветку/процессы и task34 storage, затем новый
диагностический task34 с новым каталогом на закреплённом runtime. Прерванный
run не засчитывать в приёмку. Далее остальные основные диагностики и только
после них неизменная финальная серия38. Исключение Xiaomi и исходные вложения
явно разрешены пользователем; повторного разрешения не требуется.

Evidence: `.dock/mimo-stability-20260916/resume-checkpoint-20260917-current.json`,
`task19-composite-diagnostic-audit.json`, `runs/20260917-165351-f4fc705e/user-stop.json`.

## 1. Цель и обязательные ограничения

Сделать создание аналитических сценариев через Hermes устойчивым на **Xiaomi /
mimo-v2.5 / medium**, существующее подписочное подключение. Это явно выбранный
пользователем профиль этой задачи; общая политика Sol для других задач его не
заменяет. Не менять провайдера/модель и не использовать fallback. Проверять
эффективный профиль по SQLite Hermes; это подтверждает конфигурацию агента,
но не отдельный wire-параметр medium на стороне Xiaomi.

- Рабочий каталог `/Users/kartamyshev/Git/loginom-dock`.
- Ветка `codex/mimo-scenario-stability`; HEAD/base
  `9b46c85f68d054a27afad732d74785332098c0ba`.
- Все изменения остаются незакоммиченными. Есть чужие/параллельные изменения,
  в том числе landing/Windows: не откатывать и не включать автоматически.
- Не изменять main, не сливать, не публиковать, не обновлять общий плагин.
- Loginom 7.4.2: `http://10.200.11.224/app/?testable=true`, пользователь **mimo**,
  пароля нет. Изолированные профили, каталоги, видимые максимально развёрнутые
  браузеры, `viewport:null`. Пользовательские пакеты сохранять.
- Корпус `/Users/kartamyshev/Git/analitic`: оригинальный `description.md` и
  `data/dataset.csv` через штатные вложения. Не давать модели эталоны/готовые графы.
- MiMo определяет аналитическую логику; Dock выполняет техническую работу по узлу.
- Диагностику выполнять напрямую Codex по UI/Help/E2E, затем адресные проверки,
  и только после этого новый полный Hermes. Не использовать повторы модели
  вместо локализации технического дефекта.
- Не менять `client`/инструкции плагина во время модельного запуска или независимого
  чтения, иначе потеряется сопоставимость закреплённых версий.
- Секреты, системные сообщения и скрытые рассуждения не читать/не сохранять.
  Историю извлекать штатным tools-only `evidence.export_history`.

## 2. Объём приёмки — не сокращать

Шесть основных задач **02,47,21,19,34,38 по три запуска с нуля**, остальные
20 допущенных задач по одному: **38 запусков**. Реестр
`.dock/mimo-stability-20260916/final-acceptance-ledger.json` проверен:
**38 PENDING**, final series не начата. Предварительная классификация корпуса
не является приёмкой. Пропуски требуют конкретной причины и исключаются из доли успеха.

Каждый успех: самостоятельно, без смены модели, все требования задания, сохранение,
независимое повторное открытие, проверка полных итоговых таблиц, формул, настроек,
связей и бизнес-выводов. Exit0/слова Hermes/ручной успех недостаточны.
Одна неизменная версия финальной серии, отдельные время/токены/контракты/восстановление.

## 3. Что уже сделано и проверено

Подробная хронология и evidence: `mimo-stability-report.md` в этой папке.
Ниже — сводка, не утверждение, что весь текущий diff принят.

### Технические исправления

- Согласованы многие схемы и runtime-проверки, ошибки с parameter_path,
  технические defaults, автоматическое размещение, выходные изменения и preflight.
- Исправлены масштабирование/нижний край canvas, скрытые SVG мини-карты и
  идентичность перерисованных портов. Живые экспорт/повтор same-ID/новый калькулятор
  без координат проверены; CSV 300×8 совпал полностью. Подробные runs в отчёте.
- Прямое чтение/выполнение существующего узла через dock_node_read без повторного
  открытия настроек. Есть открытое ограничение первого чтения — см. ниже.
- Уменьшение ответа без потери значений: 72×6 укладываются в 45305 символов,
  точные числовые значения сохраняются; большой ответ доходит до Hermes.
- Восстановление исходной сессии при аварийном разрыве с проверкой владельца,
  документа и пакета; неопределённый жест не повторяется. Входные мастера Filter
  и Calculator продолжены после потерянного подтверждённого Done: 12×6 / 72×6,
  полные численные аудиты, без дубликатов. Неизвестное Done остаётся ограничением.
- Исправлен ответ на обращение к уже работающему узлу: ждать исходную операцию.
- Уточнены калькуляторные контракты: bare names в формулах, replace:true означает
  замену входного поля, а не редактирование существующего выражения. Исправление
  отказа — тот же узел, новый operation_id, inputs:[]/mappings:[], replace:false
  для сохранённого пустого Expr1. Дополнительные соединения не объявлять безопасными.
- Живой calculator recovery `.dock/calculator-v3/live-1789646734022`:
  syntax refusal → replace refusal → исправление того же узла, 1узел/1связь,
  повтор same-ID: 0 browser calls. 120 строк по количеству, численно только 3 строки;
  это адресная проверка, не полный аудит. Cleanup диагностической копии подтверждён.
- Диагностическое закрытие read-only пакета с exact owner/path и запретом грязного
  состояния. Аудитор отдельно подтверждает и закрывает точное предупреждение
  read-only; ранее оно ошибочно считалось незакрытым диалогом.
- Запускатель исправлен: Hermes `-z` терял reasoning/max-turns. Используется обычный
  `hermes chat --quiet ... --reasoning medium`; профиль проверяется по session DB.
- Инструкции Hermes связывают выводы с результатами узлов: коэффициенты после
  агрегации, ранжирование через сортировку, универсальные утверждения через проверку
  исключений, различение нуля/отсутствия и округления. Эффект на надёжность ещё не доказан.

### Проверки и пределы

Последний большой client suite до последних изменений: **2065 PASS / 1 SKIP**.
Это НЕ проверка всего нынешнего diff. Позже были адресные проверки: calculator64,
cleanup+managed shutdown24, read-only warning3, Python oracle11 и другие из отчёта.
**Самая последняя правка mapping-table-view ещё вообще не тестировалась.**

### Диагностические аналитические результаты

| Задача/run | Подтверждённый результат | Ограничение |
|---|---|---|
|02 / `20260917-140005-59459432`|8 таблиц,924 строки,6276 ячеек,настройки/связи; фильтр2024=366 строк; выручка365345.55; сохранение/выход|Диагностический PASS, не один из38 |
|47 / `20260917-141717-072ebb4e`|8 таблиц,284 строки,2544 ячейки,настройки/связи PASS в read-only диагностике|Бизнес-ответ FAIL: неверные «во всех кварталах» и топ отклонений |
|38 / `20260917-134439-d16b467e`|Техническое сохранение и ряд расчётов работали|FAIL полноты/смысла: пропущенные месяцы, округлённые разности, ложное «полностью» |
|34 / `20260916-230022-afe19c54`|Часть узлов/экспортов проверена|Остановлен на размещении/порте, пакет не принят; позднее placement адресно исправлен |
|19 / `20260917-145906-f6691f46`|Импорт прошёл|Остановлен на восстановлении Calculator; затем исправлен контракт и проведён live replay |
|19 / `20260917-151127-b640c212`|Импорт/прибыль/группировки/ROI/сортировка прошли|Остановлен на выходном мастере группировки; детали ниже |

Task47 audit: `.dock/mimo-stability-20260916/task47-readonly-acknowledged/`.
Он выполнялся на отличающемся runtime (изменялся cleanup), scope явно diagnostic,
не следует выдавать его за финальную same-version приёмку. Первое прерывание
аудита47 было вызвано перезагрузкой Mac, подтверждённой пользователем.

Task02: `.dock/mimo-stability-20260916/task02-current-independent/full-audit-identity-checked.json`.
Добавлены audit-only oracle04/17 и тесты, но они не подтверждают выполнение
полных бизнес-заданий. Нельзя выводить причинность из описательных сравнений.

## 4. Последний прогон19 — остановлен, не возобновлять вслепую

`.dock/mimo-stability-20260916/runs/20260917-151127-b640c212/`

- Runtime `952003a177eb4485076e7e5c632ad936b6677ab502cb0294184e01338ca2f1d3`.
- storage `/mimo/MiMo-task19-diagnostic-retry2-20260917`, плановый `scenario.lgp`.
- PID5346 остановлен SIGTERM после повторных отказов восстановления. Завершение
  процесса проверено. Evidence экспортирован; runtime_source_unchanged=true.
- exit130, timeoutfalse, 762.49s, APIcalls40; input139824/output14864/cache3780480.
- Профиль xiaomi/mimo-v2.5/medium VERIFIED. `reasoning_tokens:0` не доказывает
  выключенное рассуждение провайдера.
- **session_teardown UNCONFIRMED**, причина CLEANUP_OR_OPERATION_UNCONFIRMED,
  save_operation_id=null. Сохранение пакета не подтверждено.
- Session `3e5dd945-19cc-4d43-bfdb-1056ed4005aa`. Не считать серверную сессию закрытой.
- Файлы: `evidence.json`, `final-answer.json`, `operator-stop.json`,
  `effective-profile-verified.json`, `private-cleanup.json`. Старые прогоны сохранены.

### Дефект A: первое чтение выхода

Успешная `grp-channel-001` выполнилась с read.ports=[]. Последующий
`read-grp-ch-001` отвергнут `buildNodeReadRequest`: нет ранее прочитанной схемы.
Модель создала лишнюю `grp-channel-003` с read.ports=[0], чтобы получить данные.
Файлы: `client/lib/node-read-contract.mjs`, `node-read-driver.mjs`,
`client/test/node-read.test.mjs`; вход `executor.mjs:startNodeRead`.
**Исправление не сделано.** Нужно получить проверенную схему из подтверждённого
output mapping/свежего наблюдения при сохранении owner/runtime/port/execution
проверок. Не разрешать caller-provided произвольные схемы/идентификаторы.

### Дефект B: мастер в режиме «Связи»

`grp-neg-channel-001` AMBIGUOUS на output_mapping:
`Node procedure readiness timeout: complete output definition page at 0; no mutation was authorized`.
Сам узел и предыдущие изменения уже могли быть внесены — это не NOT_APPLIED всей операции.

В журнале: после загрузки sources step110/111 radioTable=true; к timeout step114
radioLinks=true, table=false. Причина последующего переключения ещё НЕ доказана.
Нельзя утверждать, что исходный запрос загрузки точно вызвал гонку.

Живая отдельная копия `.dock/calculator-v3/live-1789647904764`:
- seed `/mimo/MiMo-task47-diagnostic-current-20260917/scenario.lgp`;
- copy `/mimo/MiMo-task19-diagnostic-retry2-20260917/output-diagnostic.lgp`;
- `output-view-probe.json`: открытие точного выходного порта PASS;
- `source-fetch-probe.json`: штатная загрузка 0→6 полей, таблица читается;
- `links-mode-repro.json`: принудительно выбран native «Связи» при уже загруженных
  sources; ensureGroupingOutputSources вернул состояние без возврата к таблице;
  следующий readOutputDefinitionPages воспроизвёл тот же timeout.
Это подтверждает класс дефекта, но не окончательную причину самопереключения в run19.

Скрипты сохранены в `.dock/mimo-stability-20260916/`:
`output-view-probe.operator.js`, `source-fetch-probe.operator.js`,
`links-mode-probe.operator.js`. Не зависеть от временных `/tmp` файлов.

### Незавершённая правка — проверить первой

Создан **`client/lib/mapping-table-view.mjs`** и добавлен вызов в
`client/lib/port-mapping-procedure.mjs:configureOutputField` перед чтением definitions.
Helper пытается выбрать «Таблица», сохраняя owner/native mapping и проверяя radio.
**Это черновик: тестов нет, live проверки исправления нет, ещё не считать исправленным.**
Нужны негативные тесты смены owner/root/schema/links, корректный no-op table,
проверка реального перехода Links→Table, whole-node replay. Решить, достаточно ли
одной точки вызова или необходим общий invariant перед всеми читателями definitions.
Не ослаблять блокировку неизвестных эффектов ради прохождения теста.

## 5. Порядок продолжения

1. Прочитать этот документ, AGENTS.md, текущий diff и подробный отчёт; проверить
   OpenViking health/actor scope, ветку, процессы, Loginom и сохранность артефактов.
2. Проверить завершение диагностической копии по дополнительной записи ниже.
   Состояние исходной остановленной MiMo session остаётся UNCONFIRMED; не закрывать
   чужие/непроверенные пакеты, не объявлять их сохранёнными.
3. Доработать/проверить черновик table view и локализовать позднее переключение;
   выполнить адресные тесты и живой повтор на новой диагностической копии.
4. Исправить первый read без preview. Полный проверенный output mapping не равен
   произвольной схеме; сохранить проверки свежего исполнения и идентичности.
5. Проверить неизвестное Done, остановку после промежуточного save, широкие таблицы
   и точное сообщение о неполноте. Не снимать блокировку просто закрытием мастера.
6. Только затем новый task19 Hermes в новом каталоге с неизменным runtime;
   независимый аудит ВСЕГО задания: ROI/CTR/конверсия, бюджеты, аудитории,
   отрицательные ROI и рекомендации с корректной опорой на данные.
7. Довести остальные основные задания, устранить найденные причины ошибок,
   затем закрепить версию и выполнить все38 приёмочных запусков. Не переносить
   диагностические успехи в acceptance автоматически.
8. Итоговый отчёт с честными ограничениями; merge/publish по отдельной команде.

## 6. Практические команды и файлы

Node: `/Users/kartamyshev/.loginom-dock/current/runtime/node`.
Запускатель: `python3 tools/loginom-acceptance/analytic_corpus.py --task N
--phase diagnostic --storage /mimo/NEW_UNIQUE_DIR
--dock-config .dock/mimo-stability-20260916/operator-config.json
--timeout 3600 --max-turns 120` (одной командой).
Создать/проверить каталог через `scenario-storage.mjs` до запуска.
Не печатать operator-config: содержит секреты.

`scenario_audit_plan.py` строит план, который оператор обязан проверить;
`scenario-reopen.mjs` читает сохранённый пакет на закреплённом runtime;
`audit_corpus_tables.py` и специализированные аудиторы проверяют полные таблицы.
Read-only diagnostic допускается лишь явно с acceptance_eligible=false.

Существующий интерактивный harness: `tools/loginom-acceptance/calculator-live.mjs`,
TTY обязателен, одна строка `{ "id":"step", "file":"/absolute/operator.js" }`.
Закрывать browser только после подтверждённого cleanup; не считать browser_close
доказательством закрытия серверного сеанса.

Старые значения active PID/terminal в report/state — исторические. Приоритет у
этого checkpoint и новых наблюдений, а не у статуса файла. Никаких новых агентов,
новых узлов/потоков или автоматизаций в рамках передачи не запускалось.

## 7. Подтверждённое завершение при передаче

Диагностическая копия закрыта, выход из mimo подтверждён, **изменения не
отбрасывались**: `live-1789647904764/pause-cleanup-settled.json`, statusSUCCEEDED,
packages1→0, logged_out=true, unsaved_changes_discarded=false. Первый cleanup
поймал переходное DIALOG_OR_OPERATION_OPEN; после наблюдения исчезновения
диалогов повторная проверка успешно завершилась. Это относится только к отдельной
копии output-diagnostic.lgp; исходный MiMo run19 остаётся UNCONFIRMED.
Команда закрытия диагностического harness отправлена после этого подтверждения.

Попытка перевести встроенную цель в paused вернула `this thread has no goal`: в
текущем runtime записи цели уже нет. Новая цель не создавалась, работа остановлена
по явной просьбе пользователя.

## 8. Возобновление 17 сентября: текущие подтверждения

- Память healthy/actor, ветка прежняя; старых calculator-live/analytic_corpus/
  Hermes chat/scenario-reopen процессов не обнаружено. Стенд HTTP200.
- Черновой helper проверен в отдельной копии `live-1789649016454`:
  Links→Table, 6 полей без изменения связей; cleanup SUCCEEDED, packages1→0,
  logout, без отбрасывания изменений.
- Восстановление table view перенесено в общий reader definitions (включая
  адресное чтение). Проверяются owner/root/port, весь mapping, autosync/produce;
  неизвестный жест не повторяется. 46 адресных тестов PASS.
- Первый read берёт схему из подтверждённого локального configuration.readback
  при отсутствии preview. Проверены owner, phase receipts и полный mapping;
  fresh execution и сопоставление живой схемы сохранены. 22 теста PASS.
- Runtime `b62c1fce5f5c957fbd745721a19abe95c94fd50c807cf65ec0b7f3ace71d3679`,
  `live-1789649343529`: общий reader Links→Table PASS; новая grouping без preview,
  отдельный first read — 23×6, независимый CSV audit всех138ячеек PASS (120строк
  источника, максимум разницы2E-10). Повтор same-ID без browser calls;1новый узел,
  1связь, read без конфигурационных фаз. Первоначальное ожидание20строк в
  операторском скрипте было ошибочным: CSV независимо подтвердил23группы.
- Полный suite в sandbox:2109PASS/9FAIL/1SKIP; все9сбоев требуют локальных
  сокетов/leases, запрещённых sandbox (EPERM). Нужен повтор с доступом после
  окончания живой диагностики. Не считать suite полностью пройденным.
- Журнал старого run19: на steps112→113 переключился radio без промежуточной
  команды Dock. Поздний callback возможен, но точная причина не доказана.
- Проверки широкой таблицы/остановки после промежуточного save и unknown Done
  ещё выполняются. Исходный run19 session teardown по-прежнему UNCONFIRMED;
  новая диагностика его не закрывает и не доказывает его сохранение.

### Диагностические gates перед новым task19

- Повтор4файлов, зависимых от локальных сокетов, вне sandbox:14/14PASS;
  все9исходных отказов устранены окружением, правок продукта не потребовалось.
  Совокупно полный suite2118PASS/1SKIP (основной запуск + адресный повтор).
- `live-1789649343529`: широкая таблица27полей,10из23строк;210значений1/3
  сверены, precision17significant. user-v1 явно предупреждает о10из23строк.
  После intermediate-save новые изменения дали BLOCKED/UNSAVED_CHANGES при
  cleanup безdiscard; финальный checkpoint и logoutSUCCEEDED, packages1→0.
- `text-export/live-1789649873223`: операторская инъекция скрыла доступ к
  исходной outputDone receipt (сама receipt сохранена). AMBIGUOUS и отказ
  resume без новых жестов; после возврата доступа к оригиналу inspect/resume
  того жеID SUCCEEDED, граф без новых узлов/связей. Это отрицательная проверка,
  а не разрешение восстанавливать неизвестное Done без доказательств.
- Набор recovery/persistence/preview31PASS; teardownPython2PASS.
- Audit-plan расширен для первого read безpreview: схема только из исходного
  owned readback и завершённых phase receipts; owner/schema/port/exclusion
  отрицательные случаи проверены. Python6PASS.
- Финальная серия38 всё ещёPENDING. Следующий шаг — новый диагностический
  task19 на текущем неизменном runtime и независимый аудит полного задания.

### Блокировка нового модельного запуска автоматической проверкой

Новый task19 **НЕ стартовал**: две попытки запуска отклонены auto-review
до создания процесса. Причина — общая политика Sol в AGENTS.md и требование
нового явного разрешения на Xiaomi/передачу входов. Повторная проверка с указанием
нынешней команды продолжения и профильного handoff также отклонена. Обход не
предпринимался. Пользователю задан точный вопрос о разрешении Xiaomi/MiMo/medium
и description.md+dataset.csv для диагностики и38приёмочных запусков; ждём ответа.

Готовая проверенная папка `/mimo/MiMo-task19-resume-20260917`, storage cleanup
SUCCEEDED. Оба новых диагностических harness завершены после сохранения и logout.
Старый run19 teardown остаётся UNCONFIRMED. Inbox координатора проверен:
все доставленные события handled/resolved; новых разработчиков/агентов нет.

### Явное разрешение пользователя получено

17сентября пользователь ответил «Да, разрешаю Xiaomi/MiMo и указанные вложения»
на вопрос о Hermes / существующей подписке Xiaomi / mimo-v2.5 / medium,
исходных description.md и data/dataset.csv из корпуса analitic для диагностики
и согласованной серии38запусков. Это task-scoped исключение из общей политики
Sol; другие задачи и запрет fallback не меняются. Можно запускать task19.

### Task19 завершён; аналитическая приёмка FAIL

Run `20260917-160352-21fec178`, неизменный model runtime `b62c1fce…`:
Xiaomi/mimo-v2.5/medium проверен; exit0, 862.473с, 28tool calls/26API calls,
input120925/output13323/cache2128512. Сохранение `scenario.lgp` в новой папке
подтверждено; model cleanup PASS (packages1→0, logout, безdiscard).
Это не приёмка: независимый CSV oracle и финальный ответ выявили неверные
коэффициенты каналов, ошибочный top5аудиторий, отсутствие анализа влияния бюджета,
смешение убыточных кампаний с убыточными каналами, противоречия рекомендаций и
отсутствие оговорок о периоде/активности. Подробности: run/business-audit.json.
Повторное открытие подтвердило два одноимённых калькулятора, один после отказа
calc003 оставлен при создании calc004. Полная серия38 остаётся PENDING.

Первый independent reader `task19-resume-independent` остановился на открытии
настроек второго калькулятора: exact GUID совпал, но графический ключ `@1`
отличается от native breadcrumb `-3`. Полный импорт прочитан; settings audit
и полный сценарный аудит не завершены. Cleanup BLOCKED/DIALOG_OR_OPERATION_OPEN;
сообщение harness о retained browser не является доказательством его сохранения:
после exit соответствующего процесса в списке не обнаружено. Logout не подтверждён.

После окончания reader добавлена проверяемая native GUID привязка pending
wizard breadcrumb, одинаково проверяемая host и browser mutation guard. Нельзя
подменять владельца только совпавшим label. Повторный независимый reader запущен
на audit runtime `5a2fd8cc66f4acc6b7d45acfac78155cb7ba354b6fc456ba9261ed91ab359e17`,
план `task19-resume-plan-reader-fixed.json`, output `task19-resume-independent-reader-fixed`.
Model runtime сохранён отдельно; это диагностический аудит новым reader,
не доказательство приёмки модели на новой версии. Во время reader runtime не менять.


### Уточнение диагностики одноимённых узлов

Первая правка по active controller оказалась недостаточной: до подтверждения
он ещё указывает на workflow. Live `live-1789652284597` выявил точную связь
отрисованных breadcrumb components через `_node.data.node`: GUID ModelNodeTreeNode,
его workflow и ParentNode WizardTreeNode. Эта связь теперь проверяется вместе
с DOM component binding и native graph model. Добавлены проверки открытия после
подтверждения и возврата при Cancel по GUID (без label-based выбора).

`live-1789652668363`, runtime `a7a3f594ca7f6c91567733ee303b7e81bbdb3aba910f9dbd2157facb939806dd`:
открытие нужного duplicate с деактивацией PASS, сохранённые3формулы PASS,
Cancel→тот жеGUID PASS, cleanup1→0/logout PASS, no discard. Использована отдельная
копия `duplicate-owner-diagnostic2.lgp`, оригинал не изменён.

Первый повтор на исходном пакете выявил оставшуюся lock прежнего auditor;
read-only diagnostic `task19-resume-independent-readonly-fixed` прочитал полный
импорт500×12 и калькулятор500×15. Независимое сравнение всех13500ячеек PASS,
maxdiff3.82978723404E-13; но reader остановился на lifecycle до последнего
исправления, cleanup BLOCKED. Эти данные не являются whole-scenario acceptance.

Сейчас `task19-remaining-independent` читает остальные5полных таблиц и настройки
на последнем runtime в read-only diagnostic. План явно исключает уже проверенный
импорт и повтор500строк калькулятора; калькулятор проверяется по настройкам.
Это составной диагностический аудит нескольких сессий, не38приёмочная серия.


### Task19: составной диагностический аудит завершён

`task19-composite-diagnostic-audit.json`: все7рабочих таблиц,1147строк,
15515ячеек совпали с независимыми ожиданиями; сохранённые настройки7рабочих
узлов подтверждены. Последний read-only reader проверил неизменность графа
и завершил cleanup/logout SUCCEEDED. Оставленный калькулятор присутствует
в сохранённом графе; его собственные настройки отдельно не открывались.
Численная корректность выбранных операций не исправляет неверный выбор метрик
и бизнес-выводов: общий результат task19 остаётся FAIL.

Последнее исправление duplicate lifecycle:386/386адресных тестов PASS,
`git diff --check` для затронутых файлов PASS. Прежние падения отмены в test fixture
были связаны с фиктивным постоянным surface=wizard; fixture теперь отражает
реальный переход wizard→graph и проверяет чужие GUID/document/workflow.
Нет утверждения о полной приёмке на едином runtime: исходная MiMo версия b62c…,
последний аудитор a7a3…. Final38 по-прежнему PENDING.
Следующее основное диагностическое задание:34, новый каталог и исходные вложения.


### Task34 запущен на проверенном runtime

`20260917-165351-f4fc705e`, каталог `/mimo/MiMo-task34-resume-20260917`,
phase diagnostic, runtime `a7a3f594ca7f6c91567733ee303b7e81bbdb3aba910f9dbd2157facb939806dd`.
Эффективный профиль SQLite VERIFIED Xiaomi/mimo-v2.5/medium, reasoning enabled.
Входы — исходные description.md и dataset.csv, без oracle/готового графа.
До запуска новый каталог проверен в UI и storage session logout SUCCEEDED.
Model-run активен: runtime/skill не менять. Финальная серия38 не начата.
