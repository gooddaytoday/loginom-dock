# Установка клиента с «Фильтром строк» — 11 сентября 2026

На Mac установлен **`0.1.0-dev.20260910.3-38f8be0b843b`** с подпланом06 и
четырьмя исправлениями, подтверждёнными живым ревью. Код отправлен в main:
`79f072dc51a135d251c439f073f892e715040a8d`. Плагины Codex и Hermes обновлены.

## Происхождение и целостность

[Автономная приёмка исходников](row-filter-review-live-2026-09-11.md):
49/49 frozen PASS, десять отрицательных подмен отклонены;
`openai-codex / gpt-5.6-sol / low`. Source runtime:
`fd2999ce9f87a878882a20962eda56b69941d15dcedd8d7a70852579c5819e53`.

Установочный снимок **`668d9f95a1f67d085592f800d65c6f38f3bef1cc`** — отдельный
локальный потомок main-коммита. Сохранена прежняя диагностика API в четырёх файлах:
`client/bin/diagnostic.mjs`, `client/lib/local-diagnostics.mjs`,
`client/test/local-diagnostics.test.mjs`, `plugins/loginom-dock-hermes/__init__.py`.
Они побайтно совпали с предыдущим клиентом; 343 остальных build inputs — с main.
Все 344 исходных файла, вошедшие в комплект, также сверены побайтно.
Локальный снимок не публиковался в main или GitHub Release.

Сборка выполнена на VPS в
`/opt/loginom-dock/client-build/row-filter-20260911-79f072dc/`.
Builder проверил соответствие Git-коммиту, `sourceDirty=false`.
Node 24.19.0 и закреплённые зависимости проверены по контрольным суммам.
SHA-256 архива macOS:
`f0fd1cb8eb09218e0072933d292ebb3b902769460a2f6dee859825d03ef30b71`.
Проверены все **4161** файл до установки и в установленном комплекте.

Runtime установочного снимка, VPS-комплекта, установленного клиента и живой сессии:
`f14d6452ef74c43fd75730f87bae2db8bb3d4e7faca2aeb5604a47af96d0f61e`.
Отличие от принятого source runtime обусловлено только сохранённой диагностикой;
обработчики и их контракты совпали с main.

## Проверки установки

Mac: **1288 PASS, 0 FAIL, 1 SKIP**, 40,52 с; пять Python-проверок сохранённой
диагностики также прошли. Пропуск относится к Windows CMD/EXE.
Первый Mac-прогон в sandbox дал девять ошибок из-за запрета локальных сокетов
`EPERM`; полный повтор вне sandbox прошёл без изменений кода.
Исходники ранее прошли 1287 клиентских и 475 Python-проверок.

Дополнительный VPS/Linux-прогон: 1287 PASS, 1 FAIL, 1 SKIP (221,61 с).
Сбой `native process console joins bounded grids` возник в искусственном DOM.
Повтор всего `workspace-ui.test.mjs`: 258 PASS, 2 FAIL (207,09 с); прежний тест
прошёл, но отказали `addressed output definitions cover clipped rows` и
`compact filter observation retains visible final rows`. Длительности отказов
585 и 507 мс совпадают с областью риска 500-мс ограничения сканирования;
нагрузочная причина вероятна, но не объявлена доказанной. Тесты и ограничения
runtime не изменялись ради зелёного результата. Следующая попытка изолированного
прогона трёх тестов не стартовала: VPS отклонил SSH-аутентификацию.
Полностью зелёный Linux-прогон этой поставки **не заявляется**.
Это дополнительная проверка macOS-комплекта Linux-runtime, не Linux-релиз.
Живое прохождение подтверждено отдельно ниже.

Новые MCP-подключения через установленный launcher: Codex — 48 инструментов,
Hermes — 34; оба предоставили `dock_prepare`. Шесть файлов Codex-плагина и три
Hermes совпали с комплектом. Dock config и Hermes user-profile сохранились
побайтно; Hermes config — семантически целиком. В Codex config изменился только
ожидаемый `marketplaces.loginom-dock.source`.

Сохранён профиль `openai-codex / gpt-5.6-sol / low` без fallback. Шлюз Hermes
перезапущен: прежний PID 80137, новый launchd PID 23991. Предупреждение CLI
о старом service definition сохраняется; launchd-определение не изменялось.
Текущий пользовательский URL `dev-test.bg.local/staging/app-debug/` сохранён;
диагностика установки использует отдельную сессию Loginom 7.4.2.

## Живая проверка установленного клиента

Loginom **7.4.2**, видимое максимизированное окно Chromium, `viewport: null`.
Принятый пакет открыт только для чтения; сохранена и заново открыта отдельная копия
`/user/dock-p3/row-filter-installed-20260911.lgp`. Исходный пакет
`/user/dock-p3/packages/Dock-acceptance-20260911-181616-1338ee3f.lgp` не изменялся.

После выполнения импорта через панель Loginom установленный публичный обработчик
`transform.filter_data / conditions` выполнил существующий «Отбор» с
`parameters: {}`, `inputs: []`, `mappings: []`. Получены **5 + 5 строк × 5 полей**;
все 50 значений обоих выходов сверены независимым проверяющим с CSV oracle.
Подтверждены новое выполнение, восстановление формата таблиц, прежние условия,
три mapping, GUID узлов и единственной связи. Runtime живой сессии совпал с
установленным комплектом и установочным снимком.

Независимый аудит: **11/11 PASS**. Использованы существующие проверяющие сырых
наблюдений; операторская обвязка не объявляется frozen-приёмкой Hermes.
Ожидание подписи кнопки импорта в старой обвязке завершилось таймаутом: реальная
подпись «Деактивировать узел» не содержит ожидаемого «(F9)». Фактический UI проверен
до продолжения, импорт повторно не запускался. Это ограничение диагностического
скрипта, не отказ установленного обработчика.

После выполнения пакет повторно не сохранялся; эта проверка подтверждает чтение
сохранённых настроек и выполнение установленным клиентом. Сохранение изменённых
условий и автономность Hermes подтверждены исходной приёмкой 49/49.
Диагностический браузер закрыт. Оставшийся после закрытия процесс операторской
обвязки завершён адресно; чужие процессы не затрагивались.

Доказательства: `live-1789141466433/installed-independent-audit.json`, `filter.json`,
`execution-events.jsonl`, `copy-preparation.json`, `inspect-before.json`,
`inspect-after.json`, `geometry.json` в приватном каталоге обновления.

## Откат и границы

`~/.loginom-dock/previous` указывает на
`releases/0.1.0-dev.20260910.3-7adeb19b4c91`; все 4152 файла предыдущей версии
проверены. Снимки настроек и регистраций:
`.dock/client-update-row-filter-20260911/before-install/`.
Полный откат включает runtime и регистрации обоих плагинов.
Hermes закреплён на локальное зеркало
`~/.loginom-dock/native-sources/loginom-dock-668d9f95a1f67d085592f800d65c6f38f3bef1cc.git`.

Для загрузки обновлённого skill и инструментов Codex нужна новая задача.
Публичные архивы, landing, API/MCP-сервер и его модели не обновлялись.
Отдельная автономная приёмка Hermes для установки не запускалась: 49/49 относится
к принятым исходникам. Совместимость с Loginom 7.5 и Windows не заявляется.
Общий `UI_SCAN_LIMIT` при preflight preview нового узла на 1000 полях остаётся
отдельным ограничением; широкая диагностика фильтра не подменяет этот публичный путь.

Приватные доказательства: `.dock/client-update-row-filter-20260911/` —
`combined-source.json`, `combined-runtime.json`, `combined.tar.gz.manifest.json`,
`bundle-verification.json`, `installation.json`, `configuration-verification.json`,
`native-after.json`, `mcp-smoke.json`, `final-bundle-verification.json`,
`final-runtime-verification.json`, `linux-tests-host.log`, `linux-retest.log`.
