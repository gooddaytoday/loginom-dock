# Узел 16: конкретное решение по точному чтению variant

13 сентября 2026. **Пользователь прямо разрешил ограниченный эксперимент.**

На конкретный вопрос координатора о test-1, до50строк×8столбцов и отсутствии
изменений общего плагина пользователь ответил «разрешаю». Разрешены реализация
и живой запуск только диагностического прототипа в границах ниже. Интеграция
нового reader в продукт этим не разрешена.

Empty persistence закрыт в узком проверенном объёме: source f998985c,
report41856b1d; минимальный пакет сохраняет node/port/link identity между разными
сессиями, явное выполнение source восстанавливает7полей/0строк, Collapse выдаёт
полные0×4. Ранний guard неактивного source не открывает мастер и возвращает
NOT_APPLIED/no effect/cleanup. Три сохранённых аудитора PASS и12отказов подмен
сверены помощником; повторные tests/UI не запускались. Автоматическое восстановление
старого pending и полная готовность узла не заявлены.

## Разрешённый объём

Разрешён отдельный ограниченный диагностический прототип чтения tagged binary
response IBGDataSource.AsVariant(Row,Col), method321/interface116 до ReadVariant,
в собственной test-1 сессии Loginom7.4.2, максимум50строк×8столбцов.
Это новая внутренняя RPC capability, а не текущий cached-UI reader.
Исходный план разработчика:
`.worktrees/node-16-collapse-columns/docs/loginom-dock/collapse-variant-prototype-plan-2026-09-13.md`.

До каждого чтения доказать datasource owner, document/workflow/node/port/index,
execution freshness, schema/row bounds и фиксированные build/source SHA; перепроверить
после чтения. Использовать только фиксированный read method, whitelist tags,
lossless integer/real/datetime/Null, отказ при неизвестном tag, потере owner/freshness
или неполном payload. Никаких произвольных RPC, записей через RPC, извлечения
credentials, shared hooks/plugin/transport или автоматического fallback.
Пакет/процесс/браузер диагностические, отдельные от других потоков.
Сначала проверить семантику ответа и ownership на собственном тестовом наборе;
невозможность доказать их останавливает эксперимент. Результат — evidence/report,
не автоматическое включение нового способа чтения в продукт.

ReadVariant статически теряет discriminator; прямой AsVariant proxy возвращает
уже primitive. Соответствие сырых tags исходному subtype и полный Preview caller-chain
ещё не подтверждены. Эксперимент может завершиться выводом о непригодности подхода.
Пассивный захват обычного UI-response пока не имеет доказанного owner/row binding.

Причина отдельного решения — изменение согласованной архитектурной границы:
[архитектура](architecture.md#уточнение-mvp-после-разбора-4-сентября) описывает
адаптеры cached UI state, «а не универсальный DOM API и не вызовы RPC».
Прежний запрет запуска до решения пользователя заменён этим точным разрешением;
общая архитектурная граница остальных операций сохраняется.
Это не новый отказ автоматической проверки и не разрешение менять общий плагин.

## Результат первого эксперимента

Коммит bff616f3 проверен read-only помощником: собственные15×5/8×4, fixed321/116,
публичные handlers не изменены. Реальные int64/real64/string/bool/Null/datetime
подтверждены для наблюдённых случаев; int32/real32 live не доказаны. Нет server
snapshot token, live cancellation/network loss и общей DST/zone семантики.
Full variant acceptance остаётся BLOCKED. Разработчику подтверждён приём результата.
У пользователя запрошен следующий отдельный этап усиления изолированного reader
по конкурентным изменениям, потере соединения и недоказанным типам/датам.
До ответа новая фаза и продуктовая интеграция не разрешены.

Пользователь ответил «Да, продолжить изолированные проверки». Дополнительный этап
разрешён в той же test-1 среде и лимитах50×8/fixed321/116; публичная интеграция
по-прежнему не разрешена. Проверки сбоев не затрагивают сервер/общую сеть/чужие сессии.
