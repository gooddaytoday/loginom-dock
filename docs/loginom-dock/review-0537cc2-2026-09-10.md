# Ревью 0537cc2…84103a88 — 10–11 сентября 2026

Подтверждены на живом Loginom 7.4.2 и исправлены пять дефектов. Финальный
Hermes выполнил обе операции сортировки и сохранил пакет; независимый аудит
операций прошёл 55/55 проверок, аудит содержимого после нового открытия — 12/12.
Исходники исправлены, сборка и установка нового клиента не выполнялись.

Проверены изменения от `0537cc2f1c205be60bd2a92a274ab0a03df87d8f`
**включительно** до `84103a88`: `0537cc2^..84103a88`, 118 файлов, +5760/−191.
Полное ревью выполнено Codex. Вспомогательные агенты запускали тесты,
воспроизводили конкретный установленный сбой и реализовали заданные проверки
доказательств; общее ревью им не передавалось.

## Подтверждённые дефекты и исправления

1. **[P2] Согласуйте ревизии MCP и hooks Codex** —
   `plugins/loginom-dock/.mcp.json:9`.
   MCP подготавливал сессию как `.20260910.2`, hooks передавали `.3`.
   После реального `dock_prepare` со статусом READY на 7.4.2 захват отвергал
   сессию: `Dock preparation does not belong to this adapter`, архив оставался
   неактивным. MCP manifest и версия плагина приведены к `.3`. Повторная
   live-проверка активировала архив и записала три события в локальную очередь.
   Проверено удаление реального ключа и синтетического секрета из payload
   и SQLite. Регрессия использует фактические manifests, включая Windows-команду.

2. **[P2] Проверяйте эффективный вход существующей сортировки** —
   `client/lib/sorting-preflight.mjs:7`, `client/lib/sorting-parameters.mjs:15`.
   После переименования `total_revenue` в `RevenueAlias` смена направления
   с `mappings=[]` отклонялась: `Sorting input field is missing: RevenueAlias`.
   Проверялись имена upstream-выхода вместо сохранённого входа самого узла.
   Дополнительно замена ключей требовала повторного указания существующей связи
   и отвергала `inputs=[]`: `Sorting replacement requires one explicit input`.
   Теперь существующий узел проверяется по эффективной схеме своего входа,
   до её редактирования и подтверждения. Сохранённые имена и связь можно оставить.
   Для нового узла сохранены предварительная проверка источника и обязательный
   явный вход; ограничения индекса и количества входов действуют в обоих случаях.
   У существующего узла проверка требует открытия мастера, поэтому прежняя
   гарантия отказа без возможного эффекта до его открытия не заявляется.

3. **[P2] Разрешите заменить исчезнувший ключ сортировки** —
   `client/lib/sorting-context.mjs:28`.
   После переименования входа Loginom сохраняет старую строку ключа
   с `DataType=0`. Чтение мастера отвергало её, а корректная полная замена
   ключей завершалась тайм-аутом готовности. Теперь принимается только
   наблюдённый маркер отсутствующего поля с точной идентичностью, чтобы удалить
   его при полной замене. Сохранить отсутствующий ключ или принять произвольную
   неизвестную строку нельзя; итоговые ключи проверяются повторно.

4. **[P2] Обработайте выходное соответствие после изменения входа** —
   `client/lib/sorting-node.mjs:15`.
   Next сортировки ожидал Done, но после переименования поля Loginom 7.4.2
   открывает дополнительное выходное соответствие с непривязанным старым полем.
   Добавлены проверка источников этой страницы, удаление только устаревшего
   непривязанного поля, синхронизация новых полей, проверка сохранённых полей
   и восстановление autosync. Переиспользован существующий механизм Grouping
   с отдельным контрактом схемы Sorting; разрешён соответствующий переход UI.

5. **[P2] Учитывайте восстановление формата в независимом аудиторе** —
   изменение поведения `client/lib/calculator-node.mjs:197`, исправление
   `tools/loginom-acceptance/import_output_evidence.py:63`.
   Точное чтение применяет временный формат, затем восстанавливает исходный.
   Аудитор требовал единственный Apply и отвергал реальные успешные операции
   с `one_format_and_filter_apply_required`. Второй Apply теперь принимается
   только при наличии исходных наблюдений исходного и восстановленного формата
   той же таблицы, после чтения ячеек, с закрытием диалога и возвратом в граф.
   Проверяются идентичность, порядок и типы полей, маски и настройки; одного
   флага восстановления недостаточно. Исторический вариант с одним Apply
   сохранён. Все 24 отрицательные подмены восстановления отвергнуты.

## Проверка кода и прямое воспроизведение

- Полный клиент на Node 24.19.0: **1191 PASS, 0 FAIL, 1 SKIP**, 36,88 с.
  SHA-256 всех 199 файлов `client/lib` и `client/test` до и после совпали.
  Пропущен Windows-тест выбора CMD/EXE по PATHEXT/PATH на macOS.
- Python: **443 PASS, 0 FAIL**; адресная проверка table evidence — **28 PASS**.
- Текущий runtime:
  `5a079468ca3f08a649b4b6f9d15af2cf21623894688d185e489685560bef5b77`.
  Этот же runtime использован в финальной прямой проверке, Hermes и новом
  открытии сохранённого пакета. После финального запуска он не изменялся.
- Стенд: `http://logi-test-plan.bg.local/app/?testable=true`, Loginom **7.4.2**,
  macOS/Chromium, явно выбранные аккаунт `user` и каталог `/user/dock-p3`.
  Отдельные диагностические профили, реальный MCP wire. `--start-maximized`
  и `viewport:null` подтверждены: 1508×862 внутри окна 1508×949,
  доступный экран 1512×949. Диагностические браузеры закрыты.
- На текущем runtime два запроса с `inputs=[]` прошли: переименование входа
  в `RevenueAlias` с явными input/output mappings и DESC; затем только ASC
  с `mappings=[]`. Независимо проверены исходные настройки и все три строки.
  Сохранение `/user/dock-p3/review-0537cc2-fixed-1789074934382.lgp`:
  **45/45 PASS** по исходным квитанциям конфигурации, данных и сохранения.

Исходный CSV: 12 строк, 593 байта, SHA-256
`e87d0bea23aecface0db7475ba684e90d08f101e2b18da2e7fe9e10c31aa8f9b`.
Независимый расчёт Decimal даёт ASC: **Клавиатура 131600, Монитор 287500,
Ноутбук 417000**. Валюта в CSV отсутствует и не приписывалась результату.

## Финальная автономная приёмка и сохранённый пакет

Прогон **`20260911-001652`**, существующая ChatGPT-подписка,
**`openai-codex / gpt-5.6-sol / low`**, без смены провайдера или модели.
Исходники runtime, инструменты проверки, эталон CSV и графа зафиксированы
до запуска. Подтверждены неизменность runtime/harness и отсутствие fallback.
В активный браузер Hermes Codex не вмешивался.

Hermes сам получил ссылки текущего документа/сценария, выполнил ровно две
операции существующей сортировки: `sort-rating-desc-001` и
`sort-rating-asc-001`. Первая переименовала вход в `ReviewRevenue`
(«Выручка review») и задала DESC; вторая сохранила соответствия и задала ASC.
Обе использовали `inputs=[]`, вторая — `mappings=[]`. Оба новых выполнения
вернули все три правильных значения. Остались прежние locale/cache/thread
настройки, шесть узлов обработки и пять связей.

Одна финальная операция `save-final-review-001` сохранила
**`/user/dock-p3/review-hermes-20260911-001652.lgp`** без промежуточного
сохранения и переоткрытия в обычном пути Hermes. Процесс завершился с кодом 0
за **137,02 с** (2 мин 17 с), восемь модельных вызовов. Полный usage провайдера:
201851 токен, включая 167936 прочитанных из кеша; запись в кеш — 0.

Исходный замороженный аудитор дал **54/55 PASS**: единственный отказ —
`save_after_all_work`. Он ошибочно считал обязательным последнее событие
`completed`, хотя `bridge.mjs` после него доставляет `verification_delivered`.
Отказ и исходный аудитор сохранены. Отдельная версия `audit-hermes-reviewed.py`
допускает только одну такую квитанцию того же сохранения с точным содержимым
и SHA-256 завершённого outcome; любое другое последующее действие отклоняется.
Это изменение локального аудитора сделано **после** модельного запуска;
оно не объявляется заранее замороженной проверкой. Исходные данные, продукт
и запуск не менялись, новый модельный прогон для этого не выполнялся.
Повторный независимый аудит: **55/55 PASS** с привязкой публичных ответов
user-v1 к исходным запросам, конфигурации, выполнениям и точным данным.
Пересмотренный аудитор повторно отверг **32/32** прежних отрицательных подмены
и **17/17** новых подмен квитанции после сохранения. Обе допустимые формы
окончания журнала прошли по 45/45 проверок; исходные файлы остались неизменными.
Аудитор нового открытия до модельного запуска также отверг шесть подмен
пути, узла, связи, суммы, направления и повторной настройки.

В новой отдельной сессии после завершения Hermes открыт именно сохранённый
пакет. Без повторной настройки выполнен тот же узел; новый документ и новое
выполнение подтвердили alias, ASC, все суммы и восстановление формата.
Полный граф совпал с исходным по GUID, положениям и пяти связям
(шесть узлов обработки плюс системный узел переменных).
Аудитор повторного открытия, **замороженный до запуска Hermes**, дал
**12/12 PASS**, `persisted_content_verified=true`.

## Неуспешные опыты и границы результата

| Опыт | Результат и последующее действие |
| --- | --- |
| `20260910-235309` | Остановлен до сохранения: Hermes получил имя вместо native GUID и перешёл к общему UI. Сбой переменной времени локального launcher после старта ребёнка отдельно зафиксирован; к работающему процессу подключён collector. Финальный код/usage недоступны и не подменены нулём. Codex повторно проверил GUID и публичное наблюдение на живом UI. |
| `20260911-000705` | Выявлен оставшийся запрет `inputs=[]` из дефекта № 2. Остановлен до сохранения для Codex-диагностики, код −15, usage отсутствует. Codex воспроизвёл отказ и исправил валидатор перед следующим запуском. |
| `20260911-001652` | Обе операции и финальное сохранение успешны; пересмотренный аудит 55/55, замороженный аудит нового открытия 12/12. |

При ранней прямой диагностике первый запрос после открытия столкнулся
с временным уведомлением «только чтение»; новый запрос после его исчезновения
прошёл. Этот исходный отказ сохранён и не включён в новые регрессии.

Публичное graph-наблюдение не сообщает native GUID существующего узла;
это поведение присутствовало уже в `0537cc2^`. Финальная проверка обработчика
использовала заранее подтверждённый GUID, переданный в задании Hermes.
Самостоятельный поиск этого GUID публичным API в результат не входит.

Исправления и отчёт составляют один набор изменений ревью. Сборка на VPS,
обновление установленного клиента и публикация установочных архивов не выполнялись. Проверка Windows ограничена
метаданными команд и переносимыми тестами. Проверены перечисленные регрессии
на 7.4.2; полная приёмка V4/V5 и совместимость с 7.5 здесь не заявляются.

## Расположение доказательств

Приватные материалы остаются в `.dock/review-0537cc2/`, вне Git:

- `codex-hook-live{,-fixed}/`: реальный prepare и archive до/после исправления;
- `live-1789070868153/`, `live-1789071954518/`: первичные UI-отказы;
- `live-1789072143327/`, `live-1789072476398/`: ранняя прямая проверка и reopen;
- `live-1789074223568/native-reference-diagnosis.json`: диагностика GUID;
- `live-1789074693770/no-input-before.json`: воспроизведение запрета inputs;
- `live-1789074763184/two-operations-and-save-audit.json`: текущий direct PASS;
- `client-final-no-input-result.json`, `client-final-no-input-npm.log`:
  окончательный клиентский прогон и контроль исходников;
- `verifier-format-restoration/`: отрицательные подмены формата;
- `runs/20260911-001652/`: frozen-source/manifest, исходный FAIL,
  `reviewed-independent-audit.json`, неизменённые evidence и метрики;
- `live-1789075245865/`: повторное открытие финального пакета,
  `hermes-saved-reexecute.json`, `saved-graph.json` и аудит сохранённого содержимого;
- `hermes-auditor-reviewed-tests/`: отдельная проверка пересмотренного аудитора.

Первичные отказы, промежуточные результаты и исходный замороженный аудитор
сохранены отдельно и не перезаписаны как успешные.
