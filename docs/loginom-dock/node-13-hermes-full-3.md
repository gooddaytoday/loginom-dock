# Node13: полный автономный прогон и независимый reopen прошли — 14 сентября 2026

Полный frozen-v4 аудит дал **45/45 PASS**, включая **30/30 PASS** проверок
сохранности всех шести узлов. Прогон `20260914-091807-4289644a` выполнил
девять запросов (семь Execute и два Done), подтвердил точные результаты
и один финальный `package.save_checkpoint`. Независимый сеанс заново открыл
тот же пакет и проверил все шесть узлов без исправления настроек.

Пакет: `/test-3/packages/Dock-date-time-20260914-091807-4289644a.lgp`.
Проверенная ветка: `codex/node-13-date-time`, source
`77e9cf5067871cb619b8580045c8ac3f55132ee5`, runtime
`d5fd1ba79586069c27b9f5c8626cf885f9a5fe7e28e6274b01bc0081ada9558f`.
Исправление overflow выходного порта находилось в `90fe26a0`.
Текущий документ фиксирует результат последующей проверки, не меняя runtime.

## Допуск и автономный прогон

Координатор назначил слот `node13-hermes-20260914-full3`, бюджет 14400 секунд
и 100 ходов. Исходный pending admission сохранён отдельно. После назначения
проверка документа, локальных байтов, окружения, candidate и remote pins прошла.
Использована существующая подписка `openai-codex / gpt-5.6-sol / low`, без fallback.
Launcher PID7159 и Hermes PID7631 завершились; launcher вернул exit0,
выгрузка получила `EXPORTED_PENDING_AUDIT`. Отсутствие принадлежащих прогону
процессов проверено, слот освобождён до независимого UI-аудита.

Предварительный аудит дал 44/45 PASS: отсутствовало только ещё не собранное
доказательство независимого открытия. Итоговый аудит после его сбора дал
`passed=true`, `remaining_checks=[]`. Старые результаты и отказы не заменялись.

## Независимая проверка сохранения

Новый сеанс `d2cc7fa6-bced-430e-bb6c-e526adfdf8e2` работал на том же runtime,
Loginom 7.4.2, macOS/Chromium, под test-3. Проверены фактические настройки
развёрнутого окна: `viewport:null`, окно 2044×1122, внутренняя область 2044×1035.

| Узел | Операция независимого сеанса | Результат |
|---|---|---|
| Продажи | node13-reopen-0 | SUCCEEDED, сохранённый импорт и полные 10×4 |
| Календарь | node13-reopen-1 | SUCCEEDED, настройки и полные 10×27 |
| Месяцы | node13-reopen-2 | SUCCEEDED, точные группировки и суммы |
| Кварталы | node13-reopen-3 | SUCCEEDED, точные группировки и суммы |
| Нет продаж | node13-reopen-4 | SUCCEEDED, оба выхода фильтра |
| Пустой календарь | node13-reopen-5 | SUCCEEDED, настройки и полные 0×27 |

Отдельные компонентные аудиты пяти публичных операций также прошли.
Сохранённый импорт проверен специальным диагностическим протоколом revision2:
чтение настроек, отмена мастера, проверка скачанного источника и выполнение графа.
Это не расширяет публичную поддержку `node.apply` для сохранённого импорта
в новой сессии. Общий аудит подтвердил идентичность узлов, связи, настройки,
свежие результаты и отсутствие перенастройки в диагностике.

Диагностический пакет закрыт без сохранения, `dialogs=0`. Индекс доказательств
зафиксирован после закрытия. Harness завершился с exit0; отсутствие собственного
браузера и процессов проверено. Проверка не запускала модель.

## Доказательства и границы вывода

[Полный неизменённый итог аудитора](node13-live-preflight/hermes-full-3-audit.json)
и [машинная сводка с путями и SHA](node13-live-preflight/hermes-full-3-summary.json)
содержат точные pins и результаты. Исходная выгрузка:
`.dock/node13-acceptance/runs/20260914-091807-4289644a/evidence.json`, SHA
`bfc391763cb74698f4ff874d850be006f3786cda2af5f647e1eacfe0bef67a7c`.

Пять точных транспортных ссылок Hermes на прежний running poll разрешены
заранее закреплённым строгим resolver только в памяти. Исходная выгрузка
и frozen auditor не менялись. Для UI-проверки из большого JSON выделены
18 событий prepared/checkpoint; адаптер проверил SHA оригинала и выборки.
Общий аудит использовал полную выгрузку, а не эту выборку.

`report()` в `date_time_sales_acceptance.py` вычисляет `passed` по всем checks,
но всегда записывает `node_admitted=false` и `model_started=false`. Эти флаги
оставлены буквально как в аудите. Они не описывают факт запуска исходного
Hermes и не означают формальное разрешение review, объединения или поставки.
В сообщении координатору ошибочное `node_admitted=true` немедленно исправлено.

Актуальные регрессионные доказательства: клиент **1478 PASS / 1 SKIP**, Python
**548 PASS**, continuation **31 PASS**. Девять sandbox-отказов клиентской
проверки были покрыты отдельным повтором четырёх системных файлов: **14/14 PASS**.
В этой фазе код не менялся и эти проверки без причины не повторялись.

Полная автономная проверка заявленного задания завершена в source runtime.
Исторический admission второго прогона остаётся недействительным; его ошибочный
regression receipt не используется. Серверное закрытие старого отдельного
holder-сеанса не подтверждено, его локальные процессы отсутствуют; текущий
прогон использовал другой пакет и полностью завершил собственный cleanup.
Review, новые узлы, merge, push, серверная сборка, публикация и установка
клиента не выполнялись. Следующий шаг — передача результата координатору.
