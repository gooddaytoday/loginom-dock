# Передача работы — 2026-09-06

**Работа возобновлена по просьбе пользователя.** После run203659 выполнена
отдельная live-проверка decimal field: ввод `.` + Tab оставляет `.`, переход
Next/Previous отображает `Точка (.)`. Сверено с E2E issue724 и Help text import.
Comparator принимает оба точных отображения; неизвестные и неоднозначные значения
отклоняются. 192/193 полного suite прошли до изменения устаревшего теста prompt;
после удаления проверки наличия имён tools все 62 затронутые проверки PASS.
Goal import-roundtrip переписан как требования к результату, включая полную
контрольную серию после правок и повторное чтение после reopen, без tool recipe.
Старый run остаётся FAIL из-за отсутствия исходной серии. Новый запуск начат;
runtime/harness не менять до terminal. Evidence: decimal-literal-live-review.json
и run203659-decimal-diagnostic.json в private resume-20260906.

Работа продолжена в текущей сессии по обновлённым правилам личной памяти.
HEAD пока `9afe1de5`; есть новые незакоммиченные исправления (см. ниже).

## Текущая работа

**Последняя правка уже выполнена:** no-effect NOT_APPLIED с уникальными
call/reply/journal теперь может выдавать свежие refs для следующего успешного
действия, но не включается в список выполненных переходов. 44 профильных tests
PASS, включая effect/foreign/unbound/duplicate negatives. Диагностика старого
run212312 теперь подтверждает один полный import settings/schema/mapping roundtrip:
`run212312-issued-ref-diagnostic.json`. Это не frozen PASS: transfer pre-inspect
и unscoped knowledge нарушения не сняты. Следующий шаг — свежая целевая приёмка
на обновлённых описаниях upload и ограничениях поиска. Исторические audits неизменны.

**Последний run `20260906-212312-5a621b60` завершён:** returncode0, timeoutfalse,
109 API calls, ChatGPT/Luna/medium, runtime/harness unchanged. Frozen audit19/22
SHA256 `dbf356f71a88c0e85695a42c93bff83db930ee38ec6972279b0efbb2b3a9e810`:
экспорт compaction и unscoped search не дали дойти до domain gates. Старый audit
не менялся. Экспортёр после run учитывает exact duplicate-read marker только при
архивной копии, том же timestamp/session, compression boundary и позднем точном
исходном ответе; реальные повторные чтения не объединяются. 194 Python tests PASS.
Private `run212312-export-diagnostic.json`: после исправления125calls/125replies;
unscoped search остаётся нарушением. Две transfer checks не проходят без inspect
pending upload до verify; post-verify inspect есть. Описание upload теперь требует
оба inspection, goal ограничивает knowledge search двумя source roots/list mode.

**Диагноз перед последней правкой:** issued-ref verifier игнорировал
свежие элементы из journal-bound NOT_APPLIED. Reply236/reopen-mapping-016 содержит
observation fb57d12f-f196-4835-9284-ce72e6dc8354 и выданный btnNext ref ...-968;
journal_equal=true. Следующий успешный reopen-mapping-017 использует этот ref,
но bound_receipts выбирает только SUCCEEDED, поэтому _issued возвращает None.
Нужно отдельно поддержать выдачу элементов из доказанных no-effect outcomes,
не превращая отказ в выполненный переход и не ослабляя mutation accounting.
Проверить negative cases и сохранённый evidence диагностически; до этого новый
replay не нужен. Browser runtime действий не менялся, лишь описания schema/tools.
Исторические нижние результаты не являются текущими. P3 не принят.

**Актуально после продолжения:** run `20260906-210229-3e8bd033` остановлен
операторским SIGTERM после повторения неверного expected_stage=input_mapping.
Launcher завершил экспорт, returncode Hermes=-15; frozen audit23/33 SHA256
`020ab210320a7bde140a2da54fc6e6f81eacf82bd4e081cb90ead13ad6194d5c`.
Это остановленный неуспешный run, не приёмка. Непарный последний вызов не засчитан;
usage completion отсутствует. Неизвестные эффекты не объявлены разрешёнными.
Дополнительно нет обязательного отдельного inspect resolved upload до изменений.
После terminal уточнены описание expected_stage в uiActionSchema (Text Import
next/previous, отличие input_mapping) и обязательство отдельного подтверждения
передачи в goal. 155 workspace-ui tests и 41 профильный Python test PASS.
Runtime pin изменится из-за описания schema; следующий запуск требует новых pins.
Private diagnosis: run210229-diagnosis.json. Новый replay после этих последних
уточнений ещё не запускался. Следующий шаг — новый import-roundtrip,
затем проверка baseline/reopen; шесть остальных P3 domain gates не реализованы.
Личный диагностический UI: собственный Package1/Текстовый файл, TF-1, формат,
decimal отображается Точка (.) после live next/back. Пакет не сохранён.
Production/установленный клиент не менялись, изменения не закоммичены.

**Последний результат — run `20260906-203659-96561694` завершён.**
ChatGPT/openai-codex/gpt-5.6-luna/medium, 85 API calls, returncode=0,
timed_out=false; runtime/harness unchanged. Frozen audit **52/59**, SHA256
`0ab1d25282c697c06597eb901f0484a9d68973aa5062718de243ec26ca841215`.
Transfer PASS; finish/open и повторные source/format/mapping выполнены.
wizard_settings_readback FAIL: нет полной контрольной серии source→format→mapping
после редактирования перед finish. Дополнительно до finish decimal_separator
показан как `.`, после reopen как `Точка (.)`; comparator принимает только второе.
Доказательства и точный следующий шаг: `run203659-diagnosis.json` в private
resume-20260906. Не запускать неизменённый повтор: сначала отдельная live-сверка
отображений decimal field и уточнение readback-обязательств goal-only задания,
затем профильные тесты и диагностика старого evidence без замены frozen audit.
Существующий import-roundtrip prompt содержит подробную последовательность
tools: его успех нельзя выдавать за финальную goal-only приёмку P3.
Прогон завершён, ограничение на изменение runtime/harness снято; P3 не принят.
Отдельный браузер этой задачи находится на странице входа Loginom, без входа
в чужой диагностический пакет. Production и установленный клиент не менялись.

**Продолжение по ускоренному порядку 6 сентября:** порядок закреплён в §13.2а
плана. Исправление successful_prepare проверено: 14 профильных тестов PASS.
Отдельная диагностика на сохранённом run195419 выявила ещё два расхождения
проверяющего: origin с завершающим `/` в наблюдении и без него в отказе;
отсутствие поддержки точной graph-page проекции. Оба исправлены, 65 зависимых
проверок PASS. Диагностический `run195419-graph-fixed-diagnostic.json` подтверждает
transfer_verified и wizard_settings_readback; это не новый live PASS. Старый audit
сохранён, SHA c949fc7c… не изменился. Новые guards проверяют чужой origin/path,
изменённые значения и признаки пропуска данных. Runtime браузера не менялся.
Новый целевой import-roundtrip с этим harness завершён; результат указан выше.
Нижеследующая запись о необходимости исправить prepare — история прошлого run.

**Приоритет пользователя, подтверждённый 6 сентября:** агент должен быстро
и без ошибок создавать сценарии Loginom. Пользователь явно разрешил
эксперименты и альтернативные подходы для достижения этой цели. Оценивать
изменения по времени до проверенного результата, числу вызовов инструментов,
ошибок и восстановлений, сохраняя полную проверку данных и сохранённого пакета
после reopen. Это разрешение на исследования, а не подтверждение P3 PASS.
Профиль Hermes и остальные границы проекта сохраняются; кандидаты проходят
review и отдельную приёмку, исторические аудиты остаются неизменными.

Последний Hermes `20260906-195419-26afee73` завершён (exec7119, returncode=0).
Заморозка runtime/harness снята. Неизменяемый аудит — 48/59, SHA256
`c949fc7cdac7f43289fcbdbbcf4a8410eba1a7c2ac1a0a28a255b28a218547a7`.
Разбор sanitized tools/events выявил ошибку проверяющего: повторный dock_prepare
был отклонён без изменения workspace, но оба transfer verifier посчитали его
второй подготовкой и потеряли привязку artifact. В итоге четыре transfer checks
и зависимый import readback остались FAIL. Аудит не пересчитывался.

В evidence есть единственная успешная подготовка (reply3, один
workspace_prepared event), явный отказ повторной подготовки (reply59), успешная
verify (reply73, 230 bytes и SHA fixture), finish (223), reopen (229) и повторное
output_mapping (237). Это факты отдельных операций, не новый P3 PASS.
Диагноз: `run195419-prepare-diagnosis.json`. Следующий шаг — узко исправить
отбор успешной подготовки с отдельной проверкой безопасного отказа, сохранить
строгий запрет второй реальной подготовки и повторить приёмку новым запуском.
Actual runtime по этому диагнозу ещё не менялся.

Профиль запуска подтверждён: `openai-codex` / `gpt-5.6-luna` / `medium`,
существующая подписка ChatGPT. Перед запуском прошли 357 клиентских проверок
последовательно и 187 Python-проверок. В параллельном полном клиентском прогоне
одна проверка упёрлась в scan budget; последовательный полный прогон прошёл.

Предыдущий Hermes `20260906-193127-6e07427d` завершён: 89 calls,
returncode=0. Неизменяемый аудит — 52/59, SHA256
`482b66a2ee766b3cdbf4bfa99797dc97ec7f0c22230c451abe9c6fd3ee696c1e`.
Transfer и only_supported_dock_tools прошли. Finish/reopen импорта состоялись,
но после reopen модель вызвала переход к output_mapping с observation_id
прежней страницы файла. Запрос отклонён как stale; повторный mapping readback
не получен. Это новый незавершённый UI-путь, а не прежняя ошибка verifier.
Диагноз основан на sanitized calls/replies/events:
`run193127-readback-diagnosis.json`; исходный аудит не пересчитывался.

Root воспроизвёл причину выдачи: на первой странице формата находились только
три поля, без Next. Приоритет Next/Finish перенесён перед полями; новое первое
сообщение размером 11108 bytes содержит Next, delimiter, qualifier и null marker.
Действие по выданному Next в живом UI завершилось SUCCEEDED с переходом
к output_mapping (`import-paged-next-native-fixed.json`). Stale-ref guard сохранён.



Предыдущий завершённый запуск — `20260906-191903-a62088ee`. Неизменяемый аудит:
24/32, SHA256 `d02cc0eeae30899cb857b8cc87ab3de3f7369e3f7d15538d7946377daca6aff9`.
Проверка only_supported_dock_tools прошла, но upload/verify не выполнялись:
Hermes остановился при навигации в Files на корневом каталоге `/`.
Root воспроизвёл проблему и после исправления подтвердил переход `/` → `test`
в живом UI; evidence: `files-root-fixed-native.txt`, `files-root-test-confirmed.txt`.

Предыдущий запуск `20260906-185724-f40c8463` завершился с аудитом 52/59,
SHA256 `5d81514c52e9ecc90849d9c25dde01aff106b80383fede436568abec96047e47`.
Transfer и only_supported_dock_tools прошли. Finish и повторное открытие
импорта присутствовали в выполнении, но старый projection verifier не признал
readback. Это диагностическое расхождение; исходный аудит не пересчитывался
и wizard_settings_readback в нём остаётся FAIL.

Подтверждённые ручные наблюдения в собственном диагностическом пакете:

- Calculator/Grouping/Factor: прочитаны типизированные настройки, включая
  отдельный Factor dialog после исправления области наблюдения. Эти readbacks
  не подтверждают автоматически применение, источник данных или результат.
- Table: прочитан полный список четырёх полей. Невыбранные значки видимости
  имеют opacity=0, поэтому состояние видимости для них остаётся unknown;
  после выбора каждого числового поля подтверждены painted visibility и
  пустая строка особого формата. Полнота схемы отделена от полноты видимости.
- Входное соответствие Revenue: native readback подтвердил пять наблюдаемых
  связей и полный видимый список 5 → 5 (`mapping-native-confirmed-live.txt`).
  Привязка к владельцу узла, source identity и applied settings пока не доказаны.

Все перечисленные private evidence находятся в
`.dock/post-mvp-p0/resume-20260906/`. Root единолично управляет браузером;
последняя наблюдённая область — TF5, output_mapping импорта собственного
`/test/packages/Root-diagnostic-20260906-1753.lgp`. Перед действием перепроверить UI.
Process readback, наблюдаемые маркеры типов и контекст входного порта уже
интегрированы в actual runtime. Applied/source/execution proof они не дают.
P7 crash fixture и read-only inspector остаются приватными диагностическими
кандидатами: durable barrier и resume не реализованы.

P3 и этапы P4–P9 остаются открытыми. Далее — исправить и проверить отбор prepare в transfer verifier, затем провести новый replay; аудит run195419 сохранить без пересчёта.
После этого продолжить проверку соответствий,
свежего выполнения, полных типизированных результатов и сохранения/reopen.
Production и установленный клиент не менялись; исторические audits сохраняются.

## История предыдущих проверок

Запуск `20260906-183036-7cdea805`: 59 calls, completed=true, returncode=0;
аудит 52/59, SHA256
`c93e4eaf4f01f2c251c27ef311cf81402c321622eb6132436edc99291736821f`.
Only-supported/export/transfer прошли; import остановился на metadata ref,
который не был выдан в ui.elements. Последующие исправления paging/readbacks
не меняют этот результат. Нижеследующие записи описывают состояние на момент
соответствующих запусков, а не текущий браузер или активный процесс.


Последний terminal Hermes `20260906-181352-2d892b3f`, exec13719,
94calls/completedtrue/returncode0, exact ChatGPT/Luna/medium.
Frozen audit21/22 SHA256
`c288565b5bbcfd1c2508f25968619f41b02d250761354a2abd0ed3a43e991d2f`.
only_supported PASS; auditor stopped evidence_structure on compaction retained
prepare pair141/142 immediately BEFORE summary marker143. Original2/3 inactive,
same toolcall IDs/arguments/timestamps; copy response exact truncated stub.
Exporter narrow copy recognition being corrected, oldaudit/evidence unchanged.
Client333:332PASS + tabbar scanbudget under concurrentload; standalone same testPASS.
Python182PASS before this run. Native root finishsettle3quiet1198ms PASS.
Runtimefreeze снят после terminal. Next rerun requires exporter tests first.

Root live extras: Calculator inputport is TuneDataSourceMappingWizard; links
view reveals colSourceName fields and geometry SVG (no semantic link data-tid).
ExprDataEditForm intermediate/cached owners false; replacement icon hidden0px,
visible btnReplaceField notpressed. Own savedTF5 graph reexecuted diagnostically.
Final Table filter Region=Север →1row, GoToLine1-1; DeleteAll+confirm+Apply
→3rows Север/Юг/Запад; reopened filter full empty table, checkbox ownertrue.
Evidence private calculator-input-*, calculator-*-dom.txt, filter-*.txt.
Current root UI: TF5 Table/BrowseView cleared-filter dialog open,1440×1000.

Hermes `20260906-174120-1a9e7db8` завершён: returncode0/completedtrue,
109calls, openai-codex/gpt-5.6-luna/medium. Frozen audit51/59, SHA256
`dedceaf749a6c2338b256a02339adcda0de9f8429991242b1ba14403dd043a17`.
Freeze снят. Transfer полностью PASS. Остались malformed tool_call213 безname,
auto-label default→CSV в finish210 и partial mapping (горизонтальная обрезка).
Root live: viewport1200 body932px/scrollWidth1000 → partial;
viewport1280 body1012px/scrollWidth1012 → complete_configured_rows.
Прямые schemas Hermes проверены native assemble_tool_defs без модели;
новый run config tools.tool_search.enabled=off. Browser viewport pin1280×800.
До завершённого запуска328client/180Python tests PASS. Новые fixes требуют retest.
Root diagnostic сохранён/закрыт/открыт: /test/packages/Root-diagnostic-20260906-1753.lgp,
новый TF5, внутреннее имя Root_diagnostic_20260906_1753. Reopened Reform
QuantitySum integer подтверждён native read; это не точный P3 fixture.
Текущий root UI TF5 импорт/output_mapping; диагностический viewport1200×818,
перед следующим действием перепроверить. Другие вкладки Files TF2/TF3.
Новые fixes: tabbar discovery (root live TF2→TF1 SUCCEEDED), narrow reveal
origin normalization, новый persisted no-effect UI refusal при confirmed pending
upload и его строгий auditor. Старые AMBIGUOUS/unsigned отказы не признаны.
Попытка maxturns180 отвергнута budgetvalidator до модели; actualrun100 безfallback.

Hermes `20260906-172526-2aad4e63` завершён, exec59305 terminal,
openai-codex/gpt-5.6-luna/medium подтверждён,32calls. Frozen audit47/59,
SHA256 `c89366b70b7bc332adfbcaec0586aa93a3d17bfbf08e3407957776397b57bed7`.
Verify62SUCCEEDED scroll79/downloadSHAfixture, inspect64resolved bytesverified.
Auditor transferfalse: разбираются pre-action rejected scroll и reveal binding.
После inspect модель прочитала roots/toolbar/breadcrumb и закончила до импорта.
Root подтвердил отсутствие tabbar в roots discovery; реальный Workspace;t.br
существует, добавляется узкий discovery root для возврата в собственный черновик.
Freeze снят. Перед запуском326client/174acceptance tests PASS.
Отдельный root manual scenario теперь4узла: после группировки добавлены
«Параметры полей», QuantitySum integer применён nativeapply182ms; finishSUCCEEDED
автометка «Изменение». Выполнить все на этом черновике нажат; активные порты
не объявлены fresh execution proof. Открыт ConsoleForm, включён показ завершённых
процессов; следующее исследование — id/start/finish и принадлежность запуска.

Hermes run `20260906-170701-0d99f168` завершён, exec53885 terminal. Профиль
openai-codex/gpt-5.6-luna/medium подтверждён. Однократный frozen audit24/32,
SHA256 `3deacf080c0bdf1e283339e509c613783eb4703233c194615883551a7d1c130d`.
Причина: verify унаследовал upload root дерева MapTreeForm;tree, хотя file_ref
выдан позднее в отдельном row snapshot. DOWNLOAD_REVEAL_TARGET_CHANGED до
эффекта, trace пуст. Root воспроизвёл это native code в отдельном live UI.
Internal verify observation исправлен на exact issued file_ref; native live
повтор SUCCEEDED, scroll49 + downloadSHAfixture (tree-repro-fixed.txt).
Grouping typed readback подтверждён live (grouping-typed-live.txt), остаётся
partial/sourcefalse/appliedfalse. Calculator diagnostics hardened exactnode.
174 Python acceptance tests PASS. Runtime freeze снят после terminal.
Новые fixes: internal exact-file reveal в artifact.verify (до1000px, один scroll,
read-only settle3quiet samples); typed audit trace; wordwrapped node finish.
Root live native revealSUCCEEDED scroll49, downloadSHAfixture совпал;
group finishSUCCEEDED302ms.322client/171acceptance tests PASS.

Hermes run `20260906-163929-8d2e4d83` завершён (exec19523 terminal).
Однократный frozen audit **24/32**, SHA256
`e372e82bbfbfd34622bb80c690d1b372b361a59f36ea8028d831975536cb3812`.
До импорта не дошёл: файл вне viewport; verify возвращает
DOWNLOAD_GESTURE_NOT_CONFIRMED. Важный code gap: пока artifact.upload pending,
uiAct запрещает даже scroll. Массовые10parallel reads вытеснили исходный
snapshot из cache8; последние2refs были previously issued, но expired.
Root UI-first подтвердил тот же CSV за viewport при1280x800, scroll owner
FileStorageForm;pnlFileStorage;tbl top0 max50. Реальный wheel50 показал файл,
double-click download дал точный SHA f628434c… (manual-verified-copy.csv).
Следующее исправление: узкое bounded reveal внутри exact artifact verification
или upload-bound scroll с доказательствами; не общий обход pending guard.
Профиль остаётся openai-codex/gpt-5.6-luna/medium. После исправления начат run170701, см. выше.
Предыдущий новый run `20260906-161218-a1580042` завершён: frozen audit51/58,
SHA256 `33aca2c22bb934fce7bf918055f2c2a947e99e7fa580a8c5d65a3a951a32a4b0`.
only_supported_dock_tools PASS; import readback FAIL из-за AMBIGUOUS/UI_ROOT_STALE
после единственного open_wizard (row202/reply203), не из-за отсутствия reopen UI.

Исправления после живого UI-разбора: bounded rediscovery lifecycle после
UI_ROOT_STALE; поддержка второй Table BrowseView-N; clipboard lease не
освобождается при failed browser transport close, пока жив bridge process.
Последнее не доказывает удержание после process exit и не закрывает P4.
319 client tests и167 acceptance tests PASS. P8 source closure review tool
18 tests PASS, локальный report82files; публикация/admission не выполнялись.
Старые frozen audits не пересчитывались. Production/installed client unchanged.

Живой браузер root отдельный от Hermes: test, собственный несохранённый
Package1, import прежнего CSV; второй Table BrowseView-1 открыт. Диагностический
пакет не является точным accepted scenario. Private probes/notes:
`.dock/post-mvp-p0/resume-20260906/`. Личную память восстановили exact actor/list
по Peer ниже; health healthy. Чужие процессы ~/.hermes не останавливались.


## Проверенная точка остановки

Последний Hermes run `20260906-145935-332513ca` завершён: attempt
`EXPORTED_PENDING_AUDIT`, evidence process returncode=0, timed_out=false,
97 API calls, completed=true, failed=false. Профиль openai-codex /
gpt-5.6-luna / medium (существующая подписка ChatGPT).
Старый exec handle1616 больше недоступен; terminal подтверждён evidence.json.
Аудит выполнен один раз при передаче: **50/58**, all_assertions_passed=false.
SHA256 audit.json:
`d72d566a0d190f1c373212c435dfd05c5166ef9242dca01461caee0b205dc46b`.
Все артефакты приватно: `.dock/post-mvp-p0/runs/20260906-145935-332513ca/`.
Аудит неизменяемый: не пересчитывать после исправлений.

Уточнение при продолжении 6 сентября: sanitized evidence подтверждает
finish_wizard SUCCEEDED (reply194) и open_wizard SUCCEEDED (reply204).
Reply216 — уже повторный output_mapping. Пустой
import_roundtrip_diagnostics.roundtrips и отказ
`bound_configured_roundtrip_missing` означают отсутствие признанного proof:
verifier не допускает подтверждённые отказы до эффекта на шагах мастера.
Row180/reply181 — UI_EPOCH_CHANGED без эффекта; row187/reply188 — отказ
валидации из-за лишнего expected_stage у finish_wizard. Это НЕ новая приёмка
сохранённых настроек; frozen audit остаётся 50/58.
Не прошёл `only_supported_dock_tools`: evidence содержит имя `tool_call`.
Причина установлена: row172 вызвал транспортный tool_call без name,
reply173 отверг запрос. Allowlist не расширять; новый goal уточняет обязательные
name/arguments и поля wizard_step/finish_wizard/open_wizard.
Остальные шесть отказов — пока не реализованные domain verifiers:
calculator_expression_and_mappings, group_keys_and_aggregations,
fresh_execution_identity, complete_typed_results, saved_reopened_settings,
reexecution_results.

## Уже реализовано

9afe1de5: независимый wizard_settings_readback с pinned CSV/expected hashes,
upload/verify/inspect ordering, session/context/destination binding и полным
configured schema/mapping roundtrip. 163 Python tests PASS; live gate пока FAIL.
Client не менялся с ad8b92f6: ранее 317 client и 10 packaging PASS.
ad8b92f6 исправил чтение breadcrumb labels в узком файловом наблюдении.
7ebd2849 сократил observation pages и добавил ожидание стабильности wizard_step.
P0 завершён; P1/P2 частичны; P3 в работе; P4–P9 остаются открыты.
Процент по количеству тестов не рассчитывать. Production не изменялся.

## Следующий шаг

1. Прочитать AGENTS.md, этот checkpoint, начало agent-handoff.md,
implementation-status.md, канонический план и p3-verification-gap-review.md.
Восстановить личную память OpenViking по правилам AGENTS.md: list mode,
peer_scope=actor, точный
target_uri=viking://user/kartamyshev/peers/-Users-kartamyshev-Git-loginom-dock/memories.
URI viking://resources/loginom-dock относится к отдельному серверу Dock;
не использовать его как корень личной памяти агента.
2. Проверить актуальный Git и процессы: при передаче найдены посторонние удаления
benchmark/locomo/openclaw/{eval.py,import_to_ov.py,judge.py,run_full_eval.sh,stat_judge_result.py}.
Мы их не делали и не восстанавливали. Обнаружены другие Python/Hermes процессы;
принадлежность не установлена, они не останавливались. Не убивать чужие процессы.
3. Разобрать только sanitized tool calls/replies завершённого run: почему нет
finish/reopen и откуда tool_call. Не читать hidden reasoning, system prompts,
raw requests или assistant prose из private history.
4. Самостоятельно воспроизвести проблемный шаг в реальном Loginom UI в отдельной
диагностической сессии, сверить E2E и Help, затем изменить подход.
Ручной успех не заменяет автономную приёмку Hermes.
5. После цельного исправления и профильных тестов — один новый import-roundtrip;
не менять runtime/harness/browser в ходе его выполнения. После terminal audit один раз.
Далее остальные P3 gates и P4–P9, согласно плану.

## Постоянные ограничения

Hermes: только существующая ChatGPT подписка, openai-codex/gpt-5.6-luna/medium,
без fallback; Xiaomi comparison завершён, не использовать без нового разрешения.
Тестовый Loginom account test разрешён пользователем; destination /test проверять
в UI, для пакетов /test/packages. Не выводить имя Loginom из OS/SSH username.
Реальный UI сначала; при повторных ошибках Hermes снова самостоятельная диагностика.
Параллельная работа агентов разрешена пользователем для ускорения; один владелец
живого браузера, независимые задачи и отсутствие конфликтующих правок.
Production builds только на VPS. Не публиковать секреты/сырой журнал.

Последнее известное ручное UI: TF2 Files /test, другая вкладка TF4 import format,
Package1 не сохранён. Это прежнее наблюдение, перед действием перепроверить.
