# Перепроверка Union после ревью, 2026-09-12

**Итог: подтверждённые дефекты исправлены и приняты в source runtime.**
Заключительный автономный run `20260912-202024-3ae2302f`: **84/84 PASS**,
11/11 операций, настоящее сохранение/переоткрытие и шесть точных проверок таблиц.
Все **54/54** отрицательные подмены доказательств отклонены.
Клиент: **1384 PASS / 1 SKIP**, Python: **505 PASS**.
Пакет: `/test-1/packages/Dock-acceptance-20260912-202024-3ae2302f.lgp`.
[Машинная сводка](union-review-acceptance-2026-09-12.json).
Ниже сохранена история диагностики, включая два непринятых прогона.


Пользователь запросил воспроизведение замечаний в живом Loginom и исправление подтверждённых недоработок. Работа последовательная; deployment, публикация и установка клиента не выполнялись.

## Подтверждения и изменения

Диагностика: `http://logi-test-plan.bg.local/app/?testable=true`, Loginom 7.4.2, явно выбранный `test-1`, хранилище `/test-1`. Отдельные видимые окна, `viewport: null`, размер 1508×862 при доступных 1512×949. Исходный принятый пакет плана 10 не изменялся.

1. Свободный существующий вход: старый preflight открывает пустую настройку и завершается AMBIGUOUS до создания связи. Подтверждено для порта 1 узла Review-free с подключённым портом 0. После закрытия мастера граф содержит только прежнюю связь. Исправлено определение источника схемы по фактическим связям, а не числу выделенных портов. Полная исправленная операция `review-free-fixed-active` прошла; получены 4 строки, включая две пары дубликатов, прежний GUID узла.
2. Длинный список: выбор F39 среди 40 полей приводит к UI_REFERENCE_OBSCURED. Поле имеет generic click в allowed_actions, но interaction=outside_viewport. Добавлена ограниченная прокрутка списка с проверкой видимой точки, владельца и неизменности настроек. Полная операция `review-full-Review-choices` прошла: 3 строки × 40 столбцов.
3. Частично видимая колонка: на восьмивходовом узле chk3_A виден, col3_A скрыт за правой границей при scrollLeft=0. Старый revealUnionField возвращает «Union field cannot be revealed». Исправлено сравнение отдельных колонок check/field. После scrollLeft=400 поле становится доступным, настройки не меняются. Наблюдение также ожидает окончания временного отключения нажатий при прокрутке Ext grid.
4. В полном восьмивходовом сценарии обнаружена связанная недоработка: редактор второй таблицы имеет `celleditor-1`, тогда как код ожидал `celleditor`. Живой native editingPlugin подтвердил port=2, main=A, собственный joined store и отдельный picker. Исправлено чтение фактического редактора с привязкой к строке, колонке, входному store и picker. Полная повторная проверка прошла на восьми входах: 16 строк × 3 столбца.

## Локальные доказательства

- `.dock/union-20260912/live-1789227504110`: исходный дефект свободного входа, граф до/после, источники main и wide.
- `.dock/union-20260912/live-1789228259818`: успешное исправление свободного входа; UI_REFERENCE_OBSCURED/F39, исправленное соответствие; воспроизведение частичной col3 и успешная прокрутка.
- `.dock/union-20260912/live-1789228913376`: полная успешная длинная таблица и обнаружение отдельного редактора второго входа.
- `.dock/union-review-20260912`: операторские команды, локальные тесты.

До добавления привязки редактора полный клиентский набор: 1382 PASS, 1 SKIP; Python: 499 PASS. После добавления привязки редактора focused Union: 32 PASS. Подготовлены отдельные `union-review-complete` goal, два строковых fixture и независимый auditor для полного автономного сценария 2×3 / 2×40 / 8×3 и повторного открытия. Он ещё не запускался; новая приёмка не считается завершённой. Исторические 71/71 относятся к исходному плану 10 и прежнему source pin.

Дополнительная живая проверка `.dock/union-20260912/live-1789229675666` подтвердила различие между наблюдаемой четвертной точкой частично закрытой ячейки и центром, который повторно проверяет общий исполнитель жестов. После успешной настройки первых четырёх присоединяемых таблиц прокрутка от частично видимой col2_A отклонялась до эффекта. Union теперь выбирает ячейки и опоры только с подтверждённо видимым центром. Полная повторная проверка прошла; все 16 строк прочитаны двумя страницами и совпали с независимым оракулом. Предыдущий полный набор после привязки редактора: 1383 PASS/1 SKIP; Python 502 PASS.

## Итог прямой диагностики исправлений

Текущий runtime pin: `c475d504d9d81f54807c6a3b272351b6fbbe0959d434465e384dc3e0caca190a`. Сессия `.dock/union-20260912/live-1789230109259`: `review-editor-fixed-Review-columns` — SUCCEEDED, восемь входов, семь независимо настроенных таблиц; `review-many-independent-pages.json` — все 16 строк проверены, включая семь повторений перестановки C/A/B для двухстрочного источника.

Дополнительно проверена подготовка автономного случая: полная операция `review-seed-close` создала узел с двумя связями и закрыла мастер без применения. Три штатных UI-нажатия удалили только связь входа 1; `review-seed-reconnect` подключила тот же вход, сохранила GUID и выполнила 4 строки × 3 столбца. Независимая проверка графа, preflight и единственного connect receipt прошла (имя диагностического узла приведено к имени fixture только для проверки формы доказательств). Это прямая диагностика, не автономная приёмка.

Пакет `/test-1/packages/Union-review-work-20260912.lgp` сохранён и закрыт; диагностический браузер закрыт перед Hermes. Полный клиентский набор: **1384 PASS, 1 SKIP**; приёмочный Python-набор: **503 PASS**. Preflight подтвердил Sol/low и текущий runtime pin.

Запущен автономный `union-review-complete`, run `20260912-193943-1ee7cfde`: отдельные строковые CSV, полная проверка 2×3 / 2×40 / 8×3, сохранение и переоткрытие, повторное выполнение. Разрешены ровно три UI-нажатия только для подготовки свободного входа; отдельный аудитор проверяет их привязку и удаление единственной связи. Результат автономного аудита пока ожидается.

Первый автономный review-run `20260912-193943-1ee7cfde` не принят: 11 операций SUCCEEDED, но новый аудитор имел ошибки обработки optional source_name, bytes/Path и сокращённого UI-ответа; кроме того, Hermes после save_checkpoint использовал только dock_prepare, что не доказывает закрытие/повторное открытие. Исходный отрицательный audit сохранён, задним числом не заменялся положительным. После локального разбора исправлены только goal/auditor, runtime pin остался прежним. Goal теперь явно требует package.save_as с conflict_policy=replace, затем dock_prepare.

Перед повтором Codex отдельно проверил настоящую package.save_as на живом Loginom: `.dock/union-20260912/live-1789232300419/review-save-reopen-work.json`, SUCCEEDED, reopened=true, сохранённый граф совпал после закрытия и открытия. Диагностический пакет и браузер снова закрыты. Приёмочный Python-набор после исправления аудитора: 504 PASS.

Второй review-run `20260912-195952-48104655` выполнил 11 операций и настоящее сохранение/переоткрытие. Замороженный аудит остался отрицательным из-за двух излишне строгих ожиданий: graph receipt использует имена `Длинный_список`/`Много_входов`, а нативное сохранение Union располагает сопоставленные F01/F02/F39 перед самостоятельными полями во входном списке. Все шесть выходных проверок (до/после открытия) прошли. Codex подтвердил нативный порядок отдельно в живом мастере сохранённого диагностического пакета: `.dock/union-20260912/live-1789233461531/review-native-order.json`, без применения настроек; соответствия F39→A, F01→B, F02→C и порядок 37 самостоятельных полей сохранились. Аудитор исправлен только для точной нормализации graph names и двух наблюдаемых порядков этого входа; имена/метки/типы/полнота/соответствия и строгий выходной порядок продолжают проверяться. Тесты отвергают произвольную перестановку и изменение данных. Python: 505 PASS. Отрицательные исходные аудиты не перезаписывались; требуется новый прогон с замороженным исправленным аудитором.
