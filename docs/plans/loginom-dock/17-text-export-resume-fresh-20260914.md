# Node17: оригинал доступен, свежая проверка пока неполна

Фаза `node17:resume-existing-phase:20260914`: точный оригинал `/test-2/packages/Dock-acceptance-20260914-000803-18fad485.lgp` один раз открыт в новом сеансе test-2. Отметки «только чтение» нет. Причина изменения доступности не устанавливалась; административное снятие блокировки не предполагается. Runtime совпал с модельным `c9972b58`; память здорова, actor route общий проектный.

Свежие ExportCSV/ExportTyped/ExportWide завершились:131/138/3005 байт. Независимое сравнение полных файлов с эталонами, сохранённых настроек и mapping, GUID и полного нативного графа прошло. Инвентаризация174 записей подтвердила отсутствие Done/Close-файлов и нового external-zero.csv. Это частичные диагностические проверки, не полная приёмка.

`external-zero` остановился AMBIGUOUS/input_mapping: `open_input_port` не подтвердил готовность за15 секунд, мастер отображал загрузку. Выполнение экспорта и изменение его параметров не начались. В графе Main был активен, Empty — неактивен. Диагностический сценарий ошибочно пропустил явное выполнение Empty, рассчитывая на автоматическое выполнение всех зависимостей при экспорте. После штатного закрытия мастера, подтверждения «Да» и выполнения самого Empty через наблюдённое меню его неизменённый mapping сразу показал id/text/number. Перенастройки и записи пакета не было. Это подтверждённый недостаток подготовки свежего диагностического сценария; продуктовая регрессия этим не установлена.

Исходный pending не сбрасывался, runtime не подменялся и отказ не заменялся успехом. Для следующей проверки подготовлен локальный вариант сценария с явным Empty node.apply(parameters={},inputs=[],mappings=[]) перед пустым экспортом; этот вариант ещё не выполнялся. Следующие диагностические пути должны быть новыми, чтобы сохранить текущие файлы.

Полная versioned v4-проверка со свежим каталогом честно сохранила FAIL `Missing prior session preparation`. Текущий отдельный harness сохраняет реальную подготовку в preparation.json и browser receipt, но не пишет модельный workspace_prepared в execution-events. Требуется явная независимая привязка этих фактических квитанций к original model package; создавать задним числом фиктивное событие или подменять ownership-флаг нельзя. Исходные frozen/v2/v3/v4 результаты не изменены.

Мастер закрыт, исходный пакет закрыт через «Не сохранять», диагностические изменения путей отброшены. Выход test-2 и показ формы входа подтверждены, браузер закрыт, собственные PID отсутствуют. Новая модель, копия оригинала, повторное открытие, сохранение, серверные изменения и обновление клиента не выполнялись. Изменения продуктового кода отсутствуют.

Итог: **52 модельных проверки сохранены; полная fresh-original приёмка остаётся незавершённой**. Read-only больше не текущий blocker. Следующий этап — исправленная подготовка Empty и независимая обработка fresh receipts, затем полный аудит в назначенной координатором фазе. [Квитанции, контрольные суммы и результаты](17-text-export-resume-fresh-20260914.json).
