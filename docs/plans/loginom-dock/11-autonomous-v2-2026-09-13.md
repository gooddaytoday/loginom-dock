# Узел11: автономная приёмка candidate v2 — 13 сентября 2026

**FAIL 57/59. Один прогон завершён; повторный запуск не выполнялся.**

Run `20260913-122247-a862a34d`, слот `node11-hermes-20260913-bb2fe220`.
Source `b05715340937f9c828e03bc6ec7a88f3adf086da`, runtime
`8d6d4b3cd7f5d19a3ac1e9ac6537ff8219f97f9898326227df3f3ae55dcb1380`, manifest
`bb2fe2207e01d594108efb591d1c25036adc12f67168ef895dfde755d391ac2a`.

Свежий preflight подтвердил подписку и pins. Штатный список harness содержит252
файла; полный ранее закреплённый inventory —253, добавочный `fixtures/replacement/empty.csv`.
Обе области проверены. Перед запуском обнаружены только постоянные gateway
Hermes PID743/871, других процессов acceptance не было.

Фактически использованы openai-codex / gpt-5.6-sol / low, Node24.19.0,
собственные test-2 и /test-2. Процесс завершился кодом0 без timeout.
Usage:30 API calls, input72 674, output8 193, cache read1 362 944,
reasoning1 343, total1 443 811 токенов (поля штатного usage, без пересчёта).
Auth guard установлен, blocked_attempts=0, исходное подключение неизменно.

## Проверенный результат

Независимый `replacement_acceptance.py` выполнился один раз и вернул FAIL57/59.
Все11 node.apply завершены: два импорта, создание двух Замен, три изменения
Preserved и четыре повторных выполнения после открытия. Проверки выходов и
настроек в составе аудитора прошли. Проверка двух сохранений прошла.
Итоговый пакет по подтверждённому UI пути:
`/test-2/packages/Dock-acceptance-20260913-122247-a862a34d.lgp`.

`save-checkpoint-a862a34d` сохранил новый черновик без открытия;
`save-reopen-a862a34d` выполнил overwrite→save_flow_completed→close→open.
Изоляция working/precheck, происхождение precheck и обе фактические геометрии
окна прошли: outer1508×949, available1512×949, viewport:null.
Working session `98ad4df2-15d1-437e-8d53-2890aa434abb`;
precheck `1bc29e49-d584-4c09-91b4-8f3dc7507a5d`.

## Точный FAIL

Обе проверки `persistence_expected-multi.json` и
`persistence_expected-partial-add.json` вернули по три причины:
`save_contract_identity` дважды и `save_before_graph`.
Чтение исходных квитанций установило: первое сохранение использовало
`conflict_policy=fail`, а persistence verifier требует `replace` для обоих
сохранений. В checkpoint первого сохранения прежний Package1 имеет path="",
а verifier требует уже заданный путь сохранённого пакета. При этом сам граф
совпал с ожидаемым у обеих операций. Это локализует несовпадение контракта;
аудитор не исправлялся, доказательства не подменялись, PASS не объявлен.
Дальнейшая диагностика/исправление требует нового назначения координатора.

## Доказательства и завершение

[Машинный отчёт, полный аудит и SHA артефактов](../../loginom-dock/node11-autonomous-v2-2026-09-13.json).
Evidence SHA256 `9e7954bbec597b994b843a5f88ca2043f2d1d2a18ae94dec627e7bb7c954c612`.
Audit SHA256 `714dd34e3fd7f13614fb173eec91f866bee905e3a7e51e4fe7b8ccfdb717a5cf`.

Исходный FAIL55/59, A1–A3 и candidate v2 preflight проверены по прежним SHA/размерам
и сохранены. Процессов своего run и обоих browser profiles не осталось;
пакет дополнительно вручную не открывался и не пересохранялся. Другие процессы
не затронуты. Runtime/harness/goal, VPS/current/plugin/routing/main не менялись,
push и второй Hermes run не выполнялись. Полная приёмка остаётся непройденной;
слот освобождает координатор после получения этого завершённого отчёта.
