# Node17 — fixed native account preflight

Assignment `node17:user-v1-account-preflight:1:0f37df75` authorizes one new
minimal user-v1 baseline/reject/observer/replace after this harness checkpoint.
The two previous stdio runs remain setup-only, not component evidence.

The acceptance entry retains the single browser client already connected by the
real source MCP. It verifies that client's exact session transport directory and
user-v1 metadata, then observes the account after the real dock_prepare handler
and before returning its unchanged public reply. This adds no public tool,
second browser, credential change or product/runtime modification. Full-purpose
Hermes runs do not install this component-only helper.

The fixed native probe uses visible unique Avatar/AppMenuForm controls. It reads
only the first bounded rendered nonempty text node of the menu, rejects hidden
text and button text, and requires exactly test-2. No entire menu text is read or
persisted. Actual URL/origin/build, prepared document, workflow/tab and session
are read from the live document and native preparation receipt; no name is
substituted from a request, OS user or storage path. Host profile comes from the
same actual session metadata. One open and one Escape-close occur before the
observer interval. The host/browser budget is at most 30 seconds within the
original preflight deadline. Unknown account, owner/document change, cancellation
or incomplete closure fail before baseline.

Raw limited account evidence, request code digest and before/after observations
are stored in new private account-probe records. Independent validation binds
these to the actual workspace_prepared journal event and session profile.
Focused tests cover the one-open/one-close chain, hidden and unknown accounts,
foreign origin/document/session/workflow, duplicate owners and expired budget.
Four account probe tests and 20 Text export Python tests passed before launch.
The unchanged observer retains its earlier 19-test and native diagnostic evidence.

The candidate manifest was refrozen with 155 runtime and 273 harness inputs.
Full admission still requires a real user-v1 component audit. Source handler,
22 operations / 3 deliveries / all save-reopen, catalog, runtime and global config
remain unchanged. No Hermes, review, new node, main/push, VPS, plugin or routing
work is included. A failed new attempt is preserved without automatic repetition.

## One assigned native attempt — failed before baseline

Harness checkpoint: `0bda0f6e`. Exactly one new run:
`20260913-152128-6b736e15`, own session
`a6869b09-6f05-44d1-b777-c3db49aaf4e7`, actual metadata profile user-v1.
The real preparation handler recorded one READY workspace in the internal
journal. The post-handler fixed account probe was issued through its single
owned browser client with host interval 13943.164958–43943.164958 ms (30000 ms).
The browser call then returned a transport TimeoutError before any raw probe
result could be saved. The public dock_prepare reply was therefore not delivered.

There is no verified account line or menu-closure receipt for this run. The
available evidence cannot distinguish waiting for click, menu opening, text
inspection or closure. Neither a screenshot nor a probe-step trace exists.
A browser console log appeared at shutdown, but it contains no account-probe or
timeout markers and does not establish the failing UI step. Do not infer that
Escape, the account value, or a particular CSS selector caused the timeout.

Recorded counts: artifact delivery 0, public node_apply 0, node_apply_prepared 0,
observer intervals 0, actual replace dispatch 0. The local startup input was
admitted but never uploaded to Loginom. No automatic repeat or old-session
resumption was performed. All 155 runtime and 273 harness inputs matched after
the failed attempt. The global config hash remained unchanged. The owned browser
was closed, its process check returned no PIDs; server logout is unverified.
95,967,227,904 bytes remained free.

**Status: failed account preflight; user-v1 component and full goal unaccepted.**
The accepted diagnostic observer evidence remains historical component PASS;
the previous two user-v1 setup-only attempts remain setup-only. Full admission
still fails on missing real user-v1 native component evidence. No Hermes run was
started; no user-v1 evidence was added to the admission manifest.

The frozen manifest remains SHA-256
`6187aaa5c1381d65d63623b9d79e55033f42a065fff0b7c3ebfc1236db70377a`.
Next owner: coordinator. Concrete next trigger: targeted account-probe diagnosis
with bounded per-step evidence and a browser-side deadline shorter than the host
deadline, so a partial result can return before cancellation. Another native
attempt requires a new assignment; none is queued or automatically launched.
[Exact failure, session binding, counts and raw evidence digests](17-text-export-account-preflight.json).
