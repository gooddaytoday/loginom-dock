# Узел11 «Замена»: разработка завершена — 13 сентября 2026

Статус этапа: **завершён, готов к одному отдельному ревью**.
Поддержанный source-контракт `transform.replace_columns / exact` проверен
на живом Loginom7.4.2. Автономная приёмка Hermes ещё не проводилась;
подплан целиком и production-релиз не объявляются принятыми.

[Машинная сводка](../../loginom-dock/replacement-development-2026-09-13.json):
19 групп проверок PASS, 26/26 отрицательных подмен доказательств обнаружены.
[История и контрольная точка](../../loginom-dock/replacement-progress-2026-09-12.md)
сохраняет первоначальные отказы, причины и последующие проверки.

## Изменения

Реализованы типизированные exact-правила строк, целых и вещественных чисел,
Null/пустая строка/буквальный `null`, три режима остальных значений и replace/add.
Существующий узел сохраняет неперечисленные правила; поддержаны входные и выходные
переименования, одинаковые метки, порядок и исключение выхода. Публичный результат
содержит проверенный readback, отдельные статусы выполнения и сохранения пакета.

В заключительной матрице исправлены два дефекта:

- Повторная настройка сохраняет связь исключённого поля на условной странице выхода.
  Общий механизм получает `source ?? exclusion_source`; чужие и повторные связи
  по-прежнему отклоняются.
- Перед добавлением пары в длинную таблицу обработчик возвращает заголовок в видимую
  область реальной прокруткой своей таблицы. Проверяет принадлежность поля/таблицы
  и фактическое уменьшение scroll.top; непрогрессирующая прокрутка останавливается.

Усилены независимые аудиторы: точная привязка запроса к журналу, схема с явными
метками, Close/неизменённое повторное выполнение, сохранение/reopen и потеря ответа.

## Прямые проверки

| Случай | Проверенный результат |
|---|---|
| Несколько полей, add, Int64 max, дробные значения | Все 6×11 значений и полная схема |
| Null, пустая строка и текст `null` | Раздельные пары и результаты |
| casefalse, replace, Int64 min, integer→Null | Все 6×6 значений |
| Одинаковые метки, Code→Key, aliases/order/exclusion | Правильная принадлежность всех полей |
| Частичное обновление и Done | Новые строковые правила; Key и mappings сохранены; выполнение не запрошено |
| Close после заведомо иной настройки | Черновик наблюдался и отброшен; прежние настройки и 6×6 восстановились |
| Повтор готового и работающего operation ID | Без повторных браузерных действий/операций |
| Потерянный JSON-RPC ответ | Реальный drop в локальном MCP; timeout-32001; status/wait восстановили один результат |
| Четыре неподдержанных/конфликтующих запроса | Отказ до браузерных действий |
| Длинная таблица | Все 24 пары, включая последнюю вне исходного viewport; 6×11 |
| Пустой CSV | Проверены исходные байты, импорт0×5 и выход0×7 |
| Save checkpoint → save/reopen → свежий импорт и замена | Граф9 узлов/5 связей; новые refs; прежние settings; fresh6×5 и6×11 |

Полный набор клиента: **1402 PASS, 1 SKIP, 0 FAIL**, всего1403 теста.
Проверены155 файлов закреплённого runtime manifest, расхождений нет.
Python-аудиторы компилируются; `git diff --check` прошёл.
Тесты потери ответа являются отдельной fault-injection диагностикой Codex,
а не доказательством автономной работы Hermes.

## Среда и продолжение

Проверенная ревизия source:
`8ff2de3c4e5e35dc4a2f02af1b2e1d6608ef7448e91ce72a9a67653a9005690f`.
Последняя сессия: `e1368ffd-31b0-4766-968a-076b121d1e6c`.
Собственный аккаунт test-2, пакет `/test-2/Node11-Handler-90de4e18.lgp`.
Пакет сохранён, закрытие подтверждено Home/отсутствием мастера и графа;
source harness завершён штатно. Все browser profiles и доказательства находятся
в `.dock/stream-runtime/sessions/` этого worktree. Не использовать старые
workflow/document refs при новом запуске.

Рабочая ветка `codex/node-11-replacement`; bootstrap-правка `.gitignore`
оставлена отдельно. Старый параллельный пилот, другие узлы и production не менялись.
Сборка, установка, push и merge не выполнялись.

Координатор назначает одно ревью в этом же чате Astra/medium. При замечаниях —
один раунд живой перепроверки и исправлений без повторного полного ревью.
Обязательная автономная приёмка Hermes `openai-codex / gpt-5.6-sol / low`
проводится только после выдачи координатором слота. Следующий узел и объединение
веток из этого этапа не запускаются.

## Границы

- Exact internal tables, те же типы string/integer/real, numeric precision0.
  Regex, внешние таблицы, изменение типа, date/bool остаются вне scope.
- Непроверенное не-ASCII case folding отклоняется.
- Native remaining-value real editor допускает значения, не меняющиеся при
  округлении до двух десятичных знаков. Редактор exact-пар такого округления
  в проверенных случаях не делал.
- Живой тест длинной таблицы покрывает24 пары; верхние ресурсные пределы контракта
  не означают исчерпывающего живого тестирования всех размеров.
- В Loginom7.4.2 other→Null выставляет `_Replaced=true`, в том числе Null→Null.
  Это проверено отдельным probe; пример закреплённого Help отличается.
- Candidate-каталог использован только для разработки. Принятый релиз и
  автономная приёмка не заявлены; `archiveActive=false` не доказывает глобальную
  изоляцию native hooks.
