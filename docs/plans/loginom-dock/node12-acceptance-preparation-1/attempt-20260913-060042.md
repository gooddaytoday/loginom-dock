# Узел 12 — попытка 20260913-060042-78fec37f

Приёмка не пройдена: `FAILED_BEFORE_MODEL`. Слот
`node12-hermes-20260913-1a5a4631` освобождён после завершения процесса.
Команда: `node12:autonomous-acceptance:1:1a5a46312501d20ec7e23a2db12ed95bf34d784ead45628b12cdcf1492a276ce`.

Штатный запуск остановился на MCP initialize/list_tools с
`MCP_TOOL_PRECHECK_FAILED`. Модель не запускалась; UI-действий, входа в Loginom
и проверки фактического размера окна не было. Точная низкоуровневая ошибка
скрыта штатным клиентом. Повтор только read-only tool-precheck с тем же конфигом
вне sandbox прошёл: available=true, missing=[]. Это подтверждает зависимость
от условий запуска, но не устанавливает конкретную syscall/network-причину.
Прямая UI-диагностика не требовалась: отказ произошёл до работы с Loginom.

Полный duplicates_node_acceptance.py вызван по новому run-dir и завершился
FileNotFoundError для evidence.json: runner не экспортирует evidence до старта
модели. Доказательства не синтезировались; полный audit PASS отсутствует.
Исходные request/scenario/attempt/tool-precheck сохранены без перезаписи.

Каталог: `.dock/node12-autonomous/runs/20260913-060042-78fec37f/`.
Дополнительно: tool-precheck-diagnostic.json, process-check.json,
full-audit-invocation.log, acceptance-checkpoint.json. Проверка процессов
после диагностики не обнаружила собственных acceptance/Hermes процессов.

Source 7bf88255562c59cd32397be3f68c4ec85cb55b38, preparation 8475e8adc78a8812054af250627dcb8fc4d7c93f.
Candidate `2026.09.13-node12.1-candidate`, manifest SHA
`1a5a46312501d20ec7e23a2db12ed95bf34d784ead45628b12cdcf1492a276ce`.
Runtime `81a65c886ba64fb72ea1ac180ea79cb674d50958a144971a2a0e14aea98b2505`;
harness `b73e69f020fa7a2d8b0a7c30e721d8bca940ccf6476baf1e1de1328aae94ffd0`.
Runtime/harness/goal не менялись. Объявленный полный scope и ограничения прежние;
ни одна часть автономного goal не объявляется принятой этой попыткой.

Следующий запуск требует нового слота и разрешённой среды исполнения, в которой
прошёл read-only MCP precheck. Самостоятельный повтор не выполнялся.
Merge/push/deploy/activation/общие настройки не менялись.
