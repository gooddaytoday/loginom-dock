# Узел 12 — вторая автономная попытка

**FAIL: 103/112 проверок полного аудитора прошли.** Run
`20260913-060514-47b236af`; слот `node12-hermes-20260913-1a5a4631-r2` освобождён.
Запуск выполнен через require_escalated по отдельной команде координатора.
Прежняя попытка FAILED_BEFORE_MODEL сохранена. Третьего запуска не было.

Hermes `openai-codex / gpt-5.6-sol / low`, существующая ChatGPT-подписка;
exit 0, 42 API calls. Экспорт полный, runtime/harness/native skill и auth connection
не изменились. Все 16 операций узлов SUCCEEDED, но это не полный успех сценария.
Видимый браузер: start-maximized, viewport:null; журнал prepare подтвердил
1508×862 (страница), 1508×949 (окно), available 1512×949.

## Неуспех приёмки

1. `package.save_checkpoint` прошёл. Последующий `package.save_as` с replace
   подтвердил перезапись, затем получил AMBIGUOUS:
   `Selector packages.close cardinality 0, expected one`. Trace не содержит
   закрытие/открытие/успешную postcondition. Hermes выполнил abandon_operation
   и продолжил вычисления. Реальное reopening не подтверждено; save_chain и
   all_gates_present не прошли. Цепочку не засчитываем по пути файла/графу.
2. Все три проверки persisted import отвергли отсутствие ожидаемых opening
   gesture/receipt. Hermes передал settings:{}; отдельный мастер не открывался.
   Свежие execution IDs есть, но сохранность после reopening не доказана.
   Отдельно требуется согласовать verifier с принятым обычным путём без лишнего
   открытия мастера; в этом ходе аудитор не менялся и PASS не подставлялся.
3. Четыре full_read gates для NULL-набора требуют sample_rows==10; Hermes
   запросил 8, равные полному числу строк. Все восемь проверок содержания разметки
   прошли, включая NULL и пустую независимую схему, main/key-only/restored.
   Это расхождение критерия чтения с полным восьмистрочным результатом требует
   отдельного разбора аудитора, но не отменяет самостоятельный сбой reopening.

Провалы и SHA исходных файлов: [машинная сводка](attempt-20260913-060514.json).
Полный отчёт: `.dock/node12-autonomous/runs/20260913-060514-47b236af/duplicates-node-audit.json`.
Request, scenario, evidence и исходный журнал не редактировались.

## Прямая диагностика Codex

После модели выполнена отдельная UI-диагностика с навыком loginom-automation
и источниками E2E `bg/helpers/packages.ts`, `bg/selectors.ts`.
На начальной странице меню реальный selector
`MF;MainMenuForm;btnClosePackage` присутствует и видим; это не подтверждение
проблемного перехода после overwrite. Прямое открытие сохранённого пакета через
файловый диалог дошло до видимого `Node12-main10-mark`, затем меню блокировала
`div.x-mask[data-sticky=true]`; click истёк через 30 секунд.
Полное воспроизведение причины packages.close не завершено; стабильный неверный
selector не установлен. Runtime не правился, live-матрица не повторялась.

Диагностика: `.dock/node12-r2-ui-diagnosis/observation.json`, `menu.png`,
`.dock/node12-r2-ui-diagnosis-open.mjs`. Первый deep-link оставил начальную
страницу «Пакет не выбран»; его нельзя считать успешным reopening. Второй
проход использовал штатный файловый диалог. Обе отдельные browser contexts закрыты.
Проверка процессов после завершения модели и диагностики: собственных
acceptance/Hermes/diagnostic CLI нет; `process-check.json` в run-dir.

## Pins и дальнейший шаг

Candidate `2026.09.13-node12.1-candidate`, manifest SHA
`1a5a46312501d20ec7e23a2db12ed95bf34d784ead45628b12cdcf1492a276ce`, activated=false.
Runtime `81a65c886ba64fb72ea1ac180ea79cb674d50958a144971a2a0e14aea98b2505`.
Harness `b73e69f020fa7a2d8b0a7c30e721d8bca940ccf6476baf1e1de1328aae94ffd0`.
Source `7bf88255562c59cd32397be3f68c4ec85cb55b38`, preparation `8475e8adc78a8812054af250627dcb8fc4d7c93f`.

Пакет `/test-1/packages/Dock-acceptance-20260913-060514-47b236af.lgp` сохранён
checkpoint; автономная persistence/reopening acceptance не получена.
Scope integer/string, NULL в Value, пустая строка и текст null; boolean/real/datetime
как исходные поля, NULL keys и отдельный live lost-reply не расширялись.

Требуется решение координатора о focused исправлениях save/reopen и verifier;
после изменения кода — новые pins и новый слот. Merge/push/deploy/activation
и общий клиент/настройки не менялись.
