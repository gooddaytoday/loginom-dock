# Доработка N12-R1 — 13 сентября 2026

**N12-R1 / P2 закрыт.** Исправление и тесты зафиксированы локальным коммитом
`ce10e177`. Это один назначенный раунд доработки, без повторного ревью.
[Исходный отчёт ревью](12-code-review-2026-09-13.md) сохранён без изменений.

## Подтверждение и исправление

Свежая source-сессия `167febc5-6994-495a-96aa-359602c13b2f` открыла собственный
complete-пакет и сохранила диагностическую копию
`/test-1/Node12-R1-diagnostic-20260913.lgp`. Проверены Loginom 7.4.2, аккаунт
`test-1`, отдельный профиль, видимое окно 1508×862, viewport:null.

После выполнения пустого импорта и разметки живой UI подтвердил 0 строк,
три исходных поля и четыре служебных столбца. Служебные флаги имеют тип boolean,
группы — integer, все четыре имеют дискретный вид данных. Key/Value/Id сохранили
роли 3/4/0. Наблюдаемый порядок исходных полей Key, Value, Id отличается от CSV;
исправление допускает сохранённую перестановку, не требует порядка CSV.

На отдельной копии свежих доказательств старый аудитор повторно дал ложный PASS
при `Id→WrongId` в read-части и неизменённой правильной конфигурации. Оригинальный
журнал не редактировался. Это дефект аудитора; ошибка сервера или обход
аутентификации не заявляются.

Изменены:

- `duplicates_output_evidence.py`: обязательный независимый `source_columns`;
  expected schema строится из входной fixture и явно закреплённых четырёх служебных
  полей. Проверяются точный состав, метки, типы и вид данных даже при 0 строк.
  Для сырых таблиц definitions берутся из expected, из результата — только
  подтверждённая перестановка. Дополнительно проверены raw-конфигурация,
  совпадение входной схемы и порядок/состав output mapping.
- `duplicates_node_acceptance.py`: все initial/persisted/key-only/restored
  проверки получают `f['columns']` из независимой fixture.
- `test_duplicates_output_evidence.py`: положительная пустая схема и допустимая
  перестановка; отрицательные переименование, потеря служебного поля, неверный тип,
  неверный вид данных, отсутствие independent schema и несовпадение конфигурации.

## Проверки

- Полный Python-набор: **519 PASS**. Профильный duplicates-набор: **14 PASS**.
- **11/11** проверок полного вывода исправленным аудитором прошли на исходных
  доказательствах main10/null8/empty, всех трёх persisted-результатах, key-only,
  restored/recovered и свежем empty до/после повторного открытия.
- На трёх согласованных копиях свежего empty-журнала выполнены подмены
  `Id→WrongId`, потеря `Contradiction`, integer→string у Id. Для каждой отдельно
  прошли проверка raw-согласованности/свежести (`verify_calculator_output` с
  намеренно неверным expected) и проверка прежней правильной конфигурации.
  Исправленный аудитор содержания отверг **3/3** с `duplicates_expected_schema`.
  Следовательно, отказ не объясняется лишь несовпадением digest или журнала.
- Диагностическая копия сохранена и повторно открыта, пустой импорт/разметка
  выполнены вновь. Подтверждены роли до правок, неизменная схема, новый execution ID
  `1789256569354-oj8rj369npf:558:7` вместо `:558:3`, 6 узлов и 3 связи.
- `git diff --check` прошёл. Клиент не изменялся; повтор клиентских suites для
  Python-правки не выполнялся. Результаты предыдущих клиентских проверок остаются
  историческими, не объявляются новым прогоном.

Приватные воспроизводимые доказательства в `.dock/node12-r1-fix/`:
`live-before.json`, `before-fix-probe.py`, `before-fix-negative.json`,
`negative-probe.py`, `negative-audits.json`, `recheck.py`, `positive-audits.json`,
`fresh-persistence.json`, `python-tests.log`, `pins.json`.
Живой журнал — `.dock/stream-runtime/sessions/167febc5-6994-495a-96aa-359602c13b2f/execution-events.jsonl`;
его SHA-256 после последней операции:
`ddaf988685ce83fd9df8dd889344a34c2d04a5517970273566998e85c0fd6cf6`.

## Pins и ограничения

Runtime остался
`81a65c886ba64fb72ea1ac180ea79cb674d50958a144971a2a0e14aea98b2505`.
Для изменённого harness закреплены 239 входных файлов по правилам run.py
(Python/MJS, goal, три fixtures). SHA-256 канонического JSON mapping
`path→sha256` с sorted keys и separators `(',', ':')`:
`b73e69f020fa7a2d8b0a7c30e721d8bca940ccf6476baf1e1de1328aae94ffd0`.
Полный mapping сохранён в `pins.json`. Старый autonomous/frozen export на этот
harness не переносился; положительного автономного goal пока нет.

Исторический shared candidate использован только для разрешённой диагностики;
отдельный node12 candidate URI/SHA и слот Hermes пока отсутствуют. Hermes,
build/publish/install, push/merge, общие настройки и чужие worktree не менялись.
Незакоммиченные `.gitignore` и `AGENTS.md` сохранены.

Границы boolean/real/datetime, NULL в ключе и отдельной node12 live lost-reply
проверки остаются прежними. После закрытия N12-R1 решение о candidate и обязательной
автономной приёмке принимает координатор; повторное ревью не запускалось.
