# Обычная работа Hermes и автоматическая диагностика

Согласовано и реализовано 10 сентября 2026. Изменения оформлены одним отдельным
итоговым коммитом поверх `d76c6a66`; предыдущие доказательства не переписываются.

## Итог

Обычная приёмка `20260910_184431_cf0b17` и независимый аудит 23/23 PASS завершены.
Установлен основной Hermes 7.4.2 / Sol / low; runtime совпал с VPS-комплектом.
6 мин 27 с; 715133 токена основной задачи, включая 662656 из кеша и 0 записи
(подтверждено). Отдельный служебный запрос без usage не включён в известный итог.
[Полный отчёт, ограничения и доказательства](../../loginom-dock/hermes-user-diagnostics-2026-09-10.md).
Ниже сохранены контракт и исторические этапы; их промежуточные статусы не заменяют итог.

## Контракт

- Основной локальный Hermes: Loginom 7.4.2, существующая подписка
  `openai-codex / gpt-5.6-sol / low`. Настройки Codex не переключаются.
  Конфигурация 7.5 сохраняется для отката.
- Прямой запуск и финальная приёмка используют одинаковый установленный профиль
  и четыре существующих обработчика. Поддержка 7.5 и завершение V4/V5 не заявляются.
- Hermes получает естественное задание и файл, сохраняет пакет в конце через
  `package.save_checkpoint`. Промежуточное сохранение — по просьбе пользователя
  или перед риском потери работы. `package.save_as` с переоткрытием остаётся
  доступным при соответствующем запросе. Контракты операций не переопределяются.
- Проверки объекта, применения настроек, завершения выполнения и сохранения
  остаются внутри обработчиков. Неопределённые результаты требуют восстановления.
- Первый `dock_prepare` выдаёт компактный закреплённый набор знаний и операций;
  последующие действия повторно используют кеш. Дополнительные знания — по нужде.
- Пользовательский профиль получает версионированные краткие результаты с
  идентификаторами, схемой, числом строк, образцом, ограничениями и ошибкой;
  полные результаты доступны локальной диагностике.
- Native-плагин передаёт пользовательские вложения и явно указанные пути в
  существующее хранилище артефактов без обязательных параметров испытательного запуска.
- Обычное чтение Table не меняет формат. Точное чтение временно применяет формат
  и восстанавливает прежние настройки; неподтверждённая точность обозначается.

## Локальные журналы

Использовать штатные хуки Hermes и существующий журнал Dock, без отдельного
обязательного launcher. Связать задачу, инструменты и браузерные операции:
время, параметры, результаты, наблюдения, ошибки, восстановление и сохранения.
Не делать дополнительных действий браузера или запросов модели ради журнала.

Для каждого запроса и задачи сохранять длительность, вход без кеша, выход,
общее количество токенов, `cache_read_tokens` и `cache_write_tokens`.
Ноль допустим только при подтверждённом показателе; отсутствие — `null` и текст
«не сообщается провайдером». Сводка: «Из кеша: N токенов; записано в кеш: M токенов».
Не прибавлять кеш повторно к включающему его входу. Дедупликация по запросу;
незавершённые измерения и частичные суммы обозначаются явно. Числовые показатели
из разрешённого списка переживают очистку; авторизационные токены удаляются.

До успешной подготовки — только технические сведения. После неё — задание и
сведения о файлах. Очистка до записи; системные промпты, скрытые рассуждения,
профили браузера и необработанные сетевые ответы не сохраняются. Новые подробные
журналы не отправляются на сервер. Квитанции операций остаются надёжными.
Локальная команда отчёта; хранение 30 дней и до 2 ГБ. Активные сессии и очередь
архива защищены от очистки. Обрезка и потеря записей отражаются в отчёте.

## Проверка и поставка

1. Codex проверяет реальный UI: сохранение без открытия, восстановление формата,
   поступление CSV и доступность четырёх обработчиков.
2. Тесты кратких ответов, кеша знаний, хуков, токенов, очистки, прерывания,
   ограничений журналов, конфликтов файлов/сохранений и сохранности чужих настроек.
3. Метрики: ненулевые, нулевые, отсутствующие, неполные, дубли и двойной подсчёт.
4. Нет обязательных промежуточных сохранений; явно запрошенное переоткрытие работает.
5. Сборка на VPS; установка проверенного комплекта и native-плагина с откатом.
6. Один финальный обычный Hermes получает задачу продаж и `sales.csv`, без
   требования переоткрытия, тестового рецепта или вмешательства в построение.
7. Codex отдельно проверяет сохранённый результат. Сообщает время, токены с кешем,
   знания, действия и сохранения; сверяет доступную статистику провайдера/Hermes.
   Сравнение с прежним опытом отмечает разный объём проверки.

Публичный выпуск и landing исключены. Успех тестов не заменяет автономную приёмку.

## Контрольная точка реализации

- Рабочее дерево перед началом было чистым.
- Реальный Loginom 7.4.2: открыта отдельная диагностическая сессия, выполнена
  существующая сортировка, изучен Table и диалог формата. Подтверждено, что
  прежний обработчик безусловно применяет числовой формат при чтении.
- Начат модуль нормализации статистики. Штатные хуки Hermes дают нормализованные
  нули без признака наличия исходного показателя; такие нули нельзя выдавать за
  подтверждённые значения провайдера.
- Реализация, тесты, установка и финальная приёмка ещё не завершены.


### Проверено до сборки

Полный пользовательский путь через source MCP: host-хук → CSV (593 байта, SHA-256
проверена) → шесть обработок узлов → одно save_checkpoint без переоткрытия.
Оба рейтинга совпали с независимым расчётом CSV; повторный prepare сохранил
пины и artifact ID. В отдельной сессии после открытия пакета проверены пять
существующих узлов без передачи новых настроек и свежие итоговые данные:
15 проверок прошли, 5 отрицательных подмен отклонены. Внутренний порядок входных
полей Калькулятора при autosync сравнивается по именам и типам, а не по позиции;
первоначальный отказ более строгого сравнения сохранён. Итоговый выходной порядок
продолжает проверяться. Это диагностика Codex, не автономная приёмка Hermes.

Проверены 13 фактических публичных ответов через актуальную схему user-v1.
Показатель присутствия кеша проходит полный путь hook → очистка → запись → отчёт;
нормализованный Hermes ноль при отсутствии показателя записывается как null.

Перед установкой обнаружено, что основной Hermes настроен на OpenRouter/MiniMax
medium. Согласованный профиль требует переключения на openai-codex/Sol/low;
прежний config сохранён в приватной резервной копии. Codex config не меняется.

### Первый обычный запуск и уточнение диагностики

Установленный основной Hermes, без специальных параметров и отдельного config:
сессия `20260910_173841_42e934`, Loginom 7.4.2, Sol/low. Только естественный prompt
и абсолютный путь CSV. Сохранён `/user/dock-p3/sales_revenue_leaders.lgp`:
6 узлов, одно финальное сохранение, без переоткрытия. Отдельный Codex аудит после
открытия: 23/23 PASS; 5 отрицательных подмен отклонены. Основная задача: 24 запроса,
737799 токенов (45000 вход без кеша, 686720 чтение кеша, 0 подтверждённая запись
кеша, 6079 выход), 655.178 с по Hermes DB; суммы совпали с новыми журналами.

После основной задачи Hermes запустил дополнительный запрос другого task_id,
который завершился ошибкой без usage. Он остаётся неполным, нули не подставляются.
Обнаружено вложенное поле `error.type` штатного хука; native-адаптер теперь
сохраняет тип и длительность, без сообщения с возможным сетевым телом. Текстовый
отчёт выводит итоги каждой задачи отдельно. Новые проверки прошли: 11 Node и
4 native-hook tests, воспроизведение отчёта по настоящему журналу успешно.
Перед окончательной поставкой требуется обновление комплекта и обычный финальный
запуск на этих исправленных инструментах. Первый опыт не переписывается.

### Исправление передачи ссылки на сценарий

Повторный обычный опыт `20260910_175807_83d9b8` остановлен после трёх узлов:
модель потеряла `>Модуль1` в одном длинном `navigation_path[].tid`. Защита верно
вернула NOT_APPLIED без изменений. Затем корректный по значениям existing_workflow
был отклонён из-за сравнения порядка JSON-ключей, а испорченная подготовка сняла
готовность. Исходный отказ и остановка сохранены; новый модельный запуск не
использовался для диагностики.

Codex воспроизвёл оба случая в реальном Loginom. В user-v1 node.apply/resume теперь
принимает `workflow_ref: {workflow_id}` вместе с выданным document_id. Клиент
подставляет закреплённый полный путь; внутренний контракт и живые проверки
идентичности неизменны. Чужие ID отвергаются; повреждённый existing_workflow
отклоняется до подготовки, без потери READY. Сравнение пути не зависит от порядка
ключей. Проверенные ссылки после сохранения обновляют локальное соответствие.

Source MCP QA: шесть новых узлов, одно сохранение без открытия и последующее
выполнение существующей сортировки; все публичные запросы используют короткие
ссылки. Намеренно испорченная подготовка не блокирует следующую операцию,
корректная повторно использует знания. 13 проверок прошли. Реальная проверка
JSON-порядка после исправления дала READY, повреждённый путь по-прежнему отвергнут.
Полный клиентский прогон: 1171 PASS, 1 SKIP; Python: 439 PASS, skill валиден.

Дополнительно проверен ранний отказ до `dock_prepare`: технические события теперь
записываются сразу, иначе завершение процесса могло оставить их только в памяти.
Текст задания по-прежнему не записывается до успешной подготовки. Добавлена
проверка AuthenticationError до prepare без текста задания, запроса и ответа;
нативные проверки: 5 PASS.
