# Уточнение запуска17: ручное действие не требуется

Подготовленный проект17 добавлен штатной командой codex app; создана отдельная
bootstrapзадача01a09a36-695b-7da0-b7ca-1ec521afa17e. Идёт подготовка памяти,
разработка ещё не начата. Предыдущее сообщение о ручном добавлении ниже устарело.

---

# Обновление13сентября: поток1 после приёмки11

Узел11 принят в своей ветке,59/59PASS отдельной переоценки сохранённых evidence.
Для следующего узла17TextExport созданы отдельные ветка/worktree/sourceMCP/pendingmemory.
Задача ещё не создана: требуется добавить папку `.worktrees/node-17-text-export`
как сохранённый проект приложения. Подробности в agent-handoff и подплане17.
Это уточнение новее исторической таблицы ниже; слияния и выпуска не было.

---

# Полная очередь узлов для четырёх потоков

Составлена 12 сентября 2026 по коду, сохранённым аудитам, официальному Help и живому Loginom 7.4.2. **70 исходных позиций закреплены каждая за одним потоком**; 13 сентября узел 12 принят в отдельной ветке, поток 2 начал узел 16. С 13 сентября очередь исполняет координатор по [автоматическому циклу](automatic-node-workflow.md): ревью и доработки в прежней задаче, следующий узел — в новой. Слияние только по отдельной команде пользователя.

В каталоге 78 типов узлов и отдельно 68 общих операций. В текущей палитре видны 75 типов. В принятой базе main остаются 8 типов в ограниченных режимах. Узел 12 дополнительно принят в неслитой ветке, 3 прежних узла остаются незавершёнными, узел 16 начал разработку; **65 ещё не начаты**: 62 в очереди и 3 в условном резерве. Наличие узла в палитре не доказывает выполнение его режимов.

[Машинный реестр](four-stream-node-roadmap.json) содержит устойчивые component IDs, порядок, условия допуска, первого владельца общих механизмов и предлагаемые имена веток. [Единая инструкция](node-workflow-runbook.md) задаёт порядок старта и выполнения;
[план потоков](three-stream-workflow.md) сохраняет действующие чаты и исходные проверки. [Снимок живого UI](node-roadmap-live-2026-09-12.json) сохраняет границы наблюдения.

**Excel включён:** уточнение пользователя от 12 сентября отменяет исключение от 7 сентября. Импорт и экспорт `.xlsx` — две обычные задачи потока1 после экспорта текста. На Linux-стенде открыты оба мастера; у импорта доступен фильтр `.xlsx`, у экспорта — Excel 2007. Другие форматы и фактический файловый roundtrip этой инвентаризацией не проверялись. [Help импорта](https://help.loginom.ru/userguide/integration/import/excel/), [Help экспорта](https://help.loginom.ru/userguide/integration/export/excel.html).

## Ближайшие назначения

| Поток / аккаунт | Сейчас | Следом после проверки | Затем | Всего позиций |
| --- | --- | --- | --- | ---: |
| 1 / test-2 | 11. Замена | Экспорт текста | Импорт XLSX → экспорт XLSX → экспорт LGD → импорт LGD | 20, включая 3 резерва |
| 2 / test-1 | 16. Свёртка столбцов | Соединение | Дополнение данных → Разгруппировка | 18 исходных; узел 12 принят в ветке |
| 3 / test-3 | 13. Дата и время | 15. Кросс-таблица | Скользящее окно → Автокорреляция → Корреляционный анализ | 18 |
| 4 / test-4 | 14. Заполнение пропусков | Сэмплинг | Разбиение на множества → Редактирование выбросов → Квантование | 14 |

У потока4 меньше позиций из-за сложности моделей. Условные оценки S/M/L/XL в JSON используются только для сравнения нагрузки: без резерва 60/62/60/59 баллов, это не оценка сроков. Регрессии закреплены за потоком2, временные модели — за потоком3.

Готовые восемь: текстовый импорт03, Калькулятор04, Параметры полей05, Фильтр строк06, Группировка07, Сортировка08, Слияние09, Объединение10. Аудиты и ограничения собраны в [плане потоков](three-stream-workflow.md#проверенная-исходная-точка). `executor/coverage.json` содержит отставшие статусы; здесь готовность определена по регистрации handlers и аудитам, файл coverage не переписан.

## Правила выдачи и общие зависимости

1. После завершения разработки, ревью, доработок и обязательной приёмки координатор выбирает ближайшую подходящую строку. Сначала сохранить итог текущего узла и подтвердить освобождение аккаунта/браузера. Новый узел получает **новый чат приложения, постоянный worktree, именованную ветку, профиль и свой пакет/workflow Loginom**; каждый разработчик работает на Astra/medium.
2. Ветка начинается от отдельно закреплённого принятого commit. Порядок в таблице не означает наследование неслитой предыдущей ветки. Самостоятельные CSV/LGD/XLSX/JSON fixtures допускаются; парный import/export handler не нужен для подготовки входа.
3. Общие механизмы имеют первых владельцев ниже. Подготовка недостающей части входит в первый ограниченный подплан отдельным коммитом. Другие потоки не создают вторую реализацию молча. Пока объединение отложено, зависимый узел можно пропустить при выдаче либо явно переназначить владельца с записью причины.
4. Если нет нужного подключения, runtime или принятой общей зависимости, записать блокер и при текущем переходе взять ближайшую доступную задачу того же потока. Пропущенная строка сохраняет владельца. Проверка очереди выполняется координатором по итоговому сообщению о завершении текущего узла; периодического опроса нет.
5. Подпланы11–16 сохраняют полный объём; краткие строки ниже его не сокращают. Остальные scopes — предложения для отдельного подплана, уточняемые по живому мастеру до кода. Будущие номера и ветки за пределами11–16 ещё не созданы.
6. Одно ревью Astra/medium запускается автоматически после завершения разработки в той же задаче; один раунд доработок medium — там же, без повторного ревью. Финальная автономная приёмка — Hermes Sol/low в единственном слоте координатора и независимый аудит. Все входы/выходы, свежие результаты, сохранение и отдельное переоткрытие проверяются по заявленному объёму. Слияние требует отдельной команды пользователя в чат координатора; push и установка автоматически не выполняются.

| Общая работа | Первый владелец | Подготовка |
| --- | --- | --- |
| `variant_io` | Поток2: Свёртка столбцов | Точное чтение variant входит в действующий подплан16. |
| `dynamic_schema` | Поток3: Кросс-таблица | Фактическая схема и category mapping входят в подплан15; независимый CSV вместо зависимости от13. |
| `preprocessing_training_lifecycle` | Поток4: Заполнение пропусков | Ограниченный lifecycle обучения узла14. Не объявлять его готовым универсальным контрактом моделей. |
| `training_contract` | Поток2: Линейная регрессия | До первого моделирующего кандидата: train/apply/retrain, сохранённая модель и применение на самостоятельных новых данных. |
| `typed_variables` | Поток2: Линейная регрессия | До первого потребителя: типизированное чтение переменных сводки и владельцы выходов. Не требует готовности пяти узлов группы Переменные. |
| `tree_io` | Поток2: JSON в дерево | Подготовить независимое чтение дерева/типов/путей с первым JSON-кандидатом; обратный handler не prerequisite. |
| `nested_workflow` | Поток3: Подмодель | Контекст вложенного workflow, внешние/внутренние порты и границы активации. |
| `connections` | Поток1: Импорт — База данных | Первое выделенное DB-подключение и фиксация его типов/владельцев; следующие backend проходят собственный допуск. |
| `file_artifacts` | Поток1: Экспорт — Текстовый файл | Чтение фактического экспортированного файла и независимое сравнение; развивать существующую доставку/хранилище. |

Связи, доставка файлов и табличная основа развивают существующие механизмы Dock. Назначение владельца не означает их готовности: accepted commit и доказательства в JSON пока пусты. Полный универсальный интерпретатор сценариев не вводится.

## Полный список

Порядок действует внутри потока. Состояния «очередь» и «резерв» означают отсутствие принятого handler. Особые условия перечислены в строках; точные prerequisites и проверки каждой позиции — в JSON.

### Поток 1

| № в потоке | Узел | Состояние | Первый объём | Особые условия |
| ---: | --- | --- | --- | --- |
| 1 | [11. Замена](11-replacement.md) | В работе | Сохранить подплан 11: замена по внутренней таблице, поддержанные правила и признак _Replaced. | Принятая табличная основа |
| 2 | Экспорт — Текстовый файл | Узел17: разработка | Экспорт одной таблицы в CSV/TSV с явными кодировкой, заголовком и разделителем; новый файл в выбранном каталоге. | Файл и независимое чтение |
| 3 | Импорт — Excel файл | Очередь | Импорт xlsx: один явно выбранный лист и диапазон, заголовки, типы и пропуски; без расширения на xls или макросы. | Файл и независимое чтение |
| 4 | Экспорт — Excel файл | Очередь | Экспорт одной таблицы в новый xlsx, явные лист и путь; проверить типы ячеек и полный небольшой лист. | Файл и независимое чтение |
| 5 | Экспорт — Loginom Data файл | Очередь | Экспорт одной таблицы в LGD в выбранном штатном режиме сжатия с сохранением свойств полей. | Файл и независимое чтение |
| 6 | Импорт — Loginom Data файл | Очередь | Импорт самостоятельного LGD с подтверждёнными полями, видами, типами, Null и значениями; повреждённый файл даёт отказ. | Файл и независимое чтение |
| 7 | Импорт — База данных | Очередь | Одна поддержанная СУБД и выделенное подключение: чтение таблицы или параметризованного SELECT. | Выделенное подключение; Тестовая БД |
| 8 | Экспорт — База данных | Очередь | Одна СУБД: запись в выделенную тестовую таблицу в одном явном режиме, с определённым поведением повторного запуска. | Выделенное подключение; Тестовая БД |
| 9 | SQL-скрипт | Очередь | Одна СУБД: ограниченный скрипт над выделенными данными, параметры и явно выбранная транзакция. | Выделенное подключение; Тестовая БД |
| 10 | Импорт — Deductor Warehouse | Очередь | Одна доступная схема Deductor Warehouse: чтение выбранных измерений/фактов из выделенного источника. | Warehouse; Выделенное подключение |
| 11 | Экспорт — Deductor Warehouse | Очередь | Один поддержанный режим выгрузки в выделенный объект Deductor Warehouse с проверкой сохранённых записей. | Warehouse; Выделенное подключение |
| 12 | Импорт — Kafka | Очередь | Чтение из выделенного тестового topic с ограниченным набором сообщений и явной политикой позиции чтения. | Kafka topic; Выделенное подключение |
| 13 | Экспорт — Kafka | Очередь | Отправка ограниченного набора сообщений в выделенный topic; явные ключ/значение и наблюдаемый результат доставки. | Kafka topic; Выделенное подключение |
| 14 | JavaScript | Очередь | Ограниченный детерминированный табличный пример: вход, вычисленные поля, выход и ошибка исполнения. | Разрешённый runtime |
| 15 | Python | Очередь | Ограниченный табличный пример в разрешённом отдельном Python-процессе на сервере; без установки библиотек по ходу работы. | Разрешённый runtime |
| 16 | REST-запрос | Очередь | Чтение контролируемого HTTP-сервиса по ограниченным входным строкам, параметры запроса и все результаты/ошибки. | Выделенное подключение; HTTP-стенд |
| 17 | SOAP-запрос | Очередь | Один метод контролируемого SOAP-сервиса с фиксированным WSDL, параметрами и проверяемым ответом. | Выделенное подключение; SOAP/WSDL |
| 18 | Импорт — 1С Запрос | Резерв | Условный первый объём после появления компонента: один читающий запрос к выделенной базе 1С через его собственный тип подключения. | Выделенное подключение; Доступный 1С-компонент; не наблюдался в палитре |
| 19 | Выполнение программы | Резерв | Условный первый объём после допуска: штатное выполнение безопасной тестовой программы самим серверным компонентом и проверка результата. | Поддержанная ОС/программа; не наблюдался в палитре |
| 20 | Экспорт — Tableau файл | Резерв | Условный первый объём после допуска: локальный Extract-файл выбранного поддержанного формата; без публикации на Tableau Server. | Файл и независимое чтение; не наблюдался в палитре |

### Поток 2

| № в потоке | Узел | Состояние | Первый объём | Особые условия |
| ---: | --- | --- | --- | --- |
| 1 | [12. Дубликаты и противоречия](12-duplicates.md) | Принят в ветке, не слит | Сохранить подплан 12: разметка дубликатов и противоречий без удаления исходных строк. | Принятая табличная основа |
| 2 | [16. Свёртка столбцов](16-collapse-columns.md) | В работе, новая задача | Сохранить подплан 16: информационные/транспонируемые поля, оба режима пустых значений и точный variant. | Точный variant |
| 3 | Соединение (таблицы по позиции) | Очередь | Две таблицы: соединение колонок по позиции строки, включая разные длины и пустой вход; переменные позднее. | Все входы |
| 4 | Дополнение данных | Очередь | Основная таблица и до двух справочников: ключи каждого входа, префиксы и левое дополнение. | Все входы |
| 5 | Разгруппировка | Очередь | Два входа: распределение групповых итогов по явно заданным опорным долям. | Все входы |
| 6 | Линейная регрессия | Очередь | Простой закон на чистых самостоятельных данных: роли полей, обучение, коэффициенты и прогноз новых строк. | Обучение по фактическому режиму; Типизированные переменные |
| 7 | Логистическая регрессия | Очередь | Бинарная цель без весов: обучение, соответствие классов, вероятность и явный порог решения. | Обучение по фактическому режиму; Типизированные переменные |
| 8 | JSON в дерево | Очередь | Один документ JSON ограниченной вложенности: объекты, массивы, скалярные значения и Null в типизированное дерево. | Типизированное дерево |
| 9 | Дерево в JSON | Очередь | Самостоятельное типизированное дерево в JSON с явной семантикой структуры и сохранением типов. | Типизированное дерево |
| 10 | Дерево в таблицу | Очередь | Выбранная ветвь дерева в таблицу: повторяющиеся записи, путь к полям и политика отсутствующих значений. | Типизированное дерево |
| 11 | Таблица в дерево | Очередь | Плоская таблица в ограниченную структуру дерева с явно заданными уровнями и порядком. | Типизированное дерево |
| 12 | Разбор XML | Очередь | Разбор ограниченного XML по явной схеме/структуре: элементы, атрибуты, повторения и пространства имён. | XML/XSD; Типизированное дерево |
| 13 | Формирование XML | Очередь | Формирование ограниченного XML из подготовленных данных по явной структуре и пространствам имён. | XML/XSD; Типизированное дерево |
| 14 | Импорт — XML файл | Очередь | Импорт самостоятельного XML по поддержанной схеме: повторяющиеся записи, типы и пространства имён. | XML/XSD; Файл и независимое чтение |
| 15 | Экспорт — XML файл | Очередь | Экспорт подготовленных данных в новый XML с явной схемой, кодировкой и путём. | XML/XSD; Файл и независимое чтение |
| 16 | Калькулятор (дерево) | Очередь | Ограниченные выражения для выбранных узлов дерева с сохранением остальной структуры; без произвольного расширения языка. | Типизированное дерево |
| 17 | Объединение (дерево) | Очередь | Два самостоятельных дерева совместимой структуры: явное объединение и сохранение кратностей. | Типизированное дерево; Все входы |
| 18 | Слияние (дерево) | Очередь | Два дерева: один наблюдённый штатный режим слияния по явно выбранным ключам/путям. | Типизированное дерево; Все входы |

### Поток 3

| № в потоке | Узел | Состояние | Первый объём | Особые условия |
| ---: | --- | --- | --- | --- |
| 1 | [13. Дата и время](13-date-time.md) | В работе | Сохранить подплан 13: календарные производные и нативные границы периодов для подтверждённых режимов. | Принятая табличная основа |
| 2 | [15. Кросс-таблица](15-cross-table.md) | Очередь | Сохранить подплан 15: фиксированные и скользящие колонки, агрегаты и изменение схемы; календарные поля можно задать в CSV. | Динамическая схема |
| 3 | Скользящее окно | Очередь | Заданные лаги и ограниченный горизонт на явно упорядоченном ряду с обеими согласованными политиками краёв. | Упорядоченный вход |
| 4 | Автокорреляция | Очередь | Обычная автокорреляция известного числового ряда для ограниченного набора лагов; частная отдельно. | Упорядоченный вход |
| 5 | Корреляционный анализ | Очередь | Коэффициент Пирсона для нескольких выбранных числовых полей; явная обработка констант и пропусков. | Принятая табличная основа |
| 6 | Факторный анализ | Очередь | Один наблюдённый метод, фиксированное число факторов и ограниченная настройка вращения. | Обучение по фактическому режиму |
| 7 | Качество данных | Очередь | Настройка реально добавляемого узла «Качество данных» и проверка смысла всех пяти табличных выходов; точный метод определить после discovery. | Смысл пяти выходов/Help |
| 8 | ARIMAX | Очередь | Один базовый временной прогноз с ограниченными порядками и горизонтом; таблицы прогноза/коэффициентов и переменные сводки. Сезонность, внешние факторы, остатки и интервалы — по отдельно заявленным опциям. | Упорядоченный вход; Обучение по фактическому режиму; Типизированные переменные |
| 9 | Таблица в переменные | Очередь | Выбранные значения небольшой таблицы в типизированные переменные с явной политикой строк и имён. | Типизированные переменные |
| 10 | Переменные в таблицу | Очередь | Самостоятельный набор типизированных переменных в таблицу с заданными полями и порядком. | Типизированные переменные |
| 11 | Калькулятор (переменные) | Очередь | Ограниченные выражения над типизированными переменными и явные выходные имена. | Типизированные переменные |
| 12 | Замена (переменные) | Очередь | Внутренняя таблица правил для выбранных переменных с явной политикой отсутствующего соответствия. | Типизированные переменные |
| 13 | Соединение (переменные) | Очередь | Два набора переменных с явным разрешением одинаковых имён и проверкой типов/значений. | Типизированные переменные; Все входы |
| 14 | Подмодель | Очередь | Подмодель с небольшим внутренним графом и явным отображением граничных портов; вход/выход из вложенного workflow. | Вложенный workflow |
| 15 | Узел-ссылка | Очередь | Ссылка на один разрешённый базовый компонент с проверкой источника настроек, портов и собственного выполнения. | Вложенный workflow |
| 16 | Выполнение узла | Очередь | Производный узел выполнения одного допустимого базового компонента с явными зависимостями и портами. | Вложенный workflow |
| 17 | Условие | Очередь | Одна ветвящая проверка над явно заданным значением с наблюдаемым запуском только выбранной ветви. | Вложенный workflow; Типизированные переменные |
| 18 | Цикл | Очередь | Ограниченный счётный цикл с наблюдаемым числом итераций, границами состояния и штатной остановкой. | Вложенный workflow; Типизированные переменные |

### Поток 4

| № в потоке | Узел | Состояние | Первый объём | Особые условия |
| ---: | --- | --- | --- | --- |
| 1 | [14. Заполнение пропусков](14-missing-values.md) | В работе | Сохранить подплан 14: среднее для непрерывных чисел и константа для дискретных строк; явный порог пропусков. | Обучение по фактическому режиму |
| 2 | Сэмплинг | Очередь | Два отдельных детерминированных метода: Последовательный и Отбор со смещением; размер и параметры каждого заданы отдельно. Случайные/стратифицированные режимы — последующие расширения. | Принятая табличная основа |
| 3 | Разбиение на множества | Очередь | Один наблюдённый режим разделения обучающей/тестовой выборки с явными размерами и всеми тремя выходами. | Принятая табличная основа |
| 4 | Редактирование выбросов | Очередь | Один наблюдённый метод порогов и явные параметры; обработанные данные, выбросы и экстремальные значения. | Принятая табличная основа |
| 5 | Квантование | Очередь | Один ограниченный режим интервалов; явные границы или выбранный алгоритм расчёта с сохранением полученных диапазонов. | Обучение по фактическому режиму |
| 6 | Конечные классы | Очередь | Один бинарный пример: роли полей, разбиение признака, WoE/IV и применение сохранённых классов. | Обучение по фактическому режиму |
| 7 | Сглаживание | Очередь | Один штатный метод сглаживания на известном упорядоченном ряду с явными параметрами. | Упорядоченный вход |
| 8 | Кластеризация | Очередь | Один наблюдённый алгоритм на чистых числовых данных с заданным числом кластеров и чтением всех выходов. | Обучение по фактическому режиму |
| 9 | EM Кластеризация | Очередь | Одна наблюдённая настройка EM на ограниченных данных; принадлежности/вероятности и сохранённая модель. | Обучение по фактическому режиму |
| 10 | Самоорганизующаяся сеть | Очередь | Небольшая самоорганизующаяся сеть в одном наблюдённом режиме; применение модели и проверяемые инварианты. | Обучение по фактическому режиму |
| 11 | Ассоциативные правила | Очередь | Небольшие подготовленные транзакции и ограниченный набор порогов; точные правила, поддержка и достоверность. | Транзакционные данные; Обучение по фактическому режиму |
| 12 | Кластеризация транзакций | Очередь | Подготовленные транзакции и одна наблюдённая настройка кластеризации; состав кластеров и результат применения. | Транзакционные данные; Обучение по фактическому режиму |
| 13 | Нейросеть (регрессия) | Очередь | Одна небольшая сеть для регрессии на чистом самостоятельном наборе; фиксируемые настройки обучения и прогноз. | Обучение по фактическому режиму |
| 14 | Нейросеть (классификация) | Очередь | Одна небольшая сеть для бинарной классификации; явные классы, вероятности/решения и применение модели. | Обучение по фактическому режиму |

## Что требует отдельного допуска

- **Качество данных** действительно добавляется как узел с одним табличным входом и пятью выходами. Наблюдался `DerivedDataSourceMappingEngineOutputPortWizard`; индивидуальный Help и смысл выходов ещё нужно установить. Страница одноимённого визуализатора не подменяет документацию узла.
- **Сэмплинг**: в живом мастере подтверждены пять методов, «Последовательный» и «Отбор со смещением» раздельны. Первый scope включает их отдельно. У случайного метода видно поле Random seed; его воспроизводимость ещё не проверена. [Help](https://help.loginom.ru/userguide/processors/preprocessing/sampling.html).
- **Обучаемые модели**: настройка, обучение, применение сохранённой модели к новым данным и переобучение проверяются отдельно. У ARIMAX проверять прогноз, коэффициенты и переменные сводки; дополнительные остатки/интервалы — при включённых опциях. [Обучение](https://help.loginom.ru/userguide/workflow/training-processors.html), [ARIMAX](https://help.loginom.ru/userguide/processors/datamining/arimax.html).
- **БД, Warehouse, Kafka, REST/SOAP**: присутствие в палитре не проверяет сервер, разрешения и конкретное подключение. Нужны выделенные fixtures и независимая сверка внешнего результата. Python на Linux требует разрешённого отдельного процесса и настроенного интерпретатора. [Ограничения Python](https://help.loginom.ru/userguide/processors/programming/python/python-features-restriction.html).
- **Резерв потока1**: 1С Запрос, Выполнение программы, Tableau отсутствуют в текущей палитре. Help ограничивает Выполнение программы Windows, а штатное COM-подключение 1С требует Windows; REST/OData не является этим узлом. Причина отсутствия Tableau не установлена, универсального Linux-запрета ему не приписываем. [Выполнение программы](https://help.loginom.ru/userguide/processors/integration/exec-program.html), [1С-подключение](https://help.loginom.ru/userguide/integration/connections/list/1c.html).

## Расширения принятых типов и общие операции

Они не добавляют новые строки `component.*`. В JSON вынесены отдельные предложения: другие режимы текстового импорта и Калькулятора, преобразования типов, переменные фильтра, агрегаты группировки, локали сортировки, режимы Join за пределами INNER/LEFT и дополнительная проверка широкого Union. Эти предложения ещё не являются принятыми требованиями или готовыми handlers.

68 остальных записей coverage — операции пакетов, портов, подключений, выполнения, визуализаторов и прочей общей инфраструктуры. Для них нужен отдельный объём; они не прибавлены к 70 узлам и не назначены как фиктивные новые компоненты.

## Основания и проверка

Исходная база main: `b22b73b23afd501440b4d31fd97c7dedc8e93b2b`. Сверены `client/lib/node-support.mjs`, 78 component IDs из `executor/coverage.json`, аудиты03–10, действующие подпланы11–16 и прежняя палитра05.09. SHA-256 входных реестров сохранены в JSON. Для 69 оставшихся строк сохранены каталожные пути Help; у Quality отдельный источник пока не установлен. Пути каталогов не означают, что все поздние modes уже подробно исследованы.

Открыты официальные разделы и выбранные статьи Help; для Excel дополнительно прочитаны закреплённые Dock `data/integration/import/excel/README.md` и `data/integration/export/excel.md`. E2E `bg/helpers/wizard.ts` подтвердил закрытие мастера с отдельным диалогом, `bg/helpers/packages.ts` и `bg/helpers/filedialog.ts` использовались при работе с собственным пакетом. [Компоненты сценария](https://help.loginom.ru/userguide/interface/scenario-component-panel.html).

В собственной сессии `orcestrator` подтверждены вход, каталог `/orcestrator`, палитра75 и перечисленные в live JSON мастера. Использован установленный клиент `0.1.0-dev.20260910.3-80ca61417ec7`, отдельный профиль, `viewport:null`, окно1508×949. Создан `/orcestrator/NodeRoadmap-20260912-d06b5179.lgp`; новые узлы не выполнялись, независимое переоткрытие пакета не проводилось.

При исследовании возник разрыв связи Loginom; собственная сессия восстановлена штатной кнопкой UI. Причина не установлена. К завершению планирования все четыре разработчика сохранили checkpoints из-за недоступности Dock. Координатор независимо подтвердил TCP connect0.019 s и затем SSL timeout8 s, HTTP000. Перед продолжением нужны доступный endpoint и свежие source harness/pins; причина TLS-сбоя ещё не установлена. Каталог и очередь не являются доказательством стабильности стенда.

Проверены полнота 78−8=70, уникальность component IDs, порядка и slugs, точные активные назначения, отсутствие Excel в резерве и определённость prerequisites/gates/владельцев. Независимый просмотр черновика выявил три уточнения: владельцы общей части, раздельные методы Сэмплинга и выходы ARIMAX; они внесены в этот реестр. Документационная проверка не заменяет приёмку четырёх рабочих веток.
