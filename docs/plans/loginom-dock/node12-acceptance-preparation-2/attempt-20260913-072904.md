# Узел12: автономная приёмка кандидата12.2 — FAIL112/118

Единственный разрешённый run `20260913-072904-3cc4ad0e` завершился без timeout.
Hermes: фактические `openai-codex / gpt-5.6-sol / low`, существующая подписка;
refresh/import/fallback не выполнялись. Полный независимый frozen аудит:
**112/118 PASS, итог FAIL**. Исходный экспорт и аудит сохранены без изменений.

Все16 операций с узлами выполнены. Штатный checkpoint и отдельный save_as
с overwrite/close/open прошли; все6 reopen bindings подтверждены. Прежняя ошибка
`packages.close cardinality 0` в этой попытке не повторилась. Прошли source bytes,
все14 проверок полного чтения, восемь проверок результатов разметки со схемами,
значениями и группами, включая пустой набор, а также сохранённые роли и связи.

Шесть отказов: main10/null8/empty `_import_contract` и `_persisted_import`.
Hermes задал каждому исходному импорту один output mapping, autosync:false,
с точным исходным порядком, именами и метками, excluded:false. Независимое
сравнение подтвердило identity mappings и все остальные условия import_contract.
Но закреплённый аудитор требует `mappings==[]`; persisted composite явно
отклоняет seed/request mappings как вне scope до проверки сохранённых параметров.
Текст неизменного goal требует сохранить имена/метки, но прямо не запрещает
identity mappings. Это несоответствие области аудитора допустимому представлению
запроса требует отдельного решения; frozen FAIL не переименован в PASS.
Успешные outputs не доказывают пропущенную проверку параметров импортов.

Пакет: `/test-1/packages/Dock-acceptance-20260913-072904-3cc4ad0e.lgp`.
Окно именно этой сессии PID66848 измерено read-only API macOS:1508×949
при доступной области1512×949; viewport:null и --start-maximized подтверждены.
Browser session `10f5ae4f-860b-483d-ba80-4d49b69e87e3`; отдельная metadata-only
precheck session `92e432a8-69c1-4292-94a9-8092b4e16a54`. Ручных действий не было.
Собственные процессы завершены, финальный process-check пустой.

Source0e11a3fb, run HEADb4853c97, runtime1459a3d10e84a0f4742933f68333cb5843f27347b0062656d090a306446e4248
и harnessb8d5b9ced1725cbbb64da0d92d93a74cd2f90bf9b8f8ff0efbf1a4a2f434b9d3
сохранились до конца. Полные pins, usage и hashes всех receipts —
[в машинной сводке](attempt-20260913-072904.json). Приватные доказательства:
`.dock/node12-autonomous/runs/20260913-072904-3cc4ad0e/`.

Новый Hermes run, правка runtime/goal/auditor, review, merge/push/deploy/activation,
изменение общего клиента и routing не выполнялись. Ограничения прежнего scope
сохраняются. Координатору передан блокер; следующая фаза требует его назначения.
