# Подпланы реализации Loginom Dock

Редакция: 9 сентября 2026 года. Подплан 01 реализован локально; goal-only приёмка
нового черновика — 17/17 PASS. Подплан 02 — **implemented / live_verified**:
внутренний графовый драйвер, Hermes 15/15 audit PASS; не released.
Подплан 03 — **implemented / live_verified (source runtime)**: общий node.apply,
импорт и безопасное продолжение приняты, Hermes30/30 PASS.
[Проверка каждого требования](03-completion-audit.md); не released.
Подплан 04 — **implemented / live_verified (source runtime)**, Hermes45/45 PASS.
[Проверка каждого требования](04-completion-audit.md); не released.
Подплан 07 — **implemented / live_verified**, [аудит 49/49 PASS](07-completion-audit.md).
Подпланы 05, 06, 08–10 остаются **planned**.
Это конкретизация [действующего плана](../2026-09-02-loginom-dock-implementation-plan.md),
а не отчёт о выполнении. Наличие прежнего пилота или примитива не закрывает подплан.

## Состав и порядок

| № | Конкретная работа | Основной результат | Этап |
| --- | --- | --- | --- |
| 01 | [Открытие Loginom и черновика](01-open-loginom-draft.md) | Готовая сессия и точная ссылка на принадлежащий задаче workflow | V1/V2 |
| 02 | [Добавление узлов на сценарий](02-add-nodes.md) | Создание/поиск узла, размещение и точные связи | V1/V2 |
| 03 | [Настройка текстового импорта](03-text-import.md) | Общий цикл node.apply и первый полный обработчик CSV/TSV | V1/V2 |
| 04 | [Настройка Калькулятора](04-calculator.md) | Упорядоченные выражения и проверенный выход | V3 |
| 05 | [Настройка Параметров полей](05-field-parameters.md) | Свойства, преобразования и исключение полей | V4 |
| 06 | [Настройка Фильтра строк](06-row-filter.md) | Условия и проверка обоих выходов | V4 |
| 07 | [Настройка Группировки](07-grouping.md) | Ключи, агрегаты и выходная схема | V3 |
| 08 | [Настройка Сортировки](08-sorting.md) | Порядок строк и сквозная задача продаж с двумя ветвями | V3 |
| 09 | [Настройка Слияния](09-join.md) | Внутренний и левый JOIN двух таблиц | V4 |
| 10 | [Настройка Объединения](10-union.md) | Добавление строк из двух и более таблиц | V4 |

Порядок первой законченной цепочки: **01 → 02 → 03 → 04 → 07 → 08**.
После общей основы 01/02/03 подпланы 05, 06, 09 и 10 можно разрабатывать параллельно.
Их итоговая приёмка включает зависимости, реально используемые выбранной задачей.
Нумерация документов не требует сначала выполнить 05/06, чтобы начать 07/08.

Эти документы покрывают работы V1–V4 по созданию сценария. Общие обязательства
V5 — совместимость, UI-резерв, полная матрица, native-платформы, сборка на VPS,
публикация, обновление и откат — сохраняются в
[основном плане](../2026-09-02-loginom-dock-implementation-plan.md#post-mvp-release).
UI-резерв и recovery также проверяются в V4. Завершение десяти подпланов само
по себе не означает готовность выпуска или переключение executor по умолчанию.

## Как разделена работа

Разделение подпланов описывает разработку модулей, а не дополнительные обращения
модели к каждому техническому шагу. Агент планирует граф, получает через Dock
контракты нужных типов и передаёт параметры следующего узла. Один внешний
node.apply выполняет полный локальный цикл: добавить/найти → связать → впервые
открыть → настроить вход/узел/выход → завершить → выполнить/дождаться → прочитать выход.

Подплан 02 реализует общую внутреннюю фазу создания и соединения. Для нового узла
её вызывает node.apply; для существующего использует точную ссылку. Существующие
отдельные операции добавления остаются совместимыми, но не становятся обязательным
дополнительным модельным вызовом. Не требуется заранее добавлять все пустые узлы.
Следующий узел настраивается по фактической схеме его непосредственных входов.

| Общая работа | Ответственный подплан |
| --- | --- |
| Профиль Sol/low в launcher, preflight, audit и admission; сессия и черновик | 01 |
| Общая схема запроса, registry, source pins и пакетные карточки выбранных типов | 02; импортная карточка и проверка контракта результата в 03 |
| Создание, поиск, имена, размещение, связи и дополнительные входы | 02 |
| Исполняемая оболочка node.apply, общие мастера портов, завершение, execution, output, отмена/recovery | 03 |
| Доставка файла, checkpoints, промежуточное сохранение и финальное save/reopen | 03; использование в остальных подпланах |
| Параметры и локальные особенности отдельного типа | Соответствующий подплан 03–10 |
| Сквозной сценарий продаж, два лидера, повторное выполнение сохранённого пакета | 08 с зависимостями 01/02/03/04/07 |
| Выпуск, совместимость поставки и переключение новых сессий на executor | V5 основного плана |

Общие драйверы реализуются один раз. Изменение такого драйвера требует адресной
проверки затронутых обработчиков; каждый подплан не создаёт свой запускатель,
систему ожиданий, импортёр знаний или второй исполнитель.

## Общие условия каждого подплана

- Полный node.apply реализован в source-кандидате подплана 03; его текущая приёмка
  и ограничения указаны в [checkpoint](../../loginom-dock/resume-checkpoint.md).
  Старые действия и их evidence сохраняются со своими семантиками и pins.
- Целевой Loginom работает на Linux-сервере; Excel исключён из этой реализации
  и приёмки. Резерв применяется только к доступным возможностям платформы.
- Codex сначала исследует соответствующий живой UI в отдельной сессии,
  сверяет Help и E2E через Dock, затем реализует и отлаживает настоящий MCP.
  Пути E2E/Help в документах относительны корню закреплённого источника.
  Наличие source-файла или прохождение старого теста не доказывает новый handler.
- Все будущие Hermes-прогоны используют существующую подписку ChatGPT:
  openai-codex / gpt-5.6-sol / low. Перед запуском сверяются фактические значения;
  автоматическая подмена модели/провайдера запрещена.
- «Закрыть» отменяет изменения мастера; «Готово» сохраняет и закрывает без запуска;
  «Выполнить» сохраняет, закрывает и запускает. Применение строки не сохраняет узел,
  а сохранение узла не сохраняет пакет на сервере.
- Штатный режим — выполнение и чтение выхода. Для явного «Готово»:
  execution=not_requested, output=not_refreshed. Старый результат не подтверждает
  новые настройки. Повторное открытие мастера — отдельный QA, не обычная проверка.
- Если выходной порт имеет отдельный мастер с графа, промежуточное «Готово»
  сохраняет узел, затем сохраняется mapping порта и узел выполняется с графа
  в том же node.apply. Это не финальный configure-only и не требует reopen узла.
- Все ожидания именованы, ограничены deadline и заканчиваются при готовности.
  Сохраняются settle: 0 и проверки контекста/epoch. До двух локальных обновлений
  допустимы только при строгом NOT_APPLIED до жеста и effect_possible=false.
  Возможный эффект сначала сверяется; потеря ответа не разрешает повторную мутацию.
- После принятого узла сохраняется локальный checkpoint; пакет — после импорта,
  готовой ветви и в конце. При повторном открытии пакет не запускается сам:
  аналитическая приёмка отдельно требует повторного выполнения и проверки выхода.
- Ответ содержит фазы, ссылки, execution identity, схему, число строк, до десяти
  строк, полноту и ограничения. Точные числа читаются в подтверждённом полноточном
  режиме «Таблица»; Null, пустая строка, ноль строк и отсутствие данных различаются.

## Как закрывать подплан

Каждый файл задаёт работу, источники, проверки и критерий готовности. Отмечать
planned / implemented / live_verified / released раздельно. Не ставить
live_verified по мокам, исходникам, коду возврата или одному резюме Hermes.

После Codex-отладки провести самостоятельную goal-only приёмку Hermes на
неизменном кандидате и независимый аудит заявленной задачи. Записать время
модельного планирования, локальных фаз и ожиданий, число внешних вызовов и токены.
Ошибка возвращает работу к диагностике, а не к повтору неизменного прогона.

Свидетельства и ограничения сохранять в
[implementation-status](../../loginom-dock/implementation-status.md),
текущую точку продолжения — в
[resume-checkpoint](../../loginom-dock/resume-checkpoint.md).
После принятия общей основы03, Калькулятора04 и Группировки07 следующий обработчик — подплан08
(Сортировка),
как отдельная задача. V1 в целом не завершён. Границы приёмки01 описаны
в [контракте подготовки](../../loginom-dock/workspace-preparation.md).
