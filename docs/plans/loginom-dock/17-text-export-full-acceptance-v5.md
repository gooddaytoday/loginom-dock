# Node17: полная независимая приёмка завершена

**54/54 проверки PASS** в отдельной versioned-переоценке v5 модельного run8 `20260914-000803-18fad485` и свежего оригинала. Закрыта полная заявленная цель:22 модельные операции узлов,3 доставки, все сохранения/переоткрытие, полный побайтовый результат и независимая новая сессия. Новый Hermes не запускался. Прежние frozen/v2/v3/v4 и первый fresh FAIL сохранены как самостоятельные результаты.

Точный оригинал `/test-2/packages/Dock-acceptance-20260914-000803-18fad485.lgp` открыт в новом сеансе `f8cc0bda-0b80-4b29-b9c0-a1d8a0aad27c`, Loginom7.4.2. Runtime `c9972b58cfcb36fdedad6fa495424259243c4e9204d4e28909d33653810c7c3d` совпал с исходным кандидатом. Сохранённые зависимости использованы без повторной доставки или настройки импортов. Перед нулевым экспортом явно выполнен существующий Empty с пустыми parameters/inputs/mappings; независимые проверки подтвердили сохранённое условие и оба выхода0/5 строк.

Все четыре свежих экспорта получили уникальные пути с идентификатором сессии: changed131байт, typed138байт, wide3005байт, zero0байт. Подтверждены полные байты и контрольные суммы, новые execution_id, сохранённые параметры и mapping, те же GUID и полный граф с портами/связями. Инвентаризация178 записей нативного хранилища подтвердила отсутствие Done/Close-файлов.

Новый адаптер читает фактические preparation.json и browser receipts, сверяет точную подготовительную команду с неизменённым генератором, порядок запросов/ответов и их контрольные суммы, аккаунт/путь, отдельную сессию и наблюдённую браузерную платформу. Нативные origin/epoch/node/tab/prefix и полный GUID-граф независимо связываются с исходным модельным пакетом. События workspace_prepared задним числом не создаются; ownership_verified=false сохраняет исходную семантику open_package. Для файлов исполняется прежний независимый byte/configuration/execution auditor с явным fresh-origin адаптером.

V5 восстановил и проверил все283 исходных harness blobs из `908c9b7d`. Четыре исходных model-файла request/scenario/evidence/full-audit не изменены; их hashes, точные исправления и отдельные свежие доказательства входят в provenance. Это полный PASS переоценки v5, а не изменение прежнего frozen FAIL.

**41 адресный Python-тест PASS**, включая21 свежую отрицательную подмену session/package/order/receipts/native context/graph. Проверка синтаксиса fresh harness и git diff --check прошли. Продуктовый runtime не изменялся; дополнительные полное ревью, модель или серверные операции не выполнялись.

После проверки оригинал закрыт через «Не сохранять», диагностические изменения путей отброшены. Квитанция закрытия точного пакета независимо связана с нативным ответом до logout. Выход test-2 и форма входа подтверждены; браузер закрыт, собственные PID отсутствуют. Старые диагностические файлы не заменялись.

Кандидат готов к решению координатора о слиянии. Merge, deploy, обновление общего клиента и следующие узлы в этой задаче не выполнялись; приёмка относится к указанному runtime и объявленному контракту Text export на Loginom7.4.2.

[Полная машинная сводка, пути файлов и hashes](17-text-export-full-acceptance-v5.json). Исходный подробный аудит: `.dock/node17/acceptance/runs/20260914-000803-18fad485/reevaluation-v5-full-final.json`; свежие квитанции: `.dock/text-export/live-1789376572763`.
