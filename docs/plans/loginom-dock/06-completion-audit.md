# 06. Аудит «Фильтра строк»

## Дополнение после ревью

Четыре замечания подтверждены на живом Loginom и исправлены.
[Подробный отчёт и ограничения](../../loginom-dock/row-filter-review-live-2026-09-11.md).
Client 1287 PASS / 1 SKIP, Python 475 PASS; Hermes `20260911-181616-1338ee3f`:
49/49 frozen PASS, 10 отрицательных подмен. Runtime
`fd2999ce9f87a878882a20962eda56b69941d15dcedd8d7a70852579c5819e53`.
Проверены 100 сохранённых условий, выбор невидимых полей, 1000 полей + RowNumberer,
оба широких выхода (2000 значений) и сохранение/reopen. Полный публичный preflight
нового узла на широкой схеме ограничен общим UI_SCAN_LIMIT; широкая проверка
использовала диагностический драйвер. Новая версия не установлена и не опубликована.

Ниже сохранён аудит первоначальной реализации до этих исправлений.

**Статус: implemented / live_verified (source runtime), не released.**

Граница проверки: source runtime, Loginom 7.4.2 Linux, видимый максимизированный
Chromium на macOS. Установленный клиент и опубликованный сервер не обновлялись.
[Подплан](06-row-filter.md), [контракт обработчика](../../loginom-dock/row-filter-node.md),
[история диагностики и восстановления](../../loginom-dock/row-filter-progress-2026-09-11.md).

## Реализация

`transform.filter_data`, `mode: conditions` встроен в общий `node.apply`.
Одна операция создаёт или находит узел, связывает вход, полностью задаёт группы
И/ИЛИ, проверяет настройки, настраивает оба выхода, выполняет узел и читает данные.
`parameters: {}` сохраняет поддержанные настройки существующего узла без
переприсваивания. При обычном выполнении мастер не открывается повторно для проверки.

Поддержаны сравнения, включающие границы интервалы, списки, Null, логические
предикаты, строковые contains/starts/ends и отрицания, регистр и номер строки.
Нативные редакторы и строки связываются с точным владельцем; скрытый или ещё
загружающийся редактор не разрешает жест. После возможного эффекта повтор не
создаёт условие и не запускает вычисление заново.

Оба результата имеют независимые port GUID, схемы и счётчики, общий execution ID,
до десяти полноточных строк и признак полноты. Пустые результаты читаются явно.
Исходные форматы таблиц восстанавливаются. Сохранение пакета остаётся отдельной
операцией; квитанция узла не считается доказательством сохранения.

## Проверенные требования

| Требование | Свидетельства |
|---|---|
| Все операторы на совместимых пяти типах и номер строки | `.dock/row-filter/complete-matrix-audit.json`: 90/90, повторная независимая сверка исходного CSV, raw конфигурации и обоих полных Id-выходов |
| Регистр, границы, список с запятой внутри значения | Строковые cases36–57 и74–85; дополнительные native `< Alpha` / `> preTail` в `live-1789126596945` |
| Различающий приоритет И/ИЛИ | Case86: `Id=9 OR (Id>=6 AND Id<=7)` дал [6,6,7,9], ошибочная left-associative группировка теряет9 |
| Null против пустой строки; пять полных столбцов, точные дроби, секунды, дубли | `live-1789126379733/typed-independent-audit.json`: шесть операций, 8/2 →5/5 →5/5 для двух фильтров |
| Сохранение и новое выполнение без изменения условий | `live-1789127086536/typed-independent-audit.json`, `typed-persistence-semantics.json`: оба узла5/5, сохранённые настройки и новые execution IDs |
| Чужие узлы и связи | `foreign-graph-audit.json` в обоих typed runs: восемь операций, все чужие объекты сохранены |
| «Готово» / «Закрыть», номер после сортировки, совпадение за1000 | `live-1789122652150`: 1105 строк; Done без выполнения, Close без применения и последующий Execute; после сортировки выбраны1105,1104,1103 |
| Пустой вход; источник со второго порта | `live-1789124649523`: 0/0 и1/1104, оба raw-аудита и сохранность чужого графа |
| Потерянный ответ после Add/Execute, отмена до жеста | `live-1789122801954`, `live-1789122892772`, `live-1789123307395`: по одному эффекту либо ни одного, uncertain не стал успехом, повтор ID без новых действий |
| Stale до жеста, владельцы, deadline, загрузка редакторов | Поведенческие client tests; два bounded refresh только при доказанном отсутствии эффекта, монотонное время ожидания |
| Полнота независимых аудиторов | 14 отрицательных подмен configuration/output; 10 подмен полного сценария на диагностической перепроверке первой Hermes-попытки |

`live-*` относятся к `.dock/calculator-v3/`. Матрица содержит свидетельства
нескольких source revisions, исправления повторно проверены в затронутых случаях;
полный автономный сценарий проверяет окончательный единый runtime.
Ожидаемые строки вычисляются отдельно от клиентского обработчика.
Сравнение строк oracle ограничено латинским golden fixture, не является общей
моделью Unicode/locale. E2E сортировка списка редактора не подменяет сравнение фильтра.

## Тесты и закреплённые исходники

- Client: 1280 total, 1279 PASS, 1 SKIP, 0 FAIL; `.dock/row-filter/all-client-tests-final-candidate.log`.
- Python: 475 PASS; `.dock/row-filter/python-tests-final-source-name.log`.
- Runtime: `5c945f3e189e6588acf036ca7139e35c62bf055b30b7ce78bd1fa7b2c548756d`.
- Native skill SHA-256: `714104445d37265943ab5e7d55a7c505c00fe38a62bc38a0c3c8ea24f7764b39`.
- Catalog: `viking://resources/loginom-dock/catalogs/executor-preview/releases/2026.09.08-node-apply.1-candidate/manifest.json`.
- Catalog SHA-256: `936ef73d933e85bfd8429b8b0f2b515c543ca415a2b22ba57e108232c54ddf44`.
- Golden CSV:413 bytes, SHA-256 `7ac857baaecd48b1e03f2cff470140ae7526a3e289016a209c7d41a706a2e0bb`.

## Автономные попытки

Только существующая ChatGPT subscription, `openai-codex / gpt-5.6-sol / low`.
Без fallback и операторских UI-подсказок модели.

Первая попытка `20260911-152244-2a09363f`: пять узловых операций успешны, значения,
граф, save/reopen/reexecute подтверждены; исходный frozen audit48/49 из-за
избыточного запрета optional `source_name`, равного имени CSV-столбца.
Этот результат сохранён как непринятая frozen-попытка. Прежний harness сохранён
с проверкой hashes. После отдельной Codex UI-диагностики уточнён только независимый
контракт импортных полей; чужое имя источника и перестановка по-прежнему запрещены.
Содержательная диагностическая перепроверка47/47 и10/10 отрицательных подмен
не заменяют новую frozen acceptance.

Вторая попытка `20260911-154023-843300b0`: **49/49 frozen checks PASS**,
**10/10 отрицательных подмен отклонены**. Runtime, harness и native skill
не изменялись во время запуска; итоговый аудитор сверил текущие исходники с pin.

Принятый пакет: `/user/dock-p3/packages/Dock-acceptance-20260911-154023-843300b0.lgp`.
Ровно два узла «Данные» и «Отбор», одна связь. Оба выхода содержат по пять
столбцов Id, Amount, Flag, When, Text. Первичная очистка:8/2 строк; после замены
условий:5/5; после реального reopen и нового выполнения:5/5. Последний фильтр
выполнен с `parameters: {}`, `inputs: []`, `mappings: []`. Полные значения, дубли,
условия, портовые настройки, сохранённый граф и новые execution IDs проверены.

Все request/pins, evidence, efficiency и отрицательные проверки находятся в
`.dock/row-filter/hermes-runs/20260911-154023-843300b0/`.
Audit SHA-256: `8fad61fa1cd80daf4d5e923efe6ee1548f496658cb11e20cfde40a8aee07d2d8`.
Полная матрица и пограничные проверки вместе с этим аудитом закрывают подплан06.

## Измерения

| Попытка | Frozen audit | API-вызовы модели | Вход | Выход | Чтение кеша | Всего токенов | Журнал, с |
|---|---|---:|---:|---:|---:|---:|---:|
| 20260911-152244-2a09363f | 48/49, не принята | 22 | 96521 | 5906 | 1087744 | 1190171 | 582.399 |
| 20260911-154023-843300b0 | 49/49 PASS | 23 | 98792 | 6718 | 1171584 | 1277094 | 515.473 |

Это provider-reported counters; запись кеша0 в обеих попытках. Reasoning
902 и1439 — пересекающиеся отдельные счётчики.
Журнал финальной попытки: 2026-09-11T12:41:03.936000+00:00–2026-09-11T12:49:39.409000+00:00.
Интервалы включают локальные ожидания и транспорт; отдельное время модельного
планирования из этих данных не выделяется. Суммы подтверждённых локальных фаз:

| Фаза | Секунды |
|---|---:|
| configure | 38.493 |
| execute | 8.808 |
| finish | 9.619 |
| input_mapping | 7.398 |
| node_finish | 6.291 |
| open | 14.452 |
| output_mapping | 29.493 |
| read | 133.137 |
| source | 0.026 |
| target | 3.551 |
| workflow | 0.144 |

Финальная попытка: 22 внешних вызова, в том числе5 node.apply и5 node.wait,
2 сохранения и2 prepare. Модельных UI-команд внутри узловых операций нет.
Пять начальных ответов node.apply занимали189–192 байта JSON; полный ответ
node.wait — не более 26487 байт с обоими полноточными samples.
Всего десять узловых ответов: 117404 байта UTF-8 JSON, без MCP framing.
Локальные подробные наблюдения и квитанции остаются в evidence. Измерения:
`measured-summary.json` рядом с каждым run.

## Ограничения

- Source implementation не является новым опубликованным или установленным выпуском.
- Только пять scalar-типов; переменные и variant не поддержаны данным режимом.
- До64 групп,128 строк условий/ИЛИ и128 значений списка; выборка до10 строк.
- Даты локальные, без часового пояса; integer в точном диапазоне JavaScript.
- Имена полей ограничены действующим идентификаторным контрактом аналитических обработчиков.
- Во время диагностики общий драйвер графа отказал при неполном отображении портов
  у узла в нижней части полотна; отдельный одноколоночный дискретный импорт отказал
  при недоступной прокрутке. Эти безопасные отказы не объявлены исправленными;
  принятие фильтра опирается на явно наблюдаемые узлы/порты и проверенные fixtures.
- Новый серверный выпуск, Loginom7.5, остальные подпланы и общий V5 этой проверкой не приняты.
