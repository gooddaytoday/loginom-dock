# Node17: модельная часть переоценки прошла, свежий оригинал ещё не принят

Run8 `20260914-000803-18fad485` / runtime `908c9b7d`: **52 проверки модельной части PASS** в отдельной v4-переоценке. Пройдены три доставки, три импорта и их сохранённые настройки/данные, все 11 выполненных экспортов с полными байтами, reject baseline observer, четыре экспорта после reopen и три операции save/checkpoint. Итоговый **полный PASS отсутствует**: `separate_session_gate_present` остаётся false.

Исходный frozen FAIL и промежуточные v2/v3 FAIL сохранены. V4 восстанавливает283 исходных harness blobs по неизменённому request из Git `908c9b7d`, сверяет runtime, зависимости и исходные request/scenario/evidence/full-audit до/после. Точный diff origin, исправление import assertion, смена проверки эпохи и контрольные суммы новых независимых проверок записаны в provenance. Это переоценка исходных доказательств, не повторная модель и не переименование frozen результата.

Помимо адресных исправлений [v3](17-text-export-import-origin-v3.md), восстановлена проверяемая цепочка происхождения сохранённого пакета. Флаг `ownership_verified=false` при open_package не меняется. Вместо его безусловного принятия проверяются единственный исходный owned draft, полная согласованная пара prepared/completed save_as, точный путь и старый владелец, последовательность подтверждённого сохранения/закрытия/нативного открытия, новый tab/prefix/workflow, совпадение checkpoint и наблюдённого графа. Дополнительно сверяется полный нативный граф до/после: GUID, типы, метки, входы/выходы, прочие порты и связи, включая системный узел переменных. Для сравнения с fingerprint действия исключается только его системный узел `bg-vendor-icon-modelvariables`, отсутствующий в этом fingerprint; его сохранность проверяется полным сравнением графов.

В том же Loginom-сеансе после открытия сохраняется document_id, но меняется workflow_id. Проверка сохранности теперь допускает эту наблюдённую смену: при одинаковом document_id оба workflow_id обязательны и различны. Прежний GUID, настройки, mapping и новое выполнение всё равно требуются. Тот же workflow, пустая идентичность или подменённый GUID отвергаются. Независимая связь с save/close/reopen подтверждается отдельной цепочкой происхождения выше.

**37 Python-тестов PASS**, включая21 отрицательный случай цепочки владения,20 случаев временной привязки origin, подмены source/настроек/связей/hash и проверки provenance. `git diff --check` прошёл. Код продуктового runtime, модель, учётные записи, сервер и общий клиент не менялись.

Оставшееся условие: свежая отдельная сессия должна проверить точный оригинал `/test-2/packages/Dock-acceptance-20260914-000803-18fad485.lgp`, полные файлы, сохранность графа/настроек и отсутствие Done/Close-файлов. Ранее оригинал открылся только для чтения; это фактический режим, но его причина и владение серверной блокировкой не установлены. Координатор занимается штатной проверкой этого состояния через владельца. Разработчик не повторял открытие и не создавал копию. Неизвестное происхождение свежего пакета по-прежнему отклоняется: предстоящая проверка должна связать собственные native receipts с исходным модельным пакетом, а не использовать самоотчёт или изменённый флаг.

Следующий trigger — событие координатора о возможности продолжить проверку оригинала. До неё готовность к слиянию не заявляется; новая модель для исправлений аудитора не нужна.

[Машинная сводка v4 и provenance](17-text-export-reevaluation-v4.json).
