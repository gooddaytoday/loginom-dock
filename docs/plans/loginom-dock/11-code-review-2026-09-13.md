# Узел 11 «Замена»: отдельное ревью 13 сентября 2026

**REVIEW COMPLETED — требуется исправление N11-R1 (P2).**
К автономной приёмке кандидат пока не рекомендован. Следующий допустимый этап —
назначенный координатором раунд исправления и целевой проверки замечания.

- Reviewed SHA: `8aaf6a8a425d94974d65b5bd8b27a55f362fac4d`.
- Полная база: `a3b419bde8a660e1905284ee62a46362d5a49e09`.
- Ветка: `codex/node-11-replacement`.
- Рассмотрен полный diff базы к кандидату: 43 файла, включая `e4ae1068`,
  регистрацию, общий UI/runtime, обработчик, аудиторы, fixtures и документацию.
- Исходники, тесты и проверенный пакет не изменялись. Живой браузер и Hermes
  в ревью не запускались; диагностика выполнялась на исходном коде и сохранённых
  журналах. Bootstrap-изменение `.gitignore` сохранено отдельно.

## Подтверждённый дефект

### N11-R1 — P2: проверять коллизии по итоговым правилам и режиму существующего узла

Основное место: `client/lib/replacement-parameters.mjs:54–55`.
Связанный порядок действий: `client/lib/replacement-output.mjs:28–38,46–53`;
сбор сохранённых правил: `client/lib/replacement-procedure.mjs:51–54`.

**Сценарий.** Во входе существуют строковые поля `A` и `A_Replace`. В уже
настроенном узле включена замена для `A`, режим выхода — `replace`. Это допустимая
конфигурация с полями выхода `A`, `A_Replaced`, `A_Replace`. Пользователь передаёт
только `parameters={"output_mode":"add"}`, сохраняя существующие правила.
После переключения потребуются два поля `A_Replace`: сохранённое входное и новое
производное. Контракт подплана требует отказа до изменения настроек.

**Причина.** `changed` строится только из `p.rules`. При отсутствии `rules` он
пуст, поэтому проверка коллизий не выполняется. Сохранённые правила читаются
позднее, но итоговая конфигурация повторно не проверяется на коллизии перед
`setMode`. Эта функция сначала посылает действие `dptSupplement`, и только
после неё вызывается `validateReplacementSources`. Следовательно, отказ по
известной заранее коллизии переносится за действие изменения политики выхода.
Это нарушает обещание раннего отказа и оставляет необходимость восстановления
после уже начатой настройки. Успешное сохранение ошибочного пакета не утверждается.

Вторая форма той же ошибки: в существующем `add`-узле правило было только для `B`,
во входе есть `A`, `A_Replace`, `B`. Запрос добавляет правило `A`, опуская режим.
Resolver считает только суффикс `_Replaced`, хотя сохраняемый режим требует
также `_Replace`. Проверка должна учитывать объединение сохранённых и новых
правил и фактический режим при отсутствии нового значения.

**Доказательство ревью.** На закреплённом Node 24.19.0 вызовы актуальных функций
дали следующие результаты:

| Проверка | Результат |
|---|---|
| `resolveReplacementParameters({rules:[ruleA],output_mode:'add'}, fields)` | Отказ `Replacement output collision: A_Replace` |
| Тот же resolver с `{output_mode:'add'}` | Пропустил запрос |
| Тот же resolver с `{rules:[ruleA]}` | Пропустил запрос, не имея фактического режима |
| `replacementSources({rules:[ruleA],input_fields:fields},'add')` | `A, A_Replace, A_Replaced, A_Replace` |
| `configureReplacementInlineMapping` с сохранённым `ruleA` и requested mode `add` | Дошёл до действия `btnProduceType;mn;dptSupplement` |

Последняя проверка использовала подставленный канал: исходное состояние выхода
`produce_mode='replace'`, реальные функции кандидата, запись действий и остановка
исключением непосредственно перед действием смены политики. Полученная
последовательность: Table → Links → Produce menu → Supplement. Проверка
не имитирует серверную реакцию Loginom на коллизию и не доказывает конкретный
финальный статус операции на стенде. Подтверждён именно пропуск ранней проверки.

**Требуемое исправление и проверка.** До изменения таблиц/выходной политики
проверить полную эффективную конфигурацию: сохранённые правила плюс частичный
запрос, действительный сохранённый режим либо явно запрошенный. Добавить целевые
проверки обеих форм запроса выше, отказа без изменения правил/режима, и контрольные
неконфликтующие смены `replace ↔ add` с сохранением неперечисленных правил.
В назначенном раунде воспроизвести через публичный `node.apply` на отдельной
диагностической копии пакета, проверить исходные настройки после отказа и успешный
полный результат неконфликтующего запроса. Оригинал
`/test-2/Node11-Handler-90de4e18.lgp` сохранить.

## Проверки, выполненные в ревью

Использован закреплённый runtime Node 24.19.0. Health личной памяти был healthy.

- Профильные replacement-тесты и схема публичного результата: **24/24 PASS**.
- Общий `workspace-ui.test.mjs`: **261/261 PASS**, 0 пропусков и ошибок.
- Проверены доступность и чтение 21 файла evidence из машинного отчёта.
- Повторно вычислен `verify_persistence` из исходного `execution-events.jsonl`
  с явно заданными ревизиями save-действий `2`: **PASS**.
- Повторно вычислены конфигурация/полный выход/публичная схема пустого входа:
  **PASS**, 0 строк, 7 столбцов.
- Повторно вычислен `verify_response_loss`: **PASS**, отдельная fault injection,
  один результат после потерянного ответа; автономность не утверждается.
- Повторно вычислен `verify_close`: **PASS**, иной черновик наблюдался и был
  отброшен, последующее выполнение сохранило исходные настройки.

Последние persistence/empty/response-loss проверки использовали сессию
`e1368ffd-31b0-4766-968a-076b121d1e6c`; Close —
`7809ab9a-c36e-44cf-b5b7-bf8f38de7347`. Файлы расположены в
`.dock/stream-runtime/sessions/<session>/`. Исходные журналы и результаты
разработки при повторном аудите не перезаписывались.

Полный клиентский результат **1402 PASS / 1 SKIP** проверен по сохранённому
отчёту разработки; весь набор в ревью заново не запускался. Проверенные здесь
285 тестов не выдаются за повтор полного набора. Новая live-матрица не проводилась.

## Оценка остальных областей

Типизированные Null, пустая строка и текст `null` разделены; Int64 сравниваются
без небезопасного преобразования через Number, включая локальное чтение `{lo,hi}`.
Ограничения регистра и вещественного other/value явно проверяются и описаны.
`_Replaced` сверяется вместе с данными; отличающееся поведение other→Null на
Loginom 7.4.2 отражено в истории отдельного probe, а не подменено ожиданием Help.

Partial update, Done, Close, повтор operation ID, восстановление ответа и
save/reopen имеют отдельные доказательства. Public readback продолжает явно
указывать отсутствие доказательства сохранения пакета; persistence-аудитор
отдельно проверяет путь, граф, новые refs, настройки и свежее выполнение.
Отрицательные результаты первоначальных прогонов сохранены в checkpoint.

Исправление `source ?? exclusion_source` передано как локальная стратегия
Replacement в существующий механизм mapping; общий helper не изменён этим diff.
Принадлежность исключённых источников проверяется. Возврат Add привязан к выбранному
полю и scroll-контейнеру, требует уменьшения scroll.top; есть живое доказательство
24 пар и отрицательные source-проверки отсутствия прогресса/чужого поля.
Новые UI-привязки ограничены стадией Replacement; produce menu дополнительно
обслуживается общим reader. Подтверждённых регрессий общей UI-инфраструктуры
в рассмотренных изменениях и запущенном наборе не обнаружено.

Порядок переключения `case_sensitive` после заполнения пар заслуживает проверки
перехода false→true с ключами `A`/`a` в дальнейшем целевом раунде. Ошибка нативного
редактора в таком переходе здесь не воспроизведена, поэтому это **не finding**.
В `executor/coverage.json` сохранился промежуточный текст о незавершённой live-матрице
и восстановлении Dock; его следует согласовать с финальной документацией при
следующем обновлении статуса. Статус planned до принятия сам по себе корректен.

## Обязательные следующие проверки и согласованные ограничения

**N11-G1 — приёмочный этап, не дефект разработки.** После исправления N11-R1 и
целевой проверки координатор отдельно выделяет слот единственного обычного
Hermes-прогона `openai-codex / gpt-5.6-sol / low`. Нужны сохранение пакета и
независимый аудит полной задачи. Отсутствие этого прогона не включено в список
дефектов кандидата и не заменяется результатами Codex.

Согласованный scope сохраняется: exact internal tables, string/integer/real,
numeric precision 0; без regex, внешних таблиц, преобразований типов и date/bool.
Не-ASCII case folding отклоняется. Native other/value real ограничен двумя
десятичными знаками. 24 пары подтверждают прокрутку, но не исчерпывающую проверку
ресурсных пределов 128 правил / 256 пар. Candidate-каталог не является принятым
релизом; `archiveActive=false` не доказывает изоляцию глобальных hooks.

В рамках данного ревью исправления, повторное ревью, Hermes, следующий узел,
push, merge, deployment и изменения плагинов не выполнялись.

## Результат последующего назначенного раунда

N11-R1 исправлено и проверено на отдельной live-копии; см.
[отчёт исправления](11-fix-results-2026-09-13.md). Исходный вывод ревью выше
относится к reviewed SHA и сохранён без подмены. Повторного ревью не было;
N11-G1 — автономная приёмка Hermes — остаётся за отдельным слотом координатора.
