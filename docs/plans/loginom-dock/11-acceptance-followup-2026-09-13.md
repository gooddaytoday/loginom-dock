# Узел 11: исправления подготовки автономной приёмки, 13 сентября 2026

Статус: **followup_complete / awaiting_coordinator**. Команда
`node11:acceptance-followup:1:dd95a3b0c96e1873b4cd3ef36ea83e88863bea23` выполнена.
Клиентские изменения сохранены в `7de7f23e4adbff0819df6c7e970589f93df4882b`.
Новых Hermes-прогонов, ревью, merge, push, stage, deploy и activation не было.
Узел ещё не принят: первоначальный автономный результат **FAIL 55/59** сохранён.

## N11-A1 — два последовательных действия сохранения

В естественной формулировке задачи сначала требуется сохранить пакет, оставить
его открытым и дождаться подтверждения. Только после этого отдельным шагом —
закрыть и повторно открыть сохранённый пакет. В prompt не добавлены API-команды.
Полная задача сохранена: 11 node operations, 4 узла, 2 связи, типизированные замены,
частичное изменение правил, переключения режимов и независимая проверка выхода
после reopening. Аудитор по-прежнему требует отдельный checkpoint и save_as/reopen.
Исходный scenario.txt старого прогона не изменён.

## N11-A2 — доказанная рабочая сессия и происхождение precheck

Новый `replacement_session_evidence.py` устанавливает единственную рабочую Dock
сессию из публичных ответов prepare, связывает их с вызовами одного Hermes caller,
содержимым prepare в журнале и последним состоянием metadata. Исходный journal
должен полностью совпасть с экспортированными событиями. Проверяются runtime и
полный source manifest, версии Node/SDK/Playwright/Chromium, candidate/каталоги/E2E/
ABI/executor/target, собственные profile/artifacts, каталог tools и полный комплект
навыка. Исправлена прежняя ошибка сравнения SHA главного SKILL.md с ревизией всего
комплекта: теперь ревизия заново вычисляется по всем файлам и каталогам навыка.

Дополнительная metadata-сессия допускается только при положительном доказательстве:
официальный stdio transport записывает `mcp-origin.json` с session ID, PID,
runtime/manifest, точным клиентом initialize и последовательностью методов.
`check-tools.mjs` связывает новый каталог с PID своего дочернего процесса и сохраняет
хеши origin и metadata после закрытия. Допустима лишь последовательность initialize,
initialized, list_tools; закрытие обязательно. tools/call, неизвестный метод/клиент,
prepare/workspaceReady/skill/target/workflow или execution events исключают льготу.
Отсутствие активности без origin не является доказательством. Аргументы, ответы,
текст модели и credentials в origin не записываются.

Отдельный настоящий precheck завершился успешно: сессия
`a7469b5e-13d1-47a0-9216-631c121237e7`, PID 54509. Модель и видимое Loginom-окно
для этой проверки не запускались. Отрицательные проверки отклонили чужую активную
и неизвестную сессию, дополнительные методы, подмену PID/metadata/journal/caller,
несовпадающие pins и отдельные подмены геометрии.

### Историческая диагностика без пересмотра результата

`N11-A2-historical-diagnostic.json` сохранён отдельно в
`.dock/replacement/acceptance-followup/`. Полный прежний goal-аудит повторён из
снимка `be4d31bf`; все его harness inputs сопоставлены с исходным request.json.
Его 56 предметных проверок дали три прежних отказа: save_chain и обе persistence
проверки из-за отсутствующего checkpoint. Новый session diagnostic подтвердил
рабочую сессию `ae4da67d…`, но отклонил дополнительную `e15a3c0c…`: положительной
квитанции происхождения в старом запуске нет. Это не исправляется задним числом.
Все восемь опубликованных SHA исходных артефактов, включая evidence.json,
scenario.txt и первоначальный полный FAIL, проверены и остались прежними.

## N11-A3 — измерение именно рабочего браузера

Клиент после READY внутри того же browser gate выполняет фиксированное чтение
размеров из собственной страницы. Квитанция привязана к session/operation/document/
workflow/runtime/manifest и поступает в публичный prepare, metadata и execution
journal. Измеряются реальный viewport, inner/outer размеры, доступный экран,
положение окна, видимость, origin/path и время. Внешнего выбора окна, изменения
размеров, системного захвата или вмешательства в шаги модели нет.

Аудитор требует viewport=null, видимую страницу, совпадение документа и pins,
свежесть до 30 секунд относительно журнала, заполнение доступного экрана с допуском
16 px на рамку, близкое положение и отсутствие маленького эмулированного viewport.
Эти правила проверены на подменах документа/сессии, малом/эмулированном окне,
скрытой странице, неверном положении, старой и отсутствующей квитанции.

Прямая Codex-проверка на окончательном runtime прошла в отдельной видимой сессии
`25adcc4f-24b0-46ad-a803-71753f9c9f33`, Loginom 7.4.2, test-2:
страница **1508×862**, окно **1508×949**, доступный экран **1512×949**,
позиция **4,33** при доступном начале **0,33**. Public/metadata/journal совпали;
runtime сессии совпал с окончательным исходным кодом. Собственный пустой черновик
закрыт штатно, подтверждены «Начало» и отсутствие открытого пакета. Все собственные
браузеры и precheck-процесс завершены. Это проверка механизма; следующее окно Hermes
обязано создать собственную квитанцию и пройти тот же gate.

## Проверки и границы

- Клиент: **1407 PASS / 1 SKIP / 0 FAIL** в итоговом полном наборе.
- Приёмочный инструментарий: **41 PASS / 0 FAIL**.
- Регрессия сохранённых доказательств: **7/7 групп**, включая **18/18 отрицательных**.
- Preflight: PASS, `openai-codex / gpt-5.6-sol / low`, Hermes 0.21.0; модель не запускалась.
- Архив исходников: **392 файла**, совпадение с клиентским коммитом и байты архива проверены.

Первый ограниченный запуск тестов встретил EPERM локальных сокетов. При разрешённых
локальных сокетах обнаружились две устаревшие имитации: у browser fixture отсутствовал
новый read-only ответ, у shutdown fixture отсутствовали обязательные данные сессии.
Обе обновлены; целевые 9 тестов и затем полный набор прошли.

Исходный пакет `/test-2/packages/Dock-acceptance-20260913-054024-fef0b53e.lgp`
вновь открылся только для чтения; причина остаётся неизвестной. Собственная вкладка
закрыта штатно, принудительного unlock и действий в чужих сессиях не было.
Общий save/reopen runtime fix принадлежит узлу 12 по последнему уточнению
координатора; здесь этот переход не изменялся и чужой код не заимствовался.
До следующей приёмки координатор должен учесть зависимость от результата узла 12.
Новый Hermes slot узлу 11 не выдавался.

## Точные pins и передача координатору

- Клиент/source commit: `7de7f23e4adbff0819df6c7e970589f93df4882b`.
- Runtime: `3b21e8f0c52820b058caf9c02bb00d006d7bf9b451891ec27646f8018e46e00e`.
- Goal SHA256: `d283e683fe61298a9273365ca485d63e16fb582d08b45d42dfefb70c65952a9c`.
- Harness input-map SHA256: `4ec0ac41998acdffdb939c2c18d2246224b5d6a90ad7789ae248e272a52c205b`.
- Архив `.dock/replacement/acceptance-followup/client-source-7de7f23e.tar.gz`:
  `e7a2103be3679abd0907ed6454f39adf062db0f68808f01da101308b64299ab9`, 1 019 833 байта.

**Нужны новая VPS-сборка и stage/readback.** Предложена версия
`2026.09.13-node11.2-candidate` с URI
`viking://resources/loginom-dock/catalogs/executor-preview/releases/2026.09.13-node11.2-candidate/manifest.json`.
Фактический новый SHA пока отсутствует. Старый candidate pin отклоняется новым
launch gate. `replacement-stage-vps.sh` закреплён на новый архив/source и новую
версию; запускать его вправе координатор на VPS после решения об интеграции fix12.
Если интеграция изменит runtime, этот комплект требуется заново перепиновать.
После stage/readback обязательны согласованный новый pin и отдельная выдача слота.

[Машинный отчёт и полная карта хешей](../../loginom-dock/replacement-acceptance-followup-2026-09-13.json).
