# Управляемые потоки разработки узлов

Дата плана: 12 сентября 2026; актуализация 13 сентября. **Начинать новый узел
по [единой инструкции](node-workflow-runbook.md)**. Последний проверенный checkpoint:
11/12 завершили единственный раунд доработок и ожидают candidate/Hermes;
13/14 продолжили разработку после остановки пользователем и перезапуска приложения.
Общий actor-доступ OpenViking проверен у всех четырёх. Статусы ниже — сохранённый
снимок, не мониторинг. [Подробный checkpoint](../../loginom-dock/agent-handoff.md).

Исходный запуск: **четыре отдельных рабочих чата**.
По следующему запросу пользователя и после проверки текущих потоков добавлен
поток4: узел14 «Заполнение пропусков», аккаунт test-4, Astra/medium.
Его source/UI-допуск завершён, начато исследование мастера. Узел14 снят
со следующего назначения потока1. Имя файла сохранено для существующих ссылок.

Полный остаток теперь распределён в [дорожной карте четырёх потоков](four-stream-node-roadmap.md)
и [JSON-реестре](four-stream-node-roadmap.json): 70 ещё не принятых компонентов,
включая четыре текущих. Следующие назначения — экспорт текста / 16 / 15 / Сэмплинг.
После экспорта текста в потоке1 идут отдельные импорт и экспорт XLSX.
Уточнение пользователя о поддержке XLSX на Linux отменяет прежнее исключение Excel.
С 13 сентября координатор автоматически назначает ревью и доработки в той же
задаче; после успешной проверки следующий узел получает новый чат, постоянный
worktree, ветку, профиль и отдельный пакет/workflow. Общие зависимости имеют
первых владельцев в дорожной карте; неподтверждённые режимы остаются planned.

У потока4 подтверждены Node24.19.0, свой source MCP и профиль, Loginom7.4.2,
вход test-4 и открытие /test-4. Обнаружено расхождение метаданных приложения:
`read_thread.cwd` возвращает `/`, а сводная запись имеет пустой cwd. При этом
runtime `session_meta`/`turn_context`, команды без явного workdir, sandbox writable
roots и OpenViking workspacePeerId указывают на правильный worktree14.
Модель и effort в turn_context — gpt-6-astra/medium. Индекс вручную не правился;
при последующем возобновлении снова проверить реальный cwd и область записи.
Расхождение сводки не доказывает исполнение из `/` и не снимает изоляцию worktree.

Checkpoint14 `.dock/stream-preflight.json` проверен координатором: source manifest
совпадает с файлами ветки, архив не активирован, Node24.19.0 и отдельный профиль.
Создан `/test-4/Node14-20260912-diagnostic.lgp`; viewport:null, окно развёрнуто.
После добавления найдены пять раздельных профилей у четырёх потоков (у13 два
собственных диагностических); memory_pressure -Q: 50% свободной памяти,
load average2.19/3.02/3.04, swap1523.06 MiB. Hermes-процессов в снимке нет.

Перед расширением все три прежние задачи активны и продолжают работу. Найдены
три собственных активных browser profile; общих профилей и признаков конфликта
аккаунтов в проверенных результатах нет. Локальный таймаут импорта у11 разобран
его владельцем, работа продолжена; это не объявляется отсутствием всех дефектов.
Mac: 16 GiB RAM, memory_pressure -Q сообщает 38% свободной памяти, load average
3.00/3.14/2.98, swap used 1874.75 MiB. Оснований остановить расширение по этому
снимку не найдено; полная приёмка обработчиков и native-интеграции ещё не завершена.

Пользователь дал команду «приступаем к реализации». Работают потоки 1–3,
узлы 11/12/13 на Astra medium; все три live-допуска окружений сохранены,
начато исследование мастеров. Разработчики реализуют подпланы до checkpoint
перед отдельной автономной приёмкой.
Рабочие задачи созданы в постоянных worktree, зарегистрированных отдельными
проектами приложения. Точные ID и пути приведены в реестре ниже.

Пользователь управляет работой через исходный чат координатора: получает состояние,
уточняет задания/очередь и останавливает потоки. Координатор назначает фазы по событиям.
Каждый узел получает отдельную обычную задачу приложения Codex на этом Mac,
свою Git-ветку, рабочий каталог и пакет Loginom с собственным workflow.
Ревью и следующие узлы назначаются автоматически после установленных условий.
Слияние и поставка этим не разрешены; периодического мониторинга нет.

Это новый порядок подготовки работы, а не возобновление
[остановленного пилота](../../loginom-dock/parallel-node-pilot.md).
Его worktree, черновики и доказательства сохраняются.

## Проверенная исходная точка

Проверены реестр обработчиков `client/lib/node-support.mjs`, контракты и API,
подпланы и сохранённые отчёты приёмки. На момент проверки основной checkout чист,
HEAD main: `7317cc57b7e59318a0d63d3452ee74b6b929f01e`.
Это снимок для планирования; точный общий базовый commit закрепляется при подготовке
потоков после повторной проверки main и действующих задач.

| № | Готовый обработчик | Подтверждение и границы |
| --- | --- | --- |
| 03 | Текстовый файл | CSV/TSV; [аудит подплана 30/30](03-completion-audit.md), [совместная приёмка после ревью 45/45](03-review-followup.md) |
| 04 | Калькулятор | Упорядоченные выражения; [аудит 45/45](04-completion-audit.md) |
| 05 | Параметры полей | Свойства, конверсии, исключение; [аудит подплана 49/49](05-completion-audit.md), [после ревью 49/49](../../loginom-dock/field-parameters-review-2026-09-11.md) |
| 06 | Фильтр строк | Условия и оба выхода; [матрица 90/90 и аудит 49/49](06-completion-audit.md), [после ревью 49/49](../../loginom-dock/row-filter-review-live-2026-09-11.md) |
| 07 | Группировка | Ключи и агрегаты; [аудит 49/49](07-completion-audit.md) |
| 08 | Сортировка | Ключи сортировки и цепочки; [аудит 56/56](08-completion-audit.md) |
| 09 | Слияние | INNER/LEFT двух таблиц; [аудит подплана 56/56](09-completion-audit.md), [последующие исправления и аудит 58/58](../../loginom-dock/join-review-live-2026-09-12.md) |
| 10 | Объединение | Добавление строк с сохранением дублей; [аудит подплана 71/71](10-completion-audit.md), [последующие исправления и аудит 84/84](../../loginom-dock/union-review-progress-2026-09-12.md) |

Готовность относится к реализованным режимам и проверенным ревизиям, а не ко всем
возможностям одноимённых компонентов Loginom. Подпланы 01/02 — общая основа
сессии и графа, не ещё два аналитических обработчика.
Сохраняются известные пределы: новый Фильтр на 1000 полей встречает `UI_SCAN_LIMIT`;
широкое Слияние проверено на 40 полях каждого входа, Объединение — до восьми входов.
Предел 15 входов в контракте Объединения не равен живой проверке этого предела.

Ссылка локального `~/.loginom-dock/current` указывает на
`0.1.0-dev.20260910.3-80ca61417ec7`, что совпадает с
[отчётом установки](../../loginom-dock/client-update-union-2026-09-12.md).
В нём зафиксированы установочный снимок 1385 PASS / 1 SKIP и живой аудит 13/13.
В рамках подготовки этого плана тесты и живые прогоны не повторялись.
Общий V4/V5, публичный Release и production-каталоги остаются отдельной работой.

На исходной точке полных зарегистрированных обработчиков **11–16 нет**;
все шесть подпланов были planned. Теперь 11/12/13/14 запущены, но ещё не приняты.
Для 11 и 13 в старом пилоте есть исследования и harness,
но нет принятого обработчика. Слияние и общее исправление возврата к панели
процесса уже завершены позднее: переносить их старые патчи повторно нельзя.

## Распределение узлов

| Поток | Первый узел | Следующий узел после завершения и проверки текущего | Предлагаемая учётная запись |
| --- | --- | --- | --- |
| 1 | [11. Замена](11-replacement.md) | Экспорт текста; затем импорт XLSX и экспорт XLSX | test-2 |
| 2 | [12. Дубликаты и противоречия](12-duplicates.md) | [16. Свёртка столбцов](16-collapse-columns.md) | test-1 |
| 3 | [13. Дата и время](13-date-time.md) | [15. Кросс-таблица](15-cross-table.md) | test-3 |
| 4 | [14. Заполнение пропусков](14-missing-values.md) | Сэмплинг | test-4 |

Первая тройка расширяет нормализацию, контроль качества и календарную аналитику.
Она использует уже готовую основу и может разрабатываться независимо.
Сохранённые исследования 11/13 сокращают первичное знакомство с UI; для 12
первый этап — исследование реального поведения разметки дубликатов и противоречий.

У добавленного в поток4 узла14 нужно подтвердить обучение и пороги пропусков;
у 15 — изменение выходной схемы при появлении и исчезновении категорий;
у 16 — точные значения переменного типа, включая ответ `user-v1`.
Окончание одного потока не требует ждать остальных: после ревью, доработок и
обязательной приёмки координатор автоматически запускает его следующий узел.
Ревью готового кандидата может идти одновременно с разработкой других узлов.

Полные требования остаются в индивидуальных подпланах. В частности, 12 размечает
все строки и не удаляет дубликаты; 11 не подменяет признак `_Replaced` сравнением
значений; 13 проверяет нативные границы календарных периодов. Упрощать эти требования
ради параллельности нельзя.

## Ветки и рабочие каталоги

Ветки и постоянные каталоги **11/12/13 созданы** от общего commit
`a3b419bde8a660e1905284ee62a46362d5a49e09` с согласованным планом.
Узел14 создан в `.worktrees/node-14-missing-values`, ветка
`codex/node-14-missing-values`, база `0f085c5310c67820f6f6386ae16a7df768c152ca`.
Эта база добавляет к общей базе первой тройки только запись запуска в документах.
Имена для15/16 пока предложены; перед созданием проверить отсутствие коллизий.

| Узел | Ветка | Каталог относительно основного checkout |
| --- | --- | --- |
| 11 | codex/node-11-replacement | .worktrees/node-11-replacement |
| 12 | codex/node-12-duplicates | .worktrees/node-12-duplicates |
| 13 | codex/node-13-date-time | .worktrees/node-13-date-time |
| 14 | codex/node-14-missing-values | .worktrees/node-14-missing-values |
| 15 | codex/node-15-cross-table | .worktrees/node-15-cross-table |
| 16 | codex/node-16-collapse-columns | .worktrees/node-16-collapse-columns |

Предпочтительны постоянные worktree: один узел — одна ветка — один каталог.
Обычная задача приложения должна работать именно в этом каталоге. На подготовке
проверить способ регистрации/открытия такого проекта в текущем приложении.
Если используются worktree, созданные приложением, до разработки закрепить
именованную ветку и сохранность каталога при закрытии задачи.

Проверка запуска показала, что архивирование задачи удаляет временный worktree
приложения даже при наличии Git worktree lock. Для рабочих потоков выбран
постоянный каталог `.worktrees/node-*`, открытый как самостоятельный проект через
`codex app <path>`, и задача в его local environment. Временные подготовительные
задачи оставлены в архиве; в них код обработчиков ещё не менялся. Ветки сохранены
и восстановлены в постоянных каталогах. Рабочие чаты не архивировать ради reload.

Первую тройку ответвить от одного принятого main. Каждый последующий узел получает
новую ветку от отдельно записанной принятой базы; автоматически наследовать
необъединённый предыдущий узел нельзя. Для 15 использовать независимый CSV с готовыми
календарными полями, пока 13 не интегрирован; цепочку 13 → 15 проверять отдельно
после объединения. Перенос общей зависимости между ветками — по решению координатора
в рамках запроса пользователя, с записью commit и повторной проверкой затронутой ветки.

В каждой ветке — свои код, тесты и отчёт узла. Разработчики не редактируют общий
реестр потоков в основном checkout. Общие API, registry, readers и UI helpers
менять минимально; самостоятельные общие исправления выделять отдельными коммитами
и отмечать в отчёте для последующего объединения.

## Подготовка перед первым запуском

Подготовку выполнить после команды пользователя на подготовку или запуск потока.
Это необходимая часть запуска, а не отдельная автоматическая задача.

1. Проверить действующие задачи, worktree и владельцев браузеров/аккаунтов.
   Старые каталоги `.worktrees/parallel-*` сохранять. Из них переносить только
   проверенные полезные исследования 11/13; основу брать из актуального main.
2. Закрепить окружение каждого разрешённого потока со своими config, state, очередью архива,
   артефактами, source MCP и browser profile в Dock-owned путях. Для нового узла
   создавать новое состояние; сохранять окружение завершённого кандидата для ревью.
   Секреты и сырые журналы в Git не включать.
3. Для текущего source-запуска проверить MCP каждой задачи в окружении нужного
   узла. Полная привязка native hooks/архива остаётся отдельным незакрытым условием
   native-интеграции и не блокирует source-разработку с неактивированным захватом.
   `plugins/loginom-dock/scripts/launch.sh` выбирает
   `LOGINOM_DOCK_HOME` или глобальный `~/.loginom-dock`; один worktree это не меняет.
   Нужна проверенная привязка к исходникам конкретной ветки и её stateDir.
   Возможность одновременно направить MCP и hooks через настройки отдельной задачи
   ещё не подтверждена. Если потребуется адаптер, оформить его отдельной общей
   подготовкой; не менять глобальный плагин посреди работы потоков.
4. Прочитать фактические revision/hash исходников, runtime, skill, adapter и
   manifest знаний для каждой сессии. Старый staged-каталог пилота не считать
   подходящим новой базе без проверки. Нужные новые карточки готовить в отдельном
   неизменяемом candidate-каталоге; production не переключать. Сборка поставляемых
   артефактов выполняется на VPS по проектному контракту.
5. На живом стенде подтвердить версию, вход и личное хранилище. `/test-1`,
   `/test-2`, `/test-3` были проверены в пилоте; их текущая доступность и отсутствие
   другого активного владельца требуют повторной проверки. Использовать отдельный
   `.lgp` для каждого узла, а также отдельные пакеты диагностики и приёмки.
6. Проверить изоляцию каждой добавляемой сессии: раздельные session IDs, профили,
   файлы состояния, точные workflow refs и сохранение только собственного пакета.
   Завершение одной сессии не должно закрывать остальные. Проверить общий clipboard
   lock. Для текущего source/UI-запуска подтвердить собственные профили и состояние,
   отсутствие активации захвата этих разговоров и явно записать ограничение native
   hooks. Проверка всех hook-запусков и их записей остаётся отдельным условием полной
   native-интеграции; успешный source-допуск не закрывает его.

У четырёх текущих worktree память переключена на общий проектный Peer для
чтения и записи: зарегистрированный actor health/find/read проверен у каждого.
Проектный capture вызывает официальные scripts с отдельными task/session ID и
курсорами; глобальные credentials/Peer не менялись. Новому worktree передавать
актуальные [правила памяти](../../loginom-dock/shared-project-memory.md) и AGENTS.md.
Прежний own-worktree capture и доступ к общему root через all — исторический режим.
Для будущих задач установлен механизм независимой регистрации `20260913.5`.
Перед разработкой каждой задачи пройти bootstrap/actor gate по
[единой инструкции](node-workflow-runbook.md); текущий v1 route list не расширять.

### Проверенный способ запуска на этом Mac

В каждой постоянной рабочей копии **до создания чата** записана приватная
`.codex/config.toml`. Обычный `mcp_servers.loginom-dock` запускает собственный
`client/bin/loginom-dock.mjs` на Node 24.19.0 с явными config, stateDir, agent,
adapter revision и режимом classic для прямой диагностики Codex.
State и браузерные профили находятся внутри `.dock/stream-runtime` своего worktree;
бинарники браузера и зависимости используются только для чтения.

Настройка `plugins."loginom-dock@loginom-dock".enabled=false` применяется только
к рабочей копии; глобальный установленный Dock-плагин не перенастроен.
OpenViking теперь имеет проверенные проектные overrides и hooks общей памяти;
его глобальная конфигурация не менялась.
`config/read` подтверждает локальный слой, source MCP и это значение флага.
Практический probe показал: обычное продолжение уже загруженной задачи не
перечитывает настройку; после повторной загрузки shell получил свой Dock HOME,
но перенаправление штатного plugin hook в него не подтвердилось.

**Ограничение source-разработки:** нативный архив Dock не принят. Для всех трёх
рабочих task ID в глобальной таблице activations нет записей; в source sessions
`archiveActive=false` и отсутствует `archive.json`. Захват этих разговоров не активирован.
Это не доказательство полной изоляции native hooks: отдельный `hooks/list`
перечисляет определения, а исполнение отражают события `hook/started` и
`hook/completed` с threadId. Отсутствие всех фактических запусков пока не доказано.
Не объявлять native-интеграцию или архив готовыми по флагу config. Проверки кода
и UI сохраняются локально; персональная память OpenViking используется в actor scope.
Это отдельный незакрытый пункт подготовки native-интеграции, не приёмка нового узла.

У source-сессий трёх рабочих копий подтверждены раздельные UUID и profile paths,
Node 24.19.0 и единый начальный source hash
`c475d504d9d81f54807c6a3b272351b6fbbe0959d434465e384dc3e0caca190a`.
В живом UI все три разработчика подтвердили Loginom 7.4.2, назначенный аккаунт,
свой каталог и отдельный пакет. Фактическая геометрия каждого окна — 1508×949,
внутренняя область 1508×862 при доступной 1512×949, `viewport:null`.
Полное сохранение/переоткрытие сценариев остаётся проверкой реализации узлов.
Существующие проверки clipboard прошли **5/5**, включая сериализацию отдельных
процессов и удержание блокировки при неподтверждённой вставке.

После изменения JS текущий MCP-процесс не считается обновлённым: новый код
проверяется свежим source harness/bridge из той же ветки с новыми pins и собственным
профилем. Для продолжения разработки не переустанавливать глобальный плагин и
не выполнять общий reload всех MCP приложения.

## Браузеры и общие ресурсы Mac

- Каждый поток управляет только своим экземпляром браузера и своими страницами
  через Dock/Playwright. Разные вкладки одного профиля не изолируют вход Loginom.
  Запуск видимого окна: `--start-maximized`, `viewport: null`, с проверкой геометрии.
- У каждого узла собственный `.lgp`, даже при работе в разных workflow:
  разные workflow одного пакета сохраняют общий риск перезаписи пакета.
  Файлы диагностики и приёмки имеют уникальные имена внутри подтверждённого хранилища.
- Clipboard остаётся общим для Mac. Все потоки используют существующий
  `dock_clipboard_transfer` и **один порт блокировки 46419** до подтверждённой вставки.
  Таймаут не разрешает обход блокировки. Неопределённый результат сначала разбирает
  владелец операции. Прямые системные copy/paste и глобальные клавиатурные жесты
  нельзя выполнять параллельно через Computer Use.
- Проверяющий получает пакет после сохранения и передачи владельцем. Во время
  живого ревью одного узла разработчик не меняет этот пакет и candidate commit.
  Использовать отдельный профиль ревью; при невозможности второго входа передавать
  аккаунт последовательно, сохранив диагностическое состояние.
- Финальные Hermes-прогоны первоначально выполняются **по одному на Mac** с
  отдельным состоянием Hermes и существующей подпиской. Обычная разработка и
  независимая диагностика других потоков могут продолжаться. При перегрузке Mac
  или стенда координатор ограничивает число живых прогонов, сохраняя очереди потоков.
- Общий установленный клиент, global current/previous, плагины, настройки и gateway
  не обновлять во время активных потоков. Общие перезапуски и `pkill` не применять.

## Управление через исходный чат

Актуальное решение пользователя от 13 сентября:
[автоматический цикл узла](automatic-node-workflow.md). Разработка — Astra medium;
после её завершения одно ревью — Astra medium **в той же задаче разработчика**.
Один раунд доработок — там же на medium; повторное ревью после него не запускается.
Координатор сам назначает переходы, не спрашивая повторного разрешения.

После закрытия замечаний обязательная автономная приёмка выполняется через
Hermes `openai-codex/gpt-5.6-sol/low` в единственном слоте координатора.
Успешный полный независимый аудит и подтверждение освобождения ресурсов
позволяют запустить следующий доступный узел в новом чате/worktree/ветке.
Слияние — только по отдельной команде пользователя сюда; поставка не включена.

Периодический опрос отключён; heartbeat `loginom-dock` затем удалён пользователем.
Отсутствие его конфигурации проверено при переходе узла11 к ревью. Разработчики сами отправляют один итог при завершении этапа и
внеочередно только блокеры, требующие вмешательства. Координатор по событию
проверяет завершение turn и назначает следующий этап; повторные назначения
защищены реестром task/turn ID, этапов и candidate SHA. Пользователь может запросить статус, уточнить задание,
приостановить поток или изменить очередь. Активный этап не прерывается ради
преждевременного ревью. В подготовке следующего узла сохраняются все правила
изоляции, отдельного проекта приложения и принятой базы main.

Команда доработки: «Перепроверь найденные дефекты на живом Loginom и после
подтверждения исправь то, что реально недоработано». Для каждого замечания
нужны исправление или доказательство снятия. Ревью анализирует полный diff
и evidence, не меняет исходники и тесты. Изменённый исполняемый код требует
проверок исправлений и аудита исправленного кандидата, но не повторного ревью. Прежние ручные ограничения
на ревью/доработки/следующий узел ниже в исторических записях отменены.

Передача аккаунта, пакета или следующей задачи выполняется после подтверждённого
освобождения ресурса. Отправленное сообщение «остановись» само по себе не доказывает
остановку агента, браузера или выполняющегося узла Loginom.

## Готовность кандидата и реестр

Этапы: **planned → подготовка → разработка → ревью → доработка при необходимости
→ проверки исправлений без повторного ревью → автономная приёмка → принят в ветке → следующий узел**.
Слияние, публикация и установка учитываются отдельно.

До перехода к следующему узлу нужны реализованный объём подплана, прямые
проверки реального Loginom, актуальный commit, отчёт по замечаниям, финальные pins
и полный независимый аудит автономной приёмки. Проверить сохранение, отдельное
переоткрытие и повторное выполнение пакета. Итог Hermes, exit 0 и ручной успех
сами по себе не являются приёмкой. Подробные условия и шаблоны запросов находятся
в [автоматическом workflow](automatic-node-workflow.md).

Координатор назначает свободный слот Hermes после ревью, проверяя реальный процесс
предыдущего владельца. При занятости сохраняет очередь, при неуспехе возвращает
узел на диагностику. Повторный аудит неизменного успешно проверенного кандидата
не нужен при доказанном совпадении полного объёма и pins. Текущий владелец слота
и этапы записываются в `.dock/node-streams-20260912/state.json`.

Реестр ниже меняет координатор. При запуске добавить к строке: branch/worktree,
base/candidate commit, task ID и название, учётную запись/пакет, окружение и pins,
последние проверки, замечания и следующее действие. Приватные настройки остаются
в локальном состоянии Dock; в документах только безопасные ссылки и сводки.

| Поток | Узел | Состояние | Задача / ветка / окружение | Следующее действие |
| --- | --- | --- | --- | --- |
| 1 | 11. Замена | Единственная доработка N11-R1 завершена; не принят | `01a0971e-804e-7f42-923a-4cdb1de181be`; `codex/node-11-replacement`; `.worktrees/node-11-replacement`; test-2 | Проверить candidate и выделить слот Hermes; повторного ревью нет |
| 2 | 12. Дубликаты и противоречия | Единственная доработка N12-R1 завершена; не принят | `01a0971e-9324-7cd1-8610-cf343b3838b1`; `codex/node-12-duplicates`; `.worktrees/node-12-duplicates`; test-1 | Проверить candidate и выделить слот Hermes; повторного ревью нет |
| 3 | 13. Дата и время | Разработка: source/UI-окружение проверено | `01a0971e-a2c3-7e21-898b-9b865527a484`; `codex/node-13-date-time`; `.worktrees/node-13-date-time`; test-3 | Закончить прямые проверки, сохранить checkpoint перед Hermes |
| 4 | 14. Заполнение пропусков | Разработка: source/UI-окружение проверено | `01a09738-2d64-7dc2-8cb3-634663d0b406`; `codex/node-14-missing-values`; `.worktrees/node-14-missing-values`; test-4 | Реализовать подплан и прямые проверки до checkpoint перед Hermes |

Созданные диагностические пакеты:

- 11: `/test-2/Node11-Diagnostic-20260912-f451c7ae.lgp`.
- 12: `/test-1/Node12-diagnostics-20260912-8d403b79.lgp`.
- 13: `/test-3/Node13-20260912-diagnostic.lgp`.
- 14: `/test-4/Node14-20260912-diagnostic.lgp`.

Локальный реестр координатора: `.dock/node-streams-20260912/state.json`.
Каждый разработчик сохраняет собственный `.dock/stream-preflight.json` с реальными
результатами входа, хранилища, окна и подготовки skill. ID рабочих чатов выше
заменяют ID предварительных задач в удалённых временных worktree.

Принятые ветки сохраняются до отдельного запроса на объединение. При интеграции
потребуется согласовать изменения общих API/readers и проверить совместную версию:
три успешных независимых аудита не доказывают корректность объединённого кода.
Push в main, VPS-сборка, обновление плагина и публичная поставка не входят в
автоматическое завершение потока.
