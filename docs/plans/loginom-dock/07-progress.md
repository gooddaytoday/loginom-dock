# Подплан 07: ход реализации

9 сентября 2026. Итог: **implemented / live_verified**, Hermes49/49 PASS.
[Аудит требований и границы](07-completion-audit.md). Ниже — хронология работы;
промежуточные статусы не заменяют итог в конце документа.

Подготовка: личный OpenViking healthy; прочитаны план 07, общие условия,
канонический цикл узла и текущие принятые результаты 03/04. Существовавшие
незакоммиченные изменения других задач сохраняются. Production не менялся.

Живая диагностика: Loginom 7.4.2, `logi-test-plan.bg.local`, выбранный оператором
account `user`, отдельная копия `/user/dock-p3/packages/Dock-grouping-diagnostic-20260909.lgp`.
Окно развёрнуто; viewport null, фактическая рабочая область 2044×1035.
Сессия `.dock/calculator-v3/live-1788967321873`, данные диагностики и исходники
в `.dock/grouping-v3/`. Первый запуск копирования сохранил копию, но дальнейший
переход не был готов; повторное открытие точной копии подтвердило доступ на запись.

Подтверждённые особенности UI:

- Тип в реестре — `transform.group_data`, режим `aggregate`.
- Available/used grids — ChainedStore с общим локальным Store. Disposition 0/6/7
  означает свободное поле/ключ/показатель; Order задаёт порядок внутри списка.
- При удалении последнего ключа Loginom возвращает поле с Order=-1 и добавляет
  служебную пустую запись в общий store. Она не является входным полем.
- Строки summary дублируют data-tid обычного поля; выбирать их нельзя.
- FactorEditDialog привязан к выделенной записи показателя. В текущем UI 14 функций,
  включая «Мода». Старый helper не учитывает её в нумерации после «Медиана».
- Подтверждены маски первых пяти функций: sum/count/min/max/avg = 1/2/4/8/16.
- Next ведёт на страницу описания; выход настраивается отдельным мастером
  DerivedDataSourceOutputSocketWizard после промежуточного Done.
- При первом завершении нового узла source mapping records ещё пусты: target fields
  являются вычисленными полями. После выполнения и повторного открытия источник
  уже представлен native source records и точными двусторонними связями. При нескольких функциях имена вида Revenue_Sum,
  Revenue_Count; метки «Сумма|Сумма», «Сумма|Количество».
- Наблюдённые штатные настройки: кэшировать=true, сортировать=false. Они сохраняются.

Добавлены черновые grouping-parameters/context/procedure/output. Полное чтение
конфигурации проверено на живом UI. Настройка существующего узла прошла:
ключ Note заменён на Qty; Revenue получил только avg/count; Adjusted — только
sum/max. Одинаковые метки Revenue/Adjusted не смешаны, лишние функции сняты.
Квитанция `procedure3.json` в указанной сессии; это проверка внутренней настройки,
не приёмка полного node.apply. Шесть проверок валидатора прошли.

Остаются: интеграция обработчика и контрактов, ранняя проверка входной схемы,
выходные имена/порядок/выбор полей, полный цикл и чтение результата, независимые
верификаторы и матрица пограничных случаев, сохранение/reopen, затем замороженная
goal-only Hermes-приёмка openai-codex / gpt-5.6-sol / low. Никакие ещё не
подтверждённые режимы не объявлены готовыми.

Дополнение текущей итерации:

- Через настоящий MCP wire прошёл полный `group-full-1788968548753`:
  новый узел → input mapping → grouping → output names/order → Execute → Table.
  Пять строк и пять полей прочитаны полностью, точные числа сохранены; это пока
  Codex-диагностика, не независимая автономная приёмка.
- Source pins получены из manifest: E2E `2cad5602158fd2e4836d821d644a2b8d92f571a2`,
  Help `353e506ba04b77a2926d8ddf8472b36c684b67fd`; шесть file SHA в
  `.dock/grouping-v3/sources/pins.json`.
- 325 выбранных client tests и отдельно 80 lifecycle tests прошли. Добавлены
  10 grouping parameter/context проверок; они прошли.
- Ранняя проверка нового узла использует временный Quick Preview точного source
  output и cached FColumnInfosStore. `group-invalid-1788969042095` подтвердил
  NOT_APPLIED/cleanup, node=null, effect_possible=false при MissingField.
  Предыдущие диагностические отказы сохранены: F3 отсутствовала в allowlist,
  close control не обнаруживался, неизменённая workflow-фаза сохраняла
  предположение об эффекте. Исправления проверены последующим живым отказом.
- Попытка исправленного запроса выявила коллизию n1 между preflight и основным
  node channel после уже подтверждённого создания узла. Добавлен общий счётчик
  внутренних шагов на operation; созданный узел оставлен для диагностики,
  неизвестные действия не повторялись. `full-apply3` подтвердил исправление: SUCCEEDED, 5×5, новое execution identity и checkpoint.

Открытые вопросы остаются прежними, особенно изменение уже переименованных
вычисленных выходов, доступность исключения полей native output, полная QA-матрица
и независимый аудит. Завершённость 07 не объявлена.

Последующая диагностика:

- Полный existing apply `group-full-1788970091074` — SUCCEEDED: сохранён тот же
  node ID, изменены имена/метки четырёх выходов, подтверждено новое выполнение.
- Входные назначения Группировки имеют UsageType 6/7, соответствующие группе и
  показателю; общий native mapping reader расширен этими проверенными значениями.
- При изменении ключей/функций Next может открыть
  DerivedDataSourceMappingEngineOutputPortWizard. В нём сохраняются прежние
  переименованные выходы, у удалённых функций source=null. Новые источники
  представлены полным native inventory.
- Подтверждено удаление строго выбранного obsolete output через Delete. Оно
  выключает autosync. Восстановление autosync пересоздаёт UI record IDs, сохраняя
  постоянные field IDs и полную семантику остальных полей. Добавлен ограниченный
  обработчик conditional mapping с проверкой удаления/синхронизации/восстановления.
- Ext CollectionProxy сохраняет totalCount предыдущей загрузки после удаления;
  native reader проверяет полный локальный source collection, тип proxy, отсутствие
  pending/remote paging и точный before/after контроль в обработчике.
- Прошли 308 выбранных client tests после ранней проверки/общего счётчика;
  затем 35 grouping/native-mapping tests. После последних правок требуется повтор.
- Текущая проверка `existing-prune` испытывает удаление старых ключей/агрегатов
  в полном внешнем node.apply. Скрипты данных Null/empty подготовлены, но ещё
  не выполнены. Независимого verifier и Hermes acceptance пока нет.

## 2026-09-09: точные данные и независимый аудит

- `group-full-1788971123153` подтвердил изменение существующего узла: два ключа
  Qty/Note, замена max на min и удаление устаревших выходов; полный выход 6×6.
- Получение источников через штатные «Связи» → «Получить список» → «Таблица»
  заполняет native source→target links до выполнения. Имена выходов задаются
  общим редактором порта, без сопоставления только по меткам/позиции.
  У одиночной функции источник иногда имеет суффикс (`Text_Count`, `Other_Sum`,
  `Amount_Avg`), тогда как исходный target имеет простое имя. Resolver проверяет
  допустимые точные имена, тип/метку и взаимно однозначное native соответствие.
- Fixture `tools/loginom-acceptance/fixtures/grouping/grouping.csv`: 150 bytes,
  SHA-256 `579c1df3f406bc4b6839ea9d587736d120092128404ba68f2619b2e9a720e27c`.
  Восемь строк с Null, пустой строкой, отрицательными числами и дробной точностью.
- `group-null-1788972776859`: SUCCEEDED, 3×7, execution
  `1788967323811-uvzrjx1pkj:345:14`. Отдельный Python oracle и аудит raw native
  конфигурации/табличных страниц дали PASS. В группе из одних пропусков sum,
  avg, min, max возвращают Null, count — число строк (2). Источник сверялся с
  локальными байтами отдельно; этот аудитор сам не доказывает upload/persistence.
- `group-exclusion-1788972821805`: SUCCEEDED, 4×3, два ключа, сумма integer Other,
  среднее Amount с одинаковыми метками «Сумма», исключение Segment и autosync=false.
  Независимые configuration/output audits — PASS.
- Проверка выявила два дефекта общей инфраструктуры. Попарный поиск вложенных
  ячеек превышал work budget при длинной истории процессов; заменён обходом
  родителей без изменения лимитов. После отдельного preflight-channel квитанция
  epoch-refresh использовала локальный счётчик; исправлена на общий operation
  counter. Новые целевые тесты и следующий независимый живой аудит прошли.
- Пустой CSV (только заголовок) доставлен и импортирован через полный public
  node.apply: `group-empty-import-1788972985089`, SUCCEEDED, 0×5. Группировка
  настроена и реально запущена, но полный аудит пока не принят: сначала строка
  процесса была вне видимой области, затем Loginom вытеснил старейшую группу при
  21-м запуске. Guard «previous history disappeared» честно остановил подтверждение.
  Добавлен bounded reveal точной native записи; guard исчезновения истории сохранён.
- Диагностический пакет сохранён явным `package.save_checkpoint` в
  `/user/dock-p3/packages/Dock-grouping-diagnostic-20260909.lgp`. Завершение сохранения
  подтверждено; сохранность настроек ещё требует отдельного reopen-аудита.
- 69 grouping/procedure тестов и 18 независимых sequence-аудитор tests прошли.
  После последних изменений process reveal и acceptance harness требуется повтор
  соответствующих проверок; итоговый suite PASS пока не заявлен.

Продолжение: пустой выход после нового открытия, Done/Close/Factor-cancel,
потерянный ответ применения, сохранность настроек и выходов после reopen,
тестирование нового grouping goal/auditor, затем замороженный goal-only Hermes
`openai-codex / gpt-5.6-sol / low`. Подплан остаётся in_progress.

## 2026-09-09: завершение Codex QA перед Hermes

- `group-empty-full-1788973846231`: полный запрос на пустом активном источнике,
  SUCCEEDED, 0×7, raw configuration/output audits PASS. Persisted графовый порт
  может не заполнять FPortIndex; preflight связывает единственный табличный выход
  по полной native коллекции, а не подставляет индекс при неопределённой схеме.
- `group-exclusion-reopened-1788973697397`: новый запуск сохранённого узла после
  открытия копии пакета, 4×3, оба независимых аудита PASS; readback настроек до
  сохранения и после открытия совпал (кроме новых node/receipt refs).
- `group-close-qa-1788973896904`: изменённый draft ключей/показателей отменён,
  configuration=discarded, execution=not_requested. Следующий
  `group-done-qa-1788973939508` сохранил прежние Group / Amount31 / Text2,
  execution=not_requested, output=not_refreshed. Factor Cancel
  `group-factor-cancel-1788974047351` сохранил полный native input inventory/options.
- `group-lost-factor-1788974089038`: после одного реального Factor Apply ответ
  намеренно потерян в QA transport. Результат и повтор того же operation ID —
  AMBIGUOUS, configure pending; Apply gesture=1. Codex отдельно прочитал
  применённый mask8 у Other и отменил весь draft. Это fault QA, не Hermes run.
- 1115 client tests PASS, 1 SKIP; 407 Python tests PASS. Source preflight нового
  grouping goal прошёл: Hermes0.21.0 / openai-codex / gpt-5.6-sol / low,
  model_started=false. Перед реальным запуском pins будут пересчитаны.
- Ограничения совпадают с общим mapping handler: входные исключения и восстановление
  исключённых ключей не поддержаны; из grouping output исключается только optional
  key, не агрегат. Имена полей ограничены ASCII identifier до128 символов, как у
  текущего calculator handler. Для перенастройки aliases нужен явный полный input
  mapping; повтор без изменения использует parameters={}.
- При autosync=true Loginom после открытия пакета может поместить автоматически
  сохраняемый сквозной ключ после переименованных агрегатов. Handler возвращает
  фактически наблюдённый порядок; проверенная сохранность явного порядка вместе с
  исключением ключа относится к autosync=false. Это поведение нельзя выдавать за
  доказанную сохранность auto-порядка. Пропуски и пустая схема проверены отдельно.

Источники закреплены в [07-source-pins](07-source-pins.md). Подготовлены
`grouping-node-complete` goal и отдельный полный аудитор: импорт8строк, новая
группировка3×7, изменение того же узла в4×4, сохранение/reopen и два новых запуска.
До независимого PASS Hermes статус остаётся in_progress.


## Исправление независимого аудита после первого Hermes

Run `20260909-201828-1d8068f0` завершил все пять операций и сохранение/reopen,
но исходный аудит оставлен FAIL: запрещал явные identity mappings и падал на
пустом числовом поле в общем import oracle. Это дефекты verifier, не доказательство
общего PASS. Исходный отчёт сохранён как `grouping-node-audit-original.json`.
Диагностический повтор исправленного аудита дал 48 содержательных PASS/49;
`frozen_harness=false` ожидаем и не обходится. Новый Hermes требуется после фиксации.

Codex заново открыл отдельную копию пакета, выполнил импорт и проверил native
Preview: восемь строк, Amount пустой→Null, Text quoted empty→пустая строка,
Text null marker→Null. Каталог `.dock/calculator-v3/live-1788974985085`,
`audit-preview-cache4.json`. Диагностический браузер закрыт перед новым Hermes.
Verifier принимает только полные identity mappings с точными порядком/именами/
метками, без исключений; отрицательные тесты сохранены. Добавлен regression
пустого числового поля против ложного нуля. Client1115 PASS/1 SKIP, Python409 PASS;
`git diff --check` чист. Runtime изменён только удалением trailing whitespace
после первого прогона; новый запуск закрепит актуальные исходники и harness.


## Второй автономный прогон: URI справки

`20260909-203337-3729b766`: 48/49 PASS, общий FAIL по knowledge_scope. Все пять
node.apply, точные данные, настройки, оба save и reopen прошли. Один read
использовал `%D0…` вместо Unicode в имени E2E Markdown; сервер ответил File not
found, область аудита отвергла URI с `%`. Остальные прочитанные E2E/Help были
в разрешённых корнях. Корни/правила аудита не расширены. В native skill явно
закреплены Unicode URI и scoped-поиск канонического пути без догадок; это
исправляет наблюдённую причину перед новым запуском. Исходный FAIL сохранён.
Live-логика Группировки и независимые verifier после этого не изменялись.


## Итог: подплан07 завершён

`20260909-204416-901e8de6`: **49/49 independent PASS**, Sol/low без fallback.
Повтор аудита из 256 побайтно закреплённых исходников совпал с исходным отчётом.
Требования и точные SHA приведены в [07-completion-audit](07-completion-audit.md).
После запуска изменялась только документация. Полные client1115 PASS/1 SKIP,
Python409 PASS; source runtime принят, production/client release не выполнялись.
Работ по объявленной цели07 не осталось; цепочка продаж требует отдельного подплана08.
