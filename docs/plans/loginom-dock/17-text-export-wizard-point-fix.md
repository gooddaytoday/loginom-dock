# Node17: видимая точка открытия настроек

Full7 `20260913-232417-e60ceb43` завершился самостоятельно: exit0, timeout=false, собственные model/launcher/browser PID отсутствуют. Слот `node17-hermes-20260913-ac814f07` освобождён. Независимый аудит сохранил **FAIL user_node_error**: 9 успешных nodeops,10-я — смена CSV на TSV — остановилась в `open` с AMBIGUOUS. Step21 `begin_wizard` был NOT_APPLIED/UI_REFERENCE_OBSCURED до нажатия; выполнение TSV, observer replace и save/reopen не были достигнуты. Оригинал evidence и FAIL сохранены без переоценки; свежий аудит сохранённого model package неприменим, сохранение не выполнялось.

Причина подтверждена в source и реальном Loginom7.4.2: у `ExportCSV;Setting` bbox `(1114,86,24,24)` наблюдение проверило видимую точку `(1126,104)`, но исполнитель заново выбрал центр `(1126,98)`, закрытый верхней панелью графа. Неправильный жест был безопасно отклонён, однако операция не смогла открыть мастер.

Исправление ограничено нативными controls с `wizard_open`: используются актуальная наблюдаемая точка и прежняя независимая проверка попадания `elementFromPoint`. Проверки identity/signature, геометрии, доступности и принадлежности мастера сохранены. Никакого force-click, повторения непроверенного жеста или ослабления обработки AMBIGUOUS нет.

В отдельной копии `/test-2/node17-wizard-clipping-run7.lgp` положения узлов и прокрутка были изменены только для воспроизведения точной геометрии. На одном и том же control старый код получил NOT_APPLIED/UI_REFERENCE_OBSCURED; новый код после одного проверенного нажатия открыл мастер именно ExportCSV и подтвердил native node/workflow owner. Параметры не применялись, выполнение не запускалось. Затем мастер штатно закрыт с подтверждением, копия сохранена, пакет закрыт и выполнен выход test-2; браузер закрыт. Модельные пакеты и файлы не изменялись.

Регрессионный тест с явным viewport и перекрытым центром падает на старом коде и проходит на новом. Полное перекрытие кнопки по-прежнему даёт отказ без нажатия. **341 JavaScript /24 Python PASS**, включая261 тест UI-механизма. Новая полная ревизия runtime требует актуального component/preflight; их итог и точная команда следующего запуска находятся в [машинном отчёте](17-text-export-wizard-point-fix.json).

Полная цель22nodeops/3deliveries/save-reopen/fresh original audit остаётся неизменной и непринятой. Следующий Hermes — только по новому назначению координатора. Повторного полного review, merge, deploy или изменения общего клиента не было.

Текущий component `20260913-235110-fb8639df` и независимый user-v1 аудит — **PASS**:1 download/1 dispatch/1 successful replace,124 байта; PID=[], global config unchanged. Runtime `c9972b58cfcb36fdedad6fa495424259243c4e9204d4e28909d33653810c7c3d`, admission `1b1f55b42bccba1439ae12a0c230922cb0b0f14fa958520c083abca7ba5f8cfd`. Кандидат готов к новому полному Hermes по отдельному назначению слота; прошлый run7 остаётся FAIL.
