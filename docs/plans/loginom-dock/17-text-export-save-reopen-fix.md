# Node17: ожидание Save As и виртуальный список файлов

Кандидат основан на `5226aab7`. Полная цель пока **не принята**. Координатор разрешил адресную диагностику и исправления, без повторного полного ревью; новый Hermes требует отдельного назначения слота.

## Сохранение и повторное открытие

В full6 `20260913-215018-6e242f0e` все 22 смысловых node cases были выполнены, но первая попытка размещения Empty была отклонена до эффекта и добавила 23-й запрос. Исходный аудит сохранил FAIL `user_node_error`. В этом же прогоне `package.save_as` вернул AMBIGUOUS: `Selector packages.close cardinality 0, expected one`. Последующие операции модели не доказывают реальное повторное открытие.

Проверка исходника выявила асимметрию: ожидание окончания native Save As было только в ветке сохранения без открытия. Теперь оба режима ждут завершения Save As, затем режим открытия отдельно ждёт появления команды «Закрыть». Детерминированный тест задержанного меню падал до изменения и прошёл после; добавлен также случай задержанного появления Close. Это согласуется с ранее подтверждённым исправлением node12 `0e11a3fb24ca40a9f855008d2b0d1856d6f771ad`. Точную длительность исторической гонки full6 установить нельзя.

Живой диагностический Save As на `/test-2/node17-save-reopen-fixed-full6.lgp` прошёл: сохранение → закрытие → открытие, совпали путь, 8 узлов и 5 связей. Это копия для проверки исправления, а не независимый аудит исходного model package.

Исходный пакет full6 при свежем открытии был только для чтения. Локальные процессы завершены, но это не доказывает снятие серверной блокировки. Администрирование скрыто для test-2; обхода прав и удаления сессий не было. По указанию координатора дальнейшие попытки разблокировки прекращены, original fresh audit остаётся incomplete.

## Строка, ещё отсутствующая в DOM

Первый компонентный прогон после SaveAs-правки `20260913-222625-513785bc` остановился с AMBIGUOUS `Export storage did not settle` после completed export. Отказ и исходные доказательства сохранены. Новая диагностика показала 138 записей в native store, в том числе точный CSV размером124 байта, однако виртуальный список отрисовал лишь начальную часть. Точечное DOM-наблюдение имени вернуло0 строк. Это другой случай, чем ранее исправленная строка вне видимой области, уже существующая в DOM.

Штатного поиска в наблюдаемой панели файлов нет. Добавлен проход виртуального списка только через существующую `workspace.ui` прокрутку: максимум12 жестов по1000px, проверки папки/workflow, владельца прокрутки, продвижения, конца, deadline/cancel. Уже отрисованная строка не требует прокрутки; точное имя проверяется в адресном наблюдении после появления строки. Экспорт не повторяется. Реальная ранее отсутствовавшая строка найдена, размер124 подтверждён. Все исторические файлы сохранены.

Клиентские проверки: **66/66**, включая38 executor и9 новых случаев виртуального списка. Readiness: **4/4**. Текущий интегрированный component и финальные pins приведены в машинном отчёте; успешный helper сам по себе не закрывает компонентную или полную автономную приёмку.

[Машинный отчёт](17-text-export-save-reopen-fix.json). Новый полный прогон обязан использовать собственный уникальный пакет, штатное save/reopen и независимый аудит именно его оригинала. Merge, deploy, общий клиент и Hermes в этой фазе не запускались.

Повторный component `20260913-223512-4afee206` обнаружил связанный дефект восстановления: после NOT_APPLIED/UI_EPOCH_CHANGED scroll повторялся с addressed root строки, которая исчезла после реального scroll0→1000. Результат AMBIGUOUS/UI_ROOT_STALE сохранён. Scroll-refresh теперь сохраняет стабильный root таблицы; подтверждение текущим component остаётся обязательным.

## Текущий проверенный кандидат

Component `20260913-224754-cbf8ef54` завершился **PASS**, независимый аудит также **PASS**. В насыщенной папке выполнена реальная search-прокрутка1000px, затем ровно1 контрольное скачивание,1 dispatch и1 успешная замена; результат124 байта совпал с эталоном. Процессы собственной сессии завершены, global config не изменён. Текущие проверки:80 JavaScript и24 Python PASS; новые политики отдельно отклонили16 подмен. Это компонентная готовность к полному Hermes, не приёмка всей цели.

В предшествующем component `20260913-224418-246a09ef` поиск и observer download/dispatch прошли, но чтение после completed replace вернуло общий отказ наблюдения. Добавлена передача точного native error code/message. В последующем прогоне отказ не повторился; его историческая причина остаётся неизвестной и не объявляется исправленной. Исходные материалы сохранены.

Текущий runtime `cd471237bdf5bc67bc668a02b3c5d1ad118b3e9fcfc74a235f10449ac5c1e01f`, admission `ac814f07b0e670c25a8afdba89aadfe1a934f2bd94cef58188a818c54eaf5728`. Полная цель неизменна:22 смысловых операции,3 доставки, все save/reopen и свежий независимый аудит оригинала нового прогона. Точная команда находится в JSON; запуск разрешает только новое назначение единственного Hermes-слота координатором.
