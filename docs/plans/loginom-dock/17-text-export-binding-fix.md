# Node17: исправлено сравнение идентификаторов наблюдателем

13 сентября 2026. База исправления `b6e68170`. Полная автономная приёмка
**не пройдена**; кандидат готов к новому полному прогону после выделения слота.

Третий Hermes `20260913-195328-a78a7b64` выполнил 11 операций узлов:
10 SUCCEEDED и ожидаемый conflict-reject FAILED с подтверждённой очисткой.
Перед явной заменой наблюдатель ошибочно вернул `Node/workflow owner changed`.
В запросе ключи ссылки шли document/node/workflow, а в журнале —
document/workflow/node. Значения и набор ключей совпадали; сравнение
`JSON.stringify` ошибочно зависело от порядка. Отказ воспроизведён непосредственно
на сохранённом запросе и журнале. Нативное чтение наблюдателя и замена не начались.
Последующие обращения получили `OBSERVER_GATE_BLOCKED`; полный аудитор отверг
прогон. Модель завершилась сама exit 0, timeout false, до попытки операторской
остановки. Собственные PID отсутствуют, слот передан координатору.

В `text-export-observer-binding.mjs` сравнение заменено на строгое структурное.
Подмена значений, лишние ключи, другой документ/сценарий/узел по-прежнему
отклоняются. Product runtime не изменён. Весь текущий инструментарий закреплён
контрольными суммами; исторические доказательства нативного чтения сохраняют
свой исходный binder. Новый binder отдельно проверен реальными действиями.

Проверки:

- Тот же реальный запрос и журнал: до исправления отказ, после — корректная
  привязка без изменения идентификаторов.
- 27 Node-тестов и 21 Python-тест PASS; проверены перестановка ключей,
  подмена/добавление идентификаторов и прежние отрицательные случаи наблюдателя.
- Свежий компонентный прогон `20260913-170842-59abc360` через реальный stdio
  user-v1, Loginom 7.4.2, test-2, развёрнутое видимое окно: исходный CSV,
  чистый reject, запрос replace с переставленными ключами, одно нативное
  скачивание, одно фактическое выполнение replace, точные 124 байта.
  Независимый аудит PASS и 50/50 отрицательных подмен отклонены.
- Интервал наблюдателя 1,785 с, вместе с заменой 17,75 с. Процессы закрыты,
  общий config не изменён. Подготовительная сессия `20260913-170741-5174e8e8`
  завершилась по EOF до setup/smoke; тоже закрыта, за успешную проверку не считается.
- Readiness и preflight PASS, модель на этом этапе не запускалась.

Уточнение прежнего промежуточного сообщения: Typed в полном прогоне действительно
прошёл. Его реальные обновления ссылок относились к форматированию Table, а не к
исправленному редактору импорта. Отказ редактора проверен целевым тестом;
нормальная работа импорта подтверждена прямым и автономным выполнением.

Runtime остаётся `2897649a8d2120d25e4c80af8af2c6cdd834023b5bda1a1bd5baf073c6e9ba32`.
Полный goal не сокращён: 22 операции, три доставки, все сохранения/переоткрытия
и независимая свежая сессия. Старые FAIL сохранены. Повторное review не проводилось;
merge, push, deploy, общий клиент и новые узлы не затронуты.

[Машинный отчёт и точная команда](17-text-export-binding-fix.json),
[независимый аудит компонента](17-text-export-binding-component-success.json).
