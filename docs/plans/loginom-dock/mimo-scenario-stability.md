# Устойчивость Hermes / Xiaomi MiMo 2.5

Согласованный план реализуется в `codex/mimo-scenario-stability` от
`9b46c85f68d054a27afad732d74785332098c0ba` (main на 16 сентября 2026).
Main, публикация и общие плагины не изменяются. Предыдущие 38 файлов сохранены
в `.dock/mimo-stability-20260916/preexisting` и отдельном Git stash
`pre-mimo-stability-20260916 preserved unrelated work`; не включать их в коммиты.

## Контракт и критерии

- Hermes: существующая подписка Xiaomi, `xiaomi/mimo-v2.5/medium`, без fallback.
- Стенд: `http://10.200.11.224/app/?testable=true`, отдельные browser profiles,
  отдельный аккаунт `mimo` без пароля (уточнение пользователя 16 сентября 2026).
  Для каждого запуска — отдельная папка в хранилище этого аккаунта; ожидаемый
  путь `/mimo/MiMo-*` нужно подтвердить в живом Loginom до запуска Hermes.
- Модель получает неизменное `description.md`, dataset штатным native-вложением
  и назначение сохраняемого пакета. Oracle/готовый граф модели не передаются.
- MiMo выбирает аналитическую логику, Dock исполняет один узел целиком.
  Полный интерпретатор графов и новые типы узлов вне объёма.
- Предварительно: 26 кандидатов, 24 пропуска/условных случая. Причины и хеши
  всех 50 задач: `analytic_corpus.py --inventory ...`. Условные задачи не входят
  в знаменатель успеха; подтверждённая невозможность полного ответа — пропуск.
- Задачи02/47/21/19/34/38: по три свежих полных запуска на итоговой ревизии;
  остальные допущенные — по одному. Каждый PASS требует полного независимого
  аудита вычислений, сохранения, повторного открытия и настроек/связей.
- Метрики: завершённость, корректность, ошибки контрактов, восстановления,
  tool/API calls, время и токены. Старые FAIL сохраняются, модельные рассуждения
  и секреты не экспортируются.

## Порядок изменений

1. Исходный live/UI и модельный baseline, фиксация входов и источников.
2. Согласование schema/runtime, адресные ошибки до мутаций.
3. Автоматическое размещение, технические defaults, изменения выходов отдельным
   patch-контрактом; legacy полный mappings.fields не дополнять молча.
4. Безопасное продолжение доставки/настройки по исходным receipts; таймаут
   не разрешает повтор мутации. Не снимать pending закрытием окна.
5. Краткий перечень возможностей и карточки конкретного типа, полный локальный
   журнал; обычный модельный маршрут через node.apply.
6. Адресные тесты/live воспроизведение, затем итоговая неизменная серия.

## Checkpoint 16 сентября: подготовка

- Память healthy; cwd/Peer остались прежними, новая задача памяти не создавалась.
- Ветка создана, текущая ветка чиста от прежней работы.
- До смены аккаунта Source Dock подготовил новый сценарий на новом IP: Loginom7.4.2,
  аккаунт orcestrator, viewport:null, окно2044×1035. Полный результат сохранён
  в `.dock/mimo-stability-20260916/live-before/1.json`.
- Воспроизведён запрос импорта без position: `request.validate`,
  `effect_possible:false`, `Invalid node contract object`, MCP isError:true.
  Это проверка отказа до чтения source, не приёмка импорта.
- Пользователь назначил для дальнейшей работы аккаунт `mimo` без пароля.
  Запускатель и подготовка папок переведены на него. Вход и корень хранилища
  нового аккаунта ещё не проверены; прежние доказательства `orcestrator`
  остаются историческими и не подтверждают доступ `mimo`.
- Подготовлены операторские инструменты и native corpus launcher, проверки
  запускателя и итоговая приёмка ещё не завершены.
- Реестр Hermes был свободен; обнаруженные процессы Hermes — gateway и его
  supervisor, не отдельные acceptance CLI. Перед моделью слот резервируется.

Открытые работы: baseline, все продуктовые исправления, независимые auditors,
38 плановых итоговых прогонов (если все26кандидатов допущены).

## Checkpoint: исходный прогон и первая правка

- Вход `mimo` без пароля и `/mimo/MiMo-*` проверены живым UI; создана отдельная
  папка диагностики `/mimo/MiMo-diagnostics-20260916`.
- Первый запуск `20260916-143726-1d89fc1f` завершился без вызовов Dock:
  MCP-процесс не наследовал LOGINOM_DOCK_HOME. Исправлен изолированный launcher,
  добавлена проверка обнаружения MCP до запуска модели. Это ошибка harness.
- Baseline `20260916-144005-bcf8db51` подтвердил `mimo-v2.5`, штатный приём
  вложения и доставку с проверкой SHA. Далее были вызовы недоступных read_file,
  terminal/execute_code, попытка использовать add_resource и node.add из Files.
  Полная node.apply отклонена до эффектов: пустой settings.columns. Остановлен
  после воспроизведения для адресной диагностики; это FAIL, не приёмка.
- Добавлены необязательные технические read/budgets/mappings в пользовательский
  контракт, автоматический выбор свободной позиции с сохранением в checkpoint,
  структурированный отказ с путём параметра и новым ID для исправленного запроса.
  Pending-эффекты по-прежнему требуют проверки исходной операции.
- `live-contract1`: отказ неверного запроса без MCP isError и его исправление
  проверены; новый узел без позиции создан. Дальше мастер импорта оставался
  занят до 15-секундного таймаута; полный импорт НЕ прошёл.
- `live-contract2`: воспроизведён UI_ROOT_STALE при чтении панели хранилища
  до передачи байтов. Добавлено ограниченное повторное чтение того же региона
  только в исходном документе/workspace; предшествующий клик не повторяется.
- Независимый oracle задачи02: 500 строк файла, 366 за 2024, сумма 365345.55;
  полные группировки вычислены локально и Hermes не передаются.

Дальше: закончить live-диагностику занятости мастера/доставки, затем ранняя проверка
выходных полей, patch-контракт, восстановление и компактный маршрут. Итоговой
серии и независимого аудита сохранённых сценариев ещё нет.

## Checkpoint: повторная диагностика контрактов

- `live-contract3`: после отказа 15-секундного открытия мастер стал готов через
  дополнительное наблюдение. Продлён только ожидатель исходного bound wizard
  (до45секунд в общем бюджете); повторный клик не выполняется.
- `live-contract4`: ручная полная проверка PASS — импорт500строк, исправленный
  запрос с новымID, автоматическая позиция, defaults, повтор прежнего запроса
  возвращает тот же узел. Это не автономная приёмка.
- Выявлено отличие исходного harness: `hermes -z` сам не разворачивает `@file`
  в контекст модели. Доставка файла работала, но CSV модель не видела. Подключён
  штатный Hermes `preprocess_context_references`, сохранены границы task-root
  и отсутствие oracle. Поэтому baseline2 не является чистым измерением MiMo.
- Диагностический `20260916-145831-ed2acd08`: MiMo получила CSV, доставила файл
  и запросила карточки узлов. Обнаружен дефект сокращённого user-контракта:
  валидатор игнорировал `additionalProperties:true` и отклонял `source/settings`.
  Прогон остановлен,12вызовов сохранены. Исправлен explicit-open envelope;
  строгая внутренняя проверка узла сохранена. Адресные20тестов PASS.
- Пользовательские tool schemas/instructions/список действий согласованы:
  поддержанные узлы через node.apply, низкоуровневые действия скрыты, сохранение
  доступно. Число инструментов в диагностике сократилось с34до23; влияние
  сокращения контекста на успех ещё не измерено.
- `live-contract5` не подключился: запуск использовал системный Node22 вместо
  закреплённого Node24. Следующий probe использует bundled runtime. Этот отказ
  не является ошибкой Loginom и не подтверждает исправность нового контракта.
- Read-only helper нашёл полный вертикальный UI-reader
  `collect_missing_values_table.mjs`; нужно параметризовать5столбцов, добавить
  горизонтальное чтение широких таблиц и cleanup в finally. Текущий sample-reader
  ограничен10строками и не доказывает полные20групп пересечений задачи02.

Не выполнены: patch выходных полей, ранняя полная проверка Calculator,
подтверждённое продолжение предзагрузочной доставки/выходного мастера, универсальный
полный аудит, остальные oracles и итоговая автономная серия. Аккаунт mimo без
пароля остаётся единственным аккаунтом этой кампании.

## Checkpoint: Calculator preflight и ожидания UI

- `live-contract6`: исправленный user envelope прошёл реальный импорт500строк,
  no-position/defaults и идемпотентный replay. `live-contract5` остаётся startupFAIL.
- Добавлен preflight нового калькулятора по живой схеме upstream: полнота полного
  mappings.fields, неизвестные/повторные поля, конфликты имён, replacement/order.
  Синтаксис формул по-прежнему проверяет Loginom. Existing-node пока сохраняет
  прежний путь проверки в мастере; это оставшееся ограничение.
- `live-calculator1`: исходный preflight встретил загрузку PreviewWindow и
  ошибочно отклонил её как посторонний диалог. Добавлено только wait-разрешение
  для точного PreviewWindow текущего bound workflow с его busy-mask; готовность
  требует native schema, посторонние окна по-прежнему отклоняются.
- `live-calculator2`: импорт остановился на переходе к format через6.396секунд;
  независимое последующее наблюдение показало нужную страницу. Для bound
  wizard_step ожидание исходного клика продлено до45секунд в общем deadline;
  повторный клик запрещён. Оба FAIL сохранены.
- Unit: calculator/sorting28PASS; node-procedure/workspace-ui353PASS, включая
  четыре новых случая owned preview loading / чужие dialog и mask.
- Независимые расчёты основных агрегатов подготовлены для всех6стартовых задач.
  Oracle tests6PASS (границы периода, полная таблица, ratio-of-sums, направленные
  вероятности, weighted/unweighted retention). Артефакты `*-oracle-v2.json`
  используют Decimal и заменяют ранние диагностические варианты v1. Это ещё
  не аудит всех требований задания: сегменты/пороги/сравнения, выбранные моделью,
  требуют отдельной проверки по сохранённому сценарию.

## Checkpoint: частичные выходы и продолжение доставки

- `live-calculator3` PASS: неполный полный список отклонён как NOT_APPLIED,
  effect_possible=false/cleanup=true; новый исправленный запрос выполнил500строк,
  повтор вернул тот же узел. Проверены восстановление graph после preview и
  отсутствие необходимости ручной разблокировки. Пакет этим probe не сохранялся.
- Добавлен `mappings.changes` для output: разворачивание в полный observed layout,
  сохранение неупомянутых имён/порядка/исключений, запрет смешивания с fields.
  Legacy fields остаётся полным списком. Unit186PASS.
- `live-output-changes1` FAIL показал реальное ограничение Calculator: вычисляемые
  source-поля required=true, passthrough-поля optional. Ошибочная гипотеза о
  противоположном ограничении заменена live-фактами; preflight теперь отклоняет
  исключение вычисляемого поля до создания узла. Дальше проверяется переименование
  вычисляемого поля и исключение исходного transaction_id.
- Продолжение доставки до upload добавлено только для подтверждённой навигации
  с последующим отказом чтения. Требуются исходный документ, неизменное вложение,
  свободный runtime gate; IDs навигации продолжаются монотонно. Неизвестный
  результат клика и уже начатая загрузка не получают этот путь. Unit46PASS;
  live fault-injection и новый upload/reopen аудит пока не выполнены.
- Полный вертикальный reader параметризован на1–10столбцов, добавлен finally
  для возврата отображения; это незавершённая harness-адаптация, не livePASS.

## Checkpoint: перед повторным MiMo

- `live-output-changes2` PASS: переименование calculated_total→VerifiedTotal,
  исключение исходного transaction_id, сохранение остальных полей,500строк и
  идемпотентный replay. Вычисляемые поля исключать нельзя; это теперь preflight.
- `live-import-defaults1` PASS: модельный контракт допускает только имена/типы
  колонок; source_path берётся из локально подтверждённой доставки того же
  artifact/upload ID. Остальные CSV-настройки имеют явные defaults с override.
- Полный клиент на pinned Node24:1868PASS/1SKIP/0FAIL,53.6сек. Лог:
  `.dock/mimo-stability-20260916/client-suite-1869.txt`. Python attachment11PASS,
  независимые oracle6PASS. `git diff --check` PASS.
- Начат diagnostic-task02-fix2: `20260916-153230-f976fa41`, отдельная папка
  `/mimo/MiMo-task02-fix2-20260916`,23инструмента в preflight, исходный CSV через
  официальный Hermes attachment expansion. Во время модели runtime не меняется.
  Первый импорт500строк выполнен MiMo самостоятельно; весь сценарий ещё не принят.
- Подготовлен `scenario-reopen.mjs`: новый document/browser, точные GUID и
  topology, выполнение сохранённых выходов без перенастройки, полный узкий
  UI-reader, diagnostic discard. Синтаксис проверен; живой аудит ещё не выполнен.


## Checkpoint: MiMo завершила диагностическую задачу 02

- Прогон `20260916-153230-f976fa41` завершён без ручной помощи модели,
  exit0, фактическая модель `mimo-v2.5`, runtime source unchanged=true.
  Время489.70с,27вызовов инструментов,25API-вызовов; input82768,
  output10556, cache_read1516864, total1610188 токенов по usage.
- Сохранён `/mimo/MiMo-task02-fix2-20260916/scenario.lgp`: импорт, фильтр2024,
  три группировки и три сортировки. Модель самостоятельно исправила отклонённое
  имя выходного поля и слишком большой sample_rows. Два параллельных запроса
  получили busy; обработка продолжающейся операции требует более точной подсказки.
- Независимое открытие подтвердило все8аналитических GUID/типов/имён и7связей.
  Первоначальный audit-plan ошибочно не включал штатные «Переменные сценария»;
  их исходныйGUID подтверждён `node_target_checkpoint` до создания импорта.
  FAIL сохранён, исправленный план записан отдельно.
- При отладке проверочного скрипта исправлены имя поля import_source и ожидание
  загрузки виртуальной таблицы после прокрутки. Это изменения аудитора, не Dock.
  Полное чтение500строк импорта успешно; фильтр даёт366строк. До окончательного
  сопоставления всех таблиц и настроек статус независимой приёмки PENDING.
- После reopen у фильтра date оказался первым столбцом; исходная квитанция
  имела прежний порядок. Состав/name/label/type совпадает. Задача02 порядок
  столбцов не задаёт: audit-plan-v3 разрешает только их перестановку на портах
  фильтра, с записью факта и полной сверкой значений по именам. Чтение данных
  перенесено перед открытием мастера; причину перестановки пока не утверждаем.
- Добавлен отдельный `audit_sales_scenario.py`: полная сверка импортированных и
  отфильтрованных транзакций, всех агрегатов и рейтингов с Decimal-расчётами,
  проверка сохранённых настроек.4негативных/позитивных теста PASS; строгая
  проверка переставленной схемы7адресных проверок PASS. Это не итог серии.


## Checkpoint: независимый аудит задачи02 PASS

`audit-task02-fix2-7/numerical-audit.json` подтвердил все9портов после независимого
reopen: импорт500строк по всем полям, filter366/134, полные группировки и рейтинги
5категорий,4регионов и20пересечений. Выручка365345.55. Максимальная абсолютная
погрешность агрегатов2E-11 при допустимой1E-7. Проверены настройки импорта,
обе границы периода, ключи/суммы и сортировки DESC;8аналитических узлов/7связей
не менялись. Собственный диагностический пакет закрыт с discard, logout подтверждён.
Все предыдущие аудит-FAIL сохранены. Повторение схемы до настройки формата
подтвердило: перестановка столбцов уже присутствует после reopen, не внесена
форматированием аудитора. Причина внутри сохранения/нативной синхронизации
не установлена; задаче02 это не мешает, значения сверены по точным именам.

Это первый принятый диагностический MiMo-сценарий, не итоговая серия и не выпуск.
Финальный видимый ответ модели теперь экспортируется отдельно для проверки
бизнес-выводов; exporter не запрашивает reasoning/system/API-content, отвергает
смешанные private-блоки и редактирует секреты.2теста PASS.

Следующие адресные улучшения: опубликован ASCII-pattern для имён полей,
пример полного ISO datetime, точный путь ошибочного операнда фильтра.
Для подтверждённо работающего node worker отказ конкурирующего запроса теперь
даёт dock_node_wait исходного ID, без ложного AMBIGUOUS и недоступного inspect.
Неизвестный результат сохраняет прежний inspect.80адресных тестов PASS;
полный клиент повторно проверяется перед задачей47.


## Checkpoint: задача47 выявила блокировку после экспорта

Прогон `20260916-161114-dac3a8bc` остановлен координатором после повторных
безуспешных resume/recovery/save. 49вызовов инструментов,654.71с, фактическая
модельmimo-v2.5, source unchanged=true. Изолированный пакет не сохранён;
это FAIL, не приёмка. Штатный usage-файл при остановке не появился; отдельно
сохранены native session counters:46API-вызовов,input116362/output14557,
cache_read3070144 (не выдавать их за полный закрытый billing ledger).

Первый блокер: `node-009-export-dept-v2` уже имел подтверждённый completed
execution, но чтение файла упало сUI_ROOT_STALE после подтверждённой навигации
хранилища. Фазаread осталась pending, поэтому блокировала сохранение. Повторное
создание/выполнение экспорта не требовалось. Новая running-подсказка dock_node_wait
до этого успешно обработала конкурирующий запрос — live-подтверждение есть.

`live-export-pair-before` независимо воспроизвёл ещё один сбой: проверка выбранного
header завершалась через70мс сWIZARD_OPTION_NOT_CONFIRMED; последующее UI-наблюдение
показало правильные «Имена полей». Добавлено ограниченное read-only ожидание
первого клика. Для stale корня хранилища добавляется повтор логического наблюдения
со свежими корнями (до3), без повторного действия; смена документа запрещена.
Адресные storage-тесты12PASS, живой повтор и полная регрессия ещё выполняются.

## Checkpoint: повторное открытие Files при экспорте

- Новый тест delayed header исправлен под реальные data-tid; workspace/reader
  277PASS. Полный клиент до последней правки навигации1874PASS/1SKIP/0FAIL
  (`client-suite-export-fix.txt`); localhost sockets разрешены.
- `live-export-pair-fix1`: оба мастера настроены, первый экспорт полностью
  прошёл. Второй server execution завершён, но read ожидал /mimo после входа
  в аккаунт через новую вкладку TF4. Loginom восстановил прежнюю TF3 сразу
  в /mimo/MiMo-diagnostics-20260916. Итог — did not settle; FAIL сохранён.
- Навигация теперь разрешает после входа только ожидаемый промежуточный путь
  или точную конечную папку. Посторонняя восстановленная папка по-прежнему
  отклоняется; повторных кликов нет. Пять адресных testsPASS, live fix2 идёт.
- Это техническое поведение Files, не ошибка аналитической логики MiMo.

## Checkpoint: скачивание после отказа до действия

- `live-export-pair-fix2` завершился FAIL до скачивания первого файла:
  DOWNLOAD_CONTEXT_CHANGED, NOT_APPLIED/effect_possible:false/cleanup_complete:true.
  Все проверенные признаки контекста совпали, кроме dom_epoch.revision.
- Добавлены максимум три попытки только для доказанного epoch-only отказа
  до жеста. Каждая новая попытка имеет свой ID и свежее наблюдение того же файла,
  размера, документа, вкладки и папки. При неизвестном результате, возможном
  эффекте или изменении контекста повтора нет.
- Полный клиент после правки:1880PASS/1SKIP/0FAIL; export tests37PASS.
  Evidence: `client-suite-export-fix3.txt`, `export-fix3-tests.txt`.
- `live-export-pair-fix3` выполняется. Папка следующей модели
  `/mimo/MiMo-task47-fix2-20260916` создана и проверена, MiMo ещё не запущена.

- Живой `live-export-pair-fix3` завершился PASS: два разных export execution,
  два подтверждённых файла. Оба полностью сверены с dataset по500строк/7полей,
  SHA обоих c2ddce9ba8793927ef8d1d98d547051e10055c5978d947cd03da1ad256636139.
  Доказательство `live-export-pair-fix3/file-audit.json`; это ручная диагностика.
  После проверки отсутствия других модельных процессов выделен Hermes-слот
  для повторной задачи47 в уже подтверждённой новой папке.

## Checkpoint: третий экспорт в повторной задаче47

- `20260916-164351-7b84b185`:861.34с,44вызова, actual mimo-v2.5, runtime
  071b3ab15f172d90889d6d7b6c999cea2359dedd4169e79d52a3b9ae0603e7db неизменён.
  Первые два export полностью прошли; третий выполнился на сервере, но read
  завершился Export storage did not settle. Пакет не сохранён, итог FAIL.
  Модель остановлена после повторных recovery; её PID завершён, слот освобождён.
- Новый случай отличается: после /mimo Dock кликнул точную подпапку назначения,
  затем не подтвердил переход. Для диагностики добавлена квитанция последнего
  наблюдения (папка/вкладка, guards, маски, expected condition); она не снимает
  pending и не повторяет действие. Запущен прямой export-triple вместо пары.
- Отказы неполного export-format и SUM(logical) произошли до изменений; MiMo
  исправила их самостоятельно. Busy-навигация также вернула wait исходногоID.
  Неполный формат всё ещё требует более точного контракта/технических defaults.
- Подготовлены независимые числовые/настроечные аудиторы47 с ограниченным
  whitelist арифметики и полными таблицами;6адресных Python testsPASS.
  Они ещё не применялись к saved/reopened47, принятого сценария47 пока нет.

## Checkpoint: восстановленная вкладка в корне Files

- `live-export-triple-diag1` точно повторил третий сбой: native UI после
  подтверждённого перехода к папке назначения активировал старую TF4 в корне `/`.
  Квитанция export_file_observation_unsettled: quiet=true, condition=false.
- Навигатор теперь читает фактический путь после каждого перехода и строит
  следующий шаг только из допустимого предка destination. Новая вкладка в корне
  разрешает новый осмысленный переход; повтор уже посещённого (tab,path)
  отклоняется как цикл. Неизменившийся контекст после клика не считается
  завершением навигации. Другие поддеревья не принимаются.
- `live-export-triple-fix1` PASS:3экспорта, все3CSV сверены по500строк/7полей,
  SHA идентичен исходной проверке пары;38адресных export testsPASS.
- Сверено с локальной Help `location_user_files.md` (double click папки,
  чтение/скачивание серверных файлов) и E2E `bg/helpers/filestorage.ts`
  OpenFolder253–277 / `navigation.ts` GetCurrentTabPath56–60. E2E также читает
  новый breadcrumb после перехода; самостоятельного теста такого reuse там нет.
- User-v1 нового экспорта получил обычные CSV defaults, destination обязателен,
  overwrite не включается автоматически, существующий узел сохраняет patch
  семантику. Карточка объявляет creation_defaults, внутренний строгий контракт
  сохранён, ошибки отсутствующих параметров называют точный путь.
- Начат `live-export-defaults1` с одним destination. До его результата новая
  модель не запускается. Полное восстановление pending read/output остаётся
  отдельной незавершённой работой; исправленная навигация не закрывает этот пункт.

- `live-export-defaults1` PASS: запрос содержал только destination, полученный
  CSV полностью сверён по500строк/7полей. Тесты после defaults:1882PASS/1SKIP.
- Дополнительно устранено накопление вкладок Files: после подтверждённого
  скачивания Dock локально сохраняет (prepared document, browser document,
  tab tid/ref). Следующий экспорт использует её только после свежего наблюдения
  тех же идентичностей. Удалённая/пересозданная/чужая вкладка не принимается;
  её отсутствие возвращает стандартный маршрут с проверкой фактической папки.
  Идёт `live-export-tab-reuse1` (3экспорта); это ещё не модельная приёмка.

- `live-export-tab-reuse1` PASS:3полных CSV по500строк/7полей совпали с dataset;
  export_file_tab_reused дважды подтверждает одну TF3. Полный клиент
  1883PASS/1SKIP/0FAIL (`client-suite-export-tab-reuse.txt`).
- Проверена новая папка `/mimo/MiMo-task47-fix3-20260916`. После проверки
  отсутствия конкурирующих процессов выделен слот на следующий диагностический
  MiMo-прогон47. Окончательный результат и независимый reopen ожидаются.

### Продолжение: input mapping и идентичные названия

Прогон47 `20260916-171627-d7d7c0f8` остановлен и экспортирован:22вызова,
354.05с, exit-15, фактическая модель `mimo-v2.5`, runtime
`e17013594d3aa54f25636e4246fd4b2cedb276804d2ff18a0248a8b18086cdaf` неизменен.
PID72854 отсутствует, Hermes-слот освобождён. Пакет не сохранён; итог FAIL.

Модель сначала усреднила денежное variance вместо variance_pct, заметила
ошибку и создала второй узел с тем же названием. В его input_mapping после
одного Done Loginom вернулся на граф; prepared GUID указывает точный узел
с техническим ключом `Отклонения_по_категориям_и_кварталам@1`. Dock вернул
WIZARD_FINISH_NOT_CONFIRMED. Проверяется расхождение breadcrumb/key.
Первый resume изменил target:new на existing под прежним ID и был отклонён.
Позднее идентичный исходный запрос был принят, но unresolved input_mapping
остался: у группировки нет допущенного recovery этого этапа.

Следующая работа: прямое воспроизведение одинаковых названий, проверка
GUID-привязки после Done; ID-only пользовательский resume с локальным
неизменяемым запросом и прежними проверками неопределённых эффектов.
Это диагностическая серия, а не завершение согласованной приёмки корпуса.

### Подтверждённая причина: разные ключи одного узла

Прямой `live-duplicate-label-fix1` подтвердил: input Done после исправления
прошёл, но следующий begin_wizard отказал. У того же GUID графовый ключ
заканчивается `@1`, breadcrumb — `-3`; отображаемое название одинаково.
Проверки открытия и завершения теперь используют локальную GUID-привязку
с сохранением проверок документа/сценария, владельца и названия.
Отрицательный тест выявил и закрыл fallback на одноимённый чужой узел
при несовпадении GUID. Адресные7тестов PASS, полный набор и live fix2 идут.

Пользовательский dock_node_resume принимает только operation_id. Runner
хранит независимую копию исходного полного запроса, повторно проверяет
ревизию обработчика и не дублирует работающий/успешный job. Diagnostic API
сохраняет полный идентичный запрос; частичный изменённый запрос отвергается.
Это не снимает unknown-effect блокировку и не добавляет recovery группировки.

Первый прямой probe `live-duplicate-label-before` ошибочно не указал label
меры; отказ был до изменений. Исправленный before2 остановился раньше
проверяемой границы на Graph is blocked в target. Этот отдельный transient
сохранён и не считается доказательством исправления мастеров.
E2E portWizard.ApplySettings подтверждает один Done и закрытие мастера; Help
workflow/ports/mapping-master.md описывает структуру входного/выходного
маппинга. Различие ключей установлено именно живыми квитанциями.

### Маска перед созданием связи

`live-duplicate-label-fix2` повторил отдельный target-сбой: create и rename
подтверждены, connect ещё не выполнен, его предварительный readGraph застал
маску. В adapter ожидание маски было включено только для exports.text.
Теперь все connect используют observeGraph с ограниченным ожиданием и полной
повторной сверкой исходного графа; изменение графа по-прежнему даёт NOT_APPLIED.
Ни один возможный жест не повторяется. Адресный тест подтвердил ожидание
и отказ при изменившемся графе. Live fix3 и полный набор запущены.

До этой последней правки полный набор:1886PASS/1SKIP/0FAIL
(`client-suite-resume-identity.txt`). Счётчики остановленного47fix3 сохранены:
input70563/output9244/cache_read1032896/cache_write0; это session counters,
итогового usage-файла у прерванного процесса нет.

### Общая проверка владельца мастера

`live-duplicate-label-fix3`: первый узел и ID-only resume PASS, второй прошёл
input_mapping и браузерное begin_wizard, затем обнаружилась аналогичная
проверка строк в openPreparedWizard. Она согласована с GUID-проверками:
обязательны совпадение document/workflow/node, наблюдённый владелец, исходный
путь workflow и исходная подпись узла при разных технических ключах.
85адресных тестов PASS (включая отрицательные identity/path/label).
`live-duplicate-label-fix4` выполняет полный цикл двух узлов и повторное
ID-only обращение к каждому. Новый полный набор запущен после этой правки.

Предыдущий полный набор с общим ожиданием connect:1887PASS/1SKIP/0FAIL
(`client-suite-connect-mask.txt`). Для следующего47fix4 отдельно создана
и проверена `/mimo/MiMo-task47-fix4-20260916`, окно2044×1035. Hermes ещё
не запускался; общий слот свободен.

### Возврат из карточки одноимённого узла

Live fix4 прошёл оба мастера и выполнение второго узла; отказ возник в
ожидании `executed node navigation available`. ModelForm был связан с правильным
GUID, но readNodeContext не передавал наблюдённый ключ карточки, а две проверки
ниже опять сопоставляли breadcrumb `-3` с graph `@1`. ReadPreparedNodeContext
теперь выдаёт navigation_node только при exact cached ModelNodeTreeNode GUID,
том же package/workflow и ровно одном дочернем breadcrumb. UI и возврат на
workflow используют эту проверенную привязку. Чужие node/document/workflow,
путь и подпись по-прежнему отвергаются.8адресных тестов PASS; fix5 выполняется.
Последний полный набор перед этой правкой:1887PASS/1FAIL/1SKIP (порядок двух параллельных GUI-сессий в hermes-router.test)
(`client-suite-wizard-owner.txt`). Hermes не запускался.

### Проверка полного набора — уточнение

`client-suite-wizard-owner.txt`:1887PASS/1FAIL/1SKIP. Единственный FAIL —
hermes-router.test ожидал порядок создания A,B при Promise.all; фактически B,A.
Изоляция и содержимое ответов совпали. Исправлено только ожидание порядка
независимых admission: discovery остаётся первым, затем требуется ровно A и B
без дубликатов. Runtime router не изменён. Полный набор с новой навигацией
и исправлением теста запущен (`client-suite-navigation-identity.txt`).

### Полный прямой цикл PASS; новый MiMo47fix4

`live-duplicate-label-fix5` PASS: импорт500строк и две группировки с одинаковым
названием; разные GUID, полная настройка/выполнение/чтение. Все5строк каждого
отображённого результата совпали с независимым суммированием CSV до2знаков.
Это проверка отображения, не lossless чисел и не автономная приёмка. ID-only
resume для обоих завершённых узлов вернул тот же GUID/attempt1.

`client-suite-navigation-identity.txt`:1891PASS/1SKIP/0FAIL. Рабочие процессы
диагностики завершены, перед Hermes проверено отсутствие конкурирующих
исполнителей. Прогон `20260916-174615-e0b292a7`, PID76984,
`/mimo/MiMo-task47-fix4-20260916`, Xiaomi mimo-v2.5/medium, MCP23tools,
native-вложение. Runtime заморожен на время прогона; общий слот running.
Независимый генератор audit plan доработан для ID-only resume: используется
исходный записанный apply, orphan resume и изменённый полный запрос запрещены.

### MiMo47fix4 завершён; аналитический аудит пока FAIL

20tool calls/19API,487.124с, runtime`c72a248d717f9b4917ecff17794f6898da4eab385a13522fc2f80c438baa9e9b`
неизменен, фактическая Xiaomi mimo-v2.5. input68514/output11468/cache895552,
total975534.7аналитических узлов/6связей сохранены; три group/sort ветки.
Технические blocking-сбои отсутствуют, два преждевременных параллельных
запроса модель обработала через ожидание исходного ID.

Полная задача не принята: final answer придумал неверный топ экономий
при полученных10из23строк рейтинга, смешал среднее120строк34.295% с
невзвешенным средним6категорий35.8821%, сделал противоречивый вывод о
заниженном бюджете R&D. Подробности в run/business-review.json.
Это уже подтверждённые аналитические ошибки текста, отдельно от исправленных
контрактов и UI. Пакет и все его таблицы проходят независимый reopen.

Первый audit отказал до чтения9-колоночного импорта: standalone collector
ошибочно считал лимит страницы10, штатный API допускает8. Исправлен только
аудитор:9колонок читаются двумя страницами, runtime модели не изменён.
Cleanup первого audit подтвердил discard/logout. Второй audit идёт,
4адресных теста горизонтального чтения PASS.

### Аудит47fix4 завершён; подготовлен47fix5

Независимый reopen `.dock/mimo-stability-20260916/audit-task47-fix4-2`:
7полных таблиц/186строк, все столбцы, сортировки, настройки и граф PASS;
наибольшая числовая разница2e-9 при допуске1e-7. Cleanup discard/logout PASS.
Сводный `full-audit-v2.json` честно сохраняет FAIL полного задания из-за
трёх подтверждённых ошибок итогового бизнес-текста. Генератор аудита допускает
только проверенный import autosync=true без изменений полей; поддерживает
булево или целое0/1 представление входного флага.

В user-v1 добавлено явное ограничение при неполной выборке: сколько строк
получено и запрет утверждать невидимую часть рейтинга. Общая инструкция требует
вычислять отчётные показатели узлами, различать основу среднего и не выдавать
недоказанные причины за факты. Аналитические формулы/ключи за модель не
выбираются.17адресных тестов, проверка7реальных квитанций и полный набор
1892PASS/1SKIP/0FAIL. Новый прогон47fix5 подготовлен в отдельной папке
`/mimo/MiMo-task47-fix5-20260916`; runtime заморожен при запуске.

Приватность архива: official Hermes временно хранит reasoning для native
истории MiMo. Диагностический exporter никогда не читает эти поля. Добавлена
очистка завершённой изолированной SQLite-базы после экспорта: пять отдельных
reasoning-полей NULL, secure_delete/VACUUM/checkpoint, проверка пустых колонок.
Не меняет native hooks, модельный запрос или активную историю. Packed api_content
не поддержан: при его обнаружении очистка явно откажет.8завершённых Xiaomi
архивов этой кампании очищены; прежние FAIL и пользовательские/tool evidence
сохранены. Адресный тест проверяет сохранение видимого текста и tool calls,
идемпотентность и запрет очистки незавершённого процесса.

### MiMo47fix5 завершён; полный независимый аудит продолжается

`20260916-181009-dd48d6de`:32tool calls/32API,638.227с, неизменный runtime
`16ede25eb4dc17074b3b7d07abf7cf787cba4a395c2863cc57928ea1f726f6b7`,
фактическая Xiaomi mimo-v2.5/medium. input87932/output16063/cache1908544,
total2012539.11аналитических узлов/10связей и пакет сохранены. Четыре
NOT_APPLIED до изменения графа: sample_rows23 и три неверных формы
параметров экспорта; модель исправила новые запросы самостоятельно.

Независимо сверены три native CSV:23/4/6строк, все поля, сортировки,
SHA и привязки к исходному выполнению; PASS, максимум2e-9. Новый аудитор
проверяет байты, запись скачивания и отдельный Decimal-расчёт; тест отвергает
чужой узел, изменённые байты, недостающие строки и неправильную сортировку.
Повторное открытие пакета `audit-task47-fix5-1` выполняется на том же runtime.

Бизнес FAIL остаётся: модель сообщает8из23 групп с отклонением>100%,
независимый расчёт даёт7. Использовано явно названное среднее абсолютного
процента; знаковое среднее отсутствует. Причины перерасхода/экономии в данных
не представлены. Ошибки прежнего невидимого рейтинга и среднего групп устранены
в этом запуске; это не доказывает общей надёжности. Подробности business-review.json.

Python acceptance suite:730PASS/1ERROR, устаревшая fixture test_provenance
не передавала обязательный loginom_user. Исправлена только fixture с явным
пользователем mimo; production fallback к имени пользователя не добавлялся.
Для задачи21 создана отдельная папка `/mimo/MiMo-task21-diagnostic1-20260916`,
окно2044×1035. Новый Hermes пока не запускался.

### Итог независимого reopen47fix5 и выбранная ошибка схемы

`audit-task47-fix5-1/full-audit.json`: saved graph/settings,8полных таблиц/
323строки и3экспорта PASS, бизнес FAIL.11настроек проверены через повторное
открытие; экспортные мастера только прочитаны и отменены, файлы не перезаписаны.
Cleanup discard/logout PASS. Исходная операция завершилась; runtime разрешён
к дальнейшим изменениям, старые доказательства сохранены.

Подтверждённая цепочка трёх ошибочных export-запросов уточнена: неизвестное
поле теперь названо полным путём, следующий шаг — dock_action_describe
для конкретного типа узла. Pending effects по-прежнему требуют inspect/wait.
При неверных технических read-параметрах исправление остаётся direct apply
с новым ID.24адресных теста PASS; полный клиентский набор запущен.
Fixture loginom_user проверена3тестами; полный Python-набор повторяется.
Следующая задача21 запущена последовательно, исходное description/native CSV,
модель не получила независимые расчёты или готовый граф.

### MiMo21 запущен; проверка наборов завершена

`20260916-183006-fc786284`, PID81510, `/mimo/MiMo-task21-diagnostic1-20260916`,
xiaomi/mimo-v2.5/medium,23MCPtools. Импорт300строк прошёл; runtime заморожен
до окончания модели и независимого reopen.

Python:731PASS после исправления устаревшей fixture. Клиент:1884PASS/
9FAIL/1SKIP в sandbox, все9FAIL относятся к запрещённому listen127.0.0.1
и зависимым тестам kernel clipboard lease. Адресный повтор четырёх файлов
с разрешёнными локальными сокетами:14PASS/0FAIL. С учётом повтора1893
уникальных PASS/1SKIP; тесты и production-логика блокировки не менялись.

## Пауза по просьбе пользователя — 16 сентября 2026

Текущий MiMo21 `20260916-183006-fc786284` остановлен SIGTERM,13вызовов
сохранены, пакет не подтверждён. Процессы завершены, Hermes-слот освобождён.
Новые прогоны запрещены до команды продолжить. Полная точка восстановления:
`docs/loginom-dock/mimo-stability-pause-2026-09-16.md`.

## Возобновление — 16 сентября,20:16MSK

Пользователь дал команду продолжить. Ветка/изменения сохранены, активных
исполнителей кампании нет, Loginom HTTP200. OpenViking health/find недоступны;
штатный doctor и независимый curl подтвердили таймаут ov.kartamyshev.dev.
Настройки памяти не менялись; работа продолжается по канонической документации
и live evidence согласно project policy. Проверяется папка прерванной задачи21.

### MiMo21 diagnostic2

В старой папке после ожидания окончания навигации найден только native CSV,
scenario.lgp отсутствует; первый слишком ранний снимок родительской папки
сохранён, его verified не считается доказательством содержимого.
Новая папка `/mimo/MiMo-task21-diagnostic2-20260916` проверена. Прогон
`20260916-202035-04e1852e`, PID83528,23MCPtools, Xiaomi mimo-v2.5/medium.
Runtime не меняется во время модели и последующего независимого аудита.
Добавлен операторский Decimal-аудитор task21:все300строк, выбранные формулы
IF/ABS/арифметика, суммы/средние/count и числовая сортировка; неизвестные
формулы/типы/изменения схемы отказывают.3scalar-теста и1полный synthetic
портфельный тест PASS. Общая проверка сохранённых tabular-настроек выделена
для21/47; повтор ранее сохранённых evidence47 дал полностью идентичный verdict.

### Новый подтверждённый blocker task21 и исправление

`20260916-202035-04e1852e` остановлен после18вызовов: новый калькулятор
передан без inputs. Pure validator разрешал0входов, beforeTarget preflight
выбрасывал обычное исключение, а обёртка target помечала effect_possible=true
и pending. Узел ещё не создан (node=null,приняты только source/workflow),
но дальнейшее исправление блокировалось. Это дефект Dock,не аналитики модели.
В том же запросе order содержал входные поля вместо только выражений.

Новый calculator требует ровно одну связь к input0 до запуска job; order
нового узла проверяется по полному списку выражений до UI. Оба отказа
содержат точный parameter_path. В общей source preflight отсутствие
явного входа теперь даёт trusted NOT_APPLIED до первого browser action.
64адресных теста PASS; полный клиентский набор1894PASS/1SKIP/0FAIL.
Live `live-calculator-contract-2029`: импорт500строк PASS, missing input
и wrong order немедленно NOT_APPLIED; incomplete output тожеNOT_APPLIED;
исправленный калькулятор выполняется. Старая FAIL-диагностика и runtime pin
сохранены в run/diagnosis.json,slot освобождён.

Live calculator-contract завершёнPASS:500строк, исправленный запрос, тот же GUID при повторе.
Новый MiMo21fix1 `20260916-202750-2426c41e`,PID84612,23tools, отдельная папка
`/mimo/MiMo-task21-fix1-20260916`;runtime заморожен. Для двух прерванных
task21 сохранены отдельные native sessions counters; usagefile отсутствовал,
незавершённый provider request может не входить в эти счётчики.

### Task21 fix1: построение завершено, полная приёмка FAIL / аудит заблокирован

`20260916-202750-2426c41e` завершился exit0:20вызовов инструментов,6аналитических
узлов/5связей, импорт300, калькулятор300, риск3, фильтр98+202, сортировка98, возраст4.
MiMo самостоятельно исправила read.ports фильтра новым operation_id. Сохранение
через package.save_as вернуло SUCCEEDED/reopened:true. Однако штатный teardown
получил UNSAVED_CHANGES; затем процессы владельца завершились. Две свежие попытки
`audit-task21-fix1-{1,2}` открыли оригинал только для чтения. Полные таблицы и
настройки пока НЕ проверены. Повторный вход в прежний профиль открыл новый пустой
сеанс; он явно завершён через Выход, оригинальная блокировка не снята.

Независимый business-review.json: отсутствует распределение риска по доходу;
общие средние приведены без вычисленного общего агрегата и расходятся с CSV;
вывод о непредсказательной силе рейтинга основан на двух примерах. Это FAIL задачи,
даже если последующая техническая проверка таблиц пройдёт.

Auditor теперь распознаёт обе опубликованные квитанции сохранения; save_as требует
verified/reopened:true и точное active_identity. Отрицательные случаи проверены
(3теста PASS). Runtime с MiMo-pin525ea356 пока не менялся. Следующий шаг: адресная
диагностика изменения состояния после save_as и безопасного завершения сеанса,
затем дальнейшие задачи корпуса. Финальная серия38прогонов не начата.

### Диагностическая копия21 и исправление проверочного маршрута

На отдельной копии `/mimo/MiMo-task21-fix1-20260916/diagnostic-reopened.lgp`
прямой `package.save_as` воспроизведён без ошибки: `modified:false` сразу и после
ожидания, штатное закрытие/выход подтверждены без сохранения и отбрасывания.
Материалы `.dock/text-export/live-1789580817016`. Причина UNSAVED_CHANGES исходного
сеанса пока не установлена; защита от потери изменений не ослаблялась.

`audit-task21-fix1-copy-1` остановился на221-й строке калькулятора из-за бюджета
проверочного инструмента; temporary views отброшены и Logout подтверждён.
Аудитор читает широкие таблицы блоками по10строк. Повтор `...copy-2`: все7таблиц,
1005строк, настройки6узлов и явных output-mappings совпали с независимым расчётом.
Заключительный graph_after отказал: `Visible port identity is not rendered`.
Это `PARTIAL_DIAGNOSTIC`, а не самостоятельная приёмка исходного MiMo-пакета.
Копия закрыта с отбрасыванием только временных изменений проверочного сеанса.

После окончания аудита внесены изменения source:
- наблюдение графа при отсутствии SVG видимого native-порта ждёт две отрисовки
  и заново проверяет всю идентичность; максимум3чтения, без повторения жеста;
- первый компактный prepare выдаёт стандартный save_checkpoint, save_as остаётся
  доступен по запросу; устаревшая рекомендация node.add/link.create/save_as удалена
  из основного Hermes skill;
- schema калькулятора поясняет один вход нового узла и order только выражений;
- инструкции требуют сопоставить каждый результат задания с выполненным узлом.

Новый pin `10736c8c032696f5a4cb43122a6ad162d326e59097aaf4b823a50db10778da5f`.
52адресных теста PASS. Полный набор:1894PASS/1FAIL/1SKIP; единственный FAIL —
старый VM-fixture без requestAnimationFrame. Fixture дополнен моделью отрисовки,
30адресных тестов, включая сохраняющийся отказ отсутствующего порта, PASS.
Живой `audit-task21-fix1-copy-3` запущен с новым pin; это явно диагностическая
проверка, не аудит неизменного runtime исходного MiMo-прогона.

Ускорено также узкое чтение блоками с прокруткой до конкретной невидимой строки;
в copy-3 этот маршрут проверяется живьём. Полный Python-набор741PASS до последнего
теста ROI; новый независимый тест отношения сумм и всех500строк также PASS.
Папка следующего задания готова: `/mimo/MiMo-task19-diagnostic1-20260916`,
`storage-task19-diagnostic1/storage-proof.json`. Новая модель ещё не запускалась.

### Возобновление: причина отказа графа установлена живым воспроизведением

`audit-task21-fix1-copy-3` снова прочитал 1005 строк, но отказал на graph_after;
два кадра ожидания не устранили причину. Cleanup SUCCEEDED, временные изменения
проверочного сеанса отброшены. Дальнейший полный повтор до диагностики не делался.

Адресный probe `.dock/text-export/live-1789582728606` воспроизвёл ошибку одним
открытием/Cancel выходного мастера «Статистика по риску», без исполнения и таблиц.
Loginom 7.4.2 сохраняет два SVG представления порта: `shape.node` без data-tid,
а отдельный видимый SVG с прежним data-tid остаётся в графе. Это не исчезновение
native порта и не медленная отрисовка. Все GUID, владелец, cell, data и граф прежние.

Исправление readGraph сохраняет уже наблюдённую привязку native-порта и допускает
второе SVG только при совпадении этих идентичностей, единственном прежнем data-tid
и попадании его центра в ту же native cell. DOM не изменяется; ненаблюдённый,
заменённый, отсутствующий или чужой порт по-прежнему отклоняется.
Неудачный промежуточный probe `live-1789583261396` сохранён отдельно.

Итоговый адресный live `live-1789583406801`: выходные мастера всех пяти
аналитических узлов прочитаны/отменены; graph после каждого PASS; закрытие и Logout
подтверждены без save/discard. 32 адресных теста PASS, полный клиентский набор
1898 PASS / 1 SKIP / 0 FAIL. Ранее полный Python-набор742 PASS. `git diff --check`
чистый. Runtime `ba6d64730f291da6ecd6747e324922b952d8affb71a73192b534dce23e5ebdb2`.

`audit-task21-fix1-copy-4` запущен на этой зафиксированной версии для полной
проверки таблиц/настроек/графа. Это диагностика копии; исходный task21 остаётся
business FAIL и не превращается в автономную приёмку после замены runtime.

Полный `audit-task21-fix1-copy-4` завершён exit0. Независимое сравнение всех
7 таблиц / 1005 строк, настроек6узлов и graph_before/after PASS_DIAGNOSTIC;
`diagnostic-audit.json` явно сохраняет `autonomous_acceptance:false`.
Оригинальный task21 остаётся business FAIL. После завершения замороженной проверки
начата ограниченная реализация восстановления output_mapping калькулятора:
исходная квитанция Done, неизменный граф, повторное чтение формул и полей,
отмена проверочных мастеров, подтверждённая запись фазы перед продолжением.
64 адресных теста PASS; живой fault-injection ещё выполняется. Эта возможность
пока не считается принятой. Новых Hermes запусков после task21fix1 не было.

### Output mapping recovery: принята адресная потеря ответа

Реализация ограничена выходным портом0 калькулятора и подтверждённой исходной
квитанцией `output_port_finish_verified`. Resume перечитывает без изменений
формулы, входную связь и полную привязку выходных полей, закрывает проверочные
мастера через Cancel и принимает фазу только после подтверждённой записи журнала.
Изменённые формулы/поля/GUID, неизвестная квитанция, другой этап, истёкший срок
и неудачная запись оставляют блокировку. Модель получает продолжение по исходномуID.

Первый операторский имитатор не учитывал транспортную оболочку; его обычный успех
не засчитан как восстановление (`live-1789584167272`). Исправленный имитатор
в `live-1789584368336` действительно потерял ответ; проверка обнаружила, что
Loginom после Done меняет ID только исключённой строки-заглушки (18 -> 27),
а `exclusion_source.field_id=4` и все настройки прежние. Сравнение допускает это
только при точной уникальной привязке исключённой строки к исходному полю;
идентификаторы действующих полей по-прежнему сравниваются строго.

Итоговый live `.dock/text-export/live-1789584531635` PASS:
- ровно один исходный output Done (`done-gesture-audit.json`);
- исходный ID `output-recovery-1789584560615`, узел `cf65e0d3-3e39-4414-893d-bf1cc2a42045`;
- один новый узел и одна связь, ID-only resume, повтор возвращает тот жеGUID;
- 300строк, `VerifiedLoan` переименован, `credit_score` исключён;
- три возвращённые строки независимо сверены с CSV; полного числового аудита
  всех300строк эта адресная проверка не заявляет;
- диагностические изменения отброшены, пакет закрыт, Logout подтверждён.

Проверочный сценарий: `tools/loginom-acceptance/output-recovery.operator.js`,
запускается через `text-export-live.mjs --drop-output-done-once` только на копии.
65адресных тестов PASS; полный клиент1905PASS/1SKIP/0FAIL, diff-check чистый.
Runtime `667c339f8bf2da27728474f49249dfb08bb7f435ab91bcefab15da3bd4dc2944` заморожен
для следующего MiMo task19 в `/mimo/MiMo-task19-diagnostic1-20260916`.

### Task19 завершён; независимое открытие заблокировано стендом

MiMo run `20260916-215245-ac2adbb3`: подтверждён `xiaomi / mimo-v2.5 / medium`,
неизменный runtime `667c339f8bf2da27728474f49249dfb08bb7f435ab91bcefab15da3bd4dc2944`,
518.148секунд,30вызовов инструментов/31API,113634входных+11628выходных+2410624кэшированных
токенов (всего2535886). Шесть аналитических узлов сохранены через save_checkpoint
в `/mimo/MiMo-task19-diagnostic1-20260916/scenario.lgp`; штатное закрытие и Logout PASS.
Экспорт НЕ создан: сначала отклонены input mappings; исправленный запрос с x=1300
получил `Requested drop surface is not reachable`, три попытки без изменения графа.
Следующая живая диагностика — размещение за границей видимой области, с сохранением
явно заданных координат. Не исправлять это молчаливой подменой координат.

Подготовлены независимые ожидания7таблиц/1035строк. Аудитор теперь поддерживает
штатное отсутствие source_name и сравнивает настройки импортированных полей по
уникальным именам, поскольку запрос не задаёт порядок CSV. Явные переименования,
дубликаты, неверные типы и отключённые поля проверены адресными тестами.
Business review FAIL: вместо суммарного ROI канала дан средний ROI кампаний;
несортированная выборка10из20названа топом, третье место указано неверно
(Email+Gen_X571.08%, а не Influencer+Millennials355.11%); отрицательные кампании
не заменяют отрицательные каналы; влияние бюджета и ограничения года/активности
не подтверждены. Этот статус не зависит от ещё не завершённой проверки сохранения.

`audit-task19-diagnostic1-1` остановился до входа/изменений: браузер chrome-error,
PREPARATION_INTERRUPTED на page, HTTP52/000 Empty reply from server. Подтверждено
обычным и прямым HTTP без прокси. Evidence: `task19-reopen-network-block.json`.
Процессы Hermes и аудитора завершены. Версию client/plugins оставить неизменной
до независимого открытия оригинала в новом audit-task19-diagnostic1-2.
После восстановления доступа: все1035строк, настройки, граф до/после и cleanup;
затем исправить размещение и ошибки контрактов columns/enum/выбранной справки,
живой сбой доставки, задачи34/38 и итоговая серия38. Финальная серия не начата.

Локальные проверки аудитора после task19: полный набор **745 Python-тестов PASS**
(74.526секунды), `git diff --check` PASS. Новые модельные прогоны не запускались.
Повторная прямая HTTP-проверка снова получила Empty reply; аудит оригинала остаётся
заблокирован доступом к стенду.

### Доступ восстановлен, проверяется размещение

После сообщения пользователя прямой HTTP200 подтверждён. На том же runtime
запущен `audit-task19-diagnostic1-2` оригинала; первые500+5+20+112строк
независимо совпали. До завершения аудита client/plugins не меняются.

Отдельный diagnostic-placement-20260917.lgp создан из диагностической копии21.
Живой UI: `.dock/text-export/live-1789586433905`. Полотно1178×756 CSSpx,
origin324,100; координата1300за его границей. Штатная карта сценария содержит
`cnt;tlb;b` (Уменьшить масштаб); два нажатия меняют scale1→0.82644628.
Геометрия старых узлов остаётся прежней. Режим Hand не расширяет полотно,
если оно уже полностью видно. После преобразования координат ручной drag
создал узлы около заданной точки; выяснилось, что snap нужен в координатах
модели ДО округления экранных пикселей (на половине сетки y100 дал96).
Первое чтение native geometry непосредственно после drop ещё показывало
временное положение; окончательное1304,96 подтверждено следующим наблюдением.
Оба диагностических узла отброшены, Logout/cleanup PASS. Это исследование UI,
не приёмка автоматического исправления. Подготовлен прототип преобразования
и ограниченного UI zoom вне закреплённого runtime. Help interface/workflow.md
и E2E bg/utils/workflow.ts подтверждают штатный механизм масштабирования.

### Оригинал task19 независимо проверен

`audit-task19-diagnostic1-2/full-audit.json`: graph PASS, все7таблиц/1035строк
PASS, настройки6узлов PASS, полная сортировка PASS, cleanup SUCCEEDED.
Проверялся исходный сохранённый пакет на исходном runtime667c339, без изменения
настроек и без повторного сохранения. Общий результат задания остаётся FAIL
по отдельному business-review. Ошибка доступа к стенду снята.
После завершения аудита начато исправление координат с UI zoom; добавлены
точная ошибка settings.columns и допустимые значения enum; skill уточняет
обязательное получение карточки каждого нового типа. Адресные72теста PASS,
живое размещение экспорта на новой версии ещё проверяется.

Живое исправление размещения выявило связанные особенности карты сценария.
Scale меняет только node dom_epoch; проверка допускает эту смену лишь при
тождественных GUID/портах/координатах/связях и прежнем корне графа. После
масштабирования заново закрепляется полный снимок перед drag.
`live-1789587367158`: target verified, но открытие входа отказало из-за
скрытых SVG-копий портов в мини-карте. Привязка ограничена native graph.container.
`live-1789587621954`: target и input open прошли, Done выявил аналогичную
неоднозначность node-context; чтение графического контекста также ограничено
проверенным полотном. Дубликаты ВНУТРИ полотна остаются ошибкой.
Промежуточный `live-1789587575304` выявил ошибку NodeList/filter до эффекта;
исправлен spread, mock теперь возвращает коллекцию без Array.filter.
Каждый изменённый диагностический пакет закрыт с явным discard/Logout;
оригиналы не менялись. Полный набор до последнего node-context:1915PASS/1SKIP;
последние адресные46тестов PASS. Успех всего экспорта пока не заявляется.


### Масштабирование и автоматическое размещение: живые проверки пройдены

`live-1789588591547`, runtime `17b309e839790a4909c41706edecd5ca067f795c277c376786d109a20ceed4c2`:
экспорт за границей первоначального холста прошёл создание, связь, настройку,
исполнение и штатное скачивание. Запрошенная точка1300/100 штатно привязана
к сетке1304/104; Dock сам изменил scale1→0.82644628. Повтор operation_id
вернул тот же узел. CSV12957bytes/SHA113a6c48b17a945a100d9420eac71726d29fe93e6ed0fadf227c52d64ec8bbad
совпадает с независимо проверенным предыдущим экспортом: все300строк×8полей.
Следующий калькулятор без координат успешно создан на уменьшенном масштабе
в96/408, ровно один узел и одна связь добавлены. Пакет закрыт с явным discard,
logout SUCCEEDED; исходные сохранённые сценарии не изменены.

Дополнительно исправлено чтение выходных портов после открытия мини-карты:
скрытые SVG-копии вне native canvas не являются вторым портом. Проверки
однозначности внутри полотна сохранены;26тестов output context PASS.
Повторяемая операторская проверка: `tools/loginom-acceptance/placement.operator.js`.
Полный регрессионный набор выполняется; найден и исправлен устаревший mock
opaque UI identity в executor-fixture (20адресных тестов скачивания PASS).
Это техническая диагностика Codex; новой успешной автономной MiMo-приёмкой
не считается. Итоговые38прогонов ещё не начинались.


2026-09-16, MiMo placement regression: полный client suite **1919PASS/1SKIP**,0FAIL (1920tests,52.65s). Evidence `.dock/mimo-stability-20260916/client-suite-placement-outputs.txt`. Live export + same-ID replay + automatic calculator placement PASS на runtime17b309e. Общая автономная приёмка корпуса ещё не завершена; следующий диагностический прогон — task34.


### Task34: новый диагностический прогон

`20260916-230022-afe19c54`, отдельная папка
`/mimo/MiMo-task34-diagnostic1-20260917`, runtime17b309e, модель в session DB
`mimo-v2.5`. Вложение доставлено. Первый запрос импорта (finish done + read)
отклонён NOT_APPLIED; MiMo сама исправила его с новым ID import-csv-2.
Импорт500строк и калькулятор12формул SUCCEEDED. Дальнейший прогон и аудит
ожидаются. Не менять client/lib или plugin до завершения прогона и аудита
его сохранённого пакета. Проверочные расчёты хранятся отдельно и модели
не передаются. Нужна также более точная ошибка несовместимых finish/read:
пока parameter_path=null; это отмеченный недостаток, а не новое исправление.


Task34 выявила отдельное ограничение чтения: модель запросила sample_rows32
для32строчной группировки (отказ до эффекта), затем экспортировала полныеCSV,
затем запросила coverage=full (сейчас поддержан только Collapse, снова отказ
до эффекта). Экспорт code независимо проверен:32×14, max error5e-11, PASS.
Три экспорта SUCCEEDED; общего итогового вердикта ещё нет.
После завершения неизменяемого прогона/аудита нужно разобрать ограниченное
полное чтение небольших сводок через текущий механизм вывода, сохранив
байтовые/строчные бюджеты и компактные ответы. Не расширять поддержку
без прямой живой проверки. Также точнее локализовать finish/read и coverage
ошибки (сейчас parameter_path=null), не разрешая их противоречивую комбинацию.


### Task34 остановлена на подтверждённом техническом сбое

Прогон `20260916-230022-afe19c54` остановлен оператором после повторных
отказов восстановления grp-books,45вызовов/1781.76секунды. Девять узлов
SUCCEEDED, триCSV независимо сверены полностью:32×14,6×5,500×21. Пакет не
сохранён; cleanup BLOCKED/CLEANUP_OR_OPERATION_UNCONFIRMED. Browser process
после остановки завершён. Это FAIL диагностического прогона, не приёмка.
Runtime17b309e неизменён. Подтверждены43APIcalls,input166386/output28004/
cache3518720 из session DB; это неполная статистика после SIGTERM, usage-file
не сформирован. Reasoning очищен штатным приватным cleanup без чтения.

Причина: target400/900 после zoom оказался с видимой точкой сброса, но
частично скрытым входом. Нативная операция open_input_port вернулаNOT_APPLIED,
а созданный target уже имел эффект, поэтому вся операция осталасьAMBIGUOUS.
На отдельной копии `live-1789590704427` воспроизведено без модели: canvas
324/100,1178×756,scale0.826446; export node body655/838,46×86 и вход641/871,
12×20, вне нижней границы856. Диагностическая копия discard/logoutSUCCEEDED.

Исправление проверяет после создания реальные DOM-границы узла и портов,
добавляет ограниченный UI zoom при тождественном графе. Скрытые копии
мини-карты исключены также при выборе узла для rename/move.60адресных тестов
PASS; живой повтор новой версии выполняется. Новый модельный запуск не начинать
до этой проверки. Ограничение полного чтения группировок остаётся открытым.


### Проверка нижнего края завершена

`live-1789590999310`, runtimee5f37e677ca832572989f12733cc513beaf72d1e6a96e3f7378d357a0268ab94:
export400/900→400/904 PASS целиком, same-ID replay без дубля, CSV12957bytes
с прежним провереннымSHA113a6c48. После него калькулятор без координат PASS,
автоматическая позиция1056/120. Копия закрыта с discard/logoutSUCCEEDED.
Полная регрессия1921PASS/1SKIP,0FAIL (59.43s). Затем уточнены сообщения
ошибок read.ports/read.coverage с точным parameter_path;117адресных тестовPASS.
`placement.operator.js --placement-edge` воспроизводит адресный нижний край
через тот же harness. Следующий новый сценарий — task38; задача34 не принята,
её повтор отложен до разбора полного чтения небольших сводок. Общий плагин
не обновлён, main не изменён, итоговые38прогонов не начаты.


### Task38 выполняется

`20260916-234059-16a527d8`, pid13180, source0012d80b9f6180c4b49c97ae803e3457c5efdb1bffe30bf3257ab8154e104437,
отдельная папка`/mimo/MiMo-task38-diagnostic1-20260917`, MiMo2.5medium.
Вложение доставлено, DBmodel подтверждён. Код client/lib/plugins заморожен
до окончания этого прогона и независимого аудита. Task34failure-analysis.json
содержит компактное описание остановленного предыдущего прогона и ссылки
на прямое воспроизведение/исправление. Следующие открытые участки: полное
чтение небольших сводок и безопасная граница продолжения после отказа
port-open без жеста при уже созданном узле.


В task38 снова отклонён finish=done с read.ports=[0]; исправленная ошибка
теперь содержит read.ports и конкретное finish=execute, модель исправила ID.
Источник повторяемости обнаружен в `userNodeTool`: компактное описание
заменяет длинный текст node-api, где объяснялись Done/Execute/Close, но
свойство finish остаётся голым enum (done первым). Аналогично coverage enum
объявляет full без ограничения по типу в самом кратком контракте.
После завершения замороженного task38 нужно перенести обязательную семантику
в описания полей и условную схему, не возвращая длинную справку по всем узлам.
Это несогласованность компактного опубликованного контракта; нынешние
runtime-отказы безопасны, но лишние попытки можно предотвратить.

### 17 сентября: task38 diagnostic1 остановлен после сохранения

- Run `20260916-234059-16a527d8`, Xiaomi/MiMo2.5/medium, runtime
  `0012d80b9f6180c4b49c97ae803e3457c5efdb1bffe30bf3257ab8154e104437`
  неизменен. 47 вызовов инструментов, 1260.84 секунды; остановка собственного
  PID13180 после подтверждённых отказов восстановления, returncode=-15.
- Шесть аналитических узлов завершены, пакет
  `/mimo/MiMo-task38-diagnostic1-20260917/scenario.lgp` сохранён квитанцией
  `73e5abd8-e944-4545-8f13-84827bab0109`. Сохранение само по себе не означает приёмку.
- Повторный запрос к существующему калькулятору `node-calc-read-001` передал
  уже существующие выражения как новые. Конфликт имён обнаружен в открытом
  мастере; операция осталась AMBIGUOUS/configure. Новый ID заблокирован,
  inspect/recover/resume не дали продолжения. Штатная очистка
  `CLEANUP_OR_OPERATION_UNCONFIRMED`; это открытый дефект восстановления.
- Четыре попытки размещения вне холста отказали до создания узла:
  `Exact canvas zoom-out control unavailable`. Исправление нижней границы
  остаётся подтверждённым предыдущей диагностикой, но этот новый случай
  ещё не исправлен. В свежей диагностической копии кнопка с ожидаемыми
  tid/qtip существует; причина различия требует прямого воспроизведения.
- Уже прочитанные результаты показывают NULL в `month_number=INT(month_num)`:
  вход строковый. Нельзя считать INT преобразованием строки в число.
  Сортировка выбрана по строковому month_num: 1,10,11,12,2... . Средняя кривая,
  сравнение всех когорт и итоговый ответ не завершены.
- Оригинальный аудит `audit-task38-final-output-1` отклонён из-за блокировки
  исходного пакета. Перезапуск только собственного старого профиля показал
  форму входа, исходный сеанс не восстановился; профиль закрыт. Создана отдельная
  `diagnostic-copy.lgp`; текущее чтение полной итоговой таблицы на этой копии
  не заменяет приёмку оригинала.
- SQLite очищен от закрытых рассуждений без чтения. Частичные счётчики:
  47 API, 124037 входных, 16569 выходных, 4147328 кэшированных токенов;
  возможный незавершённый API-запрос не входит в эти значения.
- Независимый аудитор дополнен ограниченными integer Collapse, одиночным
  REPLACE, числовыми INT/ROUND. Строковый INT и неоднозначная середина
  округления отклоняются, а не получают выдуманный результат. Сверено с
  [Loginom Help](https://help.loginom.ru/userguide/processors/func/calc-func/mathematical.html).
  Python-набор: 749 PASS; дополнительный тест остановленного диагностического
  плана: 4/4 PASS. Новый явный `--diagnostic-saved-only` не даёт права приёмки
  и всё равно требует неизменного runtime и подтверждённого сохранения.

### Task38: проверка таблицы и исправление конфликта существующего калькулятора

- `.dock/text-export/live-1789592742646/task38-final-table-audit.json`:
  все 72 строки ×7полей итоговой таблицы диагностической копии проверены.
  Активные клиенты, знаменатели и округлённое удержание совпали полностью;
  month_number=NULL во всех72строках; строковая сортировка подтверждена.
  Граф чтением не изменён. Настройки всех промежуточных узлов не проверялись;
  копия не даёт приёмки оригинала. Собственная копия закрыта с discard/logout.
- Отказ поиска zoom пока не воспроизведён: в свежем сеансе
  `.dock/text-export/live-1789592973308` размещение1300/200 прошло и без
  предварительного открытия мини-карты. Проверка существующего Collapse
  не выявила дублирования кнопки. Обе гипотезы остаются недоказанными;
  исправление этого нового случая не заявлено.
- Ошибка валидации patch существующего калькулятора теперь допускает
  подтверждённый отказ до правки выражений: отмена мастера, повторный выбор
  того же узла, открытие и сравнение всех выражений/входных полей, отмена,
  квитанция журнала. Только после этого снимается блокировка. Новый узел,
  запрошенные связи/привязки, потерянные ответы и изменившиеся настройки
  не получают такое разрешение. Следующий шаг содержит исходный ID и
  требует исправленный запрос с новым ID к тому же существующему узлу.
- Первый прямой повтор `live-1789593423021` безопасно остался AMBIGUOUS:
  после отмены требовалось снова выбрать узел, иначе Settings не виден.
  Исправлен выбор; сеанс закрыт с проверкой сохранности, старые логи оставлены.
- Повтор `live-1789593573195`, runtime
  `fc7db5f18c241f63925404c486057f13dbcbfaf58cd3273a736a0b44f2bd8f1e`:
  конфликт FAILED/cleanup_complete=true; исправленный запрос expressions=[]
  SUCCEEDED, тот же GUID,72строки, прежние узлы/связи/координаты. Собственный
  диагностический пакет закрыт, изменения просмотра отброшены, logout PASS.
- Компактная схема сохранила семантику execute/done/close; условная JSON Schema
  запрещает done/close+непустые порты, порты экспорта и full для неподдержанных
  типов. Карточка калькулятора объясняет patch существующих выражений и
  expressions=[] для повторного выполнения без правки формул. Ошибки
  case_sensitive указывают конкретный ключ и требуемое действие.
- Полная клиентская регрессия после этих правок:1936PASS/1SKIP,0FAIL
  (`client-suite-calculator-refusal.txt`). Новый модельный прогон после этого
  исправления ещё не запускался. Итоговая серия38прогонов не начата.

### 17 сентября: воспроизведён и исправлен динамический ID мини-карты

- Продолжение по просьбе пользователя: масштабирование, завершение сеанса и
  последующие обнаруженные ошибки остаются активной работой в этой ветке.
- `live-1789594578273`: создание дополнительной группировки и калькулятора
  в копии task38 прошло. Затем отдельный новый черновик в том же изолированном
  браузере воспроизвёл `Exact canvas zoom-out control unavailable`.
  Прямой DOM/снимок показал видимую мини-карту `cnt-1`, кнопку `cnt-1;tlb;b`
  в `MF;TF-2;ModelForm;cntDiagram`; старый `cnt;tlb;b` принадлежал скрытой карте
  первого сценария. Это подтверждённый дефект глобального фиксированного ID;
  конкретный allocator ID в старом модельном task38 не записан.
- Поиск теперь ограничен контейнером текущего workflow и toolbar мини-карты;
  учитывает динамический префикс, проверяет единственность и точное назначение
  кнопки. Появление/закрытие ожидаются ограниченно. Связи и координаты по-прежнему
  сверяются до/после; чужая карта не используется.
- Сверено с E2E `tests/toreview/helpers/minimap.ts` (Toggle/Scale ждут появления)
  и Help `data/interface/workflow.md` (карта предназначена для масштабирования).
- `live-1789594988555`, runtime b71c6c342959c8ddb2feceddc9c7d53308daf0d170d60d22e3b72c01a7a22115:
  тот же второй черновик: два шага zoom, scale0.826446, точка1300/200 видна,
  собственная карта закрыта; первая карта не затронута. Оба диагностических
  сеанса завершены: черновики отброшены, копии закрыты, logout подтверждён.
- Адресные тесты43PASS. Полная регрессия выявила лишнее создание локатора карты
  даже без нужды масштабировать (два отказа тестового драйвера exports).
  Локатор теперь создаётся только при выходе за границу; адресный повтор12PASS.
  Старый неуспешный полный журнал сохранён, полный повтор запущен отдельно.
- При закрытии второго черновика вкладка исчезла раньше обновления списка
  пакетов. Диагностическая очистка корректно отказала PACKAGE_INVENTORY_CHANGED;
  после подтверждённого завершения native close повтор прошёл. Снятие блокировки
  без проверки списка пакетов не допускается.

### Закрытие после Save As: воспроизведён dirty-state после переоткрытия

- Полный повтор после lazy-локатора карты:1938PASS/1SKIP,0FAIL,
  `client-suite-outline-scope-2.txt`.
- Новый диагностический Hermes task38: `20260917-004756-ce501443`, Xiaomi
  MiMo2.5 medium, PID19702, папка `/mimo/MiMo-task38-diagnostic2-20260917`.
  Runtime cae3802fc3eccff3b3fdd19b08a30fd6ca5de7b7144076cf73d3ef8e3389b049
  заморожен; исходники не менять до завершения. Старые evidence сохранены.
- Отдельная прямая диагностика task21 в `live-1789595311471`:
  открытие собственной диагностической копии → `package.save_as` в новый путь
  → modified=false. После выполнения зависимостей, настройки существующей
  последней группировки теми же параметрами и точного чтения таблицы:
  `package.save_as` снова SUCCEEDED, но IsPackageModified после переоткрытия=true.
  Это воспроизвело UNSAVED_CHANGES старого task21 без участия модели.
- `package.save_checkpoint` с replace только для только что созданного своего
  пути подтвердил modified=false. Строгая очистка без diagnosticDiscard:
  package_closed=true, logged_out=true, unsaved_changes_discarded=false.
  Диагностический браузер завершён. Это подтверждает доступное продолжение,
  но ещё не исправление автоматического сообщения модели о dirty-state.
- Неудачная повторная подготовка диагностического harness с тем же ID и новым
  путём корректно вернула OPERATION_CONFLICT; оператор исправлен на новый ID.
  Отказы входной группировки без inputs и с неактивным источником также остались
  до изменения узла; после явного выполнения зависимостей повтор прошёл.
- Следующая правка после разморозки: подтверждённое чтение modified-state после
  сохранения/переоткрытия, точное продолжение checkpoint при dirty=true; нельзя
  молча отбрасывать изменения или объявлять их только настройками просмотра.

### Task38 diagnostic2: новый синтаксический отказ и проверенное восстановление

- `20260917-004756-ce501443`:25вызовов,530.05секунд, runtime неизменен,
  фактическая модель mimo-v2.5. Импорт и Collapse завершены; калькулятор с
  `Int(Mid(Names, 3))` отклонён Loginom: неизвестная функция MID. configure
  остался AMBIGUOUS; inspect/recover/resume не дали продолжения. Собственный
  PID19702 остановлен SIGTERM после подтверждённого блокера. Пакет не сохранён,
  teardown BLOCKED; оставшихся процессов его профиля не обнаружено. Private
  cleanup PASS:25reasoning и25reasoning_content очищены без чтения. Это FAIL,
  не успешная приёмка и не часть итоговой серии.
- `live-1789595912775`: Codex воспроизвёл MID-отказ напрямую. Отмена мастера
  с подтверждением, повторное открытие того же калькулятора вернули исходный
  пустой Expr1; диагностическая копия закрыта с discard/logout.
- Реализована только доказанная граница синтаксической ошибки: квитанция
  WIZARD_CALCULATOR_VALIDATION_FAILED + свежая та же ошибка того же мастера,
  отмена, повторное открытие, полное сравнение выражений/входных полей, повторная
  отмена и сравнение графа. Лишь затем подтверждённый FAILED/cleanup=true.
  Изменённый запрос получает новый operation_id и тот же existing node;
  для нового калькулятора подсказка явно указывает восстановленный Expr1.
  Ошибки транспорта, чужой узел, изменившиеся настройки/граф или журнал
  сохраняют неопределённость. Связи/входы, завершённые до configure, не откатываются.
- Первый live-повтор `live-1789596367196` безопасно отказал при journal ack:
  избыточный UI в доказательстве проходит нормализацию redactor. Квитанция
  сокращена до фактов валидации; добавлен адресный тест нормализации журнала.
  Старый отказ и его logs сохранены; собственная копия закрыта.
- `live-1789596546671`, runtime5732c3fc3111ab20c78fd5260086f4e2026e38791826d26909e2d0e0856684e9:
  ошибочная формула FAILED с завершённой очисткой; исправление Expr1 новым ID
  SUCCEEDED на том же GUID,72строки, прежний граф. Три точных прочитанных
  значения PlacementProbe совпали с active_count. Это адресная проверка
  восстановления; полная бизнес-задача38 здесь не выполнялась.
- Добавлен read-only post-save probe: проверяет документ, сессию, account,
  активный собственный пакет, native identity до/после IsPackageModified.
  Save receipt не изменяется. Отдельный dock_saved_package_state сообщает
  modified=true и один следующий checkpoint того же пути с новым ID.
  При потере read-ответа сообщает unknown, не превращает завершённый save
  в неизвестную запись и не разрешает discard. MCP lifecycle-тесты7/7PASS.
- Тот же живой сеанс воспроизвёл dirty=true после Save As; probe выдал
  checkpoint, тот подтвердил modified=false. Строгое закрытие:
  package_closed=true,logged_out=true,unsaved_changes_discarded=false.
  Новый файл `/mimo/MiMo-task38-diagnostic1-20260917/syntax-recovery-persistence-1789596656923.lgp`.
- Полная клиентская регрессия1960PASS/1SKIP,0FAIL. После неё только уточнены
  описание формул (Int принимает число, Val разбирает строку с локалью,
  SubStr с тремя аргументами; Help string.md/mathematical.md) и сообщение о
  полностью NULL-полях именно прочитанной выборки. Адресные проверки этих
  изменений прошли; предупреждение не выбирает аналитику и не обобщает выборку.
- Готовится отдельный diagnostic3. До новой автономной проверки улучшение
  полной аналитической успешности MiMo не объявляется; итоговые38не начаты.

### Task38 diagnostic3: край холста отклоняется до создания узла

- `20260917-011818-215198d2`: MiMo2.5, runtime3c17de6b неизменен,
  13вызовов/343.63секунды. Модель явно задала position8/8. Импорт создался,
  но подпись/порты вышли за верхний/левый край. После сверки созданного узла
  переименование отказало `Target is covered`, два resume повторили отказ.
  Остановлен собственный PID23034; save отсутствует, cleanup BLOCKED,
  usage после SIGTERM неполон. Старый неуспешный прогон сохранён.
- Прямая проверка `live-1789597235533` воспроизвела создание8/8 и отказ
  полной видимости узла: zoom до0.3855 не решает отрицательную границу.
  Стандартный режим навигации/drag при scroll0 также не сместил видимую область;
  координаты графа остались прежними. Диагностические черновики закрыты,
  копия закрыта с явным discard, logout подтверждён.
- Опубликованная схема и внутренний контракт теперь согласованно требуют
  координаты64..10000. Ошибка содержит точный путь и совет опустить position
  для автоматического размещения. Явные координаты не подменяются. Это ограничение
  на техническую геометрию, не изменение аналитики. Проверка происходит до
  источника, браузера и журнала операции. Адресные94PASS; live-повтор выполняется.
- В той же диагностической копии проверена техническая возможность вертикального
  чтения: bound grid scrollTop0→500, строка40 стала доступна и сохранила native
  identity. Текущий normal read ограничен10строками и не выдаёт scroll action
  для Table. Расширение чтения сводок ещё не реализовано; Help описывает
  навигацию мышью и страницы по1млн строк. Raw/RPC чтение не добавлялось.

- Live `live-1789597746524`, runtimea0bb4bb6:8/8 отклонено на публичном MCP
  до браузерных изменений, полный граф после отказа совпал. Исправленный запрос
  с новым ID и без position SUCCEEDED: ровно1узел/1связь, без дублей.
  Первый оператор ожидал квитанцию вместо исключения schema validation;
  исправлен только оператор проверки, продукт не менялся.
- Полная регрессия после координат:1963PASS/1SKIP,0FAIL. Старые журналы сохранены.

### Подсказка пользователя: автоматическое упорядочивание

- Проверено на живом Loginom7.4.2 в отдельной копии:
  `.dock/text-export/live-1789598032598/layout-existing-graph.json`.
  Одно нажатие точной кнопки текущего workflow `btnLayout` переместило6узлов;
  полный граф7узлов/5связей совпал после исключения координат и DOM epoch.
  Идентификаторы, типы, подписи и состав портов сохранены. Настройки вычислений
  этим сравнением отдельно не доказываются; общий пользовательский пакет не менялся.
- В новом собственном черновике создан диагностический узел8/8 в обход
  публичной схемы для воспроизведения старого дефекта. Auto layout переместил
  его в120/40. После слишком сильного zoom одна граница ещё не проходила8px
  запас; отдельный native «Подобрать оптимальный масштаб» вернул scale1 и
  полностью видимые подпись/порты. Это перспективный путь восстановления
  геометрии, но не автоматическое снятие старого pending.
- Сверено с Help interface/workflow.md и E2E workflow_toolbar.ts516–549;
  issue648 отдельно проверяет сохранение связей при повторных нажатиях.
  E2E указывает исключения для узлов-ссылок. В текущий обычный путь новая
  кнопка пока не включена: она меняет координаты всего графа и требует
  отдельного эффекта/квитанции, сверки всех узлов/связей и восстановления
  при потере ответа. Нельзя использовать samePlacementGraph с игнорированием
  координат для других жестов или молча менять явно заданное положение.


### 17 сентября: diagnostic4 и быстрый просмотр выходного порта

- Diagnostic4 `20260917-013651-f2fd29b4`: фактическая модель mimo-v2.5,
  44 вызова, 1048.51 секунды, завершён оператором SIGTERM после повторов
  node-sort-2 без продвижения. Runtime исходного прогона не менялся.
  Экспорт завершён; отдельные reasoning-колонки очищены без чтения.
- Соединение отказало до mouse.down: `Node target link DOM binding changed`.
  В живой отдельной копии `live-1789598432955/link-hover-probe.json`
  воспроизведено: уход указателя с выбранного узла меняет его SVG/DOM epoch.
  Это не изменение аналитического графа. Добавлено предварительное наведение
  на исходный порт, затем полная сверка графа (включая координаты и связи)
  и привязка новых экранных элементов. Адресные тесты36/36PASS;
  живой полный проход исправления ещё проверяется.
- Исправлен повторный выбор уже выбранного узла перед открытием Таблицы:
  его тело может быть закрыто собственными кнопками наведения. Используется
  подтверждённый node_selected; если выбора нет, прежний жест сохранён.
  Тесты14/14PASS; прототип ранее прошёл живое чтение72×7 значений.
- По предложению пользователя открыт именно «Быстрый просмотр» двойным
  щелчком по выходному порту. В `quick-port-read.json` той же диагностики
  наблюдались7 полей, общий счётчик72 и57 отрисованных строк.
  Открытие окна465мс не является временем полной загрузки: первый ответ
  ещё содержал маску. Значения после загрузки прочитаны отдельно.
  Признак полноты нельзя вывести только из счётчика: нужен охват всех
  строк/полей и привязка к конкретному порту/выполнению.
- Help visualization/preview/quick-view.md подтверждает ограничения
  округления чисел, отображения времени и размера набора. Быстрый просмотр
  — кандидат для промежуточных проверок; точные итоги требуют точного чтения.
  Ускорение полного сценария не измерено. Автоматическое переключение
  маршрута в продукт пока не включено.
- Частный прототип candidate-preview прочитал72×7 значений с прокруткой,
  совпадение с независимым аудитом подтверждено, исходные форматы восстановлены.
  Расширение normal read до100 строк ещё не перенесено в клиент.
- Диагностическая копия закрыта с разрешённым discard, logout подтверждён.
  Основная серия38 прогонов остаётся не начатой.


### Перенос чтения100 и уточнение hover-проверки

- Прототип table-output-pages/node-table-context/workspace-ui перенесён
  в клиент с относительными импортами. Публичные request/result schemas
  и валидатор согласованы на0..100; default10 и native full50×8 сохранены.
  Лимит ячеек/байтов передаётся в limitations, sample_complete не повышается.
  Адресные105PASS; старая граница11 в двух тестах заменена на101 с проверкой100.
- На live-1789599669953 новое соединение, конфигурация и выполнение прошли;
  чтение выявило условие selected-without-view-controls. Пропуск выбора теперь
  требует и selected, и доступного open_node_views. Отдельное открытие прошло,
  но оператор сравнения ожидал порядок полей другого узла и отказал; это не
  доказательство полного публичного успеха.
- Live-1789599915969 выявил временную загрузочную маску внутри hover-проверки.
  Добавлено ограниченное ожидание её исчезновения с полной повторной проверкой
  исходной подготовки. Чужой сценарий не повторяется. Адресные15PASS.
- Все завершённые диагностические копии закрыты с подтверждённым logout.
  Новый живой проход выполняется на d62d6325 в live-1789600098341.


### Подтверждённый публичный проход после исправлений

- `live-1789600098341`, runtime `d62d6325655c2b6aff4626f7c715074cfa43caca0adff48094f7691775c37806`:
  `hover-preview100-public.json` PASS через штатный public node.apply.
  Ровно1новый узел/1связь; конфигурация, выполнение, чтение72×7,
  sample_complete=true. Возврат из Таблицы подтверждён.
- `preview100-independent-audit.json` PASS: все значения и кратности строк
  совпали с прежним независимым полным чтением после сопоставления по именам
  полей. Это техническая проверка диагностической копии, не аналитическая
  приёмка задачи38: старый ошибочный month_number не исправлялся этим тестом.
- Итоговая клиентская регрессия1993PASS/1SKIP,0FAIL;
  `.dock/mimo-stability-20260916/client-suite-preview100-final.txt`.
- Готовится отдельный MiMo diagnostic5 на неизменном runtime, без подсказок
  готового графа/эталонных ответов. Основная серия38не начата.


### Измерение стоимости чтения и следующий эксперимент

`live-1789600098341/phase-timings.json`: вся публичная операция67.051с,
read41.814с; сюда входят открытие Таблицы, настройки точности, чтение,
восстановление форматов и возврат. Это один замер на72×7, без обобщения на корпус.
Быстрый просмотр ранее открыл окно за465мс, но ещё показывал загрузку;
сравнивать эти465мс с полным read некорректно.

Следующая оптимизация: проверить короткий путь через выходной порт для
промежуточного просмотра при явно допустимой неточной числовой репрезентации.
Сохранить привязку к узлу/порту/последнему выполнению, общий счётчик,
фактический охват строк и столбцов, NULL/пустые значения и ограничения точности.
Точное чтение итогов и независимый полный аудит остаются обязательными.
Перед включением сравнить полную задержку от открытия до проверенных данных
на том же выходе. Этот маршрут пока не реализован.

MiMo diagnostic5 запущен: `20260917-021136-8dba3f97`, PID29277,
отдельное проверенное хранилище `/mimo/MiMo-task38-diagnostic5-20260917`.
Runtime на время прогона заморожен; используется Xiaomi/mimo-v2.5/medium.


### Сопоставимый объём: первые10 строк одного выхода

На отдельной копии `live-1789600577362` выполнено одно и то же сохранённое
вычисление. `quick-port-timing.json`:775мс от двойного щелчка по порту до
10 строк с проверенными индексами/recordID и подписи `Всего строк 72`.
`exact10-timing.json`:27983мс — обычное точное чтение тех же10 строк через
Таблицу, включая настройку/восстановление форматов и возврат в сценарий.
Оба замера относятся к одному выходному GUID. Быстрый замер — диагностический
DOM-прототип, без всех production-проверок, отдельный возврат в его время
не включён; он не доказывает36-кратного ускорения готового продукта.
Результат подтверждает перспективность сокращения технических шагов
промежуточного чтения. Числовая точность и полнота остаются разными свойствами.


### Diagnostic5: уточнение поздней перерисовки

- `20260917-021136-8dba3f97`:32 вызова,849.66с; фактическая MiMo2.5,
  runtime неизменен. Импорт596 строк, unpivot, фильтр,2группировки,
  соединение и калькулятор завершены. Модель сама исправила пустую policy
  и case_sensitive с новыми operation_id; группировка прочитала20строк
  вместо прежнего отказа по лимиту10.
- Sort002 снова заблокирован на connect до mouse.down: DOM изменился уже
  после предварительного наведения. Прогон остановлен; экспорт/очистка
  приватных reasoning-колонок PASS, без чтения рассуждений. Это не успешная
  автономная приёмка. Итоговая сохранность пакета не подтверждена.
- Исполнитель теперь непосредственно перед каждым доэффектным жестом
  соединения получает полный свежий граф через внутренний фиксированный
  reader. Совпасть должны документ, workflow, контейнер, GUID, типы, подписи,
  координаты, порты, связи, готовность. Только после этого переиздаются DOM
  epochs; немедленная проверка DOM перед жестом сохранена. Модель не может
  передать reader или обход проверки через публичный JSON-контракт.
- `live-1789601343853/late-link-evidence.json`, runtime3d207686:
  диагностический оператор намеренно вызвал реальную hover-перерисовку
  между подготовкой и link.create. В trace подтверждены две замены SVG
  одного и того же узла, full_graph_unchanged=true. Создана ровно1связь,
  узлы не дублировались. Собственная копия закрыта, logout подтверждён.
- Отрицательные тесты проверяют изменения документа/workflow/контейнера,
  GUID/типов/подписей/координат/портов/связей и незавершённое перетаскивание.
  Полная регрессия2008PASS/1SKIP,0FAIL; diff --check PASS.
- Новый автономный запуск после последней правки пока не выполнен. Быстрый
  портовый reader ещё прототип; серия38 и полная аналитическая приёмка остаются.
- Дополнительный аналитический сигнал diagnostic5: калькулятор вернул57строк
  после предварительной фильтрации активных клиентов, тогда как независимый
  oracle содержит72 комбинации когорта×месяц, включая нулевую активность.
  Требуется проверить полноту матрицы и восстановление нулевых комбинаций
  в последующей аналитике; успешный технический узел этого не доказывает.


Уточнение оценки скорости: в diagnostic5 MiMo не запрашивала полную числовую
точность. По фактическим phase timestamps текущий read занимал6.069–7.803с
на один порт (фильтр с двумя портами14.419с). Поэтому сравнение0.775с с27.983с
не отражает ускорение обычного MiMo-прогона:28с относится к exact-number пути.
См. diagnostic5/read-timings.json. Потенциальная экономия короткого просмотра
составляет несколько секунд на порт; влияние на полное время надо измерять.


Полнота чтения и полнота решения различаются: sample_complete означает,
что прочитаны все строки конкретного выхода. Он не доказывает, что модель
включила все требуемые разрезы, не потеряла нулевые группы или выбрала верную
формулу. Проверка задачи38 по независимому oracle должна отдельно проверить
72 комбинации6когорт×12месяцев и среднюю кривую, даже если меньшая таблица
прочитана полностью. Это не основание отдавать oracle/готовый граф модели.


Следующий подтверждённый пробел восстановления (пока не исправлен):
link.create возвращает FAILED/preconditions/effect_possible=false/cleanup=true,
а graph coordinator обрабатывает безопасный отказ только как NOT_APPLIED.
В diagnostic4/5 это оставляло pending после уже созданного узла. При доработке
нужно нормализовать только подтверждённый доэффектный отказ, сохранить исходную
квитанцию, заново доказать неизменность полного графа и выдавать новый ID жеста.
Потеря ответа, побочный эффект, чужой граф и неподтверждённый cleanup не должны
разрешать повтор. Изменения делать после завершения текущего закреплённого MiMo.


Частный recovery-прототип:35 адресных тестовPASS, включая потерю ответа
подтверждённого доэффектного отказа и запрет восстановления при чужом графе,
неполной квитанции или неверном ID. Native-проверка пока не завершена:
первый fault-injection изменил внешние options вместо исполняемого expected_build
и был исправлен только в операторе. Следующая попытка выявила отдельный отказ
`Exact tabular port disappeared`: graph reader допускает проверенный кеш порта
при перерисовке без data-tid, а link-binding resolver читает только новый SVG.
Не считать recovery-прототип принятым или этот отказ исправленным. Source
активного diagnostic6 не изменялся.

### Diagnostic6: широкий точный просмотр, 17 сентября 2026

Run `20260917-023256-63384d3d`, runtime `3d2076862389b6269c87a50713d180cb01da661ca7521de5350a7909002e371f`: фактическая модель `mimo-v2.5`, source unchanged, 24 tool calls, 816.943 s. Остановлен собственный PID30887 после подтверждённого блока; export complete, private cleanup PASS (24+24 reasoning fields cleared unread). Финальный пакет не подтверждён; расход токенов отсутствует в итоговом usage и не оценивается.

Создание/соединение калькулятора прошло. Его 12 формул настроены за24.581s, выполнение подтверждено. Чтение остановилось через120s на настройке/проверке точности широкой таблицы. Это не недостаток оставшегося общего времени: read получил полные120s. До остановки записаны1069 внутренних шагов (не модельных вызовов). В проверке форматов каждый столбец повторно выбирал первый столбец и возвращался к нужному; на широкой таблице это вызывает лишние прокрутки. `node.apply` остался pending/read, доступного завершения не было; новый запрос и recover не снимают блокировку.

Изменён обход проверки форматов: следующий столбец сам завершает редактирование предыдущего, отдельное переключение нужно только для уже выбранного проверяемого столбца. Проверка сохранённой маски сохранена. 15 focused tests PASS, включая27полей и отрицательный случай потерянного сохранения маски. Native28column reproduction выполняется отдельно в `live-1789602562785`; до её результата исправление не считается принятым.

Уточнение производительности: quick view775ms и exact Table27983ms измерены на одном10строчном диагностическом просмотре. В diagnostic5 MiMo обычно выбирала non-exact Table, который занимал6–8s/порт. Эти числа не означают кратного ускорения всего сценария; quick view пока не включён в основной маршрут.

### Проверенные обход форматов и восстановление связи — 17 сентября, продолжение

- Native `live-1789602714729`: полная публичная операция создала1калькулятор/1связь, настроила21диагностическое выражение, выполнила и прочитала10строк×28полей. Read114.476s, операция183.310s, original120s read cap ещё соблюдён. Независимый аудит210ячеек Probe=1/3 PASS. Общих строк72, `sample_complete=false` сохранён честно. Это техническая проверка, не приёмка задачи38.
- Восстановление native FAILED/preconditions с effect_possible=false проверено с намеренно неверным expected_build (реальный Loginom не менялся): немедленный отказ и потеря ответа завершились exact1node+1link каждый, отказ graph:2 → успешный новый graph:3. После отказа сверены полный граф, доменная идентичность, cleanup и оригинальная квитанция. Unknown/changed graph не разрешаются. `refusal-binding-v3-{immediate,lost}.json`,43focused PASS.
- При этой проверке устранена несогласованность двух читателей портов: соединение теперь получает nativeиндексы из полного проверенного readGraph, включая его строгий cachefallback; повторного SVG-only поиска нет. Границы владения/Guid/nativecell/hit-test сохранены, ожидание временной маски также переиспользовано. Первый v2/native отказ на временной маске не замалчивается; v3 оба случая PASS.
- Исходники перенесены после завершения браузерного оператора. Чтение теперь ограничивается оставшимся total_ms, а не configure_ms; 2теста проверяют возможность чтения дольше configure_ms и отказ после total_ms. Схема бюджетов и общий лимит не увеличены.
- Full client2021PASS/1SKIP/0FAIL (`client-suite-target-read.txt`), focused124PASS; нормализация временных абсолютных импортов проверена повторно45PASS. `git diff --check` PASS. Новый source-only nativecheck с configure_ms100000/total_ms300000 выполняется отдельно; Hermes пока не перезапущен.
- После первой неправильной позиции диагностического узла возникло перекрытие порта; после отказа собственная копия закрыта с discard/logout. После полного wide/refusal теста собственная копия также закрыта/logout, временные узлы не сохранялись. Исходные пакеты Hermes не изменялись.

### Заключительная адресная проверка перед diagnostic7

`live-1789603364355`, source `2a9f485db7743b37bbf243314beb19aff15bd4eeb3ff1ab87ad3e4b4aadfda06`: публичный node.apply с configure_ms100000/total_ms300000 прошёл за179.282s; read114.724s. Это живое подтверждение независимости чтения от лимита настройки. Точные210числовых ячеек проверены повторно; `wide-budget-independent-audit.json`. Пакет-копия закрыт/logout, диагностические изменения отброшены.

Дополнен отдельный путь inspectNodeTarget: read-only inspection теперь узнаёт тот же подтверждённый FAILED/preconditions и сохраняет checkpoint для явного resume. До этого внутреннее prepareNodeTarget продолжало корректно, но публичная обёртка могла удерживать pending. Native private probe с обязательным inspect перед lost-reply resume прошёл: один новый узел/связь в каждом случае, ID graph:2→graph:3. Source runtime-тест проверил настоящий runNodeApply→inspect→runNodeApply(resume), без жестов во время inspect и без дублирования создания;91focused PASS. Потерянный ответ до подтверждённой квитанции остаётся заблокированным. Last_graph после подтверждённого отказа обновляется проверенным графом, чтобы inspect не сравнивал старые DOM-эпохи.

Подготовлен следующий diagnostic7 на неизменной версии после финальных регрессий. Конфигурации общей установки не менялись; итоговая38runсерия по-прежнему не начата. Ошибка таймаута чтения с уже открытым мастером и неподтверждённым cleanup пока не имеет отдельного восстановления: новая политика времени снижает этот риск, но не снимает неизвестный результат автоматически.

### Diagnostic7 запущен после финальных проверок

Run `20260917-031027-d0ee7343`, PID33863, wrapper57767, storage `/mimo/MiMo-task38-diagnostic7-20260917`, source `b637a1d5b2c11e8bdd60315ff8cd1e58d42b38e5c13fea459d4971f510bed7fc`. Финальная проверка клиента2024PASS/1SKIP/0FAIL (`client-suite-inspect-final.txt`), MCPpreflight23tools PASS. Только Xiaomi/mimo-v2.5/medium, исходное description.md и штатное dataset.csv-вложение. Во время прогона client/plugin source заморожены. Model-start подтверждён; итог и сохранение ещё ожидаются.

Параллельно добавлен отдельный operator-only `audit_retention_result.py`: полная матрица12месяцев с нулевыми сочетаниями, уникальность/порядок, средняя кривая с явно проверенной weighted/unweighted семантикой. Числовая точность и полнота берутся из независимого UIчтения. Допуск округления разрешён лишь явно по проверенной формуле, не по округлённому отображению. Сохранение, provenance, сравнениеM3/M6, выбор липких когорт и рекомендации остаются отдельными обязательными проверками.5новых tests+7oracle+3wide expectation PASS. Аудитор не отправляется Hermes и не доказывает бизнес-успех текущего незавершённого прогона.

### Счётчики прерванных запусков

Новый operator-only `scenario_usage.py` читает только whitelist числовых session counters и идентичность provider/model из SQLite; prompts, model prose и reasoning не выбираются. Тест с SQLite authorizer запрещает чтение чувствительных столбцов и проходит; ещё проверены несовпадение профиля, неизвестная схема, отсутствие сессии, запрет active-run backfill и перезаписи sidecar.3tests PASS.

Для diagnostic4/5/6 добавлены отдельные `usage-recovery.json`, исходные evidence не перезаписаны. Diagnostic6:24APIcalls, input248399/output8214/cache-read1382912; billing_provider=xiaomi/model=mimo-v2.5 подтверждены. Это накопленные счётчики Hermes, незавершённый последний APIвызов может отсутствовать. Cache/reasoning считаются отдельно; общий total и денежная стоимость не вычисляются. Предыдущая запись об отсутствии итогового usage.json остаётся верной; числовые показатели теперь доступны из другого явно указанного источника.

## Diagnostic7: сохранение прошло, бизнес-проверка выявила ошибки — 17 сентября 2026

MiMo `20260917-031027-d0ee7343` завершила автономный прогон:21вызов инструмента,
908.223s, фактический Xiaomi/mimo-v2.5. Source `b637a1d5…` не изменялся.
Созданы импорт, группировка, калькулятор и экспорт. Пакет сохранён в
`/mimo/MiMo-task38-diagnostic7-20260917/scenario.lgp`; исходная сессия закрыта,
logout подтверждён без отбрасывания изменений. Два ошибочных запроса экспорта
исправлены моделью до побочных эффектов; дубликатов не создано.

Независимая сверка экспортированного CSV:6строк×26полей PASS. Матрица в ответе:
72значения PASS; сравненияM3/M6 и шесть показателей липкости численно верны.
Но средняя кривая создана в тексте, не в узлах:7из8приведённых значений неверны,
ещё4месяца отсутствуют. Одно снижение названо ростом. Бизнес-приёмка FAIL,
техническое завершение не засчитывается как выполнение всей задачи.

Первое независимое переоткрытие прочитало596×16,6×14,6×26 и настройки трёх
аналитических узлов. Аудитор остановился на экспорте вне правой границы окна;
это дефект аудитора. Его cleanup/logout PASS. Добавлено ограниченное раскрытие
точного узла штатным масштабированием с проверкой неизменности полного графа;
3адресных теста PASS. Повторный полный аудит выполняется в отдельной директории
`task38-diagnostic7-independent2`, на прежних неизменных client/plugin исходниках.

Итоговая серия38прогонов не начата. Изменения только в отдельной ветке;
main, общий плагин и сервер не обновлялись.

---


### Завершение независимого аудита diagnostic7 и подготовка diagnostic8

`task38-diagnostic7-independent2/independent-audit.json`: полные596×16,
6×14 и6×26 PASS; настройки всех4узлов PASS; матрица72ячейки PASS;
CSV6×26 проверен отдельно. Граф до/после неизменен, cleanup/logout PASS.
Экспорт раскрыт двумя штатными уменьшениями масштаба; узлы не перемещались.
Полная бизнес-приёмка diagnostic7 FAIL из-за средней кривой и неверной рекомендации.

После закрытия аудитора основной Hermes skill сокращён41647→10581байт.
Подробности обработчиков доступны через закреплённые карточки; диагностический
профиль сохраняет маршрут к своему полному skill/cache. Удалены устаревшие
подсказки обязательных координат и10строк. Введено требование вычислять новые
числовые показатели отчёта в узлах; это инструкция, её эффект пока не доказан.

Сложное общее условие ошибки экспорта разделено на точные пути mode/read/mappings/
inputs и допустимые значения.37focused tests PASS;3теста аудитора+25Python audit
PASS. Skill validator и git diff --check PASS. Общий client suite выполняется.
Отдельная папка `/mimo/MiMo-task38-diagnostic8-20260917` создана и проверена в UI.
Следующий запуск получает только исходное задание и штатное вложение; предыдущие
ответы, расчёты аудитора и готовый граф модели не передаются.

## Diagnostic7 проверен; diagnostic8 запущен — 17 сентября 2026

Независимое переоткрытие diagnostic7 завершено: все3таблицы596×16/6×14/6×26,
настройки4узлов, связи, матрица72ячейки и отдельный CSV прошли проверку.
Закрытие пакета/logout подтверждены. Бизнес-приёмка FAIL: средняя кривая
не вычислена в узлах, неполна и неверна в ответе; есть ложное описание роста.

Основной Hermes skill сокращён41647→10581байт; новые показатели предписано
вычислять в узлах. Уточнены пути ошибок экспорта. Клиент2025PASS/1SKIP/0FAIL,
37focused PASS, skill validation PASS. Исправлен аудиторский выбор экспорта за
границей окна: native2zoomsteps, граф неизменен,3адресных тестаPASS.

Новый diagnostic8 `20260917-035258-bd5157de`, PID36165, source
`65b3648ceceec6c645a11d40998453def76383ddfe7095a9fc2d95a33387d003`, запущен
на Xiaomi/mimo-v2.5/medium после23toolspreflight. Отдельная папка
`/mimo/MiMo-task38-diagnostic8-20260917`; только исходное задание и вложение.
Исходники заморожены на время запуска. Эффект сокращения инструкций пока не доказан.
Итоговая серия38прогонов не начата; main и общий плагин не изменялись.

---


### Diagnostic8: выявлена новая несогласованность контракта калькулятора

Run `20260917-035258-bd5157de` остановлен после повторяющегося отказа до эффекта;
18toolcalls/574.320s,sourceunchanged. Импорт596строк и группировка6когорт прошли.
4запроса калькулятора отказали: сначала отсутствовалreplace, затем лишнийtarget.name.
Опубликованная схема разрешала оба варианта; исполнитель их запрещал. Попытка
inspect уже отклонённого запроса вернула unknown operation. Финального сохранения
нет, strict cleanup BLOCKED/CONFIRMED_SAVE_REQUIRED. Это неуспешный прогон;
не считать закрытие/logout подтверждёнными. Numeric usage сохранён append-only
в usage-recovery.json; исходные журналы и закрытые данные рассуждений не читаются.

После завершения source-процессов схема выражения дополнена условными правилами
new/existing; ошибки исполнителя возвращают точный индекс и поле.37focused tests
PASS, включая обе записанные ошибки и сохранение частичногоexistingpatch.
Полныйclient suite и nativeотказ→исправление выполняются; Hermes не перезапущен.

Первый запуск интерактивного диагностического harness безTTY дошёл доEOF и закрыл
браузер без nativecleanup; следующая попытка обнаружила старуюкопиюreadonly.
Добавлен отказ до браузера при отсутствииTTY (pipe допускается только явно).
Для nativeпроверки создана отдельная `contract-diagnostic-copy.lgp`; старуюкопию
не разблокировали и не изменяли. Новыйоператор `live-1789607263587` исполняет
собственнуюкопию; после проверки требуется подтверждённый close/logout.

### Контракт калькулятора: live и записанные запросы проверены

Source `b265f979dac9f214280f931e854d6ff754d1db99c6946f02d54a4a847992c66c`.
Fullclient2026PASS/1SKIP/0FAIL. Native `live-1789607263587`: пропущенныйreplace
и лишнийtarget.name получили точный путь ошибки без изменения графа; исправленный
запрос создал1узел/1связь, выполнился и прочитал10из72строк.10чисел1/3 проверены
с maxdelta3.33e-17; sample_complete=false сохранён. Собственнаякопия закрыта,
logoutPASS, только диагностические изменения отброшены.

Операторский replay4записанныхcalcrequests PASS. Вcalc-003 после исправления
контракта проявилась следующая независимая ошибка: order ожидалretention_m1,
а объявленret_m1. Существующаяпроверкаorder её отклонила с точнымпутём; только
в операторскомreplay список явно исправлен, формулы не менялись. Это не помощь
модели и не приёмка её незавершённого сценария.

Подготовлен diagnostic9 на том же источнике. Неподтверждённый cleanup прерванного
Hermes8 и stale lock старой диагностическойкопии остаются отдельнымиограничениями.
Новый интерактивныйharness требуетTTY до открытия браузера. Общий плагин/main
не изменены, итоговая38runсерия не начата.

### Подготовка хранилища без лишнего пакета

Выявлено, что старый scenario-storage создавал ненужный пустойdraft и не проверял
logout. Новыйпуть входит в выделенныйаккаунт, требует0открытыхпакетов и после
работы выходит черезUI с повторнойпроверкойаккаунта/инвентаря.

Перваяnativeпопытка обнаружила startupгонку: послеLogin кнопкаFilestorage уже
видима, но FileStorageTreeNode ещё не зарегистрирован. Реальныйassert
MainForm.js:978 и прочитанныйисходникDoOpenFileStorage подтвердилипричину.
Добавлено ограниченное ожиданиеготовности кешированногоузла/записи дерева;
серверные методы для этого не вызываются. Неуспешноеevidence
storage-diagnostic9-verified сохранено; егоlogout не подтверждён из-заassertmodal.

Повторнаяпроверка `storage-diagnostic9-ready2` PASS: назначеннаяпапка найдена,
created_draft=false,packages_before=0,logged_out=true. Отдельныйcreateвариант
проверяется в storage-lifecycle-create. Sourceклиентнеизменен; этиисправления
касаются только операторскогоsetup, не основнойавтономнойприёмки.

## Исправлен контракт калькулятора; diagnostic9 запущен — 17 сентября 2026

Diagnostic8 выявил4доэффектныхотказа: схема допускала пропущенныйreplace и
лишнийtarget.name, исполнитель их отклонял. Run18calls/574.320s остановлен;
сохранения нет, cleanup BLOCKED/CONFIRMED_SAVE_REQUIRED. Его не считать успехом.

Схемаnew/existingвыражений согласована с исполнителем, ошибки возвращают путь
до индекса/поля.2026clienttestsPASS/1SKIP/0FAIL. Native2отказа без изменений→
1корректныйузел/1связь PASS;10точныхчисел проверены; диагностическаякопия
закрыта/logoutPASS. Replay4записанныхзапросов подтвердил исправления; дополнительный
устаревшийorder вcalc-003 выявляется существующейвалидацией.

Операторскаяподготовкапапки больше не создаёт пустойпакет и подтверждаетlogout.
Устранена наблюдённаяstartupгонкаLoginom при преждевременном открытиихранилища:
ожидается регистрацияегоузла. Nativeinspect/create обаPASS,0пакетов,logouttrue.
Интерактивныйharness отказывает до браузера безTTY, еслиpipe явно не разрешён.

Diagnostic9 `20260917-041651-3d5f6593`, PID37811,source `b265f979dac9f214280f931e854d6ff754d1db99c6946f02d54a4a847992c66c`
запущен после23toolspreflight, толькоXiaomi/mimo-v2.5/medium. Папка
`/mimo/MiMo-task38-diagnostic9-20260917`. Client/plugin source заморожены.
Итог ещё не получен. Последний полностьюпроверенный diagnostic7 имеет верные
таблицы/настройки/экспорт, но бизнесFAIL из-за среднейкривой и рекомендации.
Итоговая38runсерия не начата. Main/общийплагиннеизменены.

---



## Ответы предпросмотра проходят ограничение Hermes — 17 сентября 2026

Diagnostic9 `20260917-041651-3d5f6593` остановлен на подтверждённом сбое
доставки ответа: 29 вызовов, 808.765 секунды, returncode -15, исходники неизменны.
Фактическая модель Xiaomi / mimo-v2.5 / medium. Сохранение пакета и logout
не подтверждены: cleanup требует подтверждённого сохранения. Это не успех.

MiMo исправила синтаксическую ошибку SubStr и продолжила на том же калькуляторе
без дубликата; затем выполнила группировку, расчёт удержания и экспорт. Ответ
чтения 72 строк локально завершился успешно, но занял 61 018 символов. Hermes
перенёс его в spillover-файл и предложил read_file, отсутствующий в профиле.
Модель стала обращаться к неподходящим инструментам чтения. Корень этого сбоя
— доставка результата, а не неуспешное вычисление Loginom.

Добавлен бюджет обычного предпросмотра: убираются доказанно избыточные
представления значений, сохраняются тип, точность, NULL и точное значение.
При необходимости сокращается только число целых строк с явным признаком
неполноты; исходная полная квитанция сохраняется локально. Native full-table
доказательства не сокращаются. Их возможное превышение лимита остаётся открытым.
Уточнены инструкции чтения decimal и обязательность трёх аргументов SubStr.

Проверено на source `a8b24f4f5b0252b6b69e90436fa222fd8ad050dc03e95a26b4048d5216c87513`:
- Клиент: 2030 PASS, 1 SKIP, 0 FAIL.
- Повтор исходного большого ответа: 61 → 38 КБ, все 72 строки сохранены.
- Реальный Loginom, `live-1789609210796`: 72×5, все 360 ячеек совпали с полной
  квитанцией, sample_complete=true; создан ровно один узел и одна связь.
- Реальная функция ограничения Hermes с настройками по умолчанию сохранила
  ответ 41 920 символов inline без переноса в файл.
- Диагностическая копия закрыта с разрешённым discard; logout подтверждён.

Операторский аудитор дополнен явной областью проверки промежуточных узлов:
исполнение и настройки обязательны, пропуск полной промежуточной таблицы
возможен только в проверенном плане с достижимым проверяемым конечным выходом.
Исходный импорт и конечные таблицы не исключаются. Добавлена строгая проверка
происхождения калькулятора после доказанного отката ошибочной настройки.
Адресные тесты прошли; эти два расширения ещё не приняты на живом переоткрытии.

Следующий шаг — новый изолированный diagnostic10 на той же Xiaomi-модели.
Итоговая серия из 38 запусков не начата. Ошибки бизнес-полноты diagnostic7,
закрытие прерванного несохранённого сеанса и восстановление таймаута чтения
остаются открытыми. Ветка codex/mimo-scenario-stability; main и общий плагин
не изменены.



Diagnostic10 запущен: `20260917-044845-ed4e92ee`, PID39673, source `a8b24f4f5b0252b6b69e90436fa222fd8ad050dc03e95a26b4048d5216c87513`. Preflight23tools PASS; отдельная папка `/mimo/MiMo-task38-diagnostic10-20260917` проверена, подготовительный сеанс завершён logout. Только исходное задание и штатное вложение; исходники клиента/плагина заморожены до окончания.


Дополнительные проверки аудитора (17 сентября): 12 Python scenario-тестов,
6 JS-тестов reveal/read-scope и 4 теста полной табличной сверки PASS.
На реальном журнале diagnostic9 строгая проверка происхождения восстановленного
калькулятора прошла (recovered-calculator-provenance-replay.json). Это проверка
журнала, а не переоткрытие несохранённого пакета. Табличный отчёт теперь явно
перечисляет omitted_intermediate_tables с причиной и проверенными конечными
таблицами; отрицательные случаи без исполнения/конечных таблиц отклоняются.


Операторский расчёт расширен для Count/SubStr/Val только в ограниченной ASCII-
области: Val принимает исключительно целые строки из цифр без локалезависимых
разделителей; неоднозначные позиции, Unicode, пустые строки и неизвестные функции
отклоняются. Основание: Loginom Help processors/func/calc-func/string.md и
нативно подтверждённый SubStr("m_1",3,2) из diagnostic9. 6 scalar-тестов и
9 тестов независимых ожиданий PASS. Сопоставление со всеми конечными данными
нового прогона ещё не выполнялось; это не расширение обработчиков модели.


Diagnostic10: реальный Hermes получил node-read-full-003 SUCCEEDED inline,
40 155 символов, все 72 строки, sample_complete=true. Предыдущие ошибки finish
и order исправлены моделью самостоятельно. Неизвестная Length заменена моделью
на Val(SubStr(Names,3,2)) в том же калькуляторе после проверенного отката.
Пока проверена доставка/продолжение; итоговый пакет и бизнес-полнота ещё ожидаются.


Подготовлен операторский consolidate_read_orders: повторное чтение калькулятора
с expressions=[] объединяется с полной ранее записанной конфигурацией для
независимого расчёта. Исходный запрос сохраняется в provenance. order адресует
только выражения, а не унаследованные поля; если задан, он должен включать все
выражения ровно один раз. Изменения формул, mappings и неизвестные параметры
не объединяются. 3 адресных теста PASS. Живой аудит нового пакета ещё ожидается.


## Diagnostic10 завершён; повторное независимое переоткрытие — 17 сентября 2026

Run `20260917-044845-ed4e92ee`, source a8b24f4f: Xiaomi/mimo-v2.5/medium,
31 вызов инструмента, 1199.624 секунды, returncode0, source unchanged.
9 аналитических/экспортных узлов созданы. Save checkpoint, package close и logout
PASS; unsaved_changes_discarded=false. Input111950/output17869/cache2384960.

Hermes получил полную таблицу72×5 inline (40155символов). Предварительная сверка
матрицы72значений и кривой12значений PASS. Оба штатно скачанных CSV полностью
проверены:72×5 и12×4, максимальное расхождение1.25e-14.

Итоговый ответ пока FAIL: четыре дельты M3→M6 рассчитаны из округлённых чисел
(Cohort3: -3.5 вместо -3.6; Cohort4: +0.9 вместо +1.0; Cohort5: -11.7 вместо
-11.6; Cohort6: -17.3 вместо -17.2). M3/M6 и рейтинг поM6 корректны.
Утверждение «поздние нули означают отсутствие данных» не подтверждено отдельной
маской наблюдений/датой среза; при этом среднее включает все эти нули. Требуется
устранить неподтверждённую интерпретацию и считать новые дельты в узлах.

Первый независимый аудит прочитал596×16, настройки импорта и двух промежуточных
узлов, затем отказал на порядке полей группировки. Native имена/типы/подписи
совпадают, но меры предшествуют ключам. Cleanup/logout PASS с diagnostic discard.
Явно разрешена перестановка только в группировках: строгая идентичность полей
сохранена, задание не фиксирует их порядок. Начат новый полный аудит:
`task38-diagnostic10-independent2`, plan `task38-diagnostic10-audit-plan2.json`.
Не объединять первый частичный аудит с новой сессией для ложной полной приёмки.
Клиент/плагин остаются заморожены. Итоговая38runсерия не начата.


Второе переоткрытие подтвердило распространение перестановки полей группировки
на входные поля калькулятора: retention_pct остаётся первым, active_count и
 total_customers переместились перед cohort/month_offset. Все имена, типы и
подписи совпадают. Task38 использует длинную таблицу и не задаёт порядок столбцов.
В плане3 разрешена перестановка по строгой идентичности также для калькуляторов;
порядок месяцев проверяется отдельно по строкам. Запущен полный новый аудит
`task38-diagnostic10-independent3`; предыдущие partial-index сохранены, logout
после каждой попытки подтверждён. Нельзя заявлять полный аудит до его завершения.


## MiMo создала проверенный численный сценарий; текстовая приёмка ещё FAIL — 17 сентября 2026

**Подтверждено улучшение технического маршрута, но полная задача ещё не принята.**
Ветка `codex/mimo-scenario-stability`, source
`a8b24f4f5b0252b6b69e90436fa222fd8ad050dc03e95a26b4048d5216c87513`.
Main, общий плагин и развёрнутый сервер не обновлялись.

### Исправлено и проверено

В diagnostic9 ответ с 72 строками занимал 61 КБ. Hermes переносил его в файл,
для которого у модели не было инструмента чтения. Обычный предпросмотр теперь
удаляет только доказанно избыточные представления значений, сохраняя точность,
NULL и полноту. При вынужденном сокращении строк полнота явно снимается.
Полная исходная квитанция остаётся локально. Native full-table доказательства
не сокращаются; возможное превышение ими лимита остаётся отдельным ограничением.

На живом Loginom проверены 360 ячеек; штатная функция ограничения Hermes сохранила
ответ inline. В новом автономном прогоне модель получила все 72 строки (40 155
символов), продолжила вычисления, создала среднюю кривую и два экспорта. После
ошибки неизвестной Length она исправила формулу в том же калькуляторе. Ошибки
finish/order также исправлены без ручной помощи и без дублирования узлов.

### Завершённый прогон и независимая проверка

- Run `20260917-044845-ed4e92ee`: Xiaomi / mimo-v2.5 / medium, 31 вызов инструмента,
  1199.624 секунды, returncode 0, исходники неизменны. Девять созданных узлов,
  восемь связей; десятый узел — штатные переменные сценария.
- Пакет `/mimo/MiMo-task38-diagnostic10-20260917/scenario.lgp` сохранён;
  закрытие и logout подтверждены без отбрасывания изменений.
- Независимый аудит `task38-diagnostic10-independent3/independent-audit.json`:
  пять таблиц целиком — 596×16, 72×4, 72×5, 12×3, 12×4; настройки всех девяти
  узлов и неизменность связей PASS. Максимальная численная разница 1.25e-14.
- Два промежуточных выхода по 7152 строки: проверены исполнение и настройки;
  их полное чтение не заявляется. Все конечные таблицы прочитаны полностью.
- Отдельный аудит задачи подтвердил все 72 значения матрицы, все 12 значений
  взвешенной кривой и хронологический порядок месяцев. Оба CSV проверены целиком.
- В независимом аудите разрешена подтверждённая перестановка именованных полей
  группировок и их калькуляторов. Типы/подписи/значения проверены строго; задача
  не фиксирует порядок этих столбцов. Первые два частичных аудита сохранены,
  каждый завершён подтверждённым logout. Итоговый аудит проведён с нуля.

Клиент: **2030 PASS / 1 SKIP / 0 FAIL**. Адресные проверки аудитора: 28 Python
численных, 15 Python сценарных, 9 проверок ожиданий и 6 JS scope/reveal — PASS.
`git diff --check` PASS. Проверены восстановленное происхождение калькулятора,
повторное чтение без потери формул и явное перечисление непрочитанных промежуточных
таблиц. Эти проверки не равнозначны всей итоговой серии корпуса.

### Оставшиеся ошибки и точка продолжения

**Общая бизнес-приёмка diagnostic10 — FAIL.** В тексте четыре дельты M3→M6
посчитаны из округлённых процентов: ошибки по 0.1 п.п. для когорт 3–6.
Само удержание M3/M6 и рейтинг по M6 верны. Утверждение о поздних нулях как
об отсутствии наблюдений не доказано отдельной маской/датой среза и противоречит
включению этих значений в среднее. Evidence: `business-answer-audit.json`.

Далее: устранить вычисление новых чисел из округлённого текста и неподтверждённую
интерпретацию нулей; упростить повторное чтение существующего выхода без лишних
параметров настройки; улучшить диагностику order (только имена выражений).
Открыты также безопасное завершение прерванного несохранённого сеанса, продолжение
таймаута чтения с открытым форматированием и preflight пересечения явных координат.
Новый модельный прогон в момент этой записи не запущен, диагностические браузеры
закрыты. Итоговая серия **38 прогонов ещё не начата**. Пропуски задач не подменяют
неуспешные прогоны; универсальная надёжность не заявляется.


## Контрольная точка: чтение выхода без мастера — 17 сентября 2026, 11:00 MSK

Добавлен `dock_node_read`: новый operation_id, source_operation_id успешной локальной
операции и необязательные настройки чтения. Формулы, связи, mappings и finish модель
не передаёт. Общая блокировка, квитанции, ожидание и идентификатор свежего исполнения
сохранены. Конфигурация результата имеет status `not_requested`; source schema
проверяется по именам, меткам и типам, включая допустимую перестановку столбцов.
Полное native-чтение через этот API не заявляется: максимум 100 строк с явной полнотой.

Живая проверка выявила два последовательных дефекта нового маршрута: уже активный
узел нельзя просто выполнить повторно; после активации внутренний native.tooltip
остаётся «Выполнить узел», хотя отображаемая подсказка и значок уже означают деактивацию.
Для read-маршрута добавлена защищённая деактивация выбранного узла перед свежим запуском.
Проверяются конкретный узел, текущая подсказка и значок, состояние перед жестом;
потеря ответа запрещает повторный жест. Обычный launch не получил автоматическую
деактивацию. Сверено с Help interface/workflow.md, interface/packages.md и E2E
issues/00k/00k0/441.ts, issues/08k/08k0/8447.ts. Деактивация может деактивировать
потомков по штатной семантике Loginom; read не выполняет повторный экспорт файлов.

### Проверено на source 8f4113a761b796e9f6670c76ed5cb2a61b60a2d7d996d742e6d5e4d5ef5d70c2

- Публичный MCP, `.dock/text-export/live-1789631647451`, Loginom7.4.2/mimo,
  только `/mimo/MiMo-task38-diagnostic10-20260917/read-route-diagnostic-copy.lgp`.
- 12×4: PASS, 20.950с. 72×5: PASS, 35.079с. Настроечные мастера не открывались;
  граф/координаты/связи не изменились; same-ID повтор и неизвестный source не вызвали
  браузерных действий. Начальная успешная операция для source получена публичным apply.
- Независимый аудит всех 408 ячеек по исходным CSV-расчётам: PASS,
  максимальная абсолютная разница 1.2484e-14. `read-numeric-audit.json`.
- Пользовательский MCP-ответ: все 72 строки, sample_complete=true,
  37 423 символа с обёрткой Hermes, без потери точности. `read-compact.json`.
- Cleanup SUCCEEDED: диагностическая копия закрыта, logout подтверждён,
  изменения диагностических просмотров отброшены явно; исходный пакет сохранён.
- Два ранних FAIL сохранены в live-1789631218273 и live-1789631477595, оба закрыты
  с подтверждённым logout. Ошибка первоначальной проверки сравнивала изменяемый
  header_tid окна просмотра; сравнение исправлено на полную идентичность полей/ячеек.
- Полный клиент: 2038 PASS / 1 SKIP / 0 FAIL; 16 сценарных Python тестов PASS,
  включая сохранение аналитических параметров при добавлении read в план аудита.
  Исправлено также булево acceptance_eligible (раньше перекрывалось словарём операций).

### Продолжение

Нового Hermes-прогона в этой контрольной точке нет. Read API принят локальными и
живыми проверками, но его самостоятельное использование MiMo ещё не проверено.
Diagnostic10 остаётся численно корректным, общей бизнес-приёмкой FAIL из-за четырёх
дельт в тексте и неподтверждённой трактовки нулей. Следующий шаг: разобрать привязку
численных утверждений итогового ответа к выходным данным и провести diagnostic11
на неизменной версии (Xiaomi/mimo-v2.5/medium), затем независимый аудит.
Итоговая серия 38 запусков ещё не начата. Не заявлять ускорение всего сценария по этим
двум измерениям; это время только повторного выполнения/чтения. Продолжение после
неопределённого эффекта, таймаут при открытом мастере формата и безопасное завершение
прерванного несохранённого Hermes остаются отдельными ограничениями.
Все браузеры этой диагностики закрыты. Main/публикация/общий плагин не менялись.

## Diagnostic11 запущен — 17 сентября 2026

Разбор final-answer diagnostic10 подтвердил: точные значения были доставлены,
но четыре дельты вычислены из округлённых процентов; трактовка нулей как
ненаступивших месяцев не следует из description.md. Краткие правила добавлены
в успешную пользовательскую квитанцию сохранения, непосредственно перед отчётом:
производные метрики должны быть прочитаны из узлов, округление выполняется после
расчёта, отсутствие наблюдения требует явного признака. Это гипотеза улучшения
следования инструкции, не доказательство устранения ошибки. Адресные25тестовPASS.

Run `20260917-112322-218e953e`, PID47110, Xiaomi/mimo-v2.5/medium подтверждены
по sessions.model/billing_provider. Source
`d6731cb4ce2bcac938569521e669dafdb647dc5679e23bf8245c663c4169c8b0` заморожен.
Папка `/mimo/MiMo-task38-diagnostic11-20260917` создана и проверена отдельным
операторским сеансом, logoutPASS; MCPpreflight24toolsPASS. Модель получила только
исходное описание и native dataset. Импорт завершён, выполняется unpivot.
До завершения не редактировать client/plugins и не открывать второй сеанс mimo.

В операторском экспортёре добавлен dock_node_read в NODE_TOOLS: прямые ответы
не должны теряться. SQLite-тесты экспорта/compaction11PASS. Запущенный Python
launcher импортировал прежний allowlist: после завершения при необходимости
сохранить дополнительный export sidecar новым экспортёром; первоначальные
evidence.json/неуспешные попытки не перезаписывать.
Следующий триггер: завершение модельного процесса и независимая проверка пакета
и финального текста. Итоговая38серия не начата.

## Diagnostic11: выявлен разрыв соединения; исправлены контракт фильтра и диагностика — 17 сентября 2026

**Полная приёмка не пройдена.** Изменения только в `codex/mimo-scenario-stability`;
main, общий плагин и сервер не обновлялись.

### Модельный прогон

- Run `20260917-112322-218e953e`: Xiaomi / mimo-v2.5 / medium, source
  `d6731cb4ce2bcac938569521e669dafdb647dc5679e23bf8245c663c4169c8b0` неизменен.
  35 tool calls, 1485.068с до операторской остановки, exit -15, timeout=false.
  Исходное description.md и штатный dataset; ни oracle, ни готовый граф модели не давались.
- Все 72 строки матрицы коэффициентов сверены с исходным CSV: PASS,
  max delta 6.93e-17. Проценты в более широкой таблице вернулись 70/72 строк;
  неполнота явно обозначена. Это остаётся ограничением объёма, а не полной таблицей.
- Два отказа фильтра до эффектов: неверная структура groups и требование обоих
  выходов. Один запрос во время работающей операции отклонён общей блокировкой;
  модель дождалась нужной операции и продолжила без дубликатов.
- После первого save MiMo добавила M3/M6-фильтры. Затем между успешным M6 и
  запросом слияния прошло 389.552с без tool calls. Новый запрос и последующая
  инспекция упали с `Pinned browser capability call failed`. Операция слияния
  остановилась на workflow, до создания узла. После повторного отказа процесс
  остановлен для диагностики; итогового ответа нет. Улучшение финального текста
  этим прогоном не доказано, даже несмотря на доставленные reporting_constraints.
- Пакет `/mimo/MiMo-task38-diagnostic11-20260917/scenario.lgp` повторно открыт
  через отдельную копию: 8 созданных узлов + переменные, 7 связей. Два поздних
  фильтра в сохранённом файле отсутствуют. Полный независимый аудит этого пакета
  не заявляется. Original session cleanup UNCONFIRMED; диагностический logout
  не подменяет это свидетельство.
- Безопасный экспорт и private cleanup PASS. `usage-recovery.json`:32 API calls,
  input143703/output17276/cache-read2999808; итоговая сумма/цена не выводятся.

### Живая причина и границы вывода

В `.dock/text-export/live-1789635228922` при завершении мастера входного порта
воспроизведён `WIZARD_CONTEXT_CHANGED`; затем cleanup ошибочно дал ACCOUNT_CHANGED.
Прямое наблюдение показало прежние document_id/account=mimo, но Connected=false.
UI показывал «Обнаружен разрыв связи. Восстановить сессию?». Штатное восстановление
вернуло Connected=true и прежний открытый мастер. Инспекция исходной операции
при этом правильно оставила pending, без доступного resume: восстановленная связь
не доказывает завершение жеста. Оператор отменил только мастер диагностической
копии, сверил прежний граф9/7; cleanup после восстановления SUCCEEDED, без discard.

Это подтверждённый класс сбоя связи и ошибочная его классификация. Нельзя утверждать,
что именно он вызвал исходный отказ Hermes: старая версия отбросила текст ошибки
браузера. Причина транспортного разрыва (стенд/сеть/клиент) ещё не локализована.
Не менять сетевые/серверные настройки на основании этой гипотезы.

### Исправлено и проверено

- Filter execute допускает выбранный непустой набор выходов [0], [1] либо [0,1].
  Параметры фильтра и настройки обоих выходов не изменены; defaults прежние.
- Ошибки browser capability получают фиксированную безопасную категорию:
  disconnected, identity changed, browser closed, context destroyed, connection
  closed, timeout, receipt capacity, unclassified. Сырые страницы/скрипты/секреты
  не передаются в сообщение. Реальный MCP-ответ с отказом identity guard
  воспроизведён и классифицирован, unit-negative проверки утечек PASS.
- Storage identity guard различает прежнюю личность с отключённым соединением и
  смену личности. Cleanup сообщает CONNECTION_DISCONNECTED отдельно от ACCOUNT_CHANGED.
  Это диагностика; автоматического восстановления и снятия pending блокировки нет.
- Экспортёр истории сохраняет dock_node_read и прямым вызовом, и через router;
  11 Python history/compaction tests PASS. В diagnostic11 read не вызывался.
- Итоговый source `ada1c8145312d39553d5414359dd032def7121d42f7a7fbeaaa44a20486e1908`:
  полный client2046PASS/1SKIP/0FAIL. Живой public MCP в
  `.dock/text-export/live-1789635606445`: новый фильтр с read.ports=[0],12×6,
  все72ячейки PASS (max8.47e-15), один новый узел/связь, same-ID без браузерных действий.
  Источник предварительно выполнен оператором; это адресная живая проверка,
  не автономная приёмка Hermes. Диагностический новый узел отброшен только в копии;
  cleanup/logout SUCCEEDED. Все диагностические браузеры закрыты.

### Следующая стадия

1. Без нового Hermes сначала локализовать разрыв соединения и реализовать bounded
   восстановление исходной сессии с проверкой личности/документа/пакета и сверкой
   незавершённого жеста. Не снимать блокировку одним закрытием мастера.
2. Учесть безопасное сохранение при остановке после промежуточного save: в этом
   запуске два последующих узла не попали в файл. Не считать первый save достаточным.
3. Рассмотреть ограниченную проекцию/постраничное чтение широких таблиц: 70/72
   строки нельзя объявлять полной выборкой. Текущий лимит остаётся явным.
4. После адресных live-проверок провести следующий Xiaomi diagnostic, затем
   полный независимый аудит результата и финального текста. Итоговая серия38
   запусков ещё не начата; новую модельную попытку не использовать вместо диагностики.


### MiMo: исходная сессия после аварийного разрыва — 17 сентября 2026

Source cff827b8: storage guard восстанавливает только проверенную исходную
сессию через native UI; неопределённое нажатие не повторяется, pending операции
сохраняется. Live proxy fault/recovery, создание фильтра12×6 и аудит72ячеек PASS;
потеря ответа после restore — одно нажатие. Client2059PASS/1SKIP, proxy test PASS.
Полная приёмка/38прогонов не выполнены. Следующий шаг: pending node gesture после
разрыва, сохранение и широкие ответы. Подробности: docs/loginom-dock/mimo-stability-report.md.


### MiMo — входное восстановление и фактический профиль, 17 сентября2026

Фильтр и калькулятор продолжили исходную операцию после подтверждённого Input
Done, потерянного ответа и разрыва связи; полные аудиты12×6/72×6, отсутствие
дубликатов и cleanup PASS. Client2059PASS/1SKIP; новая упаковка ответа сохраняет
все72×6 в45305символах с обёрткой. Diagnostic12 обнаружил игнорирование
--reasoning в Hermes -z и остановлен. Запускатель использует обычный quiet chat;
Diagnostic13 20260917-125531-4e0733cf/PID53764 active, эффективный профиль
xiaomi/mimo-v2.5/medium подтверждён SQLite. Source307fa0c6 не менять до окончания.
Полная серия38прогонов не начата. Точка продолжения: docs/loginom-dock/mimo-stability-report.md.
