# Node17: full8 остановлен на критериях независимой приёмки

Прогон `20260914-000803-18fad485` на кандидате `908c9b7d` завершился самостоятельно: exit 0, timeout=false. Процессы модели, launcher и собственного браузера отсутствуют; координатор уведомлён об освобождении слота `node17-hermes-20260914-1b1f55b4`. Runtime, harness, native skill и подключение авторизации не изменились. Модель выполнила 22 операции узлов, включая ожидаемый отказ конфликта; save/reopen и final checkpoint получили SUCCEEDED, observer dispatch=1. Это не означает полного PASS.

Неизменённый независимый аудитор сохранил FAIL в `.dock/node17/acceptance/runs/20260914-000803-18fad485/full-audit.json`. Точная остановка — `text_export_acceptance.py:108`: повторный импорт должен иметь `parameters={}`. Однако текущий `validateTextImportNodeParameters` в `client/lib/text-import-node.mjs:22` требует ровно ключи `settings,source` также для существующего узла. Все три исходных публичных вызова Hermes содержат `settings={}`, прежние artifact/upload ID и `mappings=[]`. Передача идентификаторов сама по себе не доказывает перенастройку источника. Требование задания и аудитора несовместимо с текущим контрактом обработчика; оно не было ослаблено или исправлено задним числом.

Свежая отдельная сессия `6d7a0523-3536-4f5d-9249-7f0621f17b79` открыла именно `/test-2/packages/Dock-acceptance-20260914-000803-18fad485.lgp`. Нативная навигация подтвердила режим «только чтение». Доказательства находятся в `.dock/text-export/live-1789334851665/preparation.json`; запуск завершился до любых внешних node.apply. Копия не создавалась, настройки оригинала не изменялись. Диагностический браузер закрыт, его PID отсутствуют. Отсутствие локальных процессов не доказывает снятия серверной блокировки; штатное закрытие модельного серверного сеанса не подтверждено.

Итог: **полная цель не принята, готовность к слиянию не заявлена**. Нужны решение координатора по несовместимому контракту проверки импортов и доступный способ штатно освободить оригинал от собственного серверного сеанса. Новая модель и повторное полное ревью не запускались. Код продукта, frozen auditor, main, сервер и общий клиент не менялись.

Машинные сведения, исходные запросы и контрольная сумма первоначального FAIL: [17-text-export-full8-blocker.json](17-text-export-full8-blocker.json).
