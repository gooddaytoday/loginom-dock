import {DATE_TIME_OPERATIONS} from './date-time-parameters.mjs';
import {NODE_POSITION_MIN} from './node-contracts.mjs';
import {validateActionParameters} from './action-catalog.mjs';
import {nodeJobResultSchema,deliveryJobResultSchema} from './node-result-schema.mjs';

const object=(properties,required=Object.keys(properties))=>({type:'object',properties,required,additionalProperties:false});
const text=(maxLength=128)=>({type:'string',minLength:1,maxLength});
const fieldName={...text(128),pattern:'^[A-Za-z_][A-Za-z0-9_]{0,127}$',description:'Technical field name: ASCII letters, digits and underscore; first character is a letter or underscore. Put a human-readable or Russian name in label.'};
const id={...text(),pattern:'^[A-Za-z0-9_.:-]+$'};
const integer=(minimum,maximum)=>({type:'integer',minimum,maximum});
const choice=(...values)=>({type:'string',enum:values});
const boolean={type:'boolean'};
const array=(items,maxItems,minItems=0)=>({type:'array',items,maxItems,minItems});
const ref=object({document_id:id,workflow_id:id,node_id:id});
const sourceSettings=object({source_path:text(2048),encoding:text(80),rows_to_skip:integer(0,1000000),first_line_as_title:boolean},[]);
const format=object({delimiter:{type:'string',minLength:1,maxLength:1},decimal_separator:choice('.',','),
 null_marker:{type:'string',maxLength:256},text_qualifier:{type:'string',maxLength:1}},[]);
const column=object({source_name:text(120),name:text(120),label:text(120),
 type:choice('integer','real','string','boolean','datetime'),data_kind:choice('Неопределенное','Непрерывный','Дискретный'),used:boolean},[]);
export const duplicatesParametersSchema=object({input_fields:array(text(128),1000,1),output_fields:array(text(128),1000)});
export const calculatorExpressionSchema={...object({target:{...object({kind:choice('new','existing'),name:fieldName},['kind']),
 description:'For a new expression use only {kind:new}; its name belongs beside target. For a saved expression use {kind:existing,name:saved_name}.',
 allOf:[{if:{properties:{kind:{const:'new'}},required:['kind']},then:{not:{required:['name']}},else:{required:['name']}}]},
 name:fieldName,label:text(200),type:choice('integer','real','string','boolean','datetime'),formula:{...text(2048),description:'Loginom calculator syntax. Refer to fields by bare technical name (FieldA - FieldB), not [FieldA]. Int(number) returns the numeric integer part; it does not parse text. Val(text) parses a number using the locale. SubStr(text,start,length) requires all THREE arguments, including length; MID is not a Loginom function. Other signatures: Loginom Help, processors/func/calc-func/string.md and mathematical.md. Choose the analytical expression yourself.'},replace:{...boolean,description:'Whether the result replaces an INPUT field of the same name. This is unrelated to editing an existing expression: target.kind=existing selects the expression to edit. Keep replace=false for an added calculated field, including the blank default Expr1.'}},['target']),
 allOf:[{if:{properties:{target:{properties:{kind:{const:'new'}},required:['kind']}},required:['target']},then:{required:['name','label','type','formula','replace']}}]};
export const calculatorParametersSchema={...object({expressions:array(calculatorExpressionSchema,128),order:{...array(text(128),128,1),description:'Optional complete order of calculated expression names only. Do not include inherited input columns. Omit to keep expression declaration order.'}},['expressions']),
 description:'For a new calculator, inputs must contain one table connection to input 0. target.kind=new ADDS an expression and must not repeat a saved expression name. For an existing calculator, parameters:{expressions:[]} preserves every formula (use finish=execute to read output); edits address target:{kind:existing,name:saved_name} inside each expression. Analytical formulas remain your choice.'};
export const groupingFieldSchema=object({kind:choice('input_field'),name:fieldName});
export const groupingMeasureSchema=object({field:groupingFieldSchema,function:choice('sum','count','avg','min','max'),name:fieldName,label:text(120)});
export const groupingParametersSchema=object({group_by:array(groupingFieldSchema,128,1),measures:array(groupingMeasureSchema,256,1)},[]);
export const replacementValueSchema=object({type:choice('string','integer','real'),value:{type:['string','number','null'],maxLength:2048}});
export const replacementRuleSchema=object({field:groupingFieldSchema,type:choice('string','integer','real'),pairs:array(object({from:replacementValueSchema,to:replacementValueSchema}),256),other:object({mode:choice('keep','null','value'),value:replacementValueSchema},['mode']),case_sensitive:boolean,precision:{const:0}},['field','type','pairs','other']);
export const replacementParametersSchema=object({rules:array(replacementRuleSchema,128,1),output_mode:choice('replace','add')},[]);
export const dateTimeParametersSchema=object({fields:array(object({field:groupingFieldSchema,transformations:array(object({operation:choice(...Object.keys(DATE_TIME_OPERATIONS)),name:text(128),label:text(120)}),12)}),128)},[]);
export const missingValuesFieldSchema=object({field:groupingFieldSchema,method:choice('mean','constant'),value:{type:'string',maxLength:2048}},['field','method']);
export const missingValuesParametersSchema=object({fields:array(missingValuesFieldSchema,128,1),max_nulls_percent:{type:'integer',minimum:0,maximum:100},ordered:{const:false}},[]);
export const collapseParametersSchema=object({information:array(groupingFieldSchema,128),transposed:array(groupingFieldSchema,128,1),ignore_empty:boolean},[]);
export const sortingKeySchema=object({field:groupingFieldSchema,direction:choice('ASC','DESC'),case_sensitive:{...boolean,description:'Required boolean for string/variant keys; omit for numeric/date/boolean keys. This setting belongs to this key, not to parameters itself.'}},['field','direction']);
export const sortingParametersSchema=object({keys:array(sortingKeySchema,128,1),compare_with_locale:boolean},[]);
export const filterConditionSchema=object({field:object({kind:choice('input_field','row_number'),name:text(128)},['kind']),
 operator:choice('<','<=','>','>=','=','<>','is_null','not_null','between','not_between','in','not_in','contains','not_contains','starts_with','not_starts_with','ends_with','not_ends_with','is_true','is_false'),
 type:choice('integer','real','string','datetime','boolean'),value:{type:['string','number','boolean']},lower:{type:['string','number']},upper:{type:['string','number']},values:array({type:['string','number']},128,1),case_sensitive:boolean},['field','operator','type']);
filterConditionSchema.description='For type=datetime, every value/lower/upper/list item must be a local ISO date AND time including seconds, e.g. 2024-01-01T00:00:00 or 2024-01-01T00:00:00.000. A date alone or timezone suffix is invalid. AND conditions form one inner group; groups are OR.';
export const unionParametersSchema=object({prefixes:object({enabled:boolean,name:{type:'string',maxLength:128},label:{type:'string',maxLength:128}}),tables:array(object({port:integer(1,14),fields:array(object({source:text(128),main:{type:['string','null'],minLength:1,maxLength:128}}),1000)}),14,1)});
export const joinKeySchema=object({left:text(128),right:text(128)});
export const joinParametersSchema=object({keys:array(joinKeySchema,1000,1),case_sensitive:boolean,include_joined_keys:boolean},[]);
export const filterParametersSchema=object({groups:array(array(filterConditionSchema,128,1),64,1)},[]);
export const reformChangeSchema=object({field:groupingFieldSchema,name:text(128),label:text(128),type:choice('string','integer','real','boolean','datetime'),data_kind:choice('Неопределенное','Непрерывный','Дискретный'),usage:choice('Не задано','Активное','Выходное','Группа','Показатель','Транзакция','Элемент'),excluded:boolean},['field']);
export const reformParametersSchema=object({changes:array(reformChangeSchema,128)},['changes']);
// New nodes require complete settings; existing nodes accept a patch. The installed
// handler validates that distinction and cross-field invariants before any UI work.
export const textExportParametersSchema=object({destination:text(512),overwrite:choice('reject','replace'),encoding:choice('UTF-8'),delimiter:choice(';',',','\t'),header:choice('none','names','labels'),bom:boolean,line_ending:choice('LF','CRLF'),text_qualifier:choice('"'),decimal_separator:choice('.',','),date_separator:choice('.','/','\\','-'),time_separator:choice(':','.'),null_marker:choice('','?','null','NULL'),true_value:choice('True','Истина','Да'),false_value:choice('False','Ложь','Нет'),date_format:choice('dd/mm/yyyy','mm/dd/yyyy','yyyy/mm/dd','dd/mm/yy','mm/dd/yy','yy/mm/dd'),time_format:choice('h:mm','hh:mm','h:mm:ss','hh:mm:ss')},[]);
export const nodeApplyInputSchema=object({
 operation_id:id,contract_revision:choice('1.0.0'),document_id:id,
 workflow_ref:object({workflow_id:id,tab_tid:text(128),prefix:text(128),navigation_path:array(object({tid:text(512),label:{type:'string'}}),32,1)}),
 target:object({kind:choice('new','existing'),type:choice('exports.text','transform.collapse_columns','preprocessing.data_recovery','transform.date_time','research.duplicates','imports.text','transform.calculator','transform.group_data','transform.sorting','transform.replace_columns','transform.reform_columns','transform.filter_data','transform.join_data','transform.union_data'),label:text(200),ref,
  position:{...object({x:{type:'number',minimum:NODE_POSITION_MIN,maximum:10000},y:{type:'number',minimum:NODE_POSITION_MIN,maximum:10000}}),description:'Optional model-space coordinates, each 64..10000. The margin keeps node labels and ports away from the top/left canvas edge. Omit for automatic free placement; explicit coordinates are never silently changed.'}},['kind','type']),
 inputs:array(object({source:ref,output:integer(0,99),input:integer(0,99)}),15),
 mode:choice('unpivot','impute','calendar','mark','delimited','expression','aggregate','keys','scalar','conditions','inner','left','append_all','exact'),parameters:object({...textExportParametersSchema.properties,information:collapseParametersSchema.properties.information,transposed:collapseParametersSchema.properties.transposed,ignore_empty:boolean,input_fields:duplicatesParametersSchema.properties.input_fields,output_fields:duplicatesParametersSchema.properties.output_fields,
  source:object({artifact_id:id,upload_operation_id:id,bytes:integer(0,16777216),sha256:{...text(64),minLength:64,pattern:'^[a-f0-9]{64}$'}},['artifact_id','upload_operation_id']),
  settings:object({source:sourceSettings,format,columns:array(column,1000,1)},[]),
  fields:{anyOf:[dateTimeParametersSchema.properties.fields,missingValuesParametersSchema.properties.fields]},max_nulls_percent:missingValuesParametersSchema.properties.max_nulls_percent,ordered:missingValuesParametersSchema.properties.ordered,rules:replacementParametersSchema.properties.rules,output_mode:replacementParametersSchema.properties.output_mode,prefixes:unionParametersSchema.properties.prefixes,tables:unionParametersSchema.properties.tables,expressions:array(calculatorExpressionSchema,128),order:array(text(128),128,1),group_by:array(groupingFieldSchema,128,1),measures:array(groupingMeasureSchema,256,1),keys:array({anyOf:[sortingKeySchema,joinKeySchema]},1000,1),case_sensitive:boolean,include_joined_keys:boolean,compare_with_locale:boolean,changes:array(reformChangeSchema,128),groups:filterParametersSchema.properties.groups},[]),
 mappings:array(object({direction:choice('input','output'),port:integer(0,14),autosync:boolean,
  changes:array(object({source:object({kind:choice('configured_field'),name:text(128)}),name:text(128),label:text(120),excluded:boolean},['source']),1000),
  fields:array(object({source:object({kind:choice('configured_field'),name:text(128)}),name:text(128),label:text(120),excluded:boolean},['source']),1000,1)},['direction','port']),16),
 finish:{...choice('done','execute','close'),description:'execute commits settings, executes and reads fresh output. done only commits settings; close discards the wizard draft. done/close require read.ports=[] and cannot read output. Usually choose execute; omit read for normal defaults.'},
 read:object({ports:{...array(integer(0,1),2),description:'Only finish=execute can read ports. done/close and text export require []. Omit read for defaults.'},sample_rows:integer(0,100),require_exact_numbers:boolean,coverage:{...choice('full','sample'),description:'Default sample: 10 rows; explicitly request up to 100 rows, bounded by 10000 cells and 512 KiB of UI row data. full is restricted to executed transform.collapse_columns output 0, <=50 rows and <=8 columns with verified native provenance; other types cannot request full.'}},['ports','sample_rows','require_exact_numbers']),
 budgets:object({configure_ms:integer(1,1800000),execute_ms:integer(1,1800000),total_ms:integer(1,1800000)}),
});
// Discovery uses the same public parameter shapes. Requirements depending on
// target.kind remain in the installed handler, before any browser mutation.
export const textImportParametersSchema=object({
 source:{...structuredClone(nodeApplyInputSchema.properties.parameters.properties.source),
  description:'Use artifact_id and upload_operation_id from the completed, verified artifact delivery. Optional bytes and sha256 must match that receipt.'},
 settings:{...structuredClone(nodeApplyInputSchema.properties.parameters.properties.settings),
  description:'For target.kind=new supply all three objects: source, format, columns. source requires source_path (the verified delivery destination), encoding, rows_to_skip, first_line_as_title. format requires delimiter, decimal_separator, null_marker, text_qualifier. columns must describe every CSV field with name, label, type, data_kind, used; source_name optionally identifies an original field when renaming. At least one field must be used. For target.kind=existing settings is a patch: omitted settings are preserved, and settings:{} reuses the saved configuration. A column patch identifies its field by source_name or name.'},
});
// Cross-field restrictions survive the compact user envelope. Runtime guards
// remain authoritative after optional defaults are expanded.
nodeApplyInputSchema.allOf=[
 {if:{properties:{finish:{enum:['done','close']}},required:['finish']},
  then:{properties:{read:{properties:{ports:{maxItems:0}}}}}},
 {if:{properties:{target:{properties:{type:{const:'exports.text'}},required:['type']}},required:['target']},
  then:{properties:{read:{properties:{ports:{maxItems:0}}}}}},
 {if:{properties:{read:{properties:{coverage:{const:'full'}},required:['coverage']}},required:['read']},
  then:{properties:{finish:{const:'execute'},target:{properties:{type:{const:'transform.collapse_columns'}}},read:{properties:{ports:{const:[0]}}}}}}
];
const operation=object({operation_id:id});
const deliveryId={...id,maxLength:80};
const delivery=object({operation_id:deliveryId,artifact_id:id,upload_grant_id:id,budget_ms:integer(1000,1800000)});
const resumeDelivery=object({operation_id:deliveryId,resume_id:deliveryId,budget_ms:integer(1000,1800000)});
const tool=(name,description,inputSchema,readOnlyHint=false)=>({name,description,inputSchema,
 outputSchema:name.startsWith('dock_node_')?nodeJobResultSchema:deliveryJobResultSchema,
 annotations:{readOnlyHint,destructiveHint:!readOnlyHint,openWorldHint:false}});
export const nodeApiTools=Object.freeze([
 tool('dock_node_read','Execute and read an already completed local table node without reopening its configuration wizard. Pass the original successful node operation ID as source_operation_id and a NEW operation_id for this read. Dock retains the node identity and port schema locally; no formulas, mappings, coordinates or finish mode are accepted. It executes the existing node afresh, then returns bounded table contents and explicit sample completeness. Does not save the package. Retry/wait on the SAME read operation_id; never relaunch after an uncertain response. Default: all previously read ports,10 rows, ordinary precision; request up to100 rows and require_exact_numbers when needed.',
  object({operation_id:id,source_operation_id:id,read:object({ports:array(integer(0,1),2),sample_rows:integer(0,100),require_exact_numbers:boolean},[]),budget_ms:integer(1,1800000)},['operation_id','source_operation_id'])),
 tool('dock_node_apply','Candidate node operation: UTF-8 text export, Collapse Columns, Missing Values, Date/time, Duplicate/Contradiction marking, text import, expression Calculator, aggregate Grouping, Sorting, Replacement, Field Parameters, Row Filter, Join, or Union when its handler is installed. Replacement uses exact mode with rules per input_field name, matching type string/integer/real, typed from/to pairs, explicit other keep/null/value, string case_sensitive or numeric precision=0, and global output_mode replace/add. Each requested field replaces its complete internal table; unlisted fields remain unchanged. New nodes require rules and output_mode; existing parameters={} preserves them. Integer values beyond safe JS range use decimal strings. Null, empty string and literal null are distinct. Remaining real explicit values are limited to two decimal places by the native Loginom 7.4.2 editor; exact pairs retain their verified precision. Regex, external tables and type conversions are unsupported. Duplicates uses mark with complete input_fields and output_fields arrays of exact source names. At least one input field is required; the output list may be empty. All other fields get unspecified usage. Roles replace the complete previous configuration. All rows and source columns are retained; four service fields mark every duplicate, so Duplicate=false removes all copies. Mapping overrides and service-name collisions are rejected. Date/time uses mode calendar and fields=[{field:{kind:input_field,name},transformations:[{operation,name,label}]}]. Supported operations: year, quarter, month, day_of_month, hour, year_start, year_end, quarter_start, quarter_end, month_start, month_end, date. Inputs must already be datetime. Transformations replace the complete selection for each named field; other date fields are preserved. Existing parameters={} preserves all saved settings; transformations=[] explicitly clears one field. Output mappings address configured names and may reorder fields or exclude passthrough inputs. An explicit date output layout disables autosync; autosync=true with a fields layout is rejected. Generated date fields cannot be excluded. ISO weeks, string formats and timezone conversion are unsupported. Missing Values uses preprocessing.data_recovery / impute, explicit fields [{field:{kind:input_field,name},method:mean|constant,value?}], max_nulls_percent=0..100 integer and ordered=false. Mean requires continuous integer/real; constant requires discrete string and explicit string value. Fields replace the entire processing set; omitted fields are disabled. Existing parameters={} preserves supported saved settings. Remaining Nulls depend on the threshold; execution does not guarantee complete filling. Collapse Columns uses mode unpivot with complete ordered information/transposed input_field references and explicit ignore_empty for a new node; existing parameters={} preserves roles and policy. Input variant is deferred. Native output is information fields plus Names, DisplayNames, Values (scalar for homogeneous fields, variant for mixed fields), DataTypes. Output mappings support explicit exclusions. read.coverage defaults to sample (at most 10 rows). Collapse-only coverage full requests every cell in exact_table (at most 50 rows, 8 columns and 1 MiB serialized response), or refuses. It requires the same-session byte-verified local text import, completed executions and a verified isolated static graph. Native integer64/real64 are lossless strings with native bytes; datetime is native_serial_only OADate bytes, not civil time or epoch. Unknown or dynamic provenance refuses full. Ordinary sample reads retain their precision limitations. Text export uses exports.text/mode delimited with one table input and read={ports:[],sample_rows:0,require_exact_numbers:false}. New export requires explicit CSV/TSV destination inside the selected Loginom export directory, encoding UTF-8, delimiter (; , or tab), header (names/labels/none), bom, line_ending (LF/CRLF), decimal_separator, null_marker and text_qualifier double quote. Existing parameters are a patch; variables, fixed width and requested port mappings are unsupported. Existing file destinations reject by default; overwrite replace requires an explicit assigned destination each time. Execute returns output.file_artifacts with actual native bytes/SHA and fresh execution binding. Done/Close produce no file receipt. Union uses append_all and retains duplicate rows. Input 0 is the main table; inputs 1–14 are ordered joined tables. Supply explicit prefixes={enabled,name,label} and tables=[{port,fields:[{source,main}]}]. Every joined field needs its exact source name and a main field name or null for a separate output column. Types must match; no implicit conversions. Existing Union requests also declare all tables; adding an input preserves its node identity. Join uses mode inner/left, ordered keys [{left,right}], explicit case_sensitive and include_joined_keys booleans, with input 0 left and input 1 right. Mappings address both inputs and output 0. Existing Join parameters={} preserves saved settings and requires its actual mode. Rename conflicting input field names through explicit mappings before joining. Row Filter uses mode conditions with a nonempty groups array: each group is AND, groups are OR. Every condition identifies field={kind:input_field,name} or field={kind:row_number}, an explicit scalar type and operator, with value, lower/upper or values as required. String value predicates require explicit case_sensitive; is_null/not_null must omit it. Date literals use local ISO including seconds (optional three millisecond digits). Continuous datetime scalar/range editors accept only whole seconds. Discrete datetime fields support scalar and range milliseconds. List predicates retain milliseconds. Groups replace the complete previous filter. Existing filters may use parameters={} to preserve and validate their complete saved conditions without rewriting them. Empty groups, variables and variant types are unsupported. Filter Execute requires read.ports=[0,1]; both outputs share one fresh execution and have independent port schemas. Field Parameters uses mode scalar and changes: each change identifies an input_field/name and patches name, label, scalar type, data_kind, usage, or excluded. Unspecified properties are preserved; output selection and order use mappings. Existing Field Parameters reexecution without edits uses parameters={changes:[]}, inputs=[], mappings=[]. For exact conversion/value checks set read.require_exact_numbers=true; ordinary false reads may round numbers. Sorting uses mode keys: ordered keys with field input_field/name, direction ASC/DESC, explicit case_sensitive for string/variant fields, optional compare_with_locale (true for new nodes, preserved for existing). Existing sorting parameters={} preserves keys. Grouping uses ordered group_by input_field names and a full measures list (field, function, name, label); supported functions are sum/count/avg/min/max, count includes null rows. Import source needs only artifact_id and upload_operation_id from a verified delivery; the client resolves bytes and SHA-256 locally. Optional legacy bytes/sha256 must match when supplied. Import settings are required. Calculator accepts ordered expressions with new/existing targets and preserves unrequested settings; optional order lists every resulting expression name. New expressions require name, label, type, formula, replace. Done saves without executing; Close discards the draft and rejects Calculator input mappings, which require a separate committed wizard; Execute reads output 0 for single-output types. Done/Close require read.ports=[]. Poll the SAME operation_id; timeout never restarts work. Save the package separately.',nodeApplyInputSchema),
 tool('dock_node_resume','Continue the SAME known operation by operation_id using its locally retained original request. Retains accepted phases; unresolved effects or lost document refuse continuation. Full identical requests remain supported for diagnostics.',{...nodeApplyInputSchema,required:['operation_id']}),
 tool('dock_node_status','Read local state and accepted progress without browser access or re-execution.',operation,true),
 tool('dock_node_wait','Wait up to timeout_ms for the SAME worker. Timeout returns running; never infer termination or start a replacement.',object({operation_id:id,timeout_ms:integer(0,60000)},['operation_id']),true),
 tool('dock_node_cancel','Request local cancellation. Partial effects remain; poll until cleanup settles. Does not stop an identified server execution.',operation),
 tool('dock_node_stop','Request native stop only for an identified server execution currently awaited by this node operation. Poll the same worker for confirmed outcome.',operation),
]);
export const deliveryApiTools=Object.freeze([
 tool('dock_artifact_deliver','Candidate delivery of an authorized artifact to its grant destination, including path conflict policy and byte verification. Retain operation_id after timeout or uncertainty; never reupload with a replacement ID.',delivery),
 tool('dock_artifact_delivery_status','Read the retained delivery job without browser calls. A failed tool wait does not prove transfer termination.',operation,true),
 tool('dock_artifact_delivery_resume','Explicitly resume the SAME known delivery job after inspecting status. Uses the original upload receipt; never repeats an unresolved download. Requires the original live runtime and unchanged grant.',resumeDelivery),
]);
export const isNodeApiTool=name=>[...nodeApiTools,...deliveryApiTools].some(tool=>tool.name===name);
export async function dispatchNodeApi(runtime,name,args,{signal}={}) {
 const definition=runtime.tools.find(tool=>tool.name===name);
 if(!definition||!isNodeApiTool(name))throw Error('Node operation tool is unavailable in this session');
 validateActionParameters(definition.inputSchema,args);
 signal?.throwIfAborted();
 switch(name){
  case 'dock_node_read':return runtime.startNodeRead(args);
  case 'dock_node_apply':return runtime.startNodeApply(args);
  case 'dock_node_resume':
   if(Object.keys(args).length>1)validateActionParameters(nodeApplyInputSchema,args);
   return runtime.startNodeApply(args,{resume:true});
  case 'dock_node_status':return runtime.nodeApplyStatus(args.operation_id);
  case 'dock_node_wait':return runtime.waitNodeApply(args.operation_id,{timeoutMs:args.timeout_ms??1000,signal});
  case 'dock_node_cancel':return runtime.cancelNodeApply(args.operation_id);
  case 'dock_node_stop':return runtime.stopNodeApply(args.operation_id);
  case 'dock_artifact_deliver':return runtime.deliverArtifact(args,{signal});
  case 'dock_artifact_delivery_status':return runtime.artifactDeliveryStatus(args.operation_id);
  case 'dock_artifact_delivery_resume':return runtime.resumeArtifactDelivery(args,{signal});
 }
}
