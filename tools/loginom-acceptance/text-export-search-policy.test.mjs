import test from 'node:test';import assert from 'node:assert/strict';
import {checkStep,checkSearchScroll} from './text-export-observer-policy.mjs';
const fixture=()=>{const c={origin:'http://logi-test-plan.bg.local',baseline:{destination:'/test-2/a.csv'}},element={ref:'row',tid:'MF;TF-1;FileStorageForm;colName_old',label:'old',scroll:{ref:'table',top:0,max_top:2000},allowed_actions:['scroll'],interaction:{state:'point_observed'}},snapshot={authenticated:true,origin:c.origin,loginom_build:'7.4.2',workflow_ref:{prefix:'MF;TF-1',tab_tid:'tab'},file_storage:{directory:'/test-2'},dom_epoch:{document:'doc'},observation_root:{ref:'table',identity:{anchor_tid:'MF;TF-1;FileStorageForm;pnlFileStorage;tbl'}},ui:{elements:[element],dialogs:[],masks:[]}},step={kind:'search_scroll',name:'a.csv',snapshot,element,delta_y:1000},r={status:'SUCCEEDED',cleanup_complete:true,output:{gesture_applied:true,workflow_ref:snapshot.workflow_ref,file_storage:snapshot.file_storage,dom_epoch:snapshot.dom_epoch},trace:[{event:'ui_scroll_applied',owner_ref:'table',from:0,to:1000}]};return {c,step,r};};
test('search policy validates actual bounded movement and rejects tampering',()=>{const f=fixture();checkStep(f.step,f.c);checkSearchScroll(f.step,f.r);for(const change of [f=>f.step.name='b.csv',f=>f.step.delta_y=1001,f=>f.step.snapshot.file_storage.directory='/other',f=>f.step.element.scroll.ref='foreign',f=>f.r.trace[0].to=900,f=>f.r.trace[0].owner_ref='foreign',f=>f.r.output.dom_epoch={document:'other'},f=>f.r.cleanup_complete=false]){const f=fixture();change(f);assert.throws(()=>{checkStep(f.step,f.c);checkSearchScroll(f.step,f.r);});}});
