Подготовь и проверь текстовый экспорт Loginom 7.4.2 в отдельном новом пакете __PACKAGE_PATH__. Только аккаунт test-2, каталог __STORAGE_DIRECTORY__. У каждого файла ниже префикс __EXPORT_PREFIX__; не используй файлы иных задач. Итог требует всех перечисленных случаев, а не только одного успешного CSV. Работай полными node.apply, без UI-резерва, терминала, другого браузера, субагента или смены модели. Идентичности бери из подготовленного сценария и результатов. Не повторяй действие с неизвестным эффектом.

Доставь три предоставленных файла __MAIN_CSV__, __TYPED_CSV__, __WIDE_CSV__ через Dock artifact delivery и создай три импорта Main, Typed, Wide. UTF-8, разделитель ;, первая строка — имена, пропуск строк 0, десятичная точка, для Main NULL-маркер ?, для Typed и Wide NULL-маркер NULL; текст ограничен двойной кавычкой. Main: id integer, text string, number real. Typed: id integer, text string, number real, flag boolean, stamp datetime; даты входа дд.мм.гггг. Wide: все field01…field40 — string, метка field01 — «Первое поле», field40 — «Последнее поле», остальные метки совпадают с именами. Все поля использованы; строки и логические поля дискретные, числовые/дата непрерывные. Выполни импорты и проверь их полные схемы и данные. Main содержит 5 строк, Typed 3 строки, Wide 3 строки. Создай фильтр Empty из Main, условие row_number > 1000, проверь оба выхода: 0 и 5 строк.

Создай четыре экспорта ExportCSV из Main, ExportTyped из Typed, ExportWide из Wide, ExportEmpty из выхода 0 Empty. У каждого один табличный вход 0, режим delimited, mappings=[], read={ports:[],sample_rows:0,require_exact_numbers:false}. Для всех явно задай путь, UTF-8, двойную кавычку, параметры ниже; overwrite по умолчанию reject. Выполни каждый:
1. __EXPORT_PREFIX__-csv.csv: ;, names, BOM=false, LF, decimal_separator=., null_marker=?.
2. __EXPORT_PREFIX__-typed.csv: ;, names, BOM=false, LF, decimal_separator=запятая, null_marker=NULL, true_value=Да, false_value=Нет, date_format=yyyy/mm/dd, date_separator=-, time_format=hh:mm:ss, time_separator=:. Сохрани native поведение: время в полночь отсутствует в тексте; ненулевое время присутствует.
3. __EXPORT_PREFIX__-wide.tsv: табуляция, labels, BOM=true, CRLF, decimal_separator=., null_marker=?.
4. __EXPORT_PREFIX__-empty.csv: ;, names, BOM=false, LF, decimal_separator=., null_marker=?. Получится только заголовок.

Измени тот же ExportEmpty: destination=__EXPORT_PREFIX__-zero.csv и header=none, остальные настройки/связь сохрани; Execute должен создать настоящий файл длины 0. Измени тот же ExportCSV на __EXPORT_PREFIX__-tsv.tsv, delimiter=табуляция, header=none, BOM=true, CRLF; остальные параметры/связь сохрани. Проверь file_artifacts, новое execution_id и свежесть каждого файла; CSV-кавычки, Unicode, перенос внутри строки, пустая строка и NULL не должны теряться.

Проверь конфликт: ExportCSV запрашивает исходный __EXPORT_PREFIX__-csv.csv с decimal_separator=., text_qualifier=двойная кавычка, header=names, delimiter=;, BOM=false, LF, но без overwrite=replace. Ожидается FAILED с подтверждённым закрытием черновика, без Execute и нового file_artifact. Затем явно запроси replace того же пути с теми же полными параметрами; новая операция должна завершиться с новой execution_id и доказательством explicit replace.

На ExportCSV выполни Done с destination=__EXPORT_PREFIX__-done.csv, delimiter=запятая, header=labels, BOM=true, CRLF. Файл не создавать, новые байты/выполнение не заявлять. Затем Close с изменениями destination=__EXPORT_PREFIX__-closed.csv, delimiter=;, header=none, BOM=false, LF. Изменения Close должны быть отменены. В результате ExportCSV обязан сохранить параметры Done.

Сохрани пакет в конце через package.save_checkpoint. По явному требованию этого задания отдельной операцией переоткрой тот же пакет и проверь сохранность трёх импортов, фильтра и четырёх экспортов, их связей и настроек. Выполни существующие Main, Typed и Wide с parameters={source: прежняя подтверждённая идентификация artifact_id/upload_operation_id, settings:{}}, inputs=[] и mappings=[]. Не меняй идентификацию источника и не передавай настройки повторно; bytes/sha256, если передаёшь, должны соответствовать прежнему подтверждённому файлу. Существующий Empty выполни с parameters={}, inputs=[] и mappings=[], чтобы получить актуальные источники. Затем существующие четыре экспорта выполни с inputs=[], mappings=[] и единственным изменением destination: __EXPORT_PREFIX__-reopen-changed.csv, __EXPORT_PREFIX__-reopen-typed.csv, __EXPORT_PREFIX__-reopen-wide.tsv, __EXPORT_PREFIX__-reopen-zero.csv. Все остальные сохранённые настройки должны совпасть с последним применённым состоянием. Не передавай настройки заново для имитации сохранности. Новые execution_id и полные байты обязательны. Сохрани итоговый пакет через package.save_checkpoint.

При ошибке используй закреплённые Help/E2E и проверку того же operation_id; не выдавай частичный результат за завершённый. Итог сообщи по всем случаям, путям и ограничениям. Независимое сравнение полных скачанных файлов, отсутствие Done/Close-файлов, сохранность reject baseline и новая отдельная сессия переоткрытия проверяются внешним аудитором; само резюме Hermes приёмку не закрывает.
