# Переключение разработчиков на общую память Loginom Dock

Пользователь разрешил общую память для чтения и записи 13 сентября 2026.
Пользователь подтвердил включение. Канонические hooks установлены и доверены,
но четыре задачи пока сохраняют прежние подключения и запись в worktree Peer.
Окончательное переключение ожидает завершения работающих этапов и штатного
перезапуска приложения; оно НЕ проверено и НЕ объявлено завершённым.

## Подготовленный результат

- Общий Peer: `-Users-kartamyshev-Git-loginom-dock`.
- Только четыре точных постоянных worktree узлов 11–14 входят в маршрут.
  Основная задача координатора сохраняет обычное подключение к тому же Peer.
- Все задачи и OpenViking session ID остаются отдельными.
- MCP выбирает маршрут по доверенным metadata приложения. Аргументы модели,
  рабочий каталог процесса MCP и произвольный URI не выбирают общий Peer.
- Объявления находятся в основном `.codex/hooks.json`: linked worktrees читают
  именно его, а локальные объявления и `hooks.state` подменяются корневым слоем.
  Проектные обёртки вызывают пять неизменённых официальных hooks 0.8.1:
  SessionStart, UserPromptSubmit, Stop, PreCompact, SessionEnd.
- Только дочерний процесс получает общий Peer и отдельный каталог состояния:
  `/Users/kartamyshev/.openviking/project-states/loginom-dock`.
  Подключение, ключ и личность остаются прежними. Значения ключей не сохраняются
  в подготовленных конфигурациях и не выводятся.
- До активации unmapped workspace ничего не делает; mapped task остаётся в
  standby только при точном legacy state. Штатные legacy 0600/0644 принимаются,
  запись другим пользователям запрещена. После активации старый cursor,
  несовпадение identity/версии/receipt и неправильные права вызывают отказ.
  Активные project state и receipts требуют 0600.

Подготовленный runtime:
`/Users/kartamyshev/Git/loginom-dock/.dock/shared-project-memory/runtime/20260913.3`.

Подготовленные конфигурации и diff:
`/Users/kartamyshev/Git/loginom-dock/.dock/shared-project-memory/rollout-20260913.3`.
Они имеют суффикс `.pending` и не загружаются Codex. `manifest.json` фиксирует
исходные SHA конфигураций; перед применением их необходимо сверить заново.

Исходники расширения сохранены в `overlay/`, минимальный diff исходного адаптера —
в `adapter.patch`, контрольные хеши исходной версии — в `upstream-manifest.json`.
`assemble_runtime.py` воспроизводимо собирает runtime, отказывает при изменении
исходной версии и не перезаписывает несовпадающую сборку.

Проверки подготовленной версии: **46/46 PASS**, в том числе 18 проверок обёртки
и 11 новых проверок маршрутизации. Использованы локальные синтетические данные.
Собранные 15 файлов побайтно совпали с проверенной версией; повторная сборка
подтвердила все хеши. Реальная запись/извлечение/чтение после переключения ещё
не проверены. Штатный `hooks/list` отдельно подтвердил пять доверенных hooks
в основном checkout и четырёх worktree. 25 прямых запусков пяти событий подтвердили
бездействие до переключения: старые state не изменились, shared state не созданы.
Дополнительно 19/19 синтетических проверок migration validators прошли:
несовпадение задачи, устаревшие receipts, оставшиеся старые hooks и сброс cursor
отклоняются. Реальная миграция helper-ом ещё не выполнялась.

`prepare_rollout.py` только генерирует эти файлы, проверяет сохранение прежних
настроек и не изменяет активные конфигурации, trust records или capture cursors.

## Обязательный порядок включения

1. Дождаться окончания текущего этапа конкретной задачи; сохранить её ID,
   checkpoint и итоговый commit. Проверить отсутствие активного harness/Hermes.
2. После перезапуска открыть только координатор, оставив рабочие задачи незагруженными.
   Проверить исходные SHA конфигурации и pinned runtime. Проверить фактические
   настройки hooks этой задачи. Подтвердить новые определения через штатный
   интерфейс Codex; не записывать выдуманный `trusted_hash`, не применять
   флаги обхода hook trust. Определения должны соответствовать подготовленным
   `hooks.json.pending` и текущему проверенному коду обёртки.
3. `flush_legacy_capture.mjs N`: дозаписать и завершить прежнюю партию capture штатным кодом OpenViking под
   прежним Peer. Успех проверить по состоянию и ответу сервера: не должно остаться
   неотправленного хвоста, активной блокировки или незавершённого `ovSessionId`.
   Один лишь код выхода hook этого не доказывает.
4. `install_task_config.py N` устанавливает подготовленный MCP override и
   `plugins."openviking-memory@openviking".enabled=false` только в данной рабочей
   папке. Это отключает исходные plugin hooks; глобальные настройки плагина
   не меняются. Поскольку bundled skills также исчезнут из каталога задачи,
   ссылки на официальные SKILL.md сохранены в AGENTS.md. Снова вызвать
   `review_hooks.mjs --trust`: для этой папки originalMemoryHooks должны быть пусты.
   Подтвердить завершение возможных async workers. Не отправлять задаче новый
   запрос до окончания следующего шага.
5. `migrate_task_state.mjs N --phase-boundary-verified` требует свежие receipts
   после установки и verified idle boundary. Он берёт официальный legacy lock,
   проверяет transcript tail, сохраняет внешний backup, переносит cursor с
   chmod 0600 и запускает SessionStart с `source:"resume"`. Флаг не заменяет
   проверку координатором. При сбое после переноса не восстанавливать старый backup.
   Helper сохраняет activation record после переноса; обёртка создаёт routing
   receipt только после проверки состояния официального hook. Две активные копии
   состояния и обнуление курсора запрещены.
6. Обновить подключение MCP этой задачи и проверить реальный `find/read` в actor
   scope. До этого старый процесс не считается обновлённым от изменения файлов.
   Не архивировать задачу и не удалять worktree ради перечитывания настроек.
7. Зафиксировать один реальный подтверждённый нюанс Loginom. Проверить фактический
   Peer нового capture, извлечение нужного знания в общий раздел, точное read-back
   и адресный поиск из другой задачи. Повторить первоначальный доступ для каждого
   потока; затем обращаться к памяти по потребности, без таймера.

На установленном desktop 0.154.0-alpha.6.2 подтверждён только глобальный
`config/mcpServer/reload`; обычный send/resume не гарантирует обновление уже
загруженной задачи. Глобальное обновление при активных разработчиках запрещено.
Управление интерфейсом самого Codex также отклонено CUA: `Computer Use is not
allowed to use the app com.openai.codex for safety reasons`. Обход не выполнялся.
Поэтому нужен штатный перезапуск пользователем после завершения этапов 13/14.
Не архивировать задачи и не удалять worktree. В текущем состоянии legacy state
не переносились, исходные hooks включены во всех четырёх папках.

`review_hooks.mjs` использует bundled desktop binary и только initialize, hooks/list,
config/batchWrite. Последний с --trust записывает возвращённые currentHash пяти
проверенных определений; runtimeReload=false, модели и задачи не запускаются.
Отдельный app-server здесь проверяет конфигурацию, а не подключение работающих задач.

Узлы11/12 прошли официальный legacy flush: cursor1452/1630, ovSessionId=null,
хвост отправлен. Перед будущей миграцией перепроверить, не появились ли новые сообщения.
Нормальные исходные worker config восстановлены; пробная установка несовместимых
worktree hooks не меняла capture state. Для node12 отдельно штатно зарегистрирован
trust_level точной рабочей папки. Корневая глобальная память/identity не менялась.

## Существующая память и конкуренция

Старые worktree-записи не удаляются и не переписываются. Автоматическая смена
маршрута не переносит их. После переключения разработчик может сформулировать
подтверждённые прежние выводы в своей текущей задаче для общего capture; полные
старые журналы не переотправляются.

В исходниках сервера есть раздельные сессии, очереди по Peer и блокировки записи;
соответствие этих деталей живой версии не проверено этим набором локальных тестов.
Файловая блокировка не предотвращает смысловые противоречия. Новые знания должны
содержать версию Loginom, узел, условия, ветку/SHA и статус проверки. Незавершённая
ветка не меняет автоматически знания о принятом main или установленном плагине.

## Обратный переход

Остановить выдачу новых этапов и завершить текущий capture. Сверить, что нет
неотправленного хвоста. Вернуть исходные конфигурации только если их SHA совпадают
с планом и нет новых чужих изменений; включить штатные hooks и обычный MCP.
Перенести актуальный cursor, а не старую резервную копию. Не удалять полученные
знания и не переотправлять историю. Снова проверить фактическую маршрутизацию.

## Источники

- Текущая политика: `docs/loginom-dock/shared-project-memory.md`.
- Исходный адаптер: `/Users/kartamyshev/Git/openviking/integrations/codex-mcp-adapter`.
- Официальный плагин: `/Users/kartamyshev/.codex/plugins/cache/openviking/openviking-memory/0.8.1`.
- [Codex: загрузка и доверие к hooks](https://learn.chatgpt.com/docs/hooks#review-and-trust-hooks).

Linked-worktree discovery подтверждён [официальным App Server README](https://github.com/openai/codex/blob/main/codex-rs/app-server/README.md), раздел hooks/list, и фактическим hooks/list всех пяти cwd.
